一、做了什么
把 `src/views/MapTopologyView.tsx` 里的仿真压力展示从单纯的数值对比，改成了“初始压力 → 仿真后压力 → 变化条”的样子；每一行现在都明确标出 `初始` 和 `仿真后`，并用一条小的渐变条表示变化幅度和方向。

二、为什么这么改
之前的颜色只表达了结果，没把前后过程讲清楚，容易让人误以为某个点只有一个静态值。现在把初始值和仿真结果分开，颜色只负责辅助阅读，不再替代信息本身。

三、影响范围
这次只改了前端展示，不动后端求解和数据接口。影响的是仿真面板的输出区域，尤其是压力行的视觉表达和变化提示。

四、前后对比
| 项目 | 修改前 | 修改后 |
| --- | --- | --- |
| 压力表达 | 单行两个数值，颜色容易混 | 明确标出初始和仿真后 |
| 变化提示 | 只看得到结果，过程不直观 | 中间增加变化条，能看出趋势 |
| 阅读体验 | 容易误读颜色含义 | 重点更清楚，误解更少 |

五、验证结果
`npm run build` 已通过，说明这次 JSX 和样式改动没有破坏构建。现在如果后面还要再细化，可以继续把变化条做成按压降/抬压分段显示，但当前这版已经能把前后压力和变化过程讲清楚。

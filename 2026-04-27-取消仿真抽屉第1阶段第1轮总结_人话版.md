一、主目标
把地图拓扑页左侧“拉出的仿真页面”取消掉，保留页面内原有的“仿真”标签页，不再让页面出现一个额外的抽屉面板。

二、上下文与已知信息
1. 这个问题出在 `src/views/MapTopologyView.tsx`，页面里之前额外渲染了一个 `SimPanel` 左侧抽屉。
2. 这次同时把稳态仿真的试点 id 从错误的 `we1` 改成后端真实注册的 `mainline_zhongwei_jingbian`，避免点击仿真时继续报 `pilot not found: we1`。

三、修改逻辑
1. 删除了 `MapTopologyView.tsx` 里对 `SimPanel` 的引入和渲染，让页面不再出现可拉出的独立仿真面板。
2. 把仿真入口统一指向 `WE1_PRIMARY_PILOT_ID = 'mainline_zhongwei_jingbian'`，让前端发给后端的参数和后端试点配置对齐。

四、前后对比
1. 之前：页面左侧会弹出一个独立的稳态仿真抽屉，和主页面的“仿真”标签页并存，界面重复。
2. 现在：抽屉不再渲染，仿真只保留在主页面左侧面板里的“仿真”标签页。

五、验证结果
1. 已确认 `MapTopologyView.tsx` 里不再有 `SimPanel`、`simDrawerVisible` 和错误的 `pilot_id = 'we1'`。
2. 目前做的是静态确认，未在这一步重新跑完整前端构建；如果需要，我可以继续补一次运行验证。

六、影响范围
1. 影响的是 `MapTopologyView` 这个地图拓扑页的仿真展示方式。
2. 不影响后端仿真算法，只是去掉了重复抽屉，并修正了前端传参。


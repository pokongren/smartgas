一、做了什么
1. 这轮不是再空讲方案了，我把它直接收成了一份能拿去现场照着走的 15 分钟最终稿。
2. 新稿里把四件事钉死了：固定 5 个问题、固定 2 个场景、固定切页顺序、固定老师追问口径。
3. 还顺手补了一张主线图，现场自己过稿或者给同学对口径都方便。

二、为什么这么做
1. 之前那版主要解决“别讲错”，这轮解决的是“怎么讲才稳、怎么点才顺、老师问到哪句怎么回”。
2. 现在最大风险不是没功能，是现场一紧张就乱跳页面、乱换问题、乱抬能力。真那样，代码里有的没的就容易搅一块去。
3. 所以这轮的思路就一句话：把你答辩时每一下鼠标和每一句话都收紧到代码已经站得住的地方。

三、这轮收紧后的关键口径
1. 主线彻底定成“AI查库回答问题→手动跑仿真→保存快照→展示页基线对比→AI解释结果”。
2. 不再讲“同页 AI 自动接仿真”，因为第一张图主仿真页代码里就是把 AI 助手隐藏了。
3. 不再把“AI 直接触发仿真”塞进主流程，因为虽然后台留了能力，但前端主答辩链最稳的还是人工点运行。
4. AI 结果解释这块也收口了，最稳说法是“仿真先出结果，AI再解释结果”，可以走独立弹窗，也可以只讲后端已有评价接口。

四、这轮抓到的几个硬证据
1. `src/views/MapTopologyView.tsx` 在仿真结果出来后，会把 `scenarioId`、`selectedSnapshotRunId`、`baselineSnapshotRunId` 和 `overlay` 写进 `sessionStorage`，这说明第一张图和第三张图不是各玩各的，是有同步链的。
2. 同一个文件里还会把 `simulationScenario`、`simulationRunId`、`solverStatus`、`alertCount` 写进 AI 运行时上下文，这说明 AI 解释结果不是完全没着落，只是别硬讲成同页自动联动。
3. `src/views/TopologyView.tsx` 会主动读取前面那份同步上下文，再把场景、快照和 overlay 接回来，所以第三张图讲“验收和对比”是有代码支撑的。
4. `backend/app/routers/ai_assistant.py` 里真有 `/simulation/evaluate`，`backend/app/services/assistant_tools.py` 里也真有 `evaluate_sim_result`，所以“AI评价结果”这句话可以讲，但别把它讲成当前前端主入口。

五、前后对比
| 项目 | 上一版收口 | 这轮最终稿 |
| --- | --- | --- |
| 目标 | 先把口径跟代码对齐 | 直接变成能上场的 15 分钟稿 |
| AI结果解释 | 只说别放主链 | 明确成“独立解释已生成结果” |
| 演示内容 | 只有方向 | 固定 5 问、2 场景、切页顺序、台词 |
| 现场应对 | 有原则 | 有 30 秒故障回退短句 |

六、影响范围
1. 这轮还是没改业务功能，只改文档和答辩组织方式。
2. 对现场最直接的帮助是：你现在不用边想边讲了，照着固定问题和固定切页顺序走就行，翻车概率会小一截。

七、验证结果
1. 这轮新增的口径都能在现有代码里找到落点，尤其是“主仿真页写同步上下文、第三张图读同步上下文、AI有运行时上下文、后端有结果评价接口”这几处，已经把主线撑起来了。
2. 这轮还没做新的 UI 彩排截图，原因也很实在：先把讲法钉死，比先截一堆图更值钱。下一轮真彩排时，直接按这份最终稿计时就行。

八、这轮新增文件
1. `F:\smartgas-grid\docs\2026-04-29-按代码优化的15分钟答辩最终稿.md`
2. `F:\smartgas-grid\2026-04-29-按当前代码优化答辩方案第1阶段第2轮总结_人话版.md`

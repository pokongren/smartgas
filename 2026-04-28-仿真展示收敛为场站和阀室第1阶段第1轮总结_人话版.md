一、做了什么
把 `src/views/MapTopologyView.tsx` 里的仿真展示口径收窄了，当前仿真页只展示场站/压气站和阀室，不再把中间那些细点一起摊出来；同一口径也同步到了 `SimParamEditor` 的种子节点数据，避免面板里前后不一致。

二、为什么这么改
原来的仿真展示把沿线中间点也展开了，列表太长，用户看的是“站场和阀室怎么变”，不是每个中间点怎么变，所以这次直接把展示层收紧，减少噪音，保留对判断有用的信息。

三、影响范围
这次只动了前端展示和参数面板的数据筛选，没有改后端求解逻辑，也没有改仿真结果的数据结构。影响的是仿真页右侧的站点列表、参数编辑器的可编辑节点范围，以及页面里“站场压力”这块的展示密度。

四、前后对比
| 项目 | 修改前 | 修改后 |
| --- | --- | --- |
| 仿真节点列表 | 场站、阀室和中间细点一起显示 | 只显示场站/压气站和阀室 |
| 参数编辑器 | 直接吃完整仿真节点集 | 只吃收窄后的展示节点集 |
| 阅读成本 | 列表长，信息噪音大 | 重点更集中，更适合看结果 |

五、验证结果
已执行 `npm run build`，构建通过。当前没有再去改后端，风险点主要是如果后续要做细粒度管段分析，需要把这层过滤单独打开，而不是继续往这里加细点。

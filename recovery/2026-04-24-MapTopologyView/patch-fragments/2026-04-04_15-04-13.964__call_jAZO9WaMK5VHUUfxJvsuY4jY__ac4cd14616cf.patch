*** Begin Patch
*** Update File: F:\smartgas-grid\src\views\MapTopologyView.tsx
@@
                                 {editMode === 'draw-point' && (
                                     <div>
                                         <p className="text-[10px] text-gray-500 mb-1.5 uppercase tracking-wider">鑺傜偣绫诲瀷</p>
                                         <div className="grid grid-cols-2 gap-1.5">
                                             {(Object.keys(TOPO_LABELS) as PointType[]).map(key => (
@@
                                             ))}
                                         </div>
                                     </div>
                                 )}
 
+                                {editMode === 'merge' && (
+                                    <div className="border border-purple-500/20 bg-purple-950/20 rounded-lg p-2.5 space-y-2">
+                                        <div className="flex items-center justify-between gap-2">
+                                            <p className="text-[10px] text-purple-300 uppercase tracking-wider">手工捏合</p>
+                                            <span className="text-[10px] text-purple-200">已选 {selectedMergeNodeIds.length} 个站点</span>
+                                        </div>
+                                        <p className="text-[10px] text-gray-400 leading-4">
+                                            在地图上点选底层站点。再次点击可取消。建议同一交汇点至少选 2 个站点。
+                                        </p>
+
+                                        {selectedMergeNodeIds.length > 0 && (
+                                            <div className="max-h-28 overflow-y-auto flex flex-wrap gap-1.5">
+                                                {selectedMergeNodeIds.map(stationId => {
+                                                    const station = baseTopoNodes.find(node => node.id === stationId)
+                                                    return (
+                                                        <button
+                                                            key={stationId}
+                                                            onClick={() => setSelectedMergeNodeIds(prev => prev.filter(id => id !== stationId))}
+                                                            className="inline-flex items-center gap-1 rounded-full bg-white/10 px-2 py-1 text-[10px] text-gray-200 hover:bg-white/20 transition-colors"
+                                                        >
+                                                            <span className="truncate max-w-28">{station?.name || stationId}</span>
+                                                            <span className="material-symbols-outlined text-[11px]">close</span>
+                                                        </button>
+                                                    )
+                                                })}
+                                            </div>
+                                        )}
+
+                                        <input
+                                            type="text"
+                                            value={mergeJunctionName}
+                                            onChange={e => setMergeJunctionName(e.target.value)}
+                                            placeholder="枢纽名称（可不填，自动生成）"
+                                            className="w-full bg-gray-900/60 border border-purple-700/40 rounded-lg px-2.5 py-2 text-xs text-white placeholder-gray-500 outline-none focus:border-purple-400"
+                                        />
+                                        <input
+                                            type="text"
+                                            value={mergeJunctionDescription}
+                                            onChange={e => setMergeJunctionDescription(e.target.value)}
+                                            placeholder="备注（可选）"
+                                            className="w-full bg-gray-900/60 border border-purple-700/40 rounded-lg px-2.5 py-2 text-xs text-white placeholder-gray-500 outline-none focus:border-purple-400"
+                                        />
+
+                                        <div className="flex gap-1.5">
+                                            <button
+                                                onClick={() => setSelectedMergeNodeIds([])}
+                                                disabled={selectedMergeNodeIds.length === 0 || isMerging}
+                                                className="flex-1 bg-gray-800/60 hover:bg-gray-700 text-gray-300 py-2 rounded-lg text-xs transition-colors disabled:opacity-40"
+                                            >
+                                                清空已选
+                                            </button>
+                                            <button
+                                                onClick={() => void createManualJunction()}
+                                                disabled={selectedMergeNodeIds.length < 2 || isMerging || !manualJunctionEditingEnabled}
+                                                className="flex-1 bg-purple-700/80 hover:bg-purple-600 text-white py-2 rounded-lg text-xs transition-colors disabled:opacity-40 flex items-center justify-center gap-1"
+                                            >
+                                                <span className="material-symbols-outlined text-sm">hub</span>
+                                                {isMerging ? '创建中...' : '创建捏合'}
+                                            </button>
+                                        </div>
+
+                                        <div className="border-t border-purple-500/20 pt-2 space-y-1.5">
+                                            <div className="flex items-center justify-between">
+                                                <p className="text-[10px] text-purple-300 uppercase tracking-wider">可拆分手工捏合</p>
+                                                <span className="text-[10px] text-gray-500">{manualEditableJunctionGroups.length} 个</span>
+                                            </div>
+                                            {manualEditableJunctionGroups.length === 0 ? (
+                                                <p className="text-[10px] text-gray-500">暂无可拆分的手工捏合（运行时重编枢纽不会出现在这里）。</p>
+                                            ) : (
+                                                <div className="max-h-28 overflow-y-auto space-y-1">
+                                                    {manualEditableJunctionGroups.map(group => (
+                                                        <div key={group.id} className="rounded bg-white/5 px-2 py-1.5 flex items-center gap-2">
+                                                            <div className="flex-1 min-w-0">
+                                                                <div className="text-xs text-gray-200 truncate">{group.name}</div>
+                                                                <div className="text-[10px] text-gray-500 truncate">
+                                                                    站点 {(group.member_count ?? group.station_ids.length) || 0}
+                                                                </div>
+                                                            </div>
+                                                            <button
+                                                                onClick={() => void removeManualJunction(group)}
+                                                                disabled={deletingJunctionId === group.id || !manualJunctionEditingEnabled}
+                                                                className="bg-red-900/50 hover:bg-red-800 text-red-300 px-2 py-1 rounded text-[10px] transition-colors disabled:opacity-40"
+                                                            >
+                                                                {deletingJunctionId === group.id ? '拆分中' : '拆分'}
+                                                            </button>
+                                                        </div>
+                                                    ))}
+                                                </div>
+                                            )}
+                                        </div>
+                                    </div>
+                                )}
+
 
                                 <div className="border border-cyan-500/20 bg-cyan-950/20 rounded-lg p-2.5 space-y-2">
*** End Patch

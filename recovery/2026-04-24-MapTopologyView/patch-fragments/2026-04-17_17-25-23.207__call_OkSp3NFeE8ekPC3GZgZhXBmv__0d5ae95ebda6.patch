*** Begin Patch
*** Add File: docs/2026-04-18-WE1主仿真参数化第1阶段第3轮记录.md
+# 2026-04-18 WE1主仿真参数化第1阶段第3轮记录
+
+一、结论  
+第1阶段第3轮已完成，节点压力展示升级为“进/出站”双值，并在主仿真返回后增加0.6秒数字滚动动画，构建验证通过。
+
+二、实现内容  
+1. 在 `src/views/MapTopologyView.tsx` 新增 `animatedNodeOutPressure` 动画状态与 `requestAnimationFrame` 插值逻辑。2. 地图节点标签由单值压力改为“进|出 MPa”展示，仿真后读动画值，仿真前读seed进出站压力。3. 右侧节点详情面板同步改为动画值优先显示，未命中仿真时回退seed数据。
+
+三、为什么这样改  
+1. 你关心的是“仿真前后能看见压力变化”，双值和滚动动画比静态数字更直观。2. 动画只做数值插值，不改求解结果，风险低且不会影响后端稳定性。
+
+四、影响范围  
+1. 影响页面：`/map-topology` 的节点标注和节点详情运行状态区。2. 不影响求解公式、不影响快照数据结构。
+
+五、验证结果  
+1. 验证命令：`npm run build`。2. 验证结果：通过，无编译错误。3. 人工检查点：运行主仿真后，节点标签和详情面板压力值应有短时平滑过渡。
+
+六、前后对比  
+1. 改造前：地图主要显示单个压力值，切场景时数字跳变。2. 改造后：地图与详情面板显示进/出站压力，仿真结果落地时数字平滑滚动到新值。
*** End Patch

*** Begin Patch
*** Add File: F:\smartgas-grid\docs\2026-04-08-WE1第一张图覆盖结果清单第5阶段第1轮总结_人话版.md
+# WE1第一张图覆盖结果清单第5阶段第1轮总结
+
+## 主目标
+把 WE1 第一张图主仿真的“场景覆盖情况”收成一张能直接验收的清单，不再只靠人脑记哪些场景跑过、哪些场景还没留档。
+
+## 这轮做了什么
+1. 在 [MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx) 里把主仿真页重新接上 `useSimulation`，让页面直接读取正式仿真、快照、基线对比、试运行覆盖这些现成能力。
+2. 新增“覆盖结果清单”区块，把这几个口径收在一起展示：当前场景是否已留档、最近一次覆盖时间、当前命中节点/连线、建议演示场景、每个典型场景的关注点和现场风险。
+3. 把“试运行覆盖”和“覆盖结果清单”打通。现在补跑场景后，用户能马上在同一页看到场景有没有补齐，而不是只看到按钮点过没有。
+
+## 为什么这么做
+1. 之前主仿真页缺少一个固定收口位置，场景覆盖信息散在快照、试运行、当前结果里，验收时很难一句话说清楚“现在到底覆盖到哪了”。
+2. 这轮不改求解器，只做页面收口，成本低，风险小，但对演示和验收帮助最大。
+
+## 影响范围
+1. 影响文件：[MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx)
+2. 不影响后端求解逻辑，不改 overlay 契约，不改场景定义。
+
+## 前后对比
+| 项 | 之前 | 现在 |
+| --- | --- | --- |
+| 场景覆盖查看 | 只能分散看快照和补跑按钮 | 有单独“覆盖结果清单”集中看 |
+| 当前场景是否留档 | 需要自己翻快照 | 页面直接显示“已留档/还没留档” |
+| 建议演示场景 | 靠人工判断 | 页面给出推荐场景 |
+| 每个场景怎么看 | 没有统一口径 | 每个场景都带关注点和风险提示 |
+
+## 验证方式
+1. 运行 `npm run build`，前端生产构建通过。
+2. 运行 `python backend/scripts/verify_we1_phase34_final.py`，WE1 阶段3-4联合回归通过。
+3. 独立 subagent 已发起代码复核；这一轮先以构建和脚本通过作为继续推进门槛。
+
+## 风险和回退点
+1. 这轮主要是页面收口，最大的风险不是求解错误，而是页面展示口径和现场演示口径不一致。
+2. 如果后面发现推荐场景顺序不合适，只需要调整前端排序，不需要动后端。
+
+## 交付物
+1. [MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx)
+2. [2026-04-08-WE1第一张图覆盖结果清单第5阶段第1轮总结_人话版.md](/F:/smartgas-grid/docs/2026-04-08-WE1第一张图覆盖结果清单第5阶段第1轮总结_人话版.md)
*** Add File: F:\smartgas-grid\docs\2026-04-08-WE1第一张图固定演示口径第5阶段第2轮总结_人话版.md
+# WE1第一张图固定演示口径第5阶段第2轮总结
+
+## 主目标
+把 WE1 第一张图主仿真的讲解顺序固定下来，让人打开页面后就知道先点哪里、看哪里、怎么判断成功、出问题先查哪一层。
+
+## 这轮做了什么
+1. 在 [MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx) 里新增“固定演示口径”区块。
+2. 把演示顺序直接写进页面：先选场景，再运行正式仿真，再保存快照，再看基线对比和覆盖结果清单，最后按失败排查层级往下查。
+3. 把“推荐场景”和“建议讲法”放到同一块里，减少现场切换时的口头组织成本。
+
+## 为什么这么做
+1. 之前就算仿真能跑，也缺一个统一讲法，不同人演示时会出现顺序不一致、重点不一致的问题。
+2. 这轮把“操作说明”和“讲解口径”一起固化，后续验收、培训、复盘都更稳定。
+
+## 影响范围
+1. 影响文件：[MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx)
+2. 这一轮仍然只改前端展示，不动求解器和后端接口。
+
+## 前后对比
+| 项 | 之前 | 现在 |
+| --- | --- | --- |
+| 演示顺序 | 靠人临场组织 | 页面直接给出演示顺序 |
+| 推荐讲法 | 没有固定口径 | 页面直接显示“推荐场景 + 建议讲法” |
+| 成功判断 | 靠经验判断 | 页面明确写出成功怎么看 |
+| 失败排查 | 先查哪层不固定 | 页面明确给出失败先查哪层 |
+
+## 验证方式
+1. 运行 `npm run build`，前端生产构建通过。
+2. 运行 `python backend/scripts/verify_we1_phase34_final.py`，WE1 阶段3-4联合回归通过。
+3. 独立 subagent 已发起代码复核；如果后续复核指出问题，优先修复页面逻辑，再继续后续阶段。
+
+## 风险和回退点
+1. 这轮风险主要在口径是否足够贴近现场讲解，不在后端计算。
+2. 如果后续发现现场更适合别的讲解顺序，只需要调整前端说明文本，不影响仿真主流程。
+
+## 交付物
+1. [MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx)
+2. [2026-04-08-WE1第一张图固定演示口径第5阶段第2轮总结_人话版.md](/F:/smartgas-grid/docs/2026-04-08-WE1第一张图固定演示口径第5阶段第2轮总结_人话版.md)
*** End Patch

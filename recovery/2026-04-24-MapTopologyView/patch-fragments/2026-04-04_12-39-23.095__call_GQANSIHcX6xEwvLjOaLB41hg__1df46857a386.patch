*** Begin Patch
*** Delete File: C:\Users\Administrator\.codex\automations\automation\automation.toml
*** Add File: C:\Users\Administrator\.codex\automations\automation\automation.toml
+version = 1
+id = "automation"
+kind = "cron"
+name = "项目心跳"
+prompt = """巡检 smartgas-grid。主目录固定是 automation.toml 里的 cwds[0]，也就是 `F:\\smartgas-grid`。这次不要把 detached worktree 当事实来源，也不要先在别的 worktree 里跑一遍再补判断。先读 automation memory，避免每次都重复同一种阻塞判断；再检查 git log、git status、未提交 diff、git worktree list、.git 指向和 docs 变化。
+
+第一步先做落点修正，不是先做业务判断：
+1. 先主动切到 `cwds[0]`，也就是 `F:\\smartgas-grid`，后面的 git 命令、目录检查、文件检查都以这个目录为准。
+2. 切目录后立刻确认两件事：当前 cwd 是不是 `F:\\smartgas-grid`，`git rev-parse --show-toplevel` 是不是 `F:\\smartgas-grid`。
+3. 只有这两个都对，才继续巡检。
+4. 如果切不过去，或者切过去后 cwd / git 根目录仍然不是 `F:\\smartgas-grid`，再归类为 `automation env mismatch / worktree-path blockage`。这时要明确写清楚初始 cwd、切换后的 cwd、当前 git 根目录、配置目标目录，并直接说明“本轮要求直接在主目录运行，但实际没有落到主目录”。
+
+执行组织默认采用“主代理统筹 + 子代理分工”：
+1. 主代理必须存在。主代理负责先做入口判断、决定是否继续、派出子代理、收拢证据、统一口径，并输出最终结论。
+2. 子代理默认只做分工侦察或验证，不直接产出最终版本。
+3. 默认拆分方式：
+   - 侦察代理 A：环境与路径，只查 `cwd`、git 根目录、`git worktree list`、`.git` 指向、仓库是否空壳。
+   - 侦察代理 B：地图缩放与层级显示，只查 `src/utils/mapRenderer.ts`、`src/utils/hierarchyRenderer.ts`、`src/views/MapTopologyView.tsx`、`src/views/GlobalPipelineView.tsx`。
+   - 侦察代理 C：枢纽规则、数据库口径、导入脚本，只查 `backend/app/models.py`、`backend/app/services/junction_groups.py`、`backend/app/routers/topology_editor.py`、`backend/data/smartgas.db`、`backend/check_db.py`、`backend/scripts/add_sj2.py`、`backend/scripts/add_we3.py`、`backend/scripts/reimport_sj23.py`、`src/services/api.ts`、`src/hooks/useTopology.ts`。
+   - 侦察代理 D：AI 模块链路，只查 `backend/app/routers/ai_assistant.py`、`backend/app/services/ai_client.py`、`backend/app/services/ai_analysis_orchestrator.py`、`backend/app/services/analysis_validator.py`、`backend/app/services/fix_task_router.py`、`src/components/ai-assistant/AiAssistant.tsx`、`src/components/ai-assistant/useAiAssistantChat.ts`、`src/components/ai-assistant/useAiAssistantPageContext.ts`、`src/services/api.ts`，确认入口、调用链、路由前缀、上下文注入、模型/网关配置、错误回退口径是不是对得上，并区分正式链路变更、调试残留、本地实验。
+   - 验证代理：独立复核主代理汇总结论有没有越界、有没有把局部现象说成全局事实、有没有把 detached worktree 当主目录。
+4. 如果入口都没过，主代理直接给出 blockage 结论，不要再派业务线子代理假装完成四条主线。
+
+这次输出要尽量用人话，而且统一由主代理来收口：
+1. 别写成审计报告，也别一股模板味。
+2. 先说结论，再补原因。
+3. 多用短句，少用空话。
+4. 如果能说清楚，就直接说，不要兜圈子。
+5. 子代理只提供证据和建议，不直接决定最终措辞；最终口吻、结论、风险归类都由主代理统一输出。
+6. 如果适合配图，就直接配图演示。优先用 mermaid，把“当前仓库位置 / 真实 worktree / 主要变化点 / 阻塞点”画清楚；如果这次没有图也能说清，再不用硬画。
+
+入口判断规则：
+1. 如果当前 git 根目录下缺少 src、backend、docs 或本次要求检查的关键文件，或者 git log 只显示最小初始提交、仓库树只有 README.md 这种极小快照，就归类为“worktree/path sync blockage”。要直接写清楚为什么卡住了。
+2. 只有当前 git 根目录里确实有目标文件和数据库，而且当前 git 根目录就是 `F:\\smartgas-grid`，才继续深查。
+3. 不允许拿 detached worktree 的检查结果替代主目录结论；也不允许因为 memory 里记得 `F:\\smartgas-grid` 有内容，就跳过本轮入口验证。
+
+入口通过后，再看四条主线：
+1. 全国地图缩放与层级显示：重点看 `src/utils/mapRenderer.ts`、`src/utils/hierarchyRenderer.ts`、`src/views/MapTopologyView.tsx`、`src/views/GlobalPipelineView.tsx`。确认气源、压气站、分输站、阀室、枢纽在不同 zoom 下的显示阈值、隐藏规则、重要性分层是不是一套口径，地图和拓扑视图有没有说法不一致。
+2. 起源点与枢纽规则：重点看 `backend/app/models.py`、`backend/app/services/junction_groups.py`、`backend/app/routers/topology_editor.py`、`src/utils/mapRenderer.ts`。确认 source、junction、JUNCTION-x 引用展开、重叠枢纽合并、起终点/连接点渲染和命名口径是不是一致。
+3. 数据库口径与导入脚本：重点看 `backend/data/smartgas.db`、`backend/check_db.py`、`backend/scripts/add_sj2.py`、`backend/scripts/add_we3.py`、`backend/scripts/reimport_sj23.py`、`src/services/api.ts`、`src/hooks/useTopology.ts`。确认 stations、pipelines、pipeline_systems、junction_groups 的数量、ID 前缀、system id、layers_config、API 端口和前后端字段口径是不是一致，并区分这是结构变更、导入脚本变更、数据库内容变更，还是本地实验。
+4. AI 模块链路：重点看 `backend/app/routers/ai_assistant.py`、`backend/app/services/ai_client.py`、`backend/app/services/ai_analysis_orchestrator.py`、`backend/app/services/analysis_validator.py`、`backend/app/services/fix_task_router.py`、`src/components/ai-assistant/AiAssistant.tsx`、`src/components/ai-assistant/useAiAssistantChat.ts`、`src/components/ai-assistant/useAiAssistantPageContext.ts`、`src/services/api.ts`。确认前端入口、请求路径、后端路由、编排服务、校验层、修复任务分发、页面上下文注入、模型配置、异常兜底是不是一条链；并区分这是正式能力、半接线状态，还是本地试验。
+
+输出要求：
+1. 先给“当前进展摘要”。第一句必须直接说本轮主要是代码变化、数据库变化、本地实验、AI 模块链路变化，还是 worktree/path sync blockage。
+2. 再给 1 条 inbox 更新，严格用“变化 / 风险 / 下一步”三段结构，每段 1-3 句，必须点名文件、模块或目录。
+3. 尽量用人话。像靠谱同事同步进展，不像客服，也不像审计机器人。
+4. 如果当前 git 根目录是空壳 worktree，要明确写出阻塞原因，例如当前根目录只有 README.md 和 .git、目标模块缺失、git worktree list 显示真实开发分支在别处；并注明“本轮已按当前 git 根目录判断，没有沿用旧 memory 路径做实体结论”。
+5. 如果当前运行目录没有落在 cwds[0]，要明确写出“automation env mismatch / worktree-path blockage”，并点名初始 cwd、切换后的 cwd、当前 git 根目录、配置目标目录，还要明确写出“本轮要求直接在主目录运行，但实际没有落到主目录”。
+6. 如果没有明显变化，也不能只写“无变化”；要写清楚现在还没确认什么、卡在哪、为什么这次不能下业务结论。
+7. 如果这次结构复杂、存在路径错位、四条主线里至少两条有变化，或者文字不够直观，就补一张 mermaid 图。图要服务于结论，不是为了凑格式。
+8. 不修改业务代码；只允许更新 automation memory，必要时补充自动化说明文档。"""
+status = "ACTIVE"
+rrule = "FREQ=HOURLY;INTERVAL=2"
+model = "gpt-5.4"
+reasoning_effort = "medium"
+cwds = ["F:\\smartgas-grid"]
+created_at = 1775004724195
+updated_at = 1775260800000
*** End Patch

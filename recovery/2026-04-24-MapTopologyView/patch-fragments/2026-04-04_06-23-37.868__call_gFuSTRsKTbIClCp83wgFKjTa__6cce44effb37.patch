*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
 2026-04-04 12:31 CST
 
 - This run again landed on the configured live repo correctly: initial `cwd` was `C:\Users\Administrator\.codex\worktrees\6e57\smartgas-grid`, switched `cwd` became `F:\smartgas-grid`, and `git rev-parse --show-toplevel` returned `F:/smartgas-grid`. All conclusions stayed anchored to `F:\smartgas-grid`; detached Codex worktrees were used only as background from `git worktree list`.
 - Current progress summary: this round is still real code change + database content change + local experiment/noise mixed together, not `automation env mismatch / worktree-path blockage`. `git status --short` still spans `src/`, `backend/`, `docs/`, plus local residue under `.npm-cache/`, `.agent/workflows/start.md`, a backup markdown file, and AI-assistant related files.
 - Environment/docs line: `F:\smartgas-grid` has a real `.git` directory, not a pointer file, and all required `src` / `backend` / `docs` / DB targets exist. `git diff --stat` shows broad live changes across code and docs; docs changes currently center on `docs/TOPOLOGY_SETUP_GUIDE.md`, `docs/TOPOLOGY_COMPUTATION_USAGE.md`, and staged `docs/code-improvement-review.md`.
 - National-map chain line: the inspected chain `src/utils/hierarchyRenderer.ts` -> `src/utils/mapRenderer.ts` -> `src/views/GlobalPipelineView.tsx` is wired, but the valve rule is still internally inconsistent. Active thresholds remain `critical=6`, `high=10`, `medium=14`, `valveMinZoom=11`, `expandClusterZoom=12`, yet `getNodeImportance()` still classifies `valve` as `LOW`, so `shouldShowNodeAtZoom()` effectively keeps valves hidden until `zoom >= 14`. Keep phrasing this as a problem in the inspected national-map chain, not as a claim about every rendering path in the repo.
 - National-map chain vs editor chain line: `src/views/GlobalPipelineView.tsx` still feeds `pipelineData` into `MapView`, while `src/views/MapTopologyView.tsx` still mounts `MapView` without `pipelineData`, keeps its own collapsed-graph editor path, and maps `junction` back to `station`. The safe conclusion is that `GlobalPipelineView`'s national-map chain and `MapTopologyView`'s editor chain still do not share one node-type/zoom vocabulary.
 - Junction / DB / script line: backend runtime normalization remains cleaner than stored rows. `backend/app/services/junction_groups.py` expands `JUNCTION-x` refs and merges overlaps, but `backend/app/routers/topology_editor.py` still has split write rules where `merge_stations` can create one-station `junction_groups` while `create_junction_group` rejects fewer than two bottom-level stations. Live DB snapshot stays `stations=1256`, `pipelines=1253`, `pipeline_systems=13`, `junction_groups=16`; `pipeline_systems.id` still has no `we3`; raw storage still contains one-station groups and nested row `junction_groups.id=17=["JUNCTION-16","JXLZ-11"]`.
 - Script-vs-live-data reminder: keep treating `backend/scripts/add_sj2.py`, `backend/scripts/add_we3.py`, and `backend/scripts/reimport_sj23.py` as script churn unless the DB proves otherwise. This run's DB check still shows `SJ2=70 stations / 69 pipelines`, `SJ3=71 / 70`, `WE3=0 / 0`, so `add_we3.py` should not be described as already landed in the live DB.
 - API/docs line: mixed contract wording is now intentional in the checked docs. `src/services/api.ts` still mixes `/api/topology/...` graph/stats with direct `/topology/correct/...`, `src/hooks/useTopology.ts` still goes direct to `/topology/...`, and the two topology docs now document backend `8080` plus that mixed prefix reality instead of pretending the old single-prefix contract still holds.
 - No business code was modified by this heartbeat run. Memory updated only. Run completed at `2026-04-04 12:31:15 +08:00`.
+
+2026-04-04 14:22 CST
+
+- This run again passed the main-root landing rule the right way: initial automation cwd was the detached worktree `C:\Users\Administrator\.codex\worktrees\7117\smartgas-grid`, but all inspection moved to `F:\smartgas-grid`, and both runtime cwd plus `git rev-parse --show-toplevel` resolved to `F:\smartgas-grid` / `F:/smartgas-grid`. Judgement stayed anchored to the live repo only.
+- Current progress summary: this round is still real code change + database content change + local experiment/noise mixed together, not a worktree/path blockage. `F:\smartgas-grid\.git` is a real directory, required `src` / `backend` / `docs` / DB targets all exist, and `git worktree list --porcelain` still shows `F:\smartgas-grid` as the only live branch worktree on `HEAD eaf6ac4...` while Codex worktrees remain detached at `6410a10...`.
+- Environment/diff line: `git log --oneline -5` still shows live commits (`eaf6ac4`, `b31a010`, `d3c9d08`, `c19937f`, `3e47946`) rather than the minimal initial snapshot. `git status --short` and `git diff --stat` show broad active changes across `src/`, `backend/`, `docs/`, plus local residue in `.npm-cache/`, `.agent/workflows/start.md`, and AI-assistant related files.
+- National-map line: the inspected national-map chain is still `src/utils/hierarchyRenderer.ts` -> `src/utils/mapRenderer.ts` -> `src/views/GlobalPipelineView.tsx`, and it is not fully aligned with the topology editor chain. Active thresholds remain `critical=6`, `high=10`, `medium=14`, `valveMinZoom=11`, `expandClusterZoom=12`, but `getNodeImportance()` still classifies `valve` as `LOW`, so `shouldShowNodeAtZoom()` keeps valves hidden until `zoom >= 14`; keep wording scoped to this inspected chain only. `GlobalPipelineView.tsx` passes `pipelineData` into `MapView`, while `MapTopologyView.tsx` explicitly mounts `MapView` without `pipelineData`, keeps its own editor graph, maps `junction` to `station`, and links from `focusNode`.
+- Junction/DB line: backend runtime normalization is still cleaner than stored data. `backend/app/services/junction_groups.py` expands `JUNCTION-x` refs and merges overlapping groups, but `backend/app/routers/topology_editor.py` still has a split write rule where `merge_stations` writes `station_ids=[keep_station_id]` while `create_junction_group` rejects fewer than two bottom-level stations. Live DB snapshot from this run remains `stations=1256`, `pipelines=1253`, `pipeline_systems=13`, `junction_groups=16`; system ids are `cred,gn,gs,jxlz,ncsh,pt,sj2,sj3,sj4,we1,we2,zg,zm`; storage still contains nested row `junction_groups.id=17=["JUNCTION-16","JXLZ-11"]` and several single-station rows.
+- Script-vs-live-data line: staged backend churn is real, but script presence still must not be described as already landed. `backend/scripts/add_sj2.py` removes 长阳, `backend/scripts/add_we3.py` swaps 长阳 for 西沙屯 in the script path, and new `backend/scripts/reimport_sj23.py` is a full SJ2/SJ3 reimport flow; yet live DB counts still show `SJ2=70/69`, `SJ3=71/70`, and `WE3=0/0`, so `WE3` is still script churn rather than confirmed live content.
+- Docs/API line: docs changes are now explicitly documenting the mixed `8080` contract instead of the old `8000` story. `docs/TOPOLOGY_SETUP_GUIDE.md` and `docs/TOPOLOGY_COMPUTATION_USAGE.md` now say topology editor traffic can still be `/api/topology/...` while `useTopology.ts` goes direct to `/topology/...`; `src/services/api.ts` remains mixed in the same way.
+- No business code was modified by this heartbeat run. Memory updated only. Run completed at `2026-04-04T14:22:52+08:00`.
*** End Patch

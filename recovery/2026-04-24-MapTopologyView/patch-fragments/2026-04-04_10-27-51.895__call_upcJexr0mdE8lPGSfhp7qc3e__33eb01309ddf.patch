*** Begin Patch
*** Update File: F:\smartgas-grid\docs\2026-04-04-统一整改方案_枢纽重编版.md
@@
 ## 10. 完成标准
 
 这轮整改只有同时满足下面这些条件，才算真的收口。
@@
 8. 每个实施阶段都已经产出一份“人话版总结” Markdown
 
 如果上面任意一条还没满足，这轮只能算“推进中”，不能算“已完成”。
+
+## 11. 四次实施排期表
+
+如果按这份方案正式开工，建议把实施明确拆成 4 次。
+
+这样好处很直接：
+
+- 每次只收一个大块，不容易失控
+- 每次结束都能做独立验收
+- 真出问题时，能知道是哪一次实施引入的
+
+### 11.1 总表
+
+| 实施次序 | 实施目标 | 主责任代理 | 建议模型 | 推理强度 | 前置条件 | 主要改动范围 | 主要交付物 | 放行条件 |
+| --- | --- | --- | --- | --- | --- | --- | --- | --- |
+| 第 1 次 | 重编枢纽事实层 | 实施代理 | `gpt-5.3-codex` | `high` | 主代理已冻结 `junction` / `major_junction` 规则；侦察代理 A 结论已收口 | `backend/app/models.py`、`backend/app/services/junction_groups.py`、`backend/app/services/topology.py`、`backend/app/services/topology_computation.py`、`backend/data/smartgas.db`、新增重编/校验脚本 | 新枢纽规则说明、`rebuild_junctions.py`、`verify_junctions.py`、DB 迁移或影子结果集、阶段 1 总结 | DB 中不再依赖 nested / single-station 旧口径；新 `junction` / `major_junction` 可被脚本证明 |
+| 第 2 次 | 收后端统一输出口 | 实施代理 | `gpt-5.3-codex` | `high` | 第 1 次已产出稳定事实层；主代理确认后端以该事实层为唯一来源 | `backend/app/services/topology.py`、`backend/app/services/topology_correction.py`、`backend/app/services/pipeline_data_service.py`、`backend/app/services/position_commit.py`、`backend/app/routers/topology_editor.py`、`backend/app/routers/pipeline_packages.py` | 后端统一输出、兼容层处理说明、接口差异清单、阶段 2 总结 | topology / pipeline packages / editor 相关接口对同一枢纽返回一致类型 |
+| 第 3 次 | 收前端两条消费链 | 实施代理 | `gpt-5.3-codex` | `high` | 第 2 次接口已经稳定；侦察代理 B/C/D 结论已冻结 | `src/views/MapTopologyView.tsx`、`src/services/topologyEditorApi.ts`、`src/utils/hierarchyRenderer.ts`、`src/utils/mapRenderer.ts`、`src/views/GlobalPipelineView.tsx`、`src/components/map-view/MapView.tsx`、`src/data/pipelines/transform.ts`、`src/data/pipelines/types.ts` | 编辑器新语义、全国地图统一显示规则、前端消费收口说明、阶段 3 总结 | 编辑器不再把 `junction` 当 `station`；全国地图缩放和显示规则与新口径一致 |
+| 第 4 次 | 补导入脚本和验收闭环 | 实施代理 | `gpt-5.2` | `high` | 前 3 次已经稳定；验证代理已给出回归检查清单 | `backend/scripts/add_sj2.py`、`backend/scripts/add_we3.py`、`backend/scripts/reimport_sj23.py`、`backend/check_db.py`、`verify_junctions.py` | 导入脚本闭环、DB 验收输出、`WE3` / `SJ2` / `SJ3` 核验结果、阶段 4 总结、最终总总结 | 导入完成后 DB 能直接证明结果；独立验证通过；主代理准许封板 |
+
+### 11.2 每次实施的默认编排
+
+每次实施默认都走同一套节奏：
+
+1. 主代理发任务书
+2. 实施代理单点改动
+3. 验证代理独立验收
+4. 主代理决定是否进入下一次实施
+5. 产出一份人话版总结
+
+### 11.3 建议时长
+
+如果按正常连续推进，不穿插别的大任务，建议按下面估：
+
+| 实施次序 | 建议时长 | 说明 |
+| --- | --- | --- |
+| 第 1 次 | 2 到 4 天 | 最难，牵涉规则冻结、DB 重编、历史脏数据处理 |
+| 第 2 次 | 1 到 2 天 | 重点是统一后端出口和兼容层 |
+| 第 3 次 | 1 到 3 天 | 前端链路多，容易被细节拖长 |
+| 第 4 次 | 1 到 2 天 | 重点不是改很多，而是把验收闭环做实 |
+
+### 11.4 每次实施结束后的固定产物
+
+每次实施结束后，默认都要有下面这些东西：
+
+1. 一份代码或脚本改动
+2. 一份验证结果
+3. 一份风险复核结论
+4. 一份“人话版总结” Markdown
+
+建议固定命名：
+
+- `docs/2026-04-04-阶段1-枢纽重编总结_人话版.md`
+- `docs/2026-04-04-阶段2-后端收口总结_人话版.md`
+- `docs/2026-04-04-阶段3-前端收口总结_人话版.md`
+- `docs/2026-04-04-阶段4-导入闭环总结_人话版.md`
+- `docs/2026-04-04-统一整改最终总结_人话版.md`
*** Add File: F:\smartgas-grid\docs\2026-04-04-统一整改方案_实施排期补充总结_人话版.md
+# 这次把“四次实施排期表”补了什么
+
+## 改动目标
+
+把原来“知道要分 4 次实施，但还没有一张能直接排期的表”补成可直接派工的版本。
+
+## 改动前的问题
+
+之前的主方案已经说清楚了方向，也写了执行顺序。
+
+但真要落到项目推进上，还差一层：
+
+- 第 1 次实施和第 3 次实施边界在哪里
+- 每次谁主责
+- 每次该用什么模型
+- 每次改完要交什么
+- 什么情况下才能进下一次实施
+
+没有这层，后面排期时还是容易一锅炖。
+
+## 这次具体做了什么
+
+这次补了 1 个新章节：
+
+- 在主文档里新增“11. 四次实施排期表”
+
+这个章节里又拆了 4 块：
+
+1. 总表
+2. 每次实施的默认编排
+3. 建议时长
+4. 每次实施结束后的固定产物
+
+## 改动后的结果
+
+现在这份方案已经能直接回答这几个问题：
+
+- 一共实施几次
+- 每次实施做什么
+- 每次谁负责
+- 每次建议用什么模型和推理强度
+- 每次有哪些前置条件
+- 每次结束后要交什么
+- 什么情况下才能放行到下一次
+
+## 为什么这么改
+
+原因很现实。
+
+“四次实施”这种说法，嘴上讲很容易，真推进时最怕两种情况：
+
+- 一次实施里塞太多，最后边界全糊了
+- 下一次开始了，但上一次其实还没验干净
+
+所以这次把“实施次序、责任代理、模型、前置条件、交付物、放行条件”绑在一起写了。
+
+## 影响范围
+
+这次还是只改文档，不改业务代码。
+
+直接影响文件：
+
+- `F:\smartgas-grid\docs\2026-04-04-统一整改方案_枢纽重编版.md`
+- `F:\smartgas-grid\docs\2026-04-04-统一整改方案_实施排期补充总结_人话版.md`
+
+## 验证方式
+
+这次主要按下面 3 个点检查：
+
+1. 主文档里是否已经明确写出 4 次实施
+2. 每次实施是否都有责任人、模型、前置条件、交付物、放行条件
+3. 是否仍然符合“并行侦察 + 单点实施 + 独立验证”的总组织方式
+
+## 验证结果
+
+已完成：
+
+- 主文档已新增“四次实施排期表”
+- 每次实施都已写明主责任代理和模型档位
+- 每次实施都已写明交付物和放行条件
+- 每次实施结束后都要求产出一份人话版总结
+
+## 前后对比
+
+这次同样不适合放截图。
+
+原因：
+
+- 当前任务还是方案文档补充，不是界面改版
+- 没有合适的视觉对象可以做前后图
+
+所以这里继续用表格替代：
+
+| 对比项 | 补充前 | 补充后 |
+| --- | --- | --- |
+| 实施轮次 | 只有“建议分 4 次”的概念 | 已明确 4 次实施的完整排期表 |
+| 责任分配 | 有角色，但没有落到每次实施 | 已按每次实施绑定主责任代理 |
+| 模型分配 | 有总原则 | 已落到每次实施的具体模型和推理强度 |
+| 放行条件 | 分散在正文里 | 已写成每次实施的放行关口 |
+
+## 风险点
+
+- 这里给的是建议时长，不是死工期，真开工后还要看第 1 次事实层重编的复杂度
+- 如果第 1 次实施发现 live DB 历史脏数据远比预期多，后面 2 到 4 次的节奏要跟着顺延
+
+## 回退方式
+
+如果你后面觉得排期表太细，可以只保留总表，把“建议时长”和“固定产物”并回主方案正文。
+
+## 一句话总结
+
+现在不只是知道“要分 4 次做”，而是已经知道“这 4 次分别怎么排、谁来做、做到什么算过”。
*** End Patch

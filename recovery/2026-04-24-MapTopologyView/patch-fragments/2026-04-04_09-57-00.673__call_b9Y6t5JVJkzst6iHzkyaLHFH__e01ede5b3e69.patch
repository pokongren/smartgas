*** Begin Patch
*** Delete File: F:\smartgas-grid\docs\2026-04-04-统一整改方案_枢纽重编版.md
*** Add File: F:\smartgas-grid\docs\2026-04-04-统一整改方案_枢纽重编版.md
+# 统一整改落实方案：枢纽重编 + 渲染链路收口
+
+编制时间：2026-04-04
+
+## 1. 主目标
+
+这次不是补几个零散 bug。
+
+主目标只有一个：
+
+**把“枢纽”的定义、数据库事实、后端输出、前端编辑器、全国地图、导入脚本全部收成一套口径。**
+
+这套口径按你刚定的规则执行：
+
+- 两条不同管线在同一站点或同一交叉点汇交，就认定为 `junction`
+- 三条及以上不同管线汇交，认定为 `major_junction`
+- “两个站点合并才叫枢纽”这条旧规则，废弃
+
+一句话说：
+
+**从“按站点捏合定义枢纽”，改成“按管线交叉事实定义枢纽”。**
+
+## 2. 上下文与已知信息
+
+当前已经确认的核心问题有这几条：
+
+1. 全国地图渲染链路 `src/utils/hierarchyRenderer.ts -> src/utils/mapRenderer.ts -> src/views/GlobalPipelineView.tsx` 里，阀室缩放阈值存在“配置写 11、实际表现更像 14”的不一致。
+2. 拓扑编辑器链路 `src/views/MapTopologyView.tsx -> src/services/topologyEditorApi.ts -> backend/app/routers/topology_editor.py` 还残留旧的“站点捏合成枢纽”思路，`junction` 语义没有彻底独立出来。
+3. live DB `backend/data/smartgas.db` 里还留着 `nested junction`、`single-station junction` 这类旧口径脏数据。
+4. 导入脚本 `backend/scripts/add_sj2.py`、`backend/scripts/add_we3.py`、`backend/scripts/reimport_sj23.py` 有改动，但 `WE3` 这类变更还没有被 live DB 事实完全闭环证明。
+
+另外，从现有代码边界看，这次整改不能只盯你原文里那几份文件。下面这些文件也应该纳入侦察或验证范围：
+
+- `backend/app/routers/pipeline_packages.py`
+- `backend/app/services/pipeline_data_service.py`
+- `backend/app/services/topology_computation.py`
+- `backend/app/services/topology_correction.py`
+- `src/data/pipelines/transform.ts`
+- `src/types/pipeline.ts`
+- `src/utils/pipelineDomain.ts`
+- `src/views/global-pipeline/ScadaFloatingPanel.tsx`
+
+原因很直接：
+
+- 枢纽口径一旦改了，统一输出口、拓扑计算口、前端转换层、全国地图详情联动都可能被连带影响。
+- 这轮如果只改“显示层”或者只清库，后面还是会继续打架。
+
+### 当前 vs 目标
+
+这份文档本身是整改方案，不是界面改版说明。
+
+这里不放前后截图，原因很明确：这次更关键的是规则、事实口径和链路收口，不是某一张页面截图好不好看。
+
+所以这里继续用前后对比表替代：
+
+| 项目 | 当前口径 | 目标口径 |
+| --- | --- | --- |
+| 枢纽定义 | 多站点捏合后才算枢纽 | 两条不同管线汇交即 `junction`，三条及以上即 `major_junction` |
+| 枢纽来源 | 前端交互和历史脏数据混用 | 后端按统一规则重编，前端只消费结果 |
+| `junction_groups` | 混有 nested / single-station 旧残留 | 重编为真实交叉关系，不再保留旧脏数据 |
+| 编辑器语义 | `junction` 仍部分按 `station` 对待 | `junction` / `major_junction` 独立建模 |
+| 全国地图 | 显示阈值和规则不完全一致 | 阈值、重要性、显示层级统一 |
+| 导入脚本 | 脚本改了，但 DB 事实不一定能证明 | 每次导入结果都能被 DB 和校验脚本直接验证 |
+
+## 3. 子代理拆分
+
+这次默认采用：
+
+**主代理统筹 + 并行侦察 + 单点实施 + 独立验证**
+
+先说结论：
+
+- 真正拍板的人必须只有一个，就是主代理
+- 侦察可以并行
+- 真正改代码尽量只让一个实施代理来做
+- 验证要跟实施代理独立，不能自己改完自己说没问题
+
+### 3.1 主代理
+
+建议模型：
+
+- 模型：`gpt-5.4`
+- 推理强度：`xhigh`
+
+职责：
+
+- 把侦察代理的证据收拢成一个结论
+- 决定这轮只走哪一条实施主线，不左右横跳
+- 设定数据库口径、后端输出口径、前端消费口径的最终边界
+- 给实施代理下明确任务书，控制范围，不让它顺手改散
+- 在每个阶段结束后做 go / no-go 判定
+
+为什么主代理要用最高档：
+
+- 这轮不是单点修一个页面，而是规则、数据库、后端、前端、脚本一起收口
+- 最大难点不是写几行代码，是避免几个模块各自“看起来都对，但拼起来不对”
+- 这类跨层收敛工作，必须让最强模型做判断
+
+### 3.2 侦察代理 A：规则与数据库侦察
+
+建议模型：
+
+- 模型：`gpt-5.3-codex`
+- 推理强度：`high`
+
+负责范围：
+
+- `backend/app/models.py`
+- `backend/app/services/junction_groups.py`
+- `backend/app/services/topology.py`
+- `backend/app/services/topology_computation.py`
+- `backend/app/services/topology_correction.py`
+- `backend/data/smartgas.db`
+
+要查清的事：
+
+- 旧 `junction_groups` 到底还承载了哪些“站点捏合型枢纽”语义
+- 新规则落库后，应该是改旧表、加新字段，还是新建派生表
+- `junction` 和 `major_junction` 更适合做“类型字段”还是“等级字段”
+- 历史 `nested junction` / `single-station junction` 怎么迁移最稳
+- live DB 里哪些数据是可以重算的，哪些要保护
+
+### 3.3 侦察代理 B：编辑器与接口链路侦察
+
+建议模型：
+
+- 模型：`gpt-5.3-codex`
+- 推理强度：`medium`
+
+负责范围：
+
+- `src/views/MapTopologyView.tsx`
+- `src/services/topologyEditorApi.ts`
+- `backend/app/routers/topology_editor.py`
+- `backend/app/routers/pipeline_packages.py`
+- `backend/app/services/pipeline_data_service.py`
+- `src/data/pipelines/transform.ts`
+- `src/types/pipeline.ts`
+
+要查清的事：
+
+- 现在哪些前端交互还建立在“多站点捏合”旧定义上
+- `junction` 在前端状态、接口请求、后端落库这三段里，哪一段还在被降格成 `station`
+- 编辑器里哪些动作应该保留，哪些动作应该从“创建枢纽”改成“确认交叉 / 标记大枢纽 / 修正异常归并”
+- 统一输出口改完后，编辑器和全国地图是不是还能吃同一套结构
+
+### 3.4 侦察代理 C：全国地图与导入闭环侦察
+
+建议模型：
+
+- 模型：`gpt-5.1-codex-mini`
+- 推理强度：`medium`
+
+负责范围：
+
+- `src/utils/hierarchyRenderer.ts`
+- `src/utils/mapRenderer.ts`
+- `src/views/GlobalPipelineView.tsx`
+- `src/views/global-pipeline/ScadaFloatingPanel.tsx`
+- `src/utils/pipelineDomain.ts`
+- `backend/scripts/add_sj2.py`
+- `backend/scripts/add_we3.py`
+- `backend/scripts/reimport_sj23.py`
+- `backend/check_db.py`
+
+要查清的事：
+
+- 新枢纽口径落地后，全国地图要新增或调整哪些显示层级
+- `junction` 和 `major_junction` 在 marker、聚合、缩放阈值上应该怎么区分
+- 导入脚本怎样在落库后直接产出可验证的事实
+- `WE3` 这类脚本改动怎样闭环到 DB 验收
+
+为什么这里可以用中档模型：
+
+- 这块主要是边界确认、规则核对、链路排查
+- 复杂，但不像主代理那样需要做最终跨层决策
+
+### 3.5 实施代理
+
+建议模型：
+
+- 模型：`gpt-5.3-codex`
+- 推理强度：`high`
+
+原则：
+
+- 只允许一个实施代理主写代码
+- 侦察代理不直接落代码
+- 主代理不把实施任务拆给多个会改同一批文件的人
+
+实施边界：
+
+- 数据库重编脚本 / 迁移脚本
+- 后端枢纽计算与统一输出
+- 前端编辑器和全国地图消费口径收口
+- 导入脚本和校验脚本闭环
+
+建议新增产物：
+
+- `backend/scripts/rebuild_junctions.py`
+- `backend/scripts/verify_junctions.py`
+
+### 3.6 验证代理
+
+建议模型：
+
+- 模型：`gpt-5.2`
+- 推理强度：`high`
+
+职责：
+
+- 用独立视角复核 DB、接口、前端、脚本四条链
+- 跑回归检查，不替实施代理补逻辑
+- 明确指出“确实修了”和“还没证实”的边界
+
+### 3.7 所有代理的统一输出要求
+
+不管哪个代理，默认都按下面 5 项交付：
+
+1. 发现了什么
+2. 证据是什么
+3. 建议怎么处理
+4. 涉及哪些文件或模块
+5. 风险点和未确认项
+
+## 4. 执行顺序
+
+这次不要边看边改。
+
+建议严格按下面顺序推进。
+
+### 阶段 0：主代理先冻结口径
+
+先做两件事：
+
+1. 把“什么叫 `junction`，什么叫 `major_junction`”写成一句不会歧义的话
+2. 把“编辑器还能不能人工创建枢纽”定性清楚
+
+这里不先冻结，后面侦察结果再多也会越收越散。
+
+### 阶段 1：并行侦察
+
+并行启动 3 个侦察代理：
+
+1. 侦察代理 A 盯规则和数据库
+2. 侦察代理 B 盯编辑器和接口
+3. 侦察代理 C 盯全国地图和导入脚本
+
+阶段产物：
+
+- 三份侦察结论
+- 一份主代理汇总结论
+- 一张“本轮纳入 / 暂不纳入”的边界表
+
+### 阶段 2：主代理收敛实施主线
+
+主代理拿到证据后，只做一个动作：
+
+**选一个最可信、最稳的实施方向进入落地。**
+
+我建议默认选这个方向：
+
+1. 先重编枢纽事实层
+2. 再统一后端输出
+3. 再改前端消费
+4. 最后补脚本验收闭环
+
+不建议反过来。
+
+原因很简单：
+
+- 定义已经变了，库不先收口，前后端怎么改都会心虚
+- 先改前端，只会临时把不一致藏起来
+
+### 阶段 3：单点实施
+
+只让实施代理执行，按 4 步走。
+
+#### 3.1 先重编枢纽事实层
+
+要做的事：
+
+1. 写清新的枢纽判定规则
+2. 新增 `rebuild_junctions.py`
+3. 读取 `stations`、`pipelines`、`pipeline_systems` 等基础事实
+4. 识别同坐标交叉、同站点多系统汇交，必要时加容差归并
+5. 生成新的 `junction` / `major_junction` 结果
+6. 清理旧 `nested junction` / `single-station junction` 残留
+7. 产出可读的校验结果
+
+#### 3.2 再收后端统一输出口
+
+要做的事：
+
+1. 改 `backend/app/services/junction_groups.py`
+2. 改 `backend/app/services/topology.py`
+3. 补看 `backend/app/services/topology_computation.py` / `topology_correction.py`
+4. 改 `backend/app/routers/topology_editor.py`
+5. 补看 `backend/app/routers/pipeline_packages.py`
+6. 让 SCADA / topology / pipeline package 等读取口都消费同一套枢纽事实
+
+#### 3.3 再收前端两条消费链
+
+一条是全国地图：
+
+- `src/utils/hierarchyRenderer.ts`
+- `src/utils/mapRenderer.ts`
+- `src/views/GlobalPipelineView.tsx`
+- `src/views/global-pipeline/ScadaFloatingPanel.tsx`
+
+目标：
+
+- 统一阀室和枢纽的缩放阈值
+- 区分 `junction` 和 `major_junction` 的显示层级
+- 不再出现“配置一套、表现另一套”
+
+另一条是编辑器链路：
+
+- `src/views/MapTopologyView.tsx`
+- `src/services/topologyEditorApi.ts`
+- `src/data/pipelines/transform.ts`
+- `src/types/pipeline.ts`
+
+目标：
+
+- `junction` 不再映射回 `station`
+- 编辑器里看到的枢纽类型和后端重编结果一致
+- 如果保留人工干预，动作语义也要改名
+
+#### 3.4 最后补导入脚本闭环
+
+要做的事：
+
+1. 补 `verify_junctions.py`
+2. 让 `add_sj2.py`、`add_we3.py`、`reimport_sj23.py` 跑完后，能直接给出 DB 验证结果
+3. `backend/check_db.py` 增加针对 `junction` / `major_junction` 的检查项
+4. `WE3` 如未被 DB 证明落库，不能在对外结论里说“已完成”
+
+### 阶段 4：独立验证
+
+验证代理只做复核，不再补需求。
+
+主代理在这个阶段只做两件事：
+
+1. 看验证结论是否达到放行标准
+2. 决定是切主口径，还是继续灰度保留兼容层
+
+## 5. 验证方式
+
+这次不能只看编译过不过。
+
+至少做下面 6 类验证。
+
+### 5.1 规则验证
+
+- `junction` 是否都满足“至少 2 条不同管线汇交”
+- `major_junction` 是否都满足“至少 3 条不同管线汇交”
+- 容差归并规则是否写清楚，并能复算
+
+### 5.2 数据库验证
+
+- 不再存在 `nested junction`
+- 不再存在 `single-station junction`
+- live DB 里能查到新口径的 `junction` / `major_junction`
+- 旧表、备份表、迁移中间表的关系可追溯
+
+### 5.3 后端接口验证
+
+- topology 相关接口返回的枢纽类型一致
+- `pipeline_packages` 输出口不再夹带旧枢纽语义
+- SCADA / topology / pipeline package 查询结果里的枢纽分类一致
+
+### 5.4 前端渲染验证
+
+- 阀室在约定 zoom 出现，不再出现“写 11、显 14”
+- `junction` / `major_junction` 在全国地图上的分层正确
+- 编辑器和全国地图里，同一枢纽的类型与名称一致
+
+### 5.5 脚本验收验证
+
+- `add_sj2.py` 跑完后，`SJ2` 相关记录数量符合预期
+- `add_we3.py` 跑完后，DB 能查到 `WE3` / `we3` 相关结果
+- `reimport_sj23.py` 跑完后，`SJ2` / `SJ3` 枢纽能被重新正确计算
+- `verify_junctions.py` 输出里能直接看出新增了哪些系统、站点、管段、枢纽
+
+### 5.6 回归验证
+
+- 不影响现有非枢纽站点渲染
+- 不影响 topology 基础图构建
+- 不影响历史查询页的基本可用性
+- 旧兼容逻辑在灰度阶段仍可回退
+
+## 6. 风险与回退点
+
+### 主要风险
+
+1. 新枢纽定义会直接推翻旧 `junction_groups` 的部分业务含义，历史接口和编辑器交互都可能受影响。
+2. 如果交叉容差没定好，可能误把临近站点当成交叉点，或者把真实交叉拆散。
+3. 一旦把 `major_junction` 正式入模，地图 marker、聚合、筛选逻辑都会被连带影响。
+4. 如果先改前端再改后端，短期会进入“页面看着像对，事实层还是乱”的假稳定状态。
+5. live DB 直接重编有操作风险，必须先有备份和校验，不然回滚会很难看。
+
+### 回退点
+
+1. 先备份旧 `junction_groups` 及相关事实表，再执行重编。
+2. 新枢纽结果先落到新表或新字段，不直接覆盖主读口。
+3. 后端先兼容输出 `junction` / `major_junction` 双口径，再逐步删旧逻辑。
+4. 前端先兼容新旧字段，再切主消费口。
+5. `WE3` 相关导入如果校验不过，回退为“脚本存在，但不宣称已完成落库”。
+
+## 7. 最终交付物
+
+这次建议最终交付下面这些东西。
+
+1. 一份统一规则说明
+   - 讲清楚什么叫 `junction`，什么叫 `major_junction`
+2. 三份侦察结论
+   - 数据库侦察
+   - 编辑器链路侦察
+   - 全国地图与导入闭环侦察
+3. 一份主代理收敛结论
+   - 讲清楚最终选了哪条实施方向，为什么
+4. 一套重编与校验脚本
+   - `rebuild_junctions.py`
+   - `verify_junctions.py`
+5. 一套后端统一输出改动
+   - DB 事实层
+   - topology / pipeline packages / editor 路由收口
+6. 一套前端消费收口改动
+   - 全国地图一套规则
+   - 编辑器一套语义
+7. 一份独立验证结论
+   - 讲清楚是否真正修复、是否存在回归
+8. 每个实施阶段一份“人话版总结” Markdown
+   - 必含：改动目标、改动前的问题、改动后的结果、验证方式、影响范围、风险点
+   - 必含：前后对比图
+   - 如果当前阶段确实没有合适截图，必须明确写原因，并用前后对比表或关键代码片段替代
+
+## 推荐落地顺序
+
+如果现在就开工，我建议按这个顺序推进：
+
+1. 主代理先冻结“枢纽定义”和“编辑器权限边界”
+2. 三个侦察代理并行出证据
+3. 主代理只选一个最可信方向进入实施
+4. 实施代理先重编事实层，再收后端，再收前端，最后补脚本闭环
+5. 验证代理独立复核，主代理决定是否切主口径
+
+一句话总结：
+
+**先改“定义和事实层”，再改“服务和前端消费层”，最后补“脚本和验收闭环”。**
*** Add File: F:\smartgas-grid\docs\2026-04-04-统一整改方案_枢纽重编版-编制总结_人话版.md
+# 这次把“统一整改方案”重写成了什么样
+
+## 改动目标
+
+把 `2026-04-04-统一整改方案_枢纽重编版.md` 从“方向基本对，但还偏概念化”改成“可以直接拿来排兵布阵和落执行”的版本。
+
+这次重点补了 4 件事：
+
+- 加上主代理，不再只有子代理
+- 给不同难度工作配不同模型和推理强度
+- 把执行顺序写成可落地的阶段推进，而不是泛泛建议
+- 把每阶段要交什么、怎么验、怎么回退写清楚
+
+## 改动前的问题
+
+原文的问题不是思路错。
+
+原文主要差在这几处：
+
+- 已经有“并行侦察 + 单点实施 + 独立验证”的骨架，但主代理不够明确
+- 没把不同代理对应的模型档位写出来
+- 涉及文件大体对，但少了统一输出口、转换层、拓扑计算等二级影响面
+- 对“什么时候能开工、先做哪一步、谁拍板”写得还不够硬
+
+## 改动后结果
+
+现在这份方案已经补成下面这个结构：
+
+1. 主目标
+2. 上下文与已知信息
+3. 子代理拆分
+4. 执行顺序
+5. 验证方式
+6. 风险与回退点
+7. 最终交付物
+
+另外单独补了：
+
+- 主代理职责和放行权
+- 每个代理的模型与推理强度建议
+- 为什么这个角色用这个模型
+- 侦察、实施、验证之间的边界
+- 每个阶段的产物和验收点
+
+## 为什么这么改
+
+原因很直接。
+
+这轮不是单文件修修补补，而是枢纽定义、数据库事实、后端输出、前端编辑器、全国地图、导入脚本一起收口。
+
+这种事如果没有主代理，最后大概率会变成：
+
+- 侦察代理各说各的
+- 实施代理顺手改太多
+- 验证代理只能在结果后面擦屁股
+
+所以这次把“谁看全局、谁收证据、谁负责真正改、谁独立复核”拆清楚了。
+
+## 影响范围
+
+本次改动只影响文档，不直接改业务代码。
+
+直接影响的文件：
+
+- `F:\smartgas-grid\docs\2026-04-04-统一整改方案_枢纽重编版.md`
+
+新增的总结文件：
+
+- `F:\smartgas-grid\docs\2026-04-04-统一整改方案_枢纽重编版-编制总结_人话版.md`
+
+文档中明确纳入关注的关键模块包括：
+
+- `backend/app/services/junction_groups.py`
+- `backend/app/services/topology.py`
+- `backend/app/services/topology_computation.py`
+- `backend/app/services/topology_correction.py`
+- `backend/app/routers/topology_editor.py`
+- `backend/app/routers/pipeline_packages.py`
+- `src/views/MapTopologyView.tsx`
+- `src/views/GlobalPipelineView.tsx`
+- `src/utils/hierarchyRenderer.ts`
+- `src/utils/mapRenderer.ts`
+- `backend/scripts/add_sj2.py`
+- `backend/scripts/add_we3.py`
+- `backend/scripts/reimport_sj23.py`
+- `backend/check_db.py`
+
+## 验证结果
+
+这次做的是方案文档重编，所以验证方式以文档验收为主。
+
+已完成验证：
+
+- 文档按 UTF-8 读取正常
+- 结构符合你要求的 7 段式
+- 方案里已经明确主代理
+- 方案里已经明确不同工作难度对应的模型和推理强度
+- 方案里已经补上执行顺序、验证方式、风险和回退点
+- 方案里已经写明每阶段结束后需要产出“人话版总结”Markdown
+
+## 前后对比
+
+这次不适合放前后截图。
+
+原因很简单：当前任务是编制方案文档，不是页面视觉改版，也没有一个自然的 UI 画面对比点。
+
+所以这里用表格替代：
+
+| 对比项 | 改前 | 改后 |
+| --- | --- | --- |
+| 主代理 | 没单独立起来 | 已明确主代理职责、放行权、模型档位 |
+| 模型分配 | 没写清 | 已按工作难度区分主代理、侦察、实施、验证模型 |
+| 影响范围 | 主要覆盖第一层文件 | 已补上统一输出口、转换层、拓扑计算等二级影响面 |
+| 执行顺序 | 有方向，但偏描述性 | 已改成阶段化执行顺序和产物清单 |
+| 验收闭环 | 有验证思路 | 已明确规则、DB、接口、前端、脚本、回归 6 类验证 |
+
+## 回退方式
+
+如果你后面觉得这版方案还要再压缩，回退很简单：
+
+- 直接回看 Git 或文件历史里的上一版文档
+- 或者只保留这版里的“主代理 + 模型档位 + 执行顺序”三块，其余继续沿用旧文案
*** End Patch

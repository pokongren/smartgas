*** Begin Patch
*** Add File: F:\smartgas-grid\docs\2026-04-08-WE1第一张图基线对比第4阶段第1轮总结_人话版.md
+# WE1 第一张图基线对比第4阶段第1轮总结
+
+## 1. 这一轮做了什么
+1. 把“基线快照”正式接进了第一张图左侧仿真面板，现在你不只是能看当前这次跑出来的结果，还能选一条历史基线快照做对比。
+2. 左侧新增了“基线对比”区块，会直接告诉你当前结果和基线结果差了多少，至少能看供给、需求、平均利用率、告警数这几项摘要差值。
+3. 节点详情和连线详情里也补上了“当前 vs 基线”。也就是你点一个站点，不只看当前压力，还能看基线压力和压力差值；点一条线，不只看当前流量和利用率，也能看基线值和差值。
+4. 顺手把这一块仿真面板里残留的乱码说明一起换成人话了，避免后面做场景覆盖时还得一边猜一边用。
+
+## 2. 为什么这么做
+1. 现在第一张图已经能正式落图、能点线解释，但如果没有“当前 vs 基线”，你只能知道“现在是什么样”，不知道“比基线坏了多少”。
+2. 先把基线对比收住，后面的试运行覆盖才有意义。因为试运行不是只看能不能跑起来，而是要看不同场景跟基线比有没有明显变化、变化是不是合理。
+3. 这一轮故意只做“对比”，不做场景批量覆盖，是为了保证阶段边界清楚，先把对比链路压稳。
+
+## 3. 影响范围
+1. 前端 Hook：`src/hooks/useSimulation.ts`，补了基线 overlay 的正式暴露。
+2. 前端页面：`src/views/MapTopologyView.tsx`，新增基线选择、对比摘要、节点差值、连线差值。
+3. 这轮没有改 solver，没有改后端 overlay 契约，也没有扩到第二张图。
+
+## 4. 前后对比
+| 对比项 | 之前 | 现在 |
+| --- | --- | --- |
+| 左侧仿真面板 | 只能看当前结果和映射摘要 | 能选基线快照，看当前 vs 基线摘要差值 |
+| 节点详情 | 只看当前压力和告警 | 还能看基线压力和压力差值 |
+| 连线详情 | 只看当前流量、利用率、告警 | 还能看基线流量、基线利用率和差值 |
+| 结果判读 | 只能说“当前怎么样” | 能说“相对基线变好了还是变坏了” |
+
+## 5. 验证怎么做的
+1. 跑了 `npm run build`，前端构建通过。
+2. 跑了 `python backend/scripts/verify_we1_phase34_final.py`，联合回归通过。
+3. 补了一轮独立 subagents 复核，结论是第4阶段第1轮通过，确认基线快照、摘要差值、点线差值都已接上，而且页面层没有越界到试运行覆盖或第二张图。
+
+## 6. 这轮没放截图的原因
+1. 这轮主要是把对比链路和详情差值收口，当前线程里没有同步做页面截图。
+2. 这次更适合补两种截图：一张左侧“基线对比”区块，一张右侧“节点/连线详情里的当前 vs 基线”。如果你要，我下一轮可以直接补这两张。
*** Add File: F:\smartgas-grid\docs\2026-04-08-WE1第一张图试运行覆盖第4阶段第2轮总结_人话版.md
+# WE1 第一张图试运行覆盖第4阶段第2轮总结
+
+## 1. 这一轮做了什么
+1. 把“试运行覆盖”正式接进了第一张图左侧仿真面板。现在能直接看到已覆盖多少个场景、还差多少个场景没跑。
+2. 新增了两个操作入口：一个是“补齐未覆盖场景”，会把还没覆盖的场景批量补跑；另一个是每个场景右侧单独的“补跑/重跑”按钮，方便只重跑某一个场景。
+3. 每个场景条目也会显示最近一次运行时间和快照数量，这样你能直接判断这个场景是不是已经跑过、是不是需要补跑。
+4. 这一轮仍然走正式 `useSimulation` 主链，不是另起一套“测试专用入口”。补跑完后，最后一条结果会自动切成当前场景，方便继续看图和看解释。
+
+## 2. 为什么这么做
+1. 前一轮已经能做基线对比了，但如果场景覆盖不全，你还是不知道这套东西在几个典型场景里是不是都稳定。
+2. 试运行覆盖的目标不是做大而全的自动巡检，而是先把第一张图主链里最关键的几个场景跑通、留痕、可回看。
+3. 这一轮也刻意没扩到全网推广、批处理平台和自动编排器，就是为了先把 WE1 主样板的“能补跑、能留快照、能看结果”做稳。
+
+## 3. 影响范围
+1. 前端页面：`src/views/MapTopologyView.tsx`。
+2. 复用了已有 Hook 能力：`src/hooks/useSimulation.ts`。
+3. 这轮没有改后端求解，没有改全网逻辑，也没有碰第二张图。
+
+## 4. 前后对比
+| 对比项 | 之前 | 现在 |
+| --- | --- | --- |
+| 场景覆盖感知 | 只能靠人自己猜哪些场景跑过 | 面板直接显示已覆盖数、待覆盖数 |
+| 批量补跑 | 没有正式入口 | 一键补齐未覆盖场景 |
+| 单场景补跑 | 只能手工切场景再跑 | 每条场景都能单独补跑或重跑 |
+| 留痕判断 | 得自己去快照列表翻 | 每条场景能直接看到快照数量和最近运行时间 |
+
+## 5. 验证怎么做的
+1. 跑了 `npm run build`，前端构建通过。
+2. 跑了 `python backend/scripts/verify_we1_phase34_final.py`，联合回归通过。
+3. 又走了一轮独立 subagents 复核，结论是第4阶段第2轮通过，确认试运行覆盖面板、单场景补跑、批量补齐、快照留痕都已接上，页面层没有越界到全网推广或自动巡检编排器。
+
+## 6. 这轮没放截图的原因
+1. 这轮主要是面板操作入口和覆盖状态收口，当前线程里没有同步做点按截图。
+2. 如果后面要补图，最值得截的是左侧“试运行覆盖”区块，能一眼看出已覆盖 / 待覆盖和每条场景的补跑按钮。
*** End Patch

*** Begin Patch
*** Update File: F:/smartgas-grid/src/views/MapTopologyView.tsx
@@
     const importedOnceRef = useRef(false)
     const appliedFocusNodeRef = useRef<string | null>(null)
+    const simulationSnapshotsBootstrappedRef = useRef(false)
@@
     useEffect(() => {
         if (steadySimError) {
             setStatusMsg(`正式仿真失败：${steadySimError}`)
         }
     }, [steadySimError])
+    useEffect(() => {
+        if (activeTab !== 'simulation') return
+        if (simulationSnapshotsBootstrappedRef.current) return
+        simulationSnapshotsBootstrappedRef.current = true
+        void refreshSteadySnapshots().catch(error => {
+            console.error('[MapTopologyView] 初始化正式仿真快照失败:', error)
+        })
+    }, [activeTab, refreshSteadySnapshots])
@@
-    const shouldApplySimulationVisuals = activeTab === 'simulation' && editMode !== 'merge' && highlightedMarkersRef.current.size === 0
+    const shouldApplySimulationVisuals = Boolean(steadyOverlay) && editMode !== 'merge' && highlightedMarkersRef.current.size === 0
@@
-                        {([
-                            { id: 'edit' as PanelTab, label: '编辑', icon: 'edit' },
+                        {([
+                            { id: 'simulation' as PanelTab, label: '主仿真', icon: 'science' },
+                            { id: 'edit' as PanelTab, label: '编辑', icon: 'edit' },
                             { id: 'validate' as PanelTab, label: '验证', icon: 'verified' },
                             { id: 'search' as PanelTab, label: '搜索', icon: 'search' },
-                            { id: 'simulation' as PanelTab, label: '仿真', icon: 'science' },
                             { id: 'centrality' as PanelTab, label: '枢纽', icon: 'stars' },
                             { id: 'cutoff' as PanelTab, label: '截断', icon: 'cut' },
                         ]).map(tab => (
*** End Patch

*** Begin Patch
*** Update File: src/views/MapTopologyView.tsx
@@
-                                {steadyOverlay && <div className="rounded-lg border border-emerald-500/20 bg-emerald-950/10 p-3 space-y-2"><div className="text-[10px] uppercase tracking-wider text-emerald-200">??????</div><div className="rounded-lg bg-black/15 px-3 py-2 text-[11px] leading-5 text-gray-300"><div>???{steadyScenarioOption?.label ?? steadyOverlay.scenario_id}</div><div>?????{steadyOverlay.run_id}</div><div>?????{formatDateTimeLabel(steadyOverlay.generated_at)}</div><div>?????{SOLVER_STATUS_LABELS[steadyOverlay.solver_status]}</div></div><div className="grid grid-cols-2 gap-2 text-xs"><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">???</div><div className="mt-1 font-semibold text-white">{steadyOverlay.summary.total_supply.toFixed(2)}</div></div><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">???</div><div className="mt-1 font-semibold text-white">{steadyOverlay.summary.total_demand.toFixed(2)}</div></div><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">?????</div><div className="mt-1 font-semibold text-white">{steadyOverlay.summary.avg_utilization.toFixed(3)}</div></div><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">???</div><div className="mt-1 font-semibold text-amber-300">{steadyOverlay.summary.alert_count}</div></div></div><div className="grid grid-cols-2 gap-2 text-xs"><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">????</div><div className="mt-1 font-semibold text-white">{simulationVisualSummary?.highlightedNodeCount ?? 0}</div></div><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">????</div><div className="mt-1 font-semibold text-white">{simulationVisualSummary?.highlightedEdgeCount ?? 0}</div></div></div></div>}
+                                {steadyOverlay && <div className="rounded-lg border border-emerald-500/20 bg-emerald-950/10 p-3 space-y-2"><div className="text-[10px] uppercase tracking-wider text-emerald-200">运行结果摘要</div><div className="rounded-lg bg-black/15 px-3 py-2 text-[11px] leading-5 text-gray-300"><div>当前场景：{steadyScenarioOption?.label ?? steadyOverlay.scenario_id}</div><div>运行编号：{steadyOverlay.run_id}</div><div>生成时间：{formatDateTimeLabel(steadyOverlay.generated_at)}</div><div>求解状态：{SOLVER_STATUS_LABELS[steadyOverlay.solver_status]}</div></div><div className="grid grid-cols-2 gap-2 text-xs"><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">总供给</div><div className="mt-1 font-semibold text-white">{steadyOverlay.summary.total_supply.toFixed(2)}</div></div><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">总需求</div><div className="mt-1 font-semibold text-white">{steadyOverlay.summary.total_demand.toFixed(2)}</div></div><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">平均利用率</div><div className="mt-1 font-semibold text-white">{steadyOverlay.summary.avg_utilization.toFixed(3)}</div></div><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">告警数</div><div className="mt-1 font-semibold text-amber-300">{steadyOverlay.summary.alert_count}</div></div></div><div className="grid grid-cols-2 gap-2 text-xs"><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">命中节点</div><div className="mt-1 font-semibold text-white">{simulationVisualSummary?.highlightedNodeCount ?? 0}</div></div><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">命中连线</div><div className="mt-1 font-semibold text-white">{simulationVisualSummary?.highlightedEdgeCount ?? 0}</div></div></div></div>}
@@
-                                <div className="rounded-lg border border-violet-500/20 bg-violet-950/10 p-3 space-y-3 text-[11px] text-gray-300"><div><div className="text-[10px] uppercase tracking-wider text-violet-200">????</div><div className="mt-1 text-gray-400">?????????????????????????</div></div>{!steadyBaselineSnapshotRunId && <div className="rounded-lg bg-black/15 px-3 py-2 leading-5 text-gray-400">???????????????????????</div>}{steadyBaselineSnapshotRunId && !steadyComparison && <div className="rounded-lg bg-black/15 px-3 py-2 leading-5 text-gray-400">??????????????????????????????????</div>}{steadyComparison && <><div className="rounded-lg bg-black/15 px-3 py-2 leading-5"><div>?????{steadyComparison.current_scenario_id} | {steadyComparison.current_run_id.slice(0, 12)}</div><div>?????{steadyComparison.baseline_scenario_id} | {steadyComparison.baseline_run_id.slice(0, 12)}</div></div><div className="grid grid-cols-2 gap-2 text-xs"><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">????</div><div className={`mt-1 font-semibold ${steadyComparison.summary_delta.total_supply.delta >= 0 ? 'text-emerald-300' : 'text-red-300'}`}>{formatSignedNumber(steadyComparison.summary_delta.total_supply.delta, 2)}</div></div><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">????</div><div className={`mt-1 font-semibold ${steadyComparison.summary_delta.total_demand.delta >= 0 ? 'text-emerald-300' : 'text-red-300'}`}>{formatSignedNumber(steadyComparison.summary_delta.total_demand.delta, 2)}</div></div><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">?????</div><div className={`mt-1 font-semibold ${steadyComparison.summary_delta.avg_utilization.delta > 0 ? 'text-amber-300' : 'text-cyan-200'}`}>{formatSignedNumber(steadyComparison.summary_delta.avg_utilization.delta, 3)}</div></div><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">????</div><div className={`mt-1 font-semibold ${steadyComparison.summary_delta.alert_count.delta > 0 ? 'text-red-300' : 'text-emerald-300'}`}>{formatSignedNumber(steadyComparison.summary_delta.alert_count.delta, 0)}</div></div></div></>}
+                                <div className="rounded-lg border border-violet-500/20 bg-violet-950/10 p-3 space-y-3 text-[11px] text-gray-300"><div><div className="text-[10px] uppercase tracking-wider text-violet-200">基线对比</div><div className="mt-1 text-gray-400">用基线快照对照当前结果，方便看故障前后或限流前后的变化。</div></div>{!steadyBaselineSnapshotRunId && <div className="rounded-lg bg-black/15 px-3 py-2 leading-5 text-gray-400">先在上面选一条基线快照，这里才会显示前后差值。</div>}{steadyBaselineSnapshotRunId && !steadyComparison && <div className="rounded-lg bg-black/15 px-3 py-2 leading-5 text-gray-400">基线已选，但当前结果还没形成可对比数据；先运行主仿真或加载一条正式快照。</div>}{steadyComparison && <><div className="rounded-lg bg-black/15 px-3 py-2 leading-5"><div>当前结果：{steadyComparison.current_scenario_id} | {steadyComparison.current_run_id.slice(0, 12)}</div><div>基线结果：{steadyComparison.baseline_scenario_id} | {steadyComparison.baseline_run_id.slice(0, 12)}</div></div><div className="grid grid-cols-2 gap-2 text-xs"><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">供给差值</div><div className={`mt-1 font-semibold ${steadyComparison.summary_delta.total_supply.delta >= 0 ? 'text-emerald-300' : 'text-red-300'}`}>{formatSignedNumber(steadyComparison.summary_delta.total_supply.delta, 2)}</div></div><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">需求差值</div><div className={`mt-1 font-semibold ${steadyComparison.summary_delta.total_demand.delta >= 0 ? 'text-emerald-300' : 'text-red-300'}`}>{formatSignedNumber(steadyComparison.summary_delta.total_demand.delta, 2)}</div></div><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">利用率差值</div><div className={`mt-1 font-semibold ${steadyComparison.summary_delta.avg_utilization.delta > 0 ? 'text-amber-300' : 'text-cyan-200'}`}>{formatSignedNumber(steadyComparison.summary_delta.avg_utilization.delta, 3)}</div></div><div className="rounded-lg bg-black/15 px-3 py-2"><div className="text-[10px] text-gray-500">告警差值</div><div className={`mt-1 font-semibold ${steadyComparison.summary_delta.alert_count.delta > 0 ? 'text-red-300' : 'text-emerald-300'}`}>{formatSignedNumber(steadyComparison.summary_delta.alert_count.delta, 0)}</div></div></div></>}
*** End Patch

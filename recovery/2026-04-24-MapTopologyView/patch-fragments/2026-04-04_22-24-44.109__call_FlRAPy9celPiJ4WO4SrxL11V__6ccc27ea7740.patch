*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
+## 2026-04-05 06:24:01 +08:00
+
+- 本轮先按入口规则落到主目录执行：初始 `cwd=C:\Users\Administrator\.codex\worktrees\789d\smartgas-grid`，切换后 `cwd=F:\smartgas-grid`，`git rev-parse --show-toplevel=F:/smartgas-grid`，`.git` 是主目录里的真实目录，不是文件跳转。本轮结论只基于 `F:\smartgas-grid`，没有拿 detached worktree 代替事实来源。
+- 入口通过，不是 `automation env mismatch / worktree-path blockage`，也不是空壳 worktree。`git worktree list` 里主目录 `F:/smartgas-grid` 停在分支提交 `eaf6ac4`，`.codex/worktrees/*` 仍大多停在 `6410a10` detached，只能当背景噪音。
+- 本轮主要是主目录里的未提交代码变化 + 数据库内容变化 + 本地实验混合，且明确带着 AI 模块链路变化，不是单纯 docs 更新。`git status --short` 同时命中地图层级、runtime 枢纽、数据库/导入脚本、AI 助手、docs，也混着 `.npm-cache`、shadow json/db、临时脚本等本地产物。
+- 地图层级：`src/utils/hierarchyRenderer.ts` 里的统一 LOD 仍是 `critical/high/medium=6/10/14`，并带 `valveMinZoom=11`、`expandClusterZoom=12`；重要性口径是 `source/图层起点/关键枢纽=critical`、`compressor/storage/high-level junction=high`、`distribution/major_junction=medium`、`valve=low`。`src/utils/mapRenderer.ts` 已真实接入 `getNodeLODStrategy()` / `shouldShowNodeAtZoom()`。这轮要修正旧判断：`src/views/GlobalPipelineView.tsx` 是通过 `MapView -> mapRenderer` 吃到统一规则的；`src/views/MapTopologyView.tsx` 仍是纯底图 + 自己的 `TOPO_SIZES` 和 collapsed graph 渲染，没有直接接统一 zoom/LDO 口径，所以现在准确说法是“地图链路更接近统一，拓扑编辑视图还没完全跟齐”，不要说成“两边已经完全统一”。
+- 枢纽/runtime：`backend/app/services/junction_groups.py` 继续走“`junction_groups_rebuilt` 优先、旧 `junction_groups` 兼容兜底、manual overlay 单独覆盖”的结构，支持展开 `JUNCTION-x` / `junction-x`，并在 runtime 合并时先用 overlay 覆盖相交站点，再把剩余 rebuilt 结果保留下来。当前 `manual_overlay=1` 的旧表记录有 2 条，说明手工覆盖层已经和 runtime 事实层分开了。`backend/app/routers/topology_editor.py` 的 `/api/topology/junctions` 直接返回 `load_runtime_junction_groups()`，并带 `manual_editing_enabled` 与 `mode=runtime_readonly|legacy_editable`。
+- 数据库事实这轮必须按实测更新，不要沿用旧 memory 里的老数字：`smartgas.db` 当前是 `stations=1272`、`pipelines=1268`、`pipeline_systems=14`、`junction_groups=18`、`junction_groups_backup_20260404=16`、`junction_groups_rebuilt=7`，其中 rebuilt 里 `junction=6`、`major_junction=1`。`WE3/SJ2/SJ3` 前缀快照分别是 `16/15`、`70/69`、`71/70`（站/段）。`layers_config` 不是单独表，仍是 `pipeline_systems.layers_config` 字段。
+- 导入脚本和实验产物要继续分开说：`backend/scripts/add_sj2.py`、`backend/scripts/add_we3.py`、`backend/scripts/reimport_sj23.py` 都在直接写 `backend/data/smartgas.db`，补 `pipeline_systems.layers_config`，并把新增管段 `category` 收口到 `trunk/branch`，更像主库补数/重导链路的一部分；`backend/scripts/rebuild_junctions.py`、`backend/scripts/rebuild_sj3_from_excel.py`、`backend/scripts/verify_junctions.py` 以及 `backend/data/junction-rebuild-*.json`、`backend/data/smartgas.rebuild-preview.db`、`backend/data/raw_excel_index.db` 仍按本地重编/校验实验处理，不能当正式基线。
+- AI 链路：前端 `src/components/ai-assistant/useAiAssistantChat.ts` 会把 `context + analysis_mode=subagents` 发到 `/api/ai-assistant/chat`，`useAiAssistantPageContext.ts` 注入页面 route/title/module/summary；后端 `backend/app/routers/ai_assistant.py` 会按 `context/analysis_mode` 在 legacy 和 orchestrated 流程间分流，并串上 `backend/app/services/ai_analysis_orchestrator.py`、`analysis_validator.py`、`fix_task_router.py`。但这轮要继续保守表述：已跟踪的是 `ai_assistant.py`、`ai_client.py`、`AiAssistant.tsx`、`src/services/api.ts`，未跟踪的是 orchestrator/validator/router 和前端两个 hooks，所以它现在是“主目录里已经能接起来的本地未提交能力”，不是正式入基线能力。`backend/app/services/ai_client.py` 仍默认 `AI_BASE_URL=https://api.openai.com/v1`、`AI_DEFAULT_MODEL=gpt-3.5-turbo`，缺 `AI_API_KEY` 时走 fallback。
+- docs 变化不是装饰：`docs/TOPOLOGY_COMPUTATION_USAGE.md`、`docs/TOPOLOGY_SETUP_GUIDE.md` 这轮继续把现实口径写清楚，明确本地默认后端端口是 `8080`，并区分普通 REST `/api/...`、拓扑编辑 `/api/topology/...`、拓扑计算 `/topology/...` 这几条链。下轮别再拿旧的 `8000` 或“全都走 /api/topology”去套现状。
+- 验证口径：这轮已单独复核三件事，后续继续沿用。1）没有把 detached worktree 当主目录事实。2）没有把 `MapTopologyView` 的本地图元渲染误说成已经接入统一 LOD。3）没有把 AI 未跟踪文件拼起来的本地链路误说成正式基线。
+
 ## 2026-04-05 02:23:59 +08:00
*** End Patch

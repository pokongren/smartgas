*** Begin Patch
*** Add File: F:\smartgas-grid\docs\2026-04-04-地图拓扑手工捏合工作流修复总结.md
+# 地图拓扑手工捏合工作流修复总结（人话版）
+
+## 这次做了什么
+
+这次把地图拓扑里的“手工捏合”从只有入口按钮，补成了可用的完整流程：
+
+- `merge` 模式下，点底层站点可以加入/移除待捏合集合。
+- 左侧面板能看到本次已选站点列表、自动命名建议、名称/备注输入。
+- 可以直接创建手工捏合，调用前端既有接口 `topologyEditorApi.createJunctionGroup`。
+- 可以在面板里拆分“手工捏合”记录，调用 `topologyEditorApi.deleteJunctionGroup`。
+- 运行时重编枢纽和手工捏合做了区分：重编结果只读，不会误删。
+- 节点详情侧栏里也补了“来源识别 + 可拆分按钮（仅手工捏合可拆）”。
+
+## 为什么这么改
+
+之前最直接的问题是：用户能看到“兼容手工捏合”入口，但从入口进去没有完整闭环，尤其是“选点 -> 创建 -> 回看/拆分”这条链路不完整，容易造成“按钮像能用，实际不好用”。
+
+这次就按真实操作路径收口，重点做两件事：
+
+1. 把操作闭环补齐，能真的创建和拆分。
+2. 把“运行时重编”和“手工覆盖”分层展示，避免误操作把运行时结果当成手工记录去删。
+
+## 影响范围
+
+- 主要改动文件：
+  - `F:\smartgas-grid\src\views\MapTopologyView.tsx`
+- 本次没有改后端接口协议；前端继续使用既有 `topologyEditorApi`。
+
+## 前后对比
+
+| 对比项 | 修改前 | 修改后 |
+| --- | --- | --- |
+| 手工捏合入口 | 有按钮，但工作流不完整 | 有完整闭环（选点、创建、拆分、回看） |
+| merge 模式选点 | 不稳定/不可见 | 支持多选，面板可见已选站点 |
+| 创建动作 | 入口不完整 | 直接调 `createJunctionGroup` 落地 |
+| 拆分动作 | 容易和运行时枢纽混淆 | 仅允许拆分手工捏合，运行时重编只读 |
+| 详情面板语义 | 来源不清楚 | 明确显示来源、枢纽类型，并给可拆分入口 |
+
+## 验证情况
+
+这次做了两层验证：
+
+1. 代码链路核对：确认 `merge` 模式下有“多选 -> 创建 -> 刷新 -> 可拆分”的完整调用链。
+2. 项目构建验证：执行 `npm run build` 时，当前仓库里有**其他文件**（`TopologyView.tsx`）已有重复声明报错，导致全量构建失败；失败点不是本次改动文件。
+
+## 已知边界风险
+
+- 当前仓库存在并行改动较多，`TopologyView.tsx` 的重复声明问题会影响全量构建验收，需要主线先收掉。
+- `MapTopologyView.tsx` 历史改动较多，后续建议做一次小范围清理（同类 helper 合并、无用分支收口），降低维护成本。
*** End Patch

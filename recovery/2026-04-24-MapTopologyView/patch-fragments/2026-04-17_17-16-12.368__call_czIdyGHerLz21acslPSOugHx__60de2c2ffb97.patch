*** Begin Patch
*** Add File: docs/2026-04-18-WE1主仿真参数化第1阶段第1轮记录.md
+# 2026-04-18 WE1主仿真参数化第1阶段第1轮记录
+
+一、结论  
+第1阶段第1轮已完成，前端新增“仿真参数面板”，并把“运行主仿真”改为统一走参数覆盖入口，构建验证通过。
+
+二、实现内容  
+1. 新增参数编辑组件 `src/components/topology/SimParamEditor.tsx`，支持关键站场目标压力编辑和关键管段长度编辑。2. 在 `src/views/MapTopologyView.tsx` 接入参数状态 `nodeParamOverrides`、`edgeLengthOverrides`。3. 新增 `buildSimulationInitialInput` 与 `runMainSteadySimulation`，把页面所有“运行主仿真”入口统一到同一调用链。
+
+三、为什么这样改  
+1. 先把参数编辑和运行入口打通，后续后端字段扩展时不需要再改交互路径。2. 一个统一运行入口可以避免顶部按钮和面板按钮行为不一致。
+
+四、影响范围  
+1. 影响页面：`/map-topology` 仿真面板与顶部“运行主仿真”按钮。2. 不影响其他页面路由和已有快照机制。
+
+五、验证结果  
+1. 验证命令：`npm run build`。2. 验证结果：通过，无编译错误。3. 人工检查点：仿真面板可见“仿真参数面板”，可填写压力与长度字段。
+
+六、前后对比  
+1. 改造前：运行主仿真仅按场景触发，没有页面参数覆盖入口。2. 改造后：可在面板先填参数，再通过统一入口运行主仿真。3. 截图说明：本轮未附截图，原因是当前轮以编译与调用链打通为主，使用“变更文件+验证命令”作为等效证据。
*** End Patch

*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
 - AI 还没完全收口的点继续记住：`selection/filters` 细粒度上下文、`contextual` 模式、弹窗复用主页面真实上下文、统一 API 层收口、`[ERROR]` 协议闭环，这几块最多算半接线/预留；别说成已经完整落地。
 - 本轮运行时间：`2026-04-05 14:31:29 +08:00`
+## 2026-04-05 16:23:31 +08:00
+
+- 这轮入口继续通过，而且是直接在主目录收证据：初始会话 `cwd=C:\Users\Administrator\.codex\worktrees\5b09\smartgas-grid`，后续检查都切到 `F:\smartgas-grid`，`git rev-parse --show-toplevel=F:/smartgas-grid`。这轮结论只认 `F:\smartgas-grid`，没有沿用 detached worktree 的旧检查结果。
+- 主目录不是空壳，`.git` 在 `F:\smartgas-grid` 里是目录；`src/`、`backend/`、`docs/`、`backend/data/smartgas.db` 和点名关键文件都在。`git worktree list` 还是 1 个主目录 + 42 个 detached worktree；根目录里那个 `F?smartgas-griddocs` 继续按路径噪音看，别拿它当真实 docs 目录。
+- 当前主目录的变化口径还是“代码变化 + 数据库变化 + 本地实验 + AI 链路变化”，不是单纯 docs。现查 `git diff --stat` 是 `47 files changed, 4780 insertions(+), 2608 deletions(-)`；`docs/TOPOLOGY_COMPUTATION_USAGE.md` 和 `docs/TOPOLOGY_SETUP_GUIDE.md` 单独还是 `181 insertions / 587 deletions`，重点仍是把默认端口收口到 `8080`，并把 `/api/topology/...` 和 `/topology/...` 的并存现状写清楚。
+- 地图链路这轮没有推翻前面的判断：`src/utils/hierarchyRenderer.ts` 统一阈值还是 `critical/high/medium=6/10/14`，`valveMinZoom=11`、`expandClusterZoom=12` 也还在；`src/utils/mapRenderer.ts` 继续用 `getNodeLODStrategy()` / `shouldShowNodeAtZoom()` 做过滤；`src/views/GlobalPipelineView.tsx` 仍是 `MapView -> mapRenderer` 这条链。`src/views/MapTopologyView.tsx` 还是只拿 `MapView` 当底图，明确写了“不传 `pipelineData`”，节点大小继续走自己的 `TOPO_SIZES`，所以别把它说成已经并入统一 LOD。
+- 数据库事实这轮重查后没变：`stations=1256`、`pipelines=1251`、`pipeline_systems=13`、`junction_groups=25`、`junction_groups_rebuilt=7`，`pipeline_systems.id=cred,gn,gs,jxlz,ncsh,pt,sj2,sj3,sj4,we1,we2,zg,zm`，`WE3=0/0`，`SJ2=70/69`，`SJ3=71/70`。`junction_groups` 仍只有 `id/name/description/station_ids` 四列，`manual_overlay` 不是表字段；`layers_config` 仍挂在 `pipeline_systems.layers_config`。
+- `backend/check_db.py` 现在可以继续按验收脚本使用，不只是临时查看器。实跑结果还是 `issue_count=0`、`major_junction_count=0`、`junction_groups_rebuilt.latest.generated_at=2026-04-04T17:17:43.361829+00:00`；同时要记住 `junction_groups_rebuilt.id=7` 的 description 还写着 `cred,sj2,we3`，但真实成员只有 `CRED-2160` 和 `SJ2-61`，以后判断 runtime 事实优先看真实成员，不看 description 残字。
+- 导入脚本和数据库事实继续分开讲：`backend/scripts/add_sj2.py`、`backend/scripts/add_we3.py`、`backend/scripts/reimport_sj23.py` 都是在写主库和 `pipeline_systems.layers_config`，但当前主库里 `we3` 还是缺失，说明“脚本已改”不等于“数据库已落”。其中 `reimport_sj23.py` 更像正式重导链路，`add_sj2.py/add_we3.py` 还是更像局部补数脚本。
+- AI 链路这轮再收紧一句：前端 `src/components/ai-assistant/useAiAssistantChat.ts` 会在有页面上下文时默认带 `analysis_mode=subagents` 打 `/api/ai-assistant/chat`，`useAiAssistantPageContext.ts` 注入 `route/title/module/summary`；后端 `backend/app/routers/ai_assistant.py` 按 `context/analysis_mode` 在 legacy 和 orchestrated 间分流，`backend/app/services/ai_client.py` 默认仍是 `AI_BASE_URL=https://api.openai.com/v1`、`AI_DEFAULT_MODEL=gpt-3.5-turbo`，缺 `AI_API_KEY` 直接 fallback。`backend/app/services/ai_analysis_orchestrator.py`、`analysis_validator.py`、`fix_task_router.py` 以及前端两个 hook 这轮仍是未跟踪文件，所以对外只能继续叫“主目录里的本地未提交半接线能力”，不能说正式上线。
+- 下轮继续守 4 条边界：1) 只认 `F:\smartgas-grid` 主目录事实；2) 先重查数据库和 `check_db.py` 输出，不复用旧 memory 数字；3) 不把 `MapTopologyView.tsx` 说成已并入统一 LOD；4) 不把未跟踪 AI 文件或脚本改动直接说成正式基线。
+- 本轮运行时间：`2026-04-05 16:23:31 +08:00`
*** End Patch

*** Begin Patch
*** Add File: F:\smartgas-grid\docs\2026-04-07-WE1下一阶段-试点转产品化四步方案.md
+# WE1 下一阶段方案：试点转产品化四步推进
+
+## 主目标
+把已经跑通的 WE1 主样板、辅样板和稳态求解能力，从“技术上能算”推进到“软件里能稳定用、能回看、能扩样板”，形成试点可演示、可验收、可继续扩展的一条产品化工作线。
+
+这份方案不是在原来的 1 到 5 阶段后面再硬接一个“阶段 6”，而是基于阶段 5 已封板的结果，单独开一条新工作线。
+
+## 上下文与已知信息
+- WE1 主样板和辅样板都已经能装成统一 `solver input`。
+- 稳态求解、故障趋势、分流趋势和联合回归已经收口。
+- 后端已经有试点 seed、pilot service、solver input 和验证脚本。
+- 当前还缺的是产品层闭环：结果怎么存、前端怎么统一消费、试点怎么回放、后面怎么扩样板。
+- 仓库当前是脏工作区，后续实现时不能回滚用户已有改动，尤其前端和路由层。
+
+## 主代理与子代理分工
+### 主代理
+- 负责定义这条新工作线的边界，不去改坏已经封板的 1 到 5 阶段。
+- 决定每一步先收哪一层，再进入下一层。
+- 汇总验证结果，判断是否可以进入扩样板或试运行。
+
+### 侦察代理
+- 看结果对象现在从哪一层产出，是否已经具备落库字段。
+- 看前端地图、拓扑图、仿真面板现在各自消费哪套字段。
+- 看现有脚本和接口里，哪些地方已经能复用，哪些地方还在写死试点数据。
+
+### 实施代理
+- 只让一个实施口修改结果契约、落库、前端消费链，避免同一片代码多人并改。
+- 每一步改完都补正式文档和人话总结。
+
+### 验证代理
+- 独立跑接口、页面、快照和回归脚本。
+- 独立确认“结果一致、展示一致、回放可用、历史可查”。
+
+## 修改逻辑
+现在 WE1 的核心问题已经不是“算不算得出来”，而是“算出来以后软件怎么真正吃进去”。所以下一阶段不优先再改求解公式，而是把结果从服务层一路打通到存储层、接口层、前端展示层和试点运维层。这样后面不管接西一更多主干段，还是接新的分输支线，都有一条固定生产链，不会每扩一个样板就再临时拼一次。
+
+```mermaid
+mindmap
+  root((WE1 下一阶段))
+    第1步 结果契约收口
+      统一 summary nodes edges meta
+      明确 run_id scenario_id pilot_id
+      固定快照结构
+    第2步 结果回写与快照
+      落库或归档
+      支持历史查询
+      支持回放比对
+    第3步 前端统一展示
+      地图和拓扑图共用结果
+      面板展示压力流量告警
+      场景切换一致
+    第4步 试运行与扩样板
+      跑一轮试点验收
+      固定巡检脚本
+      准备复制到下一段
+```
+
+## 执行顺序
+### 第 1 步：结果契约收口
+先把 WE1 求解结果定义成真正稳定的产品对象，不再只停留在“某个接口能返回结果”。
+
+建议动作：
+- 盘点 [`F:\smartgas-grid\backend\app\services\topology_simulation.py`](F:\smartgas-grid\backend\app\services\topology_simulation.py)、[`F:\smartgas-grid\backend\app\routers\topology_simulation.py`](F:\smartgas-grid\backend\app\routers\topology_simulation.py)、[`F:\smartgas-grid\backend\app\services\we1_solver_input_service.py`](F:\smartgas-grid\backend\app\services\we1_solver_input_service.py) 的结果字段。
+- 固定统一结果结构：`summary`、`nodes`、`edges`、`meta`、`run_id`、`scenario_id`、`pilot_id`、`generated_at`。
+- 明确哪些字段给前端直接展示，哪些字段只用于回归和排障。
+- 为结果对象补一份样例快照，作为后续前后端共同参照物。
+
+完成标准：
+- 主样板和辅样板返回同一套结果结构。
+- 验证脚本可以直接校验结果契约，不再靠临时字段判断。
+
+### 第 2 步：结果回写与快照归档
+把一次仿真结果从“算完即丢”改成“算完可追溯、可回看、可对比”。
+
+建议动作：
+- 新增结果快照写入逻辑，先支持文件归档，后续再切数据库也不改调用口径。
+- 每次运行至少记录：试点、场景、输入摘要、输出摘要、关键节点压力、关键边流量、告警摘要。
+- 提供历史结果查询接口，支持按 `pilot_id`、`scenario_id`、时间范围查询。
+- 增加一份回放脚本，能把某次快照重新拉出来生成 overlay。
+
+完成标准：
+- 同一场景多次运行可留痕。
+- 任意一次基线运行都能被重新读取和展示。
+
+### 第 3 步：前端统一展示收口
+把地图、拓扑图、仿真面板统一到同一套结果消费链，不再各吃各的字段。
+
+建议动作：
+- 盘点 [`F:\smartgas-grid\src\hooks\useSimulation.ts`](F:\smartgas-grid\src\hooks\useSimulation.ts)、[`F:\smartgas-grid\src\components\topology\SimPanel.tsx`](F:\smartgas-grid\src\components\topology\SimPanel.tsx)、[`F:\smartgas-grid\src\views\MapTopologyView.tsx`](F:\smartgas-grid\src\views\MapTopologyView.tsx)。
+- 统一页面入口，让地图与拓扑图消费同一份 overlay / result snapshot。
+- 面板固定展示主干压力、支线流量、未满足需求、告警数量和场景说明。
+- 增加基线/故障/限流场景切换，保证前端不再需要为主样板和辅样板写两套判断。
+
+完成标准：
+- 地图、拓扑图、面板对同一次运行展示一致。
+- 前端切换场景时，关键数值和线条状态同步变化。
+
+### 第 4 步：试运行与扩样板准备
+最后用一轮真实试点使用方式来验收，并把复制到下一段样板的模板准备好。
+
+建议动作：
+- 固定一套试运行清单：基线、压气站停运、主干限流、支线限流、负荷上调。
+- 形成“每日一次回归 + 每次改动前后对比”的巡检脚本。
+- 补一份扩样板模板，明确下一个样板接入时要补哪些数据、走哪些接口、跑哪些验收。
+- 产出最终试点验收文档，给业务和开发共用。
+
+完成标准：
+- 试点场景能被连续回放和解释。
+- 新样板接入时不再从零摸索。
+
+## 验证方式
+### 验证什么
+- 结果结构是否稳定。
+- 快照是否真的可回读。
+- 前端三层展示是否一致。
+- 试运行清单是否能稳定复跑。
+
+### 怎么验证
+- 后端：复用并扩展 [`F:\smartgas-grid\backend\scripts\verify_we1_phase4.py`](F:\smartgas-grid\backend\scripts\verify_we1_phase4.py)，新增结果契约和快照回读断言。
+- 前端：固定基线截图或结果表，对比同一场景在地图、拓扑图、面板中的关键值。
+- 集成：跑一次“生成结果 -> 写快照 -> 读快照 -> 渲染 overlay -> 页面展示”的全链路检查。
+
+### 失败时先查哪一层
+1. 先查结果对象是否漂移。
+2. 再查快照写入和回读是否丢字段。
+3. 再查前端适配是否写了样板特判。
+4. 最后才查求解层是否真有回归。
+
+## 风险与回退点
+- 风险 1：这一阶段如果顺手继续改求解核心，会把已经封板的链路重新打散。
+  回退点：这一轮默认不动求解公式，只动结果契约、归档、展示和验收。
+- 风险 2：前端如果继续保留样板特判，后面扩样板会越来越乱。
+  回退点：一律以统一结果契约为准，页面不直接认试点名字。
+- 风险 3：快照只存最终结果，不存输入摘要，后面很难复盘。
+  回退点：第一版就把输入摘要和场景参数一起归档。
+
+## 最终交付物
+- 一份下一阶段正式方案文档。
+- 一份结果契约样例快照。
+- 一份结果回写与回读实现清单。
+- 一份前端统一消费改造清单。
+- 一份试运行与扩样板模板。
+
+## 当前结论
+下一阶段建议不再继续堆“求解能力”，而是按“结果契约收口 -> 结果回写归档 -> 前端统一展示 -> 试运行与扩样板准备”这 4 步推进。这样做完，WE1 才算从技术试点走到软件试点。
*** Add File: F:\smartgas-grid\docs\2026-04-07-WE1下一阶段-试点转产品化四步方案总结_人话版.md
+# WE1 下一阶段方案总结
+
+## 一句话
+下一阶段就做 4 件事：先把结果格式定死，再把结果存下来，再让前端统一吃这份结果，最后跑一轮试点验收并准备复制到下一段。
+
+## 这次写了什么
+- 新增了一份正式方案，文件是 [`F:\smartgas-grid\docs\2026-04-07-WE1下一阶段-试点转产品化四步方案.md`](F:\smartgas-grid\docs\2026-04-07-WE1下一阶段-试点转产品化四步方案.md)。
+- 这份方案不再沿用原来的 1 到 5 阶段编号，避免把已经封板的链路重新拉长。
+- 新方案把后续工作压成 4 步，重点从“求解”转到“产品化落地”。
+
+## 为什么这么定
+前面的工作已经证明 WE1 能算、能看趋势、能做联合回归。现在最缺的不是再补一个场景，而是把结果真正变成软件里的稳定对象。否则每次演示、回放、扩样板，都会重新拼接口、重新对字段、重新查哪里展示不一致。
+
+## 4 步分别在干什么
+
+| 步骤 | 人话解释 | 做完的标志 |
+| --- | --- | --- |
+| 第 1 步 | 把结果格式定死，别让主样板一套、辅样板一套 | 同一套字段能同时描述主样板和辅样板 |
+| 第 2 步 | 把每次仿真结果存下来，以后能回头看 | 任意一次运行都能查到、读到、复用 |
+| 第 3 步 | 让地图、拓扑图、面板都看同一份结果 | 同一次运行在三个地方显示一致 |
+| 第 4 步 | 按真实试点方式跑一轮，并准备复制到下一段 | 试点能回放，扩样板有模板 |
+
+## 影响范围
+- 后端重点会落在结果结构、快照写入、历史查询。
+- 前端重点会落在统一消费链，不再为不同样板写特判。
+- 验证重点会落在“能回读、能回放、能对比”。
+- 这一轮默认不碰求解公式，避免把已经稳定的结果重新改乱。
+
+## 前后对比
+
+| 项目 | 现在 | 下一阶段做完后 |
+| --- | --- | --- |
+| 结果对象 | 能返回，但更偏技术口径 | 变成前后端统一消费的产品口径 |
+| 仿真结果 | 跑完主要靠当场看 | 可以归档、查询、回放 |
+| 前端展示 | 可能还存在分层适配和样板差异 | 地图、拓扑图、面板统一 |
+| 扩样板 | 还要靠人回忆流程 | 有固定模板和验收路径 |
+
+## 下一步怎么接着做
+如果马上开工，建议先做第 1 步和第 2 步，因为这两步一旦收口，前端改造和试运行就有稳定底座了。
*** End Patch

*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-14-WE1放大白屏修复_第3阶段第5轮_人话版.md
+一、主目标
+1. 修复 WE1 第一张图在地图放大时白屏的问题，保证缩放过程中页面不崩。
+
+二、本轮修改
+1. 文件 `src/views/MapTopologyView.tsx` 中，线段悬浮事件 `mouseover/mousemove` 改为安全读取 `e?.originEvent?.clientX/clientY`，缺失时不再写入 tooltip 坐标。  
+2. 将节点和线段覆盖物的 `zooms` 从 `[2, 30]` 调整为 `[2, 20]`，与地图配置上限一致，避免极端缩放层级异常。
+
+三、为什么这样改
+1. 缩放过程中部分高德事件对象可能没有 `originEvent`，原逻辑直接取值会抛异常，前端进入白屏。  
+2. 覆盖物缩放上限高于地图上限也会放大兼容风险，统一后更稳。
+
+四、验证结果
+1. 已执行 `npm run build`，构建通过。  
+2. 预期现象是放大缩小时页面不再白屏，悬浮提示在有坐标事件时正常显示。
+
+五、交付物
+1. 修复代码：`src/views/MapTopologyView.tsx`。  
+2. 总结文档：`docs/2026-04-14-WE1放大白屏修复_第3阶段第5轮_人话版.md`。
*** End Patch

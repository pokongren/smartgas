*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-20-快照压力上图显示修复第2阶段第7轮记录.md
+# 2026-04-20 快照压力上图显示修复第2阶段第7轮记录
+
+一、主目标
+1. 修复“快照压力上图”点击后节点压力标签不显示或显示异常的问题，确保拓扑图能稳定看到进站压力、出站压力和基线压力温度。
+
+二、上下文与已知信息
+1. 问题集中在 `src/views/MapTopologyView.tsx` 节点标签拼接区，历史字符串里存在错误插值写法（`{...}` 而不是 `${...}`）和乱码文案。2. 前端按钮与状态切换已接通，但标签文本生成有冲突变量，导致上图结果不可靠。
+
+三、修改逻辑
+1. 保留旧变量块不动，新增一套干净标签变量 `snapshotPressureLabelV2`、`baselinePressureLabelV2`。2. 新变量统一使用正确模板插值，文案统一为“进/出/基进/基出/基温”。3. 节点渲染模板改为只消费 V2 标签，避免旧乱码分支继续影响显示。
+
+四、执行顺序
+1. 第2阶段第7轮侦察：定位到节点标签构建区和最终模板拼接行。2. 第2阶段第7轮实施：在同一作用域新增 V2 标签并切换渲染引用。3. 第2阶段第7轮验证：执行 `npm run build`，构建通过。
+
+五、验证方式与结果
+1. 构建验证：`npm run build` 通过，无 TypeScript 编译报错。2. 功能预期：加载任一快照后点击“快照压力上图”，节点名下应出现“进xx | 出xx MPa”；若存在基线快照，额外显示“基进/基出/基温”。
+
+六、影响范围与风险
+1. 影响范围仅限拓扑节点标签显示层，不改后端仿真求解结果。2. 当前仍保留旧乱码变量，短期不影响功能，后续建议单独做一次编码清理与死代码清理。
+
+七、回退点与交付物
+1. 回退点：`src/views/MapTopologyView.tsx` 中 V2 标签定义与模板引用改动。2. 交付物：本记录文件与已通过构建的前端修复代码。
*** End Patch

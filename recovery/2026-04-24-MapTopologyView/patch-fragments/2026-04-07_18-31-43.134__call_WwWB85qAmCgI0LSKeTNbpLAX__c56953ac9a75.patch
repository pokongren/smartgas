*** Begin Patch
*** Update File: F:\smartgas-grid\src\views\MapTopologyView.tsx
@@
     const selectedJunctionGroup = useMemo(() => {
         if (!selectedNode?.isJunction || !selectedNode.junctionId) return null
         return junctionGroups.find(group => group.id === selectedNode.junctionId) ?? null
     }, [junctionGroups, selectedNode])
     const canDeleteSelectedJunction = !!(
         selectedJunctionGroup && !isRuntimeReadonlyGroup(selectedJunctionGroup)
     )
+    const simulationOverlayMapping = useMemo(() => {
+        return buildSimulationOverlayMapping(topoNodes, topoEdges, steadyOverlay)
+    }, [steadyOverlay, topoEdges, topoNodes])
+    const selectedNodeSimulationMatch = useMemo(() => {
+        if (!selectedNode || !simulationOverlayMapping) return null
+        return simulationOverlayMapping.nodeMatchesByDisplayId.get(selectedNode.id) ?? null
+    }, [selectedNode, simulationOverlayMapping])
+    const selectedNodeRelatedEdgeMatches = useMemo(() => {
+        if (!selectedNode || !simulationOverlayMapping) return []
+        return topoEdges
+            .filter(edge => edge.startNodeId === selectedNode.id || edge.endNodeId === selectedNode.id)
+            .map(edge => ({
+                edge,
+                match: simulationOverlayMapping.edgeMatchesByDisplayId.get(edge.id) ?? null,
+            }))
+    }, [selectedNode, simulationOverlayMapping, topoEdges])
 
     const buildCollapsedGraph = useCallback((
         nodes: BaseTopoNode[],
         edges: BaseTopoEdge[],
*** End Patch

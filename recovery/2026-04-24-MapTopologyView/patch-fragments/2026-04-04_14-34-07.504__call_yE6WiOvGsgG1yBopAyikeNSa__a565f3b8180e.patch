*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
+# 2026-04-04 22:33:16 +08:00
+
+- 本轮先按要求从 `C:\Users\Administrator\.codex\worktrees\01d3\smartgas-grid` 切到 `F:\smartgas-grid`；切换后 `cwd` 和 `git rev-parse --show-toplevel` 都落在 `F:\smartgas-grid`，入口通过，不是 `automation env mismatch / worktree-path blockage`，也不是空壳 worktree。
+- 这轮主判断是“代码变化 + 数据库变化 + AI 模块链路变化”，同时有 docs 收口和本地实验噪音。主目录 `git status --short` 里既有地图/枢纽/DB 相关改动，也有 `backend/app/routers/ai_assistant.py`、`backend/app/services/ai_client.py`、`backend/app/services/ai_analysis_orchestrator.py`、`backend/app/services/analysis_validator.py`、`backend/app/services/fix_task_router.py`、`src/components/ai-assistant/*` 这条 AI 链路新增或修改；另外还有 `.npm-cache/`、`backend/data/*preview*.json|db`、`backend/scripts/rebuild_junctions.py`、`backend/scripts/verify_junctions.py`、`tmp_debug.txt` 这类本地实验/缓存痕迹。
+- 地图层级这条线可以继续按“关键阈值已统一，但不是整条视图链完全同实现”来写：`src/utils/hierarchyRenderer.ts` 现在明确 `critical/high/medium = 6/10/14`、`valveMinZoom = 11`、`expandClusterZoom = 12`，`src/utils/mapRenderer.ts` 已经用 `getNodeLODStrategy()` / `shouldShowNodeAtZoom()` 接口统一筛选；但 `src/views/MapTopologyView.tsx` 不是完整复用这套 LOD，只是补了 `focusNode` 联动、枢纽模式展示和 `topologyEditorApi` 接入，`src/views/GlobalPipelineView.tsx` 则补了 `focusNode` 跳转到拓扑页。
+- 枢纽/数据库口径要更新旧 memory，不能再沿用“we3 未落库”或“默认已只读”。当前主库实测：`stations=1272`、`pipelines=1268`、`pipeline_systems=14`、`junction_groups=16`、`junction_groups_rebuilt=12`；`we3/sj2/sj3` 都已落库，前缀计数分别是 `WE3=16/15`、`SJ2=70/69`、`SJ3=71/70`。`backend/app/services/junction_groups.py` 已改成运行时优先读 `junction_groups_rebuilt`，但 `backend/app/routers/topology_editor.py` 里 `TOPOLOGY_MANUAL_JUNCTION_EDIT` 默认值仍允许手工 merge/create/delete，所以不要直接写成“默认完全只读”，更稳的说法是“运行时入口已切 shadow 表优先，手工编辑已加闸门，但默认环境值仍可能可编辑”。
+- 导入脚本和数据库不是“只有脚本改了”。`backend/scripts/add_sj2.py`、`backend/scripts/add_we3.py`、`backend/scripts/reimport_sj23.py` 都已收敛到写 `backend/data/smartgas.db`，并把新导入管段 `category` 收紧到 `trunk`；但全库仍有 `229` 条 `pipelines.category` 不在 `trunk/branch`，所以只能说“新导入口径在收紧”，不能说“历史数据已清洗统一”。
+- API 口径和 AI 链路这轮都在往 `8080 + apiBase/BACKEND_BASE_URL` 收敛，但前缀没有完全统一。`src/services/apiBase.ts` 拆出了 `API_BASE_URL` / `BACKEND_BASE_URL`，`vite.config.ts` 代理 `/api`、`/topology`、`/docs`、`/openapi.json` 到 `8080`；`src/hooks/useTopology.ts` 走 `BACKEND_BASE_URL + /topology/...`，`src/services/api.ts` 仍混用 `/api/topology/...`、`/api/emergency/...` 和 `/topology/correct/...`。AI 侧现在是前端 `src/components/ai-assistant/useAiAssistantChat.ts` 通过 `/api/ai-assistant/chat` 送 `context + analysis_mode`，后端 `backend/app/routers/ai_assistant.py` 接入 `AiAnalysisOrchestrator`、`analysis_validator`、`fix_task_router`，`backend/app/services/ai_client.py` 统一模型调用并保留缺 key fallback。巡检结论里可以说“AI 模块链路有正式接线改动”，但别说“整条链已完全稳定验收”，因为这轮没跑联调。
+- docs 变化集中在 `docs/TOPOLOGY_COMPUTATION_USAGE.md` 和 `docs/TOPOLOGY_SETUP_GUIDE.md`，都在把旧的 `8000` / 单一路径说法改成当前真实状态：后端端口 `8080`，并明确 `/api/topology/...` 和 `/topology/...` 双轨并存。这两份文档是现状收口，不是“所有接口都已经彻底统一”的证明。
+- 当前运行时间：`2026-04-04 22:33:16 +08:00`
*** End Patch

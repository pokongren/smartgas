*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-22-主仿真面板文案优化第1阶段第7轮记录.md
+# 2026-04-22 主仿真面板文案优化第1阶段第7轮记录
+
+一、主目标
+1. 把主仿真面板里意思不清楚、表达重复的文案改成人话版本。
+
+二、做了什么
+1. 将“当前场景”改为“场景选择”，将“结果快照”改为“载入历史结果”，将“基线快照”改为“对比基线”。2. 将“展开站场参数与管段长度”压缩为“展开参数设置”，避免按钮名过长。3. 将“运行主仿真、保存快照、刷新快照、清空主仿真、读取当前压力并仿真（带动画）、快照压力上图”等按钮改成更直接的说法。4. 将状态说明从“主仿真还没运行”压缩为“未运行”，底部状态条改为“当前状态：...”，减少重复。5. 将“AI分析已迁移到 AI 助手界面，这里只展示拓扑与仿真结果”改成更短的说明。
+
+三、为什么这么做
+1. 原面板同时在标题、按钮、状态条里重复表达“主仿真、快照、状态”，用户看起来会觉得绕。2. 这轮只改文案，不改操作逻辑，风险最低，但能明显提升可读性。
+
+四、影响范围
+1. 文件：`src/views/MapTopologyView.tsx`。2. 影响模块：主仿真面板文案与状态提示。3. 不影响：仿真计算、快照存取、AI调用逻辑。
+
+五、验证结果
+1. 已执行 `npm run build`，构建通过。2. 页面刷新后，主仿真面板文案更短、更直接、重复更少。
*** End Patch

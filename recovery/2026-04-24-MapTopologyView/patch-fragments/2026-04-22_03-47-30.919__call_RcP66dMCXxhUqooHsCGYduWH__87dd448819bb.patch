*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-22-起始压力上图可见性修复第1阶段第3轮记录.md
+# 2026-04-22 起始压力上图可见性修复第1阶段第3轮记录
+
+一、主目标
+1. 修复“第1步读取起始压力后图上看不到压力”的问题，让无仿真结果时也能显示起始压力标签。
+
+二、做了什么
+1. 在 `MapTopologyView` 的“无 simulationMapping”节点渲染分支里新增起始压力标签渲染，标签显示“起进/起出 MPa”。2. 保留原有节点样式逻辑，只有在开启压力上图且存在 seed 压力时才叠加标签。3. 在仿真分支新增干净的 `snapshotPressureLabelV3/baselinePressureLabelV3`，避免旧乱码插值导致标签异常。
+
+三、影响范围
+1. 文件：`src/views/MapTopologyView.tsx`。2. 后端接口未改动。
+
+四、验证
+1. 构建验证：`npm run build` 通过。2. 预期行为：无仿真结果时，点击“快照压力上图”也能看到节点起始压力。
+
+五、风险与后续
+1. 当前文件仍存在历史乱码文案，后续建议单独做一次编码与死代码清理，不影响本轮功能可用性。
*** End Patch

*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
+## 2026-04-05 04:23:28 +08:00
+
+- 本轮先按入口规则落到主目录执行：初始 `cwd=C:\Users\Administrator\.codex\worktrees\d0db\smartgas-grid`，切换后 `cwd=F:\smartgas-grid`，`git rev-parse --show-toplevel=F:/smartgas-grid`。结论只基于 `F:\smartgas-grid`，没有沿用 detached worktree 或旧 memory 直接下实体判断。
+- 入口通过，不是 `automation env mismatch / worktree-path blockage`，也不是空壳仓库。侦察代理 A 复核到 `src/`、`backend/`、`docs/` 都在主目录，`git worktree list` 里主目录 `HEAD=eaf6ac4` 在分支上，`.codex/worktrees/*` 仍大量停在 `6410a10` detached，只能当背景噪音。
+- 本轮主要是主目录里的未提交代码变化 + 数据库内容变化 + 本地实验混入 + AI 链路变化，不是单纯 docs 改字。`git diff --name-only` 覆盖地图层级、枢纽运行时口径、导入脚本、AI 助手链路、docs；同时混有 `.npm-cache`、shadow db/json、临时脚本等本地实验残留。
+- 地图层级线：`src/utils/hierarchyRenderer.ts` 已形成统一阈值 `critical/high/medium=6/10/14`，并收口 `valveMinZoom=11`、`expandClusterZoom=12`；`src/utils/mapRenderer.ts` 已真实接入 `getNodeLODStrategy()` / `shouldShowNodeAtZoom()`，并把 source / junction / major_junction 纳入聚合与扇出。`src/views/GlobalPipelineView.tsx` 通过 `MapView + getNodeMarkerMap()` 吃到了地图渲染口径；`src/views/MapTopologyView.tsx` 仍维持自己的 `TOPO_SIZES`、collapsed graph 和编辑器表达，所以现状更像“地图链路已收口，拓扑编辑视图没完全跟齐同一套 zoom 口径”。
+- 枢纽规则与数据库线：`backend/app/services/junction_groups.py` 已改成“运行时优先读 `junction_groups_rebuilt`，旧 `junction_groups` 只保留 manual overlay 兼容层”，支持展开 `JUNCTION-x` 引用、站点跨度校验、冲突校验；`backend/app/routers/topology_editor.py` 的 `/api/topology/junctions` 已返回 `load_runtime_junction_groups()`，并带 `mode=runtime_readonly|legacy_editable`。实库当前是 `stations=1272`、`pipelines=1268`、`pipeline_systems=14`、`junction_groups=18`、`junction_groups_rebuilt=7`、`junction_groups_backup_20260404=16`；`WE3/SJ2/SJ3` 计数分别是 `16/15`、`70/69`、`71/70`（站/段），`layers_config` 不是独立表，仍在 `pipeline_systems.layers_config` 字段。
+- 导入脚本与本地实验边界：`backend/scripts/add_sj2.py`、`backend/scripts/add_we3.py`、`backend/scripts/reimport_sj23.py` 都在往 `pipeline_systems.layers_config` 和主库 `smartgas.db` 收口，属于主链路补数/重导的一部分；`backend/check_db.py` 也已升级到校验 `junction_groups_rebuilt`。但 `backend/scripts/rebuild_junctions.py`、`backend/scripts/rebuild_sj3_from_excel.py`、`backend/scripts/verify_junctions.py`，以及 `backend/data/junction-rebuild-*.json`、`backend/data/smartgas.rebuild-preview.db`、`backend/data/raw_excel_index.db` 更像本地重编/验收产物，不能直接当正式基线。
+- AI 线：`src/components/ai-assistant/useAiAssistantChat.ts` 已固定请求 `resolveApiPath('/api/ai-assistant/chat')`，有 `context` 就会带 `analysis_mode='subagents'`；`backend/app/routers/ai_assistant.py` 已接上 `AiAnalysisOrchestrator`，串 `AnalysisValidator` 和 `FixTaskRouter`；`backend/app/services/ai_client.py` 仍默认 `AI_BASE_URL=https://api.openai.com/v1`、`AI_DEFAULT_MODEL=gpt-3.5-turbo`，缺 `AI_API_KEY` 时走 fallback 文案。要继续保守表述成“主目录里的本地未提交能力”，因为 `backend/app/services/ai_analysis_orchestrator.py`、`backend/app/services/analysis_validator.py`、`backend/app/services/fix_task_router.py`、`src/components/ai-assistant/useAiAssistantChat.ts`、`src/components/ai-assistant/useAiAssistantPageContext.ts` 目前都还没纳入正式提交。
+- docs 线不是装饰性更新。`docs/TOPOLOGY_COMPUTATION_USAGE.md`、`docs/TOPOLOGY_SETUP_GUIDE.md` 正在把口径从旧 `8000` 收到 `8080`，并明确区分 `/api/topology/...` 的编辑器链路和 `/topology/...` 的计算链路，和 `src/services/api.ts`、`src/hooks/useTopology.ts` 现在的双口径现状对得上。
+- 验证边界：本轮已按主目录事实复核，没有把 detached worktree 当主目录，也没有把局部现象说成“全仓已统一”。下轮继续优先盯三件事：1) 仍只在 `F:\smartgas-grid` 巡检；2) 重点复核 `MapTopologyView` 是否需要显式接统一 LOD，还是继续保持编辑器独立表达；3) 持续区分 `junction_groups_rebuilt` / manual overlay / rebuild 实验产物，避免把本地实验说成正式运行时基线。
+- 本轮运行时间：`2026-04-05 04:23:28 +08:00`
*** End Patch

*** Begin Patch
*** Add File: docs/2026-04-20-仿真参数面板默认隐藏第2阶段第3轮记录.md
+# 2026-04-20 仿真参数面板默认隐藏第2阶段第3轮记录
+
+一、结论  
+已实现“站场参数和管段长度面板默认隐藏”，并新增一个按钮用于手动展开和收起，页面默认更干净。
+
+二、实现内容  
+1. 在 `src/views/MapTopologyView.tsx` 增加状态 `showSimParamEditor`，默认值为 `false`。2. 在仿真面板里新增按钮，文案会在“展开站场参数与管段长度”和“收起站场参数与管段长度”之间切换。3. 将 `SimParamEditor` 改为条件渲染，仅在 `showSimParamEditor` 为 `true` 时显示。
+
+三、为什么这么改  
+1. 你希望默认界面不被大量参数输入占满，先保证操作主流程简洁。2. 参数编辑功能仍保留，只有在需要调参时才展开，兼顾易用性和专业性。
+
+四、影响范围  
+1. 影响页面：`/#/map-topology` 左侧仿真操作面板。2. 不影响仿真求解、快照保存/加载、场景切换和参数写入后端逻辑。
+
+五、验证结果  
+1. 执行 `npm run build`，构建通过。2. 人工检查点：进入页面后参数面板默认不显示，点击按钮后显示，再次点击可收起。
*** End Patch

*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-08-WE1-maintenance-round13-regression-patrol-summary.md
+一、主目标
+1. 本轮只做 WE1 第一张图五阶段封板后的维护模式第13轮巡检，确认正式入口、调试页边界、快照归档和主链契约都没有回退，不进入下一阶段。
+
+二、上下文与已知信息
+1. automation memory 已确认五阶段主体完成，当前只能做巡检、回归和修复，不能继续扩阶段。
+2. 上一轮已把 `/topology` 收成默认调试页，本轮重点是确认这条边界没有被后续改动打破。
+3. 当前仓库存在较多未归属本轮的改动，因此本轮默认先做只读侦察和独立验证，避免误改别人的工作区内容。
+
+三、主代理与子代理分工
+1. 主代理负责读取方案、memory、最近总结，判定当前处于维护模式第13轮，并决定本轮只做巡检落盘。
+2. 侦察子代理A负责核对前端入口语义，重点看 `App.tsx`、`MapTopologyView.tsx`、`TopologyView.tsx`、`useSimulation.ts`、`useAiAssistantPageContext.ts`。
+3. 侦察子代理B负责核对维护脚本、快照归档和主链契约，重点看 `verify_we1_maintenance.py`、`verify_we1_phase34_final.py`、`verify_we1_map_phase5.py`、`verify_we1_snapshot_archive.py`、`topology_simulation.py`、`topology_simulation.py service` 契约。
+4. 实施子代理本轮不改业务代码，只新增本轮总结并更新 automation memory。
+5. 验证子代理独立复跑维护巡检脚本，确认主链和维护模式仍成立。
+
+四、修改逻辑
+1. 两路侦察都没有发现需要进入代码修复的唯一缺陷方向，最可信结论是当前正式入口、调试页边界、快照归档和主链契约均保持稳定。
+2. 因为没有验证失败，所以本轮不触发修复轮，也不为了“有动作”去碰求解公式、扩场景或新造页面。
+3. 这一轮真正的实施动作只有两件：新增人话版维护总结，更新 automation memory，把第13轮巡检结论写死，方便下一轮直接接着巡检。
+
+五、执行顺序
+1. 第1阶段第13轮先读五阶段方案、自动化执行方案、现有阶段总结和 memory，确认本轮是维护模式巡检。
+2. 第1阶段第13轮并行跑两路侦察，一路查入口语义，一路查脚本契约与快照归档。
+3. 主代理汇总证据后确认不进入代码修复，只落文档和 memory。
+4. 验证子代理独立运行 `python F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py`，确认维护模式巡检继续通过。
+
+六、验证方式
+1. 验证什么：验证 `/map-topology` 仍是正式主入口，`/topology` 仍是默认收起高级调试操作的调试页，WE1 主链契约与快照归档没有回退。
+2. 怎么验证：运行 `python F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py`，并结合脚本输出里的 `entry_semantics`、`payload_checks`、`next_action` 判断是否仍为 maintenance。
+3. 结果怎么看：本轮脚本返回 `status=passed`、`mode=maintenance`，`phase34_status_passed=true`、`phase5_status_passed=true`、`snapshot_archive_status_passed=true`，说明主链、快照和入口语义都还稳。
+4. 失败时先查哪层：先查入口层与前端消费层，再查映射层和 source/overlay ID 命中关系，再查快照归档层，最后查 backend solver input 与稳态求解主链。
+
+七、风险与回退点
+1. 当前最大风险不是主链失效，而是后续其他改动把 `/topology` 的默认调试边界重新放开，或把第一张图重新混入旧入口语义。
+2. 本轮没有业务代码改动，所以回退点只涉及本轮新增总结和 memory 记录，不影响现有仿真链路。
+3. 如果下一轮巡检失败，仍按“入口层 -> 映射层 -> 快照层 -> 求解层”顺序排查，不直接怀疑求解公式。
+
+八、最终交付物
+1. 新增总结文件：`F:/smartgas-grid/docs/2026-04-08-WE1-maintenance-round13-regression-patrol-summary.md`。
+2. 更新 automation memory：`C:/Users/Administrator/.codex/automations/we1/memory.md`。
+3. 本轮没有业务代码改动，交付物以巡检结论和维护记录为主。
+
+九、影响范围
+1. 本轮只影响自动化维护记录，不影响前端页面行为、后端路由、求解逻辑和快照归档数据结构。
+
+十、验证结果
+1. `python F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py` 通过，维护模式仍成立。
+2. 本轮未触发修复轮，也未出现连续两次修复失败阻塞。
+3. 下一轮继续停留在维护模式，不进入新阶段。
+
+十一、前后对比
+| 项目 | 本轮之前 | 本轮之后 |
+| --- | --- | --- |
+| 当前模式 | 维护模式第12轮已完成 | 维护模式第13轮巡检已完成 |
+| 正式入口口径 | `/map-topology` 为正式主入口 | 保持不变，并由巡检脚本再次确认 |
+| 调试页口径 | `/topology` 为默认收起高级调试操作的调试页 | 保持不变，并由巡检脚本再次确认 |
+| 快照归档状态 | 上轮已通过 | 本轮巡检再次确认仍可用 |
+| 业务代码改动 | 无要求 | 仍无改动，只更新文档与 memory |
+
+十二、截图与替代
+1. 本轮没有新增截图，因为这是纯巡检轮，没有浏览器可视化输出，也没有页面差异需要展示。
+2. 本轮用验证脚本结果和前后对比表替代截图，已经足够说明当前状态。
*** Update File: C:/Users/Administrator/.codex/automations/we1/memory.md
@@
-一、运行时间
-1. 2026-04-08 17:21:00 +08:00。
-
-二、当前状态
-1. WE1 第一张图五阶段主体已完成，当前处于维护模式第12轮，不进入下一阶段。
-2. 本轮完成 `/topology` 默认高级调试操作面收口，继续保持 `MapTopologyView` 为正式主入口。
-
-三、本轮动作
-1. 主代理先读五阶段方案、最近阶段总结和现有 memory，确认当前只能做维护模式巡检、回归和修复。
-2. 两名侦察子代理分别核对前端入口语义与后端维护脚本，收敛出唯一修复方向：`/topology` 默认仍暴露完整 `SimPanel`，不符合维护模式调试页边界。
-3. 实施子代理方向由主代理接管后落到两处代码：`TopologyView.tsx` 增加 `debugActions=1` 显式开关，默认收起高级调试操作面并引导回 `/map-topology`；`verify_we1_maintenance.py` 同步校验默认隐藏高级调试操作面。
-4. 验证子代理独立回归 `verify_we1_maintenance.py`、`verify_we1_phase34_final.py`、`verify_we1_map_phase5.py`、`verify_we1_snapshot_archive.py`，全部通过。
-5. 新增总结 [2026-04-08-WE1-maintenance-round12-topology-debug-guard-summary.md](F:/smartgas-grid/docs/2026-04-08-WE1-maintenance-round12-topology-debug-guard-summary.md)。
-
-四、改动文件
-1. [TopologyView.tsx](F:/smartgas-grid/src/views/TopologyView.tsx)。
-2. [verify_we1_maintenance.py](F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py)。
-3. [2026-04-08-WE1-maintenance-round12-topology-debug-guard-summary.md](F:/smartgas-grid/docs/2026-04-08-WE1-maintenance-round12-topology-debug-guard-summary.md)。
-4. [memory.md](C:/Users/Administrator/.codex/automations/we1/memory.md)。
-
-五、验证结果
-1. `python F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py` 通过，`status=passed`、`mode=maintenance`，新增 `topology_view_hides_debug_actions_by_default=true`。
-2. `python F:/smartgas-grid/backend/scripts/verify_we1_phase34_final.py` 通过，主辅 overlay 契约一致，`steady_status=failure_status=converged`。
-3. `python F:/smartgas-grid/backend/scripts/verify_we1_map_phase5.py` 通过，`next_mode=maintenance`，四个主样板场景覆盖完整。
-4. `python F:/smartgas-grid/backend/scripts/verify_we1_snapshot_archive.py` 通过，`list_count=10`，快照归档仍可读写。
-5. 所有脚本均通过，不触发修复轮，也未进入“连续两次修复失败”阻塞。
-
-六、失败先查哪层
-1. 先查 `TopologyView.tsx` 和 `verify_we1_maintenance.py` 的入口守卫，再查 `MapTopologyView.tsx` 与 `useSimulation.ts` 的前端消费层，其次查映射层和 source/overlay ID 命中关系，再查快照归档层，最后才查 backend solver input 与稳态主链。
-
-七、下一轮判断
-1. 不进入下一阶段，继续维护模式巡检与回归。
-2. 下一轮若再失败，优先检查 `/topology` 默认守卫是否被打破，再按入口层 -> 映射层 -> 快照层 -> 求解层继续排查。
+一、运行时间
+1. 2026-04-08 18:06:03 +08:00。
+
+二、当前状态
+1. WE1 第一张图五阶段主体已完成，当前处于维护模式第13轮，不进入下一阶段。
+2. 本轮未发现需要进入代码修复的唯一问题，继续保持 `MapTopologyView` 为正式主入口、`/topology` 为调试页。
+
+三、本轮动作
+1. 主代理先读五阶段方案、自动化执行方案、最近总结和现有 memory，确认本轮只允许做维护模式巡检、回归和修复。
+2. 侦察子代理A核对 `App.tsx`、`MapTopologyView.tsx`、`TopologyView.tsx`、`useSimulation.ts`、`useAiAssistantPageContext.ts`，确认第一张图正式入口语义和 `/topology` 调试页边界仍成立。
+3. 侦察子代理B核对 `verify_we1_maintenance.py`、`verify_we1_phase34_final.py`、`verify_we1_map_phase5.py`、`verify_we1_snapshot_archive.py`、`topology_simulation.py`、`src/types/simulation.ts`、`src/services/api.ts`，确认主链契约、快照归档和维护巡检口径仍一致。
+4. 实施子代理本轮不改业务代码，只新增总结 [2026-04-08-WE1-maintenance-round13-regression-patrol-summary.md](F:/smartgas-grid/docs/2026-04-08-WE1-maintenance-round13-regression-patrol-summary.md) 并更新本 memory。
+5. 验证子代理独立运行 `python F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py`，结果通过，未触发修复轮。
+
+四、改动文件
+1. [2026-04-08-WE1-maintenance-round13-regression-patrol-summary.md](F:/smartgas-grid/docs/2026-04-08-WE1-maintenance-round13-regression-patrol-summary.md)。
+2. [memory.md](C:/Users/Administrator/.codex/automations/we1/memory.md)。
+
+五、验证结果
+1. `python F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py` 通过，返回 `status=passed`、`mode=maintenance`。
+2. 维护脚本聚合结果里 `phase34_status_passed=true`、`phase5_status_passed=true`、`snapshot_archive_status_passed=true`，说明主链契约、第一张图第5阶段验收、快照归档都未回退。
+3. `entry_semantics` 中 `/map-topology` 正式入口、`/topology` 调试页、默认隐藏高级调试操作面的检查均通过。
+4. 本轮没有验证失败，因此不触发修复轮，也未进入“连续两次修复失败”阻塞。
+
+六、失败先查哪层
+1. 先查入口层和前端消费层，再查映射层和 source/overlay ID 命中关系，再查快照归档层，最后才查 backend solver input 与稳态求解主链。
+
+七、下一轮判断
+1. 不进入下一阶段，继续维护模式巡检与回归。
+2. 下一轮若失败，先查 `/map-topology` 与 `/topology` 的入口语义是否回退，再按入口层 -> 映射层 -> 快照层 -> 求解层继续排查。
*** End Patch

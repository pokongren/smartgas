*** Begin Patch
*** Add File: F:/smartgas-grid/docs/automation/project-heartbeat-2026-04-06-142308.md
+# 项目心跳巡检总结（2026-04-06 14:23 +08:00）
+
+## 主目标
+
+固定在 `F:\smartgas-grid` 主目录做一次只读巡检，先校验主目录落点和 git 事实，再复核地图缩放与层级显示、枢纽规则与数据库口径、导入脚本边界、AI 模块链路；本轮只更新巡检文档和 automation memory，不改业务代码。
+
+## 上下文与已知信息
+
+- 本轮主目录校验通过：`git rev-parse --show-toplevel` 返回 `F:/smartgas-grid`，`git rev-parse --absolute-git-dir` 返回 `F:/smartgas-grid/.git`。
+- `.git` 在主目录里是隐藏实体目录，不是 detached worktree 常见的指针文件。
+- 主仓最新提交已经是 `602f715`，不再是旧 memory 里的 `eaf6ac4`。
+- 当前分支名是 `拓扑设想`。
+- `git worktree list --porcelain` 显示主目录 `F:/smartgas-grid` 挂分支，`C:\Users\Administrator\.codex\worktrees\...\smartgas-grid` 仍有大量 `6410a10` 的 detached worktree；本轮结论只认主目录。
+
+## 主代理与子代理分工
+
+| 角色 | 负责内容 | 输出 |
+| --- | --- | --- |
+| 主代理 | 校验主目录、汇总证据、决定落盘口径 | 本文档 + memory 更新 |
+| 侦察 A | git log、git status、未提交 diff、worktree、docs 变化 | 主仓 git 事实 |
+| 侦察 B | 地图缩放/层级显示、枢纽运行时、数据库口径、导入脚本 | 地图与数据链路事实 |
+| 侦察 C | AI 前后端链路与分流 | AI 模块事实 |
+| 验证代理 | 用独立命令复核主目录、数据库检查脚本、关键 grep 命中 | 复核结果写入“验证方式” |
+
+## 修改逻辑
+
+- 不改业务代码，只把本轮巡检事实写进 `docs/automation` 和 automation memory。
+- 旧 memory 里有一批基于旧主仓状态的结论，本轮按主目录实时结果纠偏，重点纠正主仓 HEAD、未提交 diff 规模和 docs 变化。
+
+## 执行顺序
+
+1. 进入 `F:\smartgas-grid`，用 `git rev-parse`、`.git` 实体检查确认这是主目录，不拿当前 detached worktree 顶结论。
+2. 读取 git log、git status、git diff --stat、git worktree list，确认当前主仓提交、工作区和 worktree 分布。
+3. 检查 docs 跟踪改动，重点核对 `docs/TOPOLOGY_SETUP_GUIDE.md` 和 `docs/TOPOLOGY_COMPUTATION_USAGE.md`。
+4. 复核地图主渲染链、`MapTopologyView` 是否接入、枢纽运行时优先级、数据库脚本输出、导入脚本边界。
+5. 复核 AI 前端入口、`analysis_mode`、后端 direct/orchestrated/legacy 分流以及工具/模型出口角色。
+6. 只落盘本次文档和 automation memory。
+
+## 当前进展摘要
+
+- git 主目录事实已确认：主仓 HEAD 是 `602f715`，最近 5 条提交从新到旧是 `602f715`、`eaf6ac4`、`b31a010`、`d3c9d08`、`c19937f`。
+- 当前未提交 tracked 改动只有 3 个文件：`backend/app/routers/pipeline_packages.py`、`backend/data/smartgas.db`、`src/data/pipelines/we1.ts`；另外有 6 个 `tmp_*` / `we1_pilot_service.py` 未跟踪文件。
+- `git diff --stat` 当前是 `3 files changed, 448 insertions(+), 322 deletions(-)`，和旧 memory 里 `47 files changed, 5434 insertions(+), 2761 deletions(-)` 已经不是同一状态。
+- 本轮 `git diff --name-only -- docs` 为空；`docs/TOPOLOGY_SETUP_GUIDE.md` 和 `docs/TOPOLOGY_COMPUTATION_USAGE.md` 当前没有 tracked diff。
+- 地图主链仍是 `src/views/GlobalPipelineView.tsx -> src/components/map-view/MapView.tsx -> src/utils/mapRenderer.ts -> src/utils/hierarchyRenderer.ts`。
+- LOD 阈值仍是 `critical/high/medium=6/10/14`、`valveMinZoom=11`、`expandClusterZoom=12`。
+- `MapTopologyView.tsx` 仍未接入主地图渲染链；文件里明确写了“不传 `pipelineData` 给 `MapView`”，只把 `MapView` 当底图，拓扑节点和边由页面自己折叠并绘制。
+- 枢纽运行时口径未变：`backend/app/services/junction_groups.py` 仍是“优先 `junction_groups_rebuilt`，缺失时回退 `junction_groups`，再叠加 manual overlay”。
+- `python backend/check_db.py` 当前口径是：`pipeline_systems=13`，存在 `we1/we2/sj2/sj3`，`we3` 缺失；`WE3=0/0`，`SJ2=70/69`，`SJ3=71/70`；`junction_groups=25`，`junction_groups_backup_20260404=16`，`junction_groups_rebuilt=7`，`issue_count=0`，影子表最新生成时间 `2026-04-04T17:17:43.361829+00:00`。
+- 导入脚本边界未变：`reimport_sj23.py` 会删除并重写 `SJ2/SJ3` 相关数据，属于离线重导；`rebuild_junctions.py` 重建影子表 `junction_groups_rebuilt`；`add_sj2.py` / `add_we3.py` 仍更像局部补写脚本，不是运行时主链。
+- AI 模块链路未变：前端 `src/components/ai-assistant/useAiAssistantChat.ts` 固定请求 `/api/ai-assistant/chat`，有页面上下文时把 `analysis_mode` 设成 `subagents`；后端 `backend/app/routers/ai_assistant.py` 依据 `should_use_orchestration()` 在 orchestrated 和 legacy 间分流，模型出口在 `backend/app/services/ai_client.py`，工具能力在 `backend/app/services/assistant_tools.py`。
+
+## 验证方式
+
+- 主目录验证：执行 `git rev-parse --show-toplevel`、`git rev-parse --absolute-git-dir`、`Get-ChildItem -Force .git`，确认主仓位置与 `.git` 实体目录属性。
+- git 事实验证：执行 `git log -5 --oneline`、`git status --short`、`git diff --stat`、`git worktree list --porcelain`。
+- docs 变化验证：执行 `git diff --name-only -- docs` 以及两份拓扑文档的定向 diff，结果均为空。
+- 模块链路验证：对地图、枢纽、AI 相关文件执行 `rg -n` 命中检查。
+- 数据库验证：执行 `python backend/check_db.py`，以脚本输出为本轮数据库口径。
+
+## 风险与回退点
+
+- 风险 1：如果后续有人继续沿用旧 memory，会把主仓 HEAD、未提交 diff 规模和 docs 变化说成旧状态。
+- 风险 2：`MapTopologyView` 仍是独立口径，不能写成“地图和拓扑视图已经统一到同一 LOD 渲染链”。
+- 风险 3：`WE3` 在 `pipeline_systems` 里仍缺失，不能写成主库正式落成。
+- 回退点：如果下轮发现主仓再次前进或工作区继续变化，应优先重新跑 `git diff --stat` 和 `python backend/check_db.py`，不要复用本轮数字。
+
+## 前后对比
+
+| 项目 | 旧口径 | 本轮事实 |
+| --- | --- | --- |
+| 主仓 HEAD | `eaf6ac4` | `602f715` |
+| tracked diff 规模 | `47 files changed, 5434 insertions(+), 2761 deletions(-)` | `3 files changed, 448 insertions(+), 322 deletions(-)` |
+| docs tracked 变化 | 聚焦两份 topology 文档 | 当前为空 |
+| 地图主链 | `GlobalPipelineView -> MapView -> mapRenderer -> hierarchyRenderer` | 不变 |
+| `MapTopologyView` | 独立口径 | 不变 |
+| DB 口径 | `we3` 缺失，`junction_groups_rebuilt=7` | 不变 |
+
+## 截图说明
+
+本轮是仓库与代码链路巡检，没有启动前端页面，也没有合适的 UI 截图场景，所以不附截图，改用上表和脚本输出摘要做前后对比。
+
+## 最终交付物
+
+- 本巡检文档：`F:\smartgas-grid\docs\automation\project-heartbeat-2026-04-06-142308.md`
+- automation memory 追加本轮摘要：`C:\Users\Administrator\.codex\automations\automation\memory.md`
*** Update File: C:/Users/Administrator/.codex/automations/automation/memory.md
@@
 ## 2026-04-06 11:21:40 +08:00
 
 - 更正版摘要：本轮项目心跳继续只认 F:\smartgas-grid 主目录事实，主仓为 eaf6ac4，detached worktree 继续只算并行环境，不拿来顶替主仓判断。
 - Git 事实未变：当前未提交变更规模仍为 47 files changed, 5434 insertions(+), 2761 deletions(-)，docs 变化继续集中在 docs/TOPOLOGY_SETUP_GUIDE.md 和 docs/TOPOLOGY_COMPUTATION_USAGE.md。
 - 地图链路未变：GlobalPipelineView -> MapView -> mapRenderer -> hierarchyRenderer；LOD 阈值仍为 critical/high/medium=6/10/14、valveMinZoom=11、expandClusterZoom=12；MapTopologyView 仍是独立口径。
 - 枢纽与数据库口径未变：运行时继续 rebuilt 优先、legacy 回退、manual overlay 叠加；check_db.py 结果仍是 pipeline_systems=13、we3 缺失、SJ2=70/69、SJ3=71/70、junction_groups=25、junction_groups_rebuilt=7、issue_count=0。
 - 导入脚本边界未变：reimport_sj23.py 属于离线重导，rebuild_junctions.py 属于影子表重建，add_sj2.py 和 add_we3.py 更像局部补写。
 - AI 链路未变：前端 useAiAssistantChat.ts 固定请求 /api/ai-assistant/chat；后端 ai_assistant.py 仍是 direct、orchestrated、legacy 三路并存。
 - 本轮新增文档：F:\smartgas-grid\docs\automation\project-heartbeat-2026-04-06-111853.md。运行时间：2026-04-06 11:21 +08:00。
+
+## 2026-04-06 14:23:08 +08:00
+
+- 本轮继续严格只认 `F:\smartgas-grid` 主目录事实；`git rev-parse --show-toplevel` 返回 `F:/smartgas-grid`，`git rev-parse --absolute-git-dir` 返回 `F:/smartgas-grid/.git`，`.git` 是隐藏实体目录，不是 worktree 指针文件。
+- 主仓状态已更新，不再是旧口径里的 `eaf6ac4`：当前主仓 HEAD 是 `602f715`，分支名是 `拓扑设想`；其余 `C:\Users\Administrator\.codex\worktrees\...\smartgas-grid` 仍是 `6410a10` 的 detached worktree，只算并行环境。
+- Git 事实这轮明显变了：`git status --short` 当前只有 3 个 tracked 改动文件 `backend/app/routers/pipeline_packages.py`、`backend/data/smartgas.db`、`src/data/pipelines/we1.ts`，另有 `backend/app/services/we1_pilot_service.py` 和多份 `tmp_*` 未跟踪文件；`git diff --stat` 现为 `3 files changed, 448 insertions(+), 322 deletions(-)`。
+- docs 事实这轮也变了：`git diff --name-only -- docs` 为空，`docs/TOPOLOGY_SETUP_GUIDE.md` 和 `docs/TOPOLOGY_COMPUTATION_USAGE.md` 当前都没有 tracked diff，不能再沿用“docs 正在改这两份 topology 文档”的旧说法。
+- 地图缩放与层级显示口径未变：主链仍是 `GlobalPipelineView -> MapView -> mapRenderer -> hierarchyRenderer`；LOD 阈值仍是 `critical/high/medium=6/10/14`、`valveMinZoom=11`、`expandClusterZoom=12`；`MapTopologyView.tsx` 仍不传 `pipelineData` 给 `MapView`，只把它当底图，拓扑节点/边继续由页面自己折叠渲染。
+- 枢纽运行时与数据库口径未变：`load_runtime_junction_groups()` 仍是 rebuilt 优先、legacy 回退、manual overlay 叠加；`python backend/check_db.py` 结果仍是 `pipeline_systems=13`、`we3` 缺失、`SJ2=70/69`、`SJ3=71/70`、`junction_groups=25`、`junction_groups_rebuilt=7`、`issue_count=0`，影子表最新生成时间 `2026-04-04T17:17:43.361829+00:00`。
+- 导入脚本边界未变：`reimport_sj23.py` 是离线重导 `SJ2/SJ3` 工具，`rebuild_junctions.py` 是影子表重建工具，`add_sj2.py` / `add_we3.py` 更像局部补写脚本。
+- AI 链路未变：前端 `useAiAssistantChat.ts` 固定发 `/api/ai-assistant/chat`，有页面上下文时带 `analysis_mode=subagents`；后端 `ai_assistant.py` 仍按 `should_use_orchestration()` 在 orchestrated 和 legacy 间分流，模型出口是 `ai_client.py`，工具出口是 `assistant_tools.py`。
+- 本轮新增文档：`F:\smartgas-grid\docs\automation\project-heartbeat-2026-04-06-142308.md`。本轮运行时间：`2026-04-06 14:23 +08:00`。
*** End Patch

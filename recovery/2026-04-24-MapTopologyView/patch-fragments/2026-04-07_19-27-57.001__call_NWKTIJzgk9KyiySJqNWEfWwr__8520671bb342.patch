*** Begin Patch
*** Update File: F:\smartgas-grid\src\views\MapTopologyView.tsx
@@
     const {
         overlay: steadyOverlay,
         isLoading: steadySimLoading,
@@
     } = useSimulation({
         pilotId: 'mainline_zhongwei_jingbian',
         scenarios: MAINLINE_SCENARIOS,
     })
+    const [isSimulating, setIsSimulating] = useState(false)
+    const [simResults, setSimResults] = useState<Array<{
+        step: number
+        affected_nodes: string[]
+        pressure_drops: Record<string, number>
+        is_stable: boolean
+    }>>([])
+    const simulationOverlay = steadyOverlay
+    const runSimulation = runSteadySimulation
*** End Patch

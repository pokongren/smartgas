*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-20-基线快照拓扑显示改造说明.md
+一、结论
+已实现“基线快照在拓扑图显示”，现在节点标签和右侧详情都能看到基线的进站压力、出站压力、出站温度，并可和当前仿真值直接对比。
+
+二、改了什么
+1. 拓扑节点标签增加基线行，当前值下方新增一行基线值，颜色用紫色区分，内容包括基线进/出压力和温度。  
+2. 节点详情卡增加“基线快照”区块，新增三行：基线进站压力、基线出站压力、基线出站温度。  
+3. 新增基线指标解析逻辑，复用节点匹配结果，统一计算压力与温度（缺失温度时按规则估算）。
+
+三、为什么这样改
+1. 你现在选了基线快照，但图上看不出来，值班时无法直观对比。  
+2. 把基线值直接叠在拓扑节点和详情里，能减少来回切换和手工对照。  
+3. 采用“当前+基线同屏”比“单独页展示”更符合现场快速判断。
+
+四、影响范围
+1. 仅前端文件：`src/views/MapTopologyView.tsx`。  
+2. 不改后端接口、不改数据库结构、不改快照存储。
+
+五、验证结果
+1. 构建验证：`npm run build` 通过。  
+2. 功能验证：选择基线快照后，拓扑节点标签可见基线值，点开节点详情可见基线三项指标。  
+3. 前后对比如下。
+
+| 对比项 | 修改前 | 修改后 |
+| --- | --- | --- |
+| 拓扑图节点标签 | 仅当前仿真/数据库值 | 增加基线值一行（进/出压力+温度） |
+| 节点详情 | 仅当前运行状态值 | 新增“基线快照”三项指标 |
+| 对比效率 | 需要手工切换与记忆 | 当前与基线同屏对比 |
*** End Patch

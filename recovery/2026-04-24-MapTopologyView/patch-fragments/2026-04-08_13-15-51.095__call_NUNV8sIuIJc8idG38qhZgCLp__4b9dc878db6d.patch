*** Begin Patch
*** Add File: docs/2026-04-08-WE1主仿真前端状态显示与轻动画总结_人话版.md
+# WE1 主仿真前端状态显示与轻动画总结
+
+## 1. 这次做了什么
+
+这次在主仿真操作面板上方补了一层很薄的前端显示，让人一眼能看出来这是仿真，不再像普通表单。显示内容包括仿真状态、当前场景、运行编号、生成时间，以及 4 个小指标卡。
+
+## 2. 为什么这么做
+
+前一版把页面收得太干净了，虽然操作按钮还在，但用户很难一眼判断“这是不是仿真页面”“现在有没有跑起来”。所以这次没有把大块说明卡片搬回来，而是加一层轻量状态头，既保留简洁，也保留仿真感。
+
+## 3. 前端显示现在有什么
+
+1. 状态头：显示 `主仿真已收敛 / 求解中 / 等待运行 / 失败` 这类状态。
+2. 状态标签：按不同状态显示不同颜色。
+3. 运行信息：显示当前场景、`run_id`、生成时间。
+4. 指标卡：未运行时显示场景、基线、快照数量；运行后显示总供给、总需求、平均利用率、告警数。
+
+## 4. 动画效果做了什么
+
+1. 求解中时，状态圆点会脉冲闪动。
+2. 已有仿真结果时，4 个小指标卡会做轻微呼吸动画，而且带一点错峰延迟，不会一起僵硬地闪。
+3. 鼠标经过指标卡时会有轻微上浮，界面会更像“活的面板”，不是死表单。
+
+## 5. 改动位置
+
+改动文件是 [MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx)。
+
+## 6. 验证结果
+
+1. 执行 `npm run build`，通过。
+2. 当前前端已经能直接看出“这是仿真”，同时没有把之前那几大块说明面板重新堆回去。
+
+## 7. 当前结果
+
+现在主仿真页的感觉是“上面看状态，下面点操作”，既干净，又能看出仿真系统的运行感。
*** End Patch

*** Begin Patch
*** Add File: docs/2026-04-08-WE1第一张图主仿真导出演示清单第6阶段第1轮总结_人话版.md
+# WE1 第一张图主仿真导出演示清单第6阶段第1轮总结
+
+## 1. 这一轮做了什么
+
+这轮把 WE1 第一张图主仿真页的“导出演示清单”正式接上了，同时把这块页面里混进去的乱码文案一起修掉了。现在用户在主仿真页可以直接点“导出演示清单”，拿到一份 Markdown 清单，里面会带上当前场景、运行结果、基线对比、覆盖结果清单、固定演示口径，不用再手工整理演示材料。
+
+## 2. 为什么这么做
+
+前一轮已经把主仿真、基线对比、覆盖结果清单和固定演示口径都接上了，但现场演示时还差一个“能直接拿走”的输出物。导出演示清单补上后，页面里的信息不再只停留在界面里，验收、汇报、联调复盘都能直接复用。与此同时，子代理复核发现主仿真区真的存在 `????` 乱码，如果不修，页面和导出内容都会一起变脏，所以这一轮把它当成阻断问题一并修完。
+
+## 3. 改了哪一层
+
+1. 页面层：只改了 [MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx)。
+2. 功能层：新增 `exportSimulationReport`，把主仿真当前状态整理成 Markdown 并下载到本地。
+3. 展示层：把主仿真入口、运行结果摘要、基线对比、试运行覆盖、覆盖结果清单、固定演示口径里的乱码文案全部替换成人话文案。
+4. 状态层：把覆盖结果清单里的 readinessLabel 颜色判断改回真实中文值，避免“可完整演示 / 可局部演示 / 还不能演示”这三种状态显示错色。
+
+## 4. 前后对比
+
+| 项目 | 修改前 | 修改后 |
+| --- | --- | --- |
+| 导出能力 | 页面里只能看，不能直接导出 | 可以一键导出演示清单 Markdown |
+| 主仿真文案 | 主仿真区存在 `????` 乱码 | 全部换成中文人话文案 |
+| 覆盖状态显示 | readinessLabel 判断值损坏，颜色逻辑不稳 | 按真实中文状态稳定显示 |
+| 演示交付 | 需要手工摘页面信息 | 页面直接产出演示材料 |
+
+## 5. 影响范围
+
+影响范围集中在 WE1 第一张图主仿真页，不碰后端、不碰求解器、不碰别的视图。具体入口在 [MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx) 的 `simulation` 页签和导出函数附近。
+
+## 6. 怎么验证的
+
+1. 构建验证：执行 `npm run build`，通过。
+2. 回归验证：执行 `python backend/scripts/verify_we1_phase34_final.py`，通过。
+3. 子代理验证：独立子代理复核 [MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx) 当前主仿真页，结论为通过，确认“试运行覆盖”“覆盖结果清单”“固定演示口径”和“导出演示清单”入口没有明显功能性阻断。
+
+## 7. 截图与替代说明
+
+这一轮没有附截图，因为当前重点是页面文案修复和导出内容闭环，用表格和验证结果更能准确说明改动；如果后面需要做操作说明书，再补 UI 截图更合适。
+
+## 8. 当前结果
+
+第6阶段第1轮已经收口，主仿真页现在既能导出，也能正常展示人话文案，可以进入下一轮的“演示快捷操作”增强。
*** End Patch

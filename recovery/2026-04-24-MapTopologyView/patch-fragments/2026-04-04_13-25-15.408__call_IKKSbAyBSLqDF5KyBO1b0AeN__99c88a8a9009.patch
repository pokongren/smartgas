*** Begin Patch
*** Add File: F:\smartgas-grid\docs\2026-04-04-地图拓扑乱码修复总结.md
+# 地图拓扑乱码修复总结
+
+## 改动目标
+
+把地图拓扑编辑器里那颗紫色按钮的乱码文案修正为正常中文，避免用户把“手工捏合入口”看成一串不可读字符。
+
+## 改动前的问题
+
+- 页面位置：地图拓扑编辑器左侧编辑面板
+- 具体问题：紫色按钮文案显示成乱码
+- 实际影响：
+  - 用户看不懂按钮含义
+  - 会误判这是编码坏了、配置坏了，或者功能本身不可用
+  - 会降低“运行时枢纽 / 旧流程兼容”这一段交互的可信度
+
+## 改动后的结果
+
+- 按钮文案现在应显示为：`兼容手工捏合（非主流）`
+- 同一块区域里的相关文案保持正常：
+  - `浏览`
+  - `添点`
+  - `连线`
+  - `运行时枢纽`
+  - `legacy_editable / 旧流程兼容`
+
+## 为什么这么做
+
+这类问题不是样式问题，也不是浏览器渲染问题，本质上就是文案本身出现了历史错码。  
+这次处理目标很明确：不扩大改动面，只把用户当前能看到、最影响理解的那条乱码收口，同时确认前端重新构建后不会继续带旧包。
+
+## 影响范围
+
+- 直接影响：
+  - 地图拓扑编辑器左侧编辑面板中的“手工捏合兼容入口”
+- 不影响：
+  - 拓扑计算逻辑
+  - 枢纽加载逻辑
+  - 点位保存 / 预览 / 连线 / 验证等功能行为
+
+## 关键文件
+
+- `F:\smartgas-grid\src\views\MapTopologyView.tsx`
+- `F:\smartgas-grid\docs\2026-04-04-地图拓扑乱码修复总结.md`
+
+## 验证方式
+
+### 1. 代码核对
+
+- 核对 `MapTopologyView.tsx` 中对应按钮文案，确认当前源码为：
+  - `兼容手工捏合（非主流）`
+
+### 2. 构建验证
+
+- 执行：`npm run build`
+- 结果：构建通过，没有出现编译错误
+
+## 验证结果
+
+| 项目 | 结果 |
+| --- | --- |
+| 目标文案源码是否正常 | 通过 |
+| 前端生产构建是否通过 | 通过 |
+| 是否改动业务逻辑 | 否 |
+
+## 前后对比
+
+### 说明
+
+这次任务是单条界面文案修复，没有在本次执行里自动采集新的页面截图。  
+所以这里用可读表格代替前后对比图。
+
+| 对比项 | 改动前 | 改动后 |
+| --- | --- | --- |
+| 紫色按钮文案 | 乱码，无法识别 | `兼容手工捏合（非主流）` |
+| 用户理解成本 | 高，容易误判功能异常 | 低，能直接理解入口含义 |
+| 交互可信度 | 受影响 | 恢复正常 |
+
+## 风险点
+
+- 如果当前运行中的前端页面还挂着旧 bundle，界面上可能暂时还会继续看到旧文案
+- 这不是本次修复失败，更像是运行中的页面没有切到最新产物
+
+## 回退方式
+
+- 这次是单条文案修正
+- 如果需要回退，只需恢复 `F:\smartgas-grid\src\views\MapTopologyView.tsx` 对应文案即可
*** End Patch

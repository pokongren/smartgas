*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
 2026-04-03 12:19 CST
 
 - This run inspected the current `cwd`/git root `C:\Users\Administrator\.codex\worktrees\6c4b\smartgas-grid` only, as requested, and did not follow the historical live-repo path from older memory entries.
 - `git -c safe.directory=... rev-parse --show-toplevel` confirms the active root is the current worktree; `git log -5` still shows only `6410a10 Initial commit`, while `git status --short`, `git diff --stat`, and docs checks show no uncommitted file changes in this root.
 - The active root contains only `.git` and `README.md`; all requested inspection targets remain absent, including `src/utils/mapRenderer.ts`, `src/utils/hierarchyRenderer.ts`, `src/views/MapTopologyView.tsx`, `src/views/GlobalPipelineView.tsx`, `backend/app/models.py`, `backend/app/services/junction_groups.py`, `backend/app/routers/topology_editor.py`, `backend/data/smartgas.db`, `backend/check_db.py`, `backend/scripts/add_sj2.py`, `backend/scripts/add_we3.py`, `backend/scripts/reimport_sj23.py`, `src/services/api.ts`, `src/hooks/useTopology.ts`, and the `docs` directory.
 - `git worktree list --porcelain` again shows the real development branch living at `F:\smartgas-grid` while the current worktree is detached at the minimal `6410a10` snapshot, so the mismatch is between the current git root and the live project worktree rather than a newly observed regression in code or database content.
 - Conclusion for this run: classify the current state as a path/sync blockage, not as code changes, database changes, or local experimental data under the active git root; runtime recorded at `2026-04-03 12:19:50 +08:00`.
+
+2026-04-03 12:27 CST
+
+- Updated `C:\Users\Administrator\.codex\automations\automation\automation.toml` to make the heartbeat prompt explicitly classify empty detached worktrees as `worktree/path sync blockage` before attempting the three inspection tracks.
+- Added `C:\Users\Administrator\.codex\automations\automation\HEARTBEAT_GUIDE.md` as a plain-language operator guide covering entry checks, when to stop at blockage, required output structure, and allowed write scope.
+- Purpose of this change: stop future heartbeat runs from repeatedly producing pseudo-empty business conclusions when the active git root only contains `.git` and `README.md`; runtime recorded at `2026-04-03 12:27 +08:00`.
*** End Patch

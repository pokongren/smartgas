*** Begin Patch
*** Add File: docs/2026-04-20-快照标签乱码修复第2阶段第5轮记录.md
+# 2026-04-20 快照标签乱码修复第2阶段第5轮记录
+
+一、结论  
+已修复地图上快照标签的乱码显示，当前会以正常中文显示“进/出/基线温度”等信息，同时保持快照压力覆盖展示。
+
+二、上下文与已知信息  
+1. 你反馈加载快照后画面出现一堆乱码。2. 乱码集中在地图节点标签的快照文案区。3. 该区域文案来自旧的编码污染字符串。
+
+三、主代理与子代理分工  
+1. 侦察：定位到 `MapTopologyView.tsx` 的 `extraLabel` 与 `baselineExtraLabel` 文案模板。2. 实施：新增干净的显示模板并接入渲染。3. 验证：执行前端构建确认编译通过。
+
+四、修改逻辑  
+1. 在地图节点内容拼装中新增 `displayExtraLabel` 与 `displayBaselineExtraLabel`，文案统一为正常中文。2. 最终渲染改用新模板输出，避免继续使用旧乱码模板。
+
+五、执行顺序  
+1. 定位乱码来源。2. 覆盖渲染输出模板。3. 构建验证。
+
+六、验证方式  
+1. 命令验证：`npm run build`。2. 结果：通过。3. 页面预期：加载快照后，节点标签展示“进xx | 出xx MPa / 基进xx | 基出xx MPa | 基温xx°C”。
+
+七、风险与回退点  
+1. 风险较低，仅改前端展示文本，不改求解逻辑。2. 回退点：单文件 `src/views/MapTopologyView.tsx`。
+
+八、最终交付物  
+1. 代码文件：`src/views/MapTopologyView.tsx`。2. 记录文件：`docs/2026-04-20-快照标签乱码修复第2阶段第5轮记录.md`。
*** End Patch

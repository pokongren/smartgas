*** Begin Patch
*** Add File: F:/smartgas-grid/docs/automation/project-heartbeat-2026-04-08-051448.md
+项目心跳巡检总结（2026-04-08 05:14:48 +08:00）
+一、主目标
+固定在 `F:\smartgas-grid` 主目录做项目心跳巡检，只认主目录事实，不拿 detached worktree 顶结论；核对 git 现场、docs 变化和 6 个固定必查项；本轮只新增巡检文档并更新 automation memory，不改业务代码。
+二、上下文与已知信息
+1. 主目录事实：`git rev-parse --show-toplevel` 返回 `F:/smartgas-grid`，`.git` 就在主目录下，不是 worktree 指针文件；`git worktree list --porcelain` 显示主目录工作树 `HEAD=f75536d67424e7fd48dbfc34d3d9223f986836b4`，分支为主目录分支，`C:\Users\Administrator\.codex\worktrees\*\smartgas-grid` 才是 detached worktree。2. git 现场：最近 5 条提交头部是 `f75536d`、`5ec5af0`、`602f715`、`eaf6ac4`、`b31a010`；未提交改动集中在 AI 助手、WE1 仿真链、地图层级和校验脚本，`docs` 当前没有已跟踪文件 diff，但主目录下有 1 个未跟踪中文 Markdown，总体说明本轮开发重心仍在 AI 与 WE1。3. docs 现场：`docs` 目录最近新增内容主要是 2026-04-08 的 WE1 收口文档，`docs/automation` 最近一份项目心跳文档是 [`F:/smartgas-grid/docs/automation/project-heartbeat-2026-04-08-022117.md`](/F:/smartgas-grid/docs/automation/project-heartbeat-2026-04-08-022117.md)。
+三、主代理与子代理分工
+1. 主代理：锁定主目录事实，汇总 git、源码链路、脚本验证和文档变化，统一写巡检结论与 memory。2. 侦察代理：分路核对地图缩放与层级显示、甪直 AI 分析、拓扑仿真、枢纽规则与数据库口径、导入脚本、AI 模块链路。3. 验证代理：独立运行只读校验脚本，补齐“通过标准”和“失败时先查哪层”。
+四、修改逻辑
+1. 本轮不动业务代码，只做事实核验和归档，避免把巡检动作混进当前大量未提交开发改动。2. 结论按“主目录事实 + 源码主链 + 独立脚本验证”三段证据收口，优先给出能落地的排查顺序，不做 detached worktree 推断。
+五、执行顺序
+1. 第1阶段第1轮：校验 `F:\smartgas-grid` 主目录、`.git`、`git log`、`git status`、`git worktree list`。2. 第1阶段第2轮：并行侦察地图层级、甪直 AI、拓扑仿真、枢纽规则、导入脚本和 AI 模块链路。3. 第1阶段第3轮：运行 `python backend/check_db.py --require-system we3`、`python backend/scripts/verify_luzhi_pilot_phase6.py`、`python backend/scripts/verify_luzhi_pilot_phase7.py` 做只读验证。4. 第1阶段第4轮：写入本次巡检文档与 automation memory。
+六、巡检结果
+1. 地图缩放与层级显示：(1) 主链仍是 [`F:/smartgas-grid/src/utils/hierarchyRenderer.ts`](/F:/smartgas-grid/src/utils/hierarchyRenderer.ts) 第14至18行定义阈值，再由渲染链消费；当前阈值是 `criticalZoom=4`、`highZoom=9`、`mediumZoom=12`、`valveMinZoom=11`、`expandClusterZoom=12`，并且 `isHub===true` 仍被强制提升为 `CRITICAL`，保证全国视图也不会把枢纽节点压没。(2) 通过标准：阈值定义只有一套，枢纽节点在低缩放级仍可见，阀室只在 `zoom>=11` 出现。失败时先查 [`F:/smartgas-grid/src/utils/hierarchyRenderer.ts`](/F:/smartgas-grid/src/utils/hierarchyRenderer.ts)，再查渲染消费层。3) 风险判断：当前这条链没有发现主目录级回退迹象。
+2. 甪直 AI 分析：(1) 三个接口都落在 [`F:/smartgas-grid/backend/app/routers/ai_assistant.py`](/F:/smartgas-grid/backend/app/routers/ai_assistant.py) 第150至203行，分别是 `GET /api/ai-assistant/luzhi-pilot/trace`、`summary`、`report`；甪直试点分支命中顺序仍是第130至145行的 `direct -> luzhi pilot -> orchestrated -> legacy`，也就是先走直答，再判断 `try_luzhi_pilot_reply`，再落编排或 legacy。(2) 页面上下文与运行时上下文注入链仍闭合：[`F:/smartgas-grid/src/views/GlobalPipelineView.tsx`](/F:/smartgas-grid/src/views/GlobalPipelineView.tsx) 第1297至1391行负责拉 trace/summary/report，第1803至1905行负责 trace 面板消费；[`F:/smartgas-grid/src/components/ai-assistant/runtimeAssistantContext.ts`](/F:/smartgas-grid/src/components/ai-assistant/runtimeAssistantContext.ts) 第15至31行负责运行时选择态注入；[`F:/smartgas-grid/src/components/ai-assistant/useAiAssistantPageContext.ts`](/F:/smartgas-grid/src/components/ai-assistant/useAiAssistantPageContext.ts) 第52至67行把 route、page、selection、filters 一起送进 chat 上下文；[`F:/smartgas-grid/src/components/scada/LuzhiHistoryPanel.tsx`](/F:/smartgas-grid/src/components/scada/LuzhiHistoryPanel.tsx) 第26至33行新增 `initialHours`、`initialViewMode`、`focusHint`，说明页面回链上下文也在补强。(3) trace 文件是 [`F:/smartgas-grid/backend/data/ai_traces/luzhi_pilot_trace.jsonl`](/F:/smartgas-grid/backend/data/ai_traces/luzhi_pilot_trace.jsonl)，本轮读到的最近更新时间是 `2026-04-08 02:16:43 +08:00`，说明 trace 仍在刷新。(4) 独立验证结果：`verify_luzhi_pilot_phase6.py` PASS，`verify_luzhi_pilot_phase7.py` PASS。(5) 通过标准：`trace/summary/report` 三接口都能返回完整结构，trace 文件持续更新，页面上下文和运行时上下文都能进入 chat request，甪直分析类提问能优先命中 `luzhi pilot`。失败时先查页面注入层 [`F:/smartgas-grid/src/views/GlobalPipelineView.tsx`](/F:/smartgas-grid/src/views/GlobalPipelineView.tsx)，再查上下文拼装层 [`F:/smartgas-grid/src/components/ai-assistant/useAiAssistantPageContext.ts`](/F:/smartgas-grid/src/components/ai-assistant/useAiAssistantPageContext.ts)，再查后端分支判定和 trace 持久化层 [`F:/smartgas-grid/backend/app/routers/ai_assistant.py`](/F:/smartgas-grid/backend/app/routers/ai_assistant.py)。
+3. 拓扑仿真功能：(1) `simulation-overlay` 主链成立，前端主入口是 [`F:/smartgas-grid/src/hooks/useSimulation.ts`](/F:/smartgas-grid/src/hooks/useSimulation.ts) 第199至231行，请求 `GET /api/pipeline-packages/simulation-overlay?pilot_id=...&scenario_id=...`；后端路由是 [`F:/smartgas-grid/backend/app/routers/topology_simulation.py`](/F:/smartgas-grid/backend/app/routers/topology_simulation.py) 第197至227行，内部先 `_build_solver_input_or_raise()`，再落到 [`F:/smartgas-grid/backend/app/services/we1_solver_input_service.py`](/F:/smartgas-grid/backend/app/services/we1_solver_input_service.py) 第242至308行组装 solver input，最终交给 [`F:/smartgas-grid/backend/app/services/topology_simulation.py`](/F:/smartgas-grid/backend/app/services/topology_simulation.py) steady solver 求解。(2) `MapTopologyView` 独立仿真入口现在已经接上主链，不再是旧 `/api/emergency/simulate-failure`：[`F:/smartgas-grid/src/views/MapTopologyView.tsx`](/F:/smartgas-grid/src/views/MapTopologyView.tsx) 第280至309行直接解构 `useSimulation()`，第1415至1428行和第1965至1970行都在调用 `runSteadySimulation()`、`saveSteadySnapshot()`、`refreshSteadySnapshots()`；也就是地图页自己的入口已经和 `simulation-overlay + snapshots` 主链对齐。(3) 旧链路仍有兼容残留：[`F:/smartgas-grid/src/services/api.ts`](/F:/smartgas-grid/src/services/api.ts) 第95至100行仍保留 `topologyAPI.simulate()` 到 `/api/emergency/simulate-failure`，但它已不是 `MapTopologyView` 的主入口，只是遗留兼容接口。(4) WE1 校验脚本与当前主链基本一致：`verify_we1_phase34_final.py` 直接校验 router 层 `solve_steady_api/get_simulation_overlay` 契约；`verify_we1_phase5.py` 校验 `overlay/meta/summary` 契约和 `MapTopologyView`、`useAiAssistantPageContext` 消费点；`verify_we1_map_phase2.py` 校验 overlay source-id 映射摘要；`verify_we1_map_phase5.py` 校验 map 页 overlay、snapshot、场景覆盖与失败定位层。主链视角看，这几类脚本都围绕当前 `simulation-overlay + snapshots + mapping` 在收口，是一致的。(5) 通过标准：前端入口真实走 `useSimulation`，router 能装配 `build_we1_solver_input`，solver input 能给出完整 `nodes/edges/scenarios`，steady solver 返回 `solver_status/nodes/edges/summary/meta/run_id/generated_at`。失败时先查前端入口 [`F:/smartgas-grid/src/views/MapTopologyView.tsx`](/F:/smartgas-grid/src/views/MapTopologyView.tsx) 和 [`F:/smartgas-grid/src/hooks/useSimulation.ts`](/F:/smartgas-grid/src/hooks/useSimulation.ts)，再查 router [`F:/smartgas-grid/backend/app/routers/topology_simulation.py`](/F:/smartgas-grid/backend/app/routers/topology_simulation.py)，再查 solver input [`F:/smartgas-grid/backend/app/services/we1_solver_input_service.py`](/F:/smartgas-grid/backend/app/services/we1_solver_input_service.py)，最后查 steady solver [`F:/smartgas-grid/backend/app/services/topology_simulation.py`](/F:/smartgas-grid/backend/app/services/topology_simulation.py)。
+4. 枢纽规则与数据库口径：(1) `python backend/check_db.py --require-system we3` 结果显示 `pipeline_systems.count=13`，`we3` 仍缺失，`sj2/sj3` 存在，`junction_groups_rebuilt.count=7`、`issue_count=0`、`major_junction_count=0`，最新 rebuilt 生成时间是 `2026-04-04T17:17:43.361829+00:00`。(2) 运行时口径仍应优先认 rebuilt 影子表，源码证据在 [`F:/smartgas-grid/backend/app/services/junction_groups.py`](/F:/smartgas-grid/backend/app/services/junction_groups.py) 第321行以后和第453行以后：先取 `junction_groups_rebuilt`，再叠手工 overlay，再排序输出，老表只做回退兼容。(3) 通过标准：`check_db` 无 rebuilt issue，运行时优先 rebuilt，`we3` 缺失被如实暴露而不是被脚本存在感掩盖。失败时先查 DB 文件与 [`F:/smartgas-grid/backend/check_db.py`](/F:/smartgas-grid/backend/check_db.py)，再查 [`F:/smartgas-grid/backend/app/services/junction_groups.py`](/F:/smartgas-grid/backend/app/services/junction_groups.py) 的运行时读取口径。
+5. 导入脚本：(1) 甪直导入脚本主入口是 [`F:/smartgas-grid/backend/import_luzhi_scada.py`](/F:/smartgas-grid/backend/import_luzhi_scada.py) 第28至39行和第95至124行，当前仍按固定 Excel 路径、固定列映射写 `scada_history`，覆盖 `we1/we2/cred` 三组 pressure/temperature 数据。(2) 通过标准：列映射与目标管线 ID 一致，写入前先删站点历史，写入后按 pipeline + metric 回查数量。失败时先查 Excel 路径和列映射，再查 `ScadaHistory` 写库，再查回查计数输出。3) 风险判断：它仍是一次性脚本形态，路径硬编码没改，适合人工执行，不适合拿它当自动化稳定入口。
+6. AI 模块链路：(1) 当前 AI 主链仍是页面上下文 + 运行时上下文双注入后进入 chat，再由后端决定 `direct / luzhi pilot / orchestrated / legacy`；甪直试点是这条大链里的优先分支，不是旁路功能。(2) 通过标准：上下文对象里 route/page/selection/filters 持续存在，甪直页面能带着 `luzhi_snapshot` 回链，后端分支顺序不被 orchestrated 或 legacy 抢先吃掉。失败时先查前端上下文，再查后端分支顺序。
+七、验证方式
+1. 命令验证：`python backend/check_db.py --require-system we3` 已执行，结果是 rebuilt 口径通过、`we3` 缺失仍未补齐。2. 接口验证：`python backend/scripts/verify_luzhi_pilot_phase6.py`、`python backend/scripts/verify_luzhi_pilot_phase7.py` 已执行，结果均为 PASS。3. 源码回归：已核对 `GlobalPipelineView`、`useAiAssistantPageContext`、`runtimeAssistantContext`、`MapTopologyView`、`useSimulation`、`topology_simulation.py`、`we1_solver_input_service.py`、`hierarchyRenderer.ts` 等关键链路文件，源码口径与脚本验证结论一致。
+八、风险与回退点
+1. 当前最主要风险不是甪直 AI 接口缺失，也不是 WE1 steady solver 主链断开，而是仓库里仍保留 `src/services/api.ts` 里的旧 `simulate-failure` 兼容入口，后续如果别的页面继续调用这个旧方法，容易把“地图页已收口”误读成“全仓都已切主链”。2. `we3` 仍未落库，这个事实已经被 `check_db` 明确暴露，后续任何“导入已完成”的说法都必须先过 DB 证据。3. 本轮回退点很简单，只新增本文件和 memory 里的 1 条巡检记录，删掉这两处文本即可完整回退。
+九、最终交付物
+1. 巡检文档：[`F:/smartgas-grid/docs/automation/project-heartbeat-2026-04-08-051448.md`](/F:/smartgas-grid/docs/automation/project-heartbeat-2026-04-08-051448.md)。2. automation memory：[`C:/Users/Administrator/.codex/automations/automation/memory.md`](/C:/Users/Administrator/.codex/automations/automation/memory.md)。
+十、前后对比
+1. 本轮前：需要重新确认主目录事实、甪直 pilot 三接口是否稳定、MapTopologyView 是否还挂旧仿真链、DB 口径是否仍优先 rebuilt。2. 本轮后：主目录事实已锁定为 `F:\smartgas-grid`；甪直 `trace/summary/report` 三接口和 trace 更新时间都正常，phase6/7 校验 PASS；`MapTopologyView` 独立仿真入口已并到 `useSimulation -> simulation-overlay + snapshots` 主链；DB rebuilt 口径通过但 `we3` 仍缺失。3. 本轮没有界面改版，也没有新增验收截图，因此用人话表和源码链路替代截图对比。
+十一、事实对照表
+| 项目 | 巡检前疑问 | 巡检后结论 |
+| --- | --- | --- |
+| 主目录落点 | 会不会只是 detached worktree 视角 | 主目录事实就是 `F:\smartgas-grid`，`.git` 在根目录 |
+| 甪直 AI | `trace/summary/report` 是否齐、trace 是否还在写 | 三接口齐全，trace 最近更新时间 `2026-04-08 02:16:43 +08:00`，phase6/7 PASS |
+| MapTopologyView | 是否还在走旧 `/api/emergency/simulate-failure` | 地图页主入口已走 `useSimulation` 主链，旧接口只剩兼容残留 |
+| WE1 校验 | 脚本是否和主链脱节 | `phase34_final/phase5/map_phase2/map_phase5` 都围绕 overlay、snapshot、mapping 主链 |
+| 枢纽/DB | 运行时认哪张表，`we3` 到没到库 | 运行时优先 `junction_groups_rebuilt`，`we3` 仍缺失 |
*** Update File: C:/Users/Administrator/.codex/automations/automation/memory.md
@@
 2026-04-08 02:21:17 +08:00 项目心跳：主目录事实锁定于 `F:\smartgas-grid`，`.git` 在主目录，`拓扑设想` 分支 ahead 3；甪直 pilot 三支校验 PASS，trace 文件 `backend/data/ai_traces/luzhi_pilot_trace.jsonl` 刷新到 2026-04-08 02:16:43 +08:00，分支顺序仍是 direct -> luzhi pilot -> orchestrated -> legacy；WE1 联合回归 PASS，`simulation-overlay -> build_we1_solver_input -> solve_steady` 主链成立，但 `MapTopologyView` 仍走 `/api/emergency/simulate-failure` 旧链；`check_db` PASS，`junction_groups_rebuilt.count=7`、`issue_count=0`，`we3` 仍 missing；本轮仅新增巡检文档 `docs/automation/project-heartbeat-2026-04-08-022117.md`，未改业务代码。运行时长约 18 分钟。
+2026-04-08 05:14:48 +08:00 项目心跳：主目录事实再次锁定于 `F:\smartgas-grid`，主目录 `.git` 为真实仓库根，worktree 列表里的 detached worktree 只作旁证；`git status` 显示改动仍集中在 AI 助手、WE1 仿真、地图层级与校验脚本，docs 没有新增 tracked diff；甪直巡检确认 `trace/summary/report` 三接口都在 `backend/app/routers/ai_assistant.py`，`verify_luzhi_pilot_phase6.py` 与 `verify_luzhi_pilot_phase7.py` 均 PASS，trace 文件 `backend/data/ai_traces/luzhi_pilot_trace.jsonl` 最近更新时间仍为 2026-04-08 02:16:43 +08:00，页面上下文与运行时上下文注入链仍是 `GlobalPipelineView -> runtimeAssistantContext -> useAiAssistantPageContext`，实际命中顺序仍是 direct -> luzhi pilot -> orchestrated -> legacy；WE1 巡检确认 `MapTopologyView` 现已接入 `useSimulation -> /api/pipeline-packages/simulation-overlay -> build_we1_solver_input -> solve_steady` 主链，旧 `/api/emergency/simulate-failure` 仅在 `src/services/api.ts` 保留兼容方法，`verify_we1_*` 脚本口径与 overlay/snapshot/mapping 主链一致；`check_db --require-system we3` 结果为 rebuilt 口径 PASS，`junction_groups_rebuilt.count=7`、`issue_count=0`，但 `we3` 仍 missing；导入脚本 `backend/import_luzhi_scada.py` 仍是硬编码 Excel 的人工脚本入口。本轮仅新增巡检文档 `docs/automation/project-heartbeat-2026-04-08-051448.md` 并更新 memory，未改业务代码。运行时长约 5 分钟。
*** End Patch

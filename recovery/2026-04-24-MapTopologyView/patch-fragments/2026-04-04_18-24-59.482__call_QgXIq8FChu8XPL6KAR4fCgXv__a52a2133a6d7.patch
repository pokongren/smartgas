*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
+## 2026-04-05 02:24:19 +08:00
+
+- 本轮先按入口规则落到主目录执行：初始 `cwd=C:\Users\Administrator\.codex\worktrees\d06e\smartgas-grid`，切换后 `cwd=F:\smartgas-grid`，`git rev-parse --show-toplevel=F:/smartgas-grid`，`git rev-parse --git-dir=.git`，`git rev-parse --git-common-dir=.git`。结论只基于 `F:\smartgas-grid`，没有沿用 detached worktree 当事实源。
+- 主目录不是空壳。`F:\smartgas-grid` 下存在 `.git/`、`src/`、`backend/`、`docs/`，且本轮要求检查的地图、拓扑、数据库、AI 相关关键文件都在主目录里。
+- `git worktree list` 继续显示：主目录 `F:/smartgas-grid` 在分支头 `eaf6ac4`；大量 `.codex/worktrees/*/smartgas-grid` 都停在 `6410a10` detached。以后先复用这个判断模板，但每轮仍要重新核对入口，不能拿旧 memory 直接代替。
+- 本轮主要是主目录里的未提交代码变化 + 数据库变化 + 本地实验混合，不是 `worktree/path blockage`。`git status` 里四条主线都有改动：地图层级（`src/utils/hierarchyRenderer.ts`、`src/utils/mapRenderer.ts`、`src/views/GlobalPipelineView.tsx`、`src/views/MapTopologyView.tsx`）、枢纽/拓扑（`backend/app/services/junction_groups.py`、`backend/app/routers/topology_editor.py`）、数据库/导入脚本（`backend/data/smartgas.db`、`backend/check_db.py`、`backend/scripts/add_sj2.py`、`backend/scripts/add_we3.py`、`backend/scripts/reimport_sj23.py`、`src/services/api.ts`、`src/hooks/useTopology.ts`）、AI 链路（`backend/app/routers/ai_assistant.py`、`backend/app/services/ai_client.py`、`src/components/ai-assistant/AiAssistant.tsx`）。
+- 地图层级：统一 LOD 规则已经落在 `src/utils/hierarchyRenderer.ts` + `src/utils/mapRenderer.ts`，阈值是 `critical/high/medium=6/10/14`、`valveMinZoom=11`、`expandClusterZoom=12`，并且 source / junction / compressor / distribution / valve 的重要性口径已切到 `rawType` / `junction_kind`。但 `MapTopologyView.tsx` 仍然是自己在主视图里构造 collapsed graph，不直接走 `mapRenderer`；`GlobalPipelineView.tsx` 通过 `MapView -> mapRenderer` 间接吃到了这套规则。以后不要把它说成“两种视图已经完全统一”。
+- 枢纽规则：`backend/app/services/junction_groups.py` 已经形成“运行时优先读 `junction_groups_rebuilt`，旧 `junction_groups` 兜底，manual overlay 另叠一层”的口径；`JUNCTION-x` 引用展开、同组归并、`junction_kind` / `system_ids` / `member_count` 运行时字段都在这里做。`backend/app/routers/topology_editor.py` 的 `/api/topology/junctions` 已切到 `load_runtime_junction_groups()`，并暴露 `manual_editing_enabled` + `mode(runtime_readonly|legacy_editable)`；手工 merge / create / delete 受 `TOPOLOGY_MANUAL_JUNCTION_EDIT` 控制。
+- 数据库口径：`smartgas.db` 当前实测 `stations=1272`、`pipelines=1268`、`pipeline_systems=14`、`junction_groups=18`、`junction_groups_rebuilt=7`，没有 `layers_config` 表；`junction_groups_backup_20260404` 表存在。ID/system 口径目前是 `WE1/WE2/CRED/ZM/ZG/GN/SJ2/SJ3/SJ4/PT/NCSH/GS/WE3/JXLZ` 这 14 个系统；`WE3` 站点 16、管段 15，`SJ2` 站点 70、`SJ3` 站点 71，三条线零坐标都为 0。`backend/scripts/add_we3.py`、`backend/scripts/add_sj2.py`、`backend/scripts/reimport_sj23.py` 这轮共同在把管段 `category` 收口到 `trunk/branch` 并补验证输出，更像导入脚本和数据修正一起推进，不要草率定性为唯一正式导入链路。
+- API/端口口径：主项目已切到 `8080`。`backend/run.py` 和 `vite.config.ts` 都对齐 `8080`；`src/services/apiBase.ts` 把 `VITE_API_BASE_URL` 拆成 `API_BASE_URL=/api` 和 `BACKEND_BASE_URL`。现在前端是双口径并存：`src/services/topologyEditorApi.ts`、`src/services/api.ts` 里的普通拓扑编辑接口走 `/api/topology/...`，`src/hooks/useTopology.ts` 和 `src/services/api.ts` 里的拓扑计算/纠偏走直连 `/topology/...`。docs 已经在往这个新口径收，但还要继续盯。
+- AI 链路：主目录里前后端入口已经接上，`backend/app/main.py` 已 include `ai_assistant.router`，前端 `src/components/ai-assistant/AiAssistant.tsx` -> `useAiAssistantChat.ts` -> `/api/ai-assistant/chat`，后端 `backend/app/routers/ai_assistant.py` 会根据 `analysis_mode/context` 走 `AiAnalysisOrchestrator`，再串 `AnalysisValidator` 和 `FixTaskRouter`。但 `backend/app/services/ai_analysis_orchestrator.py`、`backend/app/services/analysis_validator.py`、`backend/app/services/fix_task_router.py`、`src/components/ai-assistant/useAiAssistantChat.ts`、`src/components/ai-assistant/useAiAssistantPageContext.ts`、`src/services/apiBase.ts`、`src/services/topologyEditorApi.ts`、`src/utils/pipelineDomain.ts`、`src/views/global-pipeline/` 目前还是未跟踪文件，所以对外只能说“主目录里的本地未提交能力/半接线状态”，不能说“已纳入正式提交基线”。
+- AI 额外风险：`backend/app/services/ai_client.py` 保留了缺失 `AI_API_KEY` 的 fallback，但多处中文提示和修复提示字符串出现乱码；前端 `useAiAssistantChat.ts` 的错误文案也有乱码痕迹。下轮如果继续看 AI，优先复核这些乱码文本和未跟踪文件是否需要收编。
+- docs 变化本轮明确存在：`docs/TOPOLOGY_COMPUTATION_USAGE.md`、`docs/TOPOLOGY_SETUP_GUIDE.md` 正在把旧 `8000` / `/api/topology/...` 说法改成 `8080` + “`/api/topology` 与 `/topology` 并存”的现实口径。以后如果业务面没法下很死的结论，先说明文档正在收口，不要一句“无变化”带过。
+- 本轮运行时间：`2026-04-05T02:24:19.6548210+08:00`
*** End Patch

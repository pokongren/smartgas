*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-09-WE1第三张图仿真展示专页第4阶段第1轮总结_人话版.md
+一、主目标
+
+1. 这一轮的目标不是再加新功能，而是给上一轮的联动链收尾。重点有两个，一是第三张图要更像正式展示页，二是第一张图写过去的联动上下文不能一直残留。
+
+二、上下文与已知信息
+
+1. 上一轮已经打通了“第一张图主仿真上下文单向带到第三张图”的主链。也就是说，第一张图能把场景、快照、基线和 overlay 带到第三张图。
+
+2. 上一轮还留了两个尾巴。第一个尾巴是第三张图默认页还带着调试入口，展示页边界不够清楚。第二个尾巴是 `sessionStorage` 里的联动上下文虽然能读，但需要更明确的失效和清理规则。
+
+三、主代理与子代理分工
+
+1. 主代理负责收敛范围、改代码、跑构建、写总结。
+
+2. 侦察子代理负责只读检查“展示页边界”和“上下文残留”两个点，给出最小改动建议。它明确建议把第三张图默认入口收口到正式展示页，同时让空状态不再续命、显式清空时直接删 key。
+
+3. 验证子代理负责独立复核本轮改动，确认这次收尾是否达到了“失效控制已加、展示页口径更清楚”的目标。
+
+四、修改逻辑
+
+1. 修改了 [simulationShowcaseSync.ts](/F:/smartgas-grid/src/utils/simulationShowcaseSync.ts)。这次补了三件事。第一件事是给联动上下文加 2 小时 TTL。第二件事是新增显式清理函数 `clearSimulationShowcaseSyncContext()`。第三件事是读取时如果发现时间无效、超过 2 小时，或者解析失败，就直接清掉旧上下文。
+
+2. 修改了 [MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx)。这里做了两层收口。第一层是“空状态不续命”，也就是当正式 overlay 和已选快照都没有时，不再继续往 `sessionStorage` 里写展示上下文，而是直接清掉。第二层是“显式清空就立刻删 key”，也就是用户点“清空主仿真”时，不只清页面结果，也会同步清掉第三张图要读的联动上下文。
+
+3. 修改了 [TopologyView.tsx](/F:/smartgas-grid/src/views/TopologyView.tsx)。这里把展示页口径继续收紧了一步。现在第三张图会明确告诉用户“当前处于正式展示模式”还是“当前处于调试模式”，同时把“联动上下文 2 小时内有效”直接显示出来。默认展示页下，原来那个“打开高级调试操作面”按钮已经去掉，只在确实已经进入调试态时保留“收起调试操作面”。
+
+4. 修改了 [App.tsx](/F:/smartgas-grid/src/App.tsx)。全局悬浮入口原来直接跳第三张图调试态，这轮改成默认跳第三张图正式展示页。这样外层入口口径也和“第三张图是展示页”保持一致。
+
+五、执行顺序
+
+1. 先从联动上下文工具层下手，加 TTL 和清理函数。
+
+2. 再改第一张图的写入规则，解决“空状态续命”和“清空后残留”。
+
+3. 再改第三张图展示页，让展示态和调试态的口径更清楚，并去掉默认态调试入口。
+
+4. 最后改全局入口，让外层导航默认进入第三张图展示页。
+
+六、验证方式
+
+1. 主代理已执行 `npm run build`，构建通过。
+
+2. 独立验证子代理复核后给出的结论是可以放行。它确认两件事已经做到，一是联动上下文确实加了 2 小时失效控制，二是第三张图现在比上一轮更明确是展示页。
+
+3. 验证重点看的是四条线。第一条是超过 2 小时的旧上下文会不会被自动清掉。第二条是第一张图清空主仿真后，第三张图还会不会读到旧 run。第三条是第三张图默认页还会不会继续暴露调试入口。第四条是构建有没有因为这些收尾修改被打坏。
+
+七、风险与回退点
+
+1. 当前第三张图仍然支持手工带 `?debugActions=1` 进入调试态，所以它不是“彻底封死的纯展示页”，而是“默认展示、显式调试”的模式。这是有意保留的，不算 bug。
+
+2. TTL 现在设的是 2 小时，已经能解决“旧上下文长期残留”的问题。如果后面觉得时间还偏长，可以再往下收，但这一轮先不把门槛压得太激进，避免用户刚跑完仿真过一会儿回来就读不到。
+
+八、前后对比
+
+1. 修改前后对比如下。
+
+| 项目 | 修改前 | 修改后 |
+| --- | --- | --- |
+| 联动上下文过期 | 没有明确过期口径 | 2 小时后自动失效并清理 |
+| 读取异常处理 | 读坏了只返回空 | 读坏了会顺手清理旧 key |
+| 第一张图空状态处理 | 可能继续写上下文，形成空状态续命 | 没有 overlay 且没有快照时直接清理 |
+| 第一张图清空主仿真 | 页面清空了，但第三张图上下文可能还在 | 页面清空时同步清理第三张图上下文 |
+| 第三张图默认态 | 仍有“打开高级调试操作面”入口 | 默认展示态不再暴露调试入口 |
+| 全局第三张图入口 | 默认进调试态 | 默认进正式展示页 |
+
+2. 这轮没有补截图。原因是这次主要是入口、联动状态和过期控制的收口，文字和表格比截图更能准确说明差异。
+
+九、最终交付物
+
+1. 修改文件：[simulationShowcaseSync.ts](/F:/smartgas-grid/src/utils/simulationShowcaseSync.ts)、[MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx)、[TopologyView.tsx](/F:/smartgas-grid/src/views/TopologyView.tsx)、[App.tsx](/F:/smartgas-grid/src/App.tsx)。
+
+2. 本轮总结文件就是当前这份：[2026-04-09-WE1第三张图仿真展示专页第4阶段第1轮总结_人话版.md](/F:/smartgas-grid/docs/2026-04-09-WE1第三张图仿真展示专页第4阶段第1轮总结_人话版.md)。
*** End Patch

*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
 2026-04-04 15:45 CST
 
 - This run landed in the configured live repo root successfully: initial `cwd` was `C:\Users\Administrator\.codex\worktrees\c2e3\smartgas-grid`, but after switching, both runtime `cwd` and `git rev-parse --show-toplevel` resolved to `F:\smartgas-grid` / `F:/smartgas-grid`. Judgement was made from the live repo only, not from the detached worktree.
 - Current progress summary: this round is still real code change + database change + local experiment/noise mixed together, not a blockage. `git status --short` shows active edits across `src/`, `backend/`, `docs/`, plus local residue under `.npm-cache/`, `.agent/workflows/start.md`, and a garbled backup file.
 - Repo shape remains split: `git worktree list --porcelain` shows the live branch at `F:\smartgas-grid` on `HEAD eaf6ac4...`, while the automation worktree `C:\Users\Administrator\.codex\worktrees\c2e3\smartgas-grid` is detached at `6410a10...`. Future runs should keep reporting both heads when comparing live repo vs detached worktree.
@@
 - API contract drift remains mixed, though the current files in scope no longer hardcode a port: `src/services/api.ts` now splits between proxied `/api/...` routes and direct `/topology/...` correction routes; `src/hooks/useTopology.ts` now depends on shared base config and still speaks in `source/target` graph terms. Do not describe this as full API unification yet.
 - Local experiment residue worth remembering next run: `stations.properties` contains 89 `positionMeta` entries from sources like `map-topology` and `segment-reproject`, which look like local topology-position experiments. Combined with `MM` state in `src/services/api.ts` and `src/hooks/useTopology.ts`, this branch still mixes formal changes with local iteration.
 - No business code was modified by this heartbeat run. Memory updated only. Run completed at `2026-04-04 15:45:00 +08:00`.
+
+2026-04-04 10:23 CST
+
+- This run again passed the main-root entry check the right way: initial `cwd` was `C:\Users\Administrator\.codex\worktrees\7c57\smartgas-grid`, but all actual inspection was done after switching into `F:\smartgas-grid`, where both runtime `cwd` and `git rev-parse --show-toplevel` resolve to the live repo root.
+- Current progress summary: this round is still real code change + database content change + local experiment/noise mixed together, not a blockage. `git log -5` shows the live branch head `eaf6ac4...`; `git status --short` still has active edits in `src/`, `backend/`, and `docs/`, plus residue under `.npm-cache/`, `.agent/workflows/start.md`, and a garbled markdown filename.
+- Environment/path line stayed stable: `F:\smartgas-grid` is the only non-detached worktree in `git worktree list --porcelain`, while the Codex worktrees remain detached at `6410a10...`; `.git` in `F:\smartgas-grid` is a real git dir, not an empty pointer file, and all required inspection targets (`src`, `backend`, `docs`, DB, target files) exist under this root.
+- Map zoom / hierarchy line: `src/utils/hierarchyRenderer.ts` now defines live LOD thresholds (`critical=6`, `high=10`, `medium=14`, `valveMinZoom=11`, `expandClusterZoom=12`) and `src/utils/mapRenderer.ts` consumes them through `getNodeLODStrategy()` / `shouldShowNodeAtZoom()`. But the rule is still not fully self-consistent: valve nodes are classified as `LOW`, so they still remain hidden until `zoom >= 14` even though `valveMinZoom` says `11`; `src/views/GlobalPipelineView.tsx` uses the map render chain, while `src/views/MapTopologyView.tsx` still renders its own collapsed topology graph and even mounts `MapView` without passing `pipelineData`.
+- Source / junction line: backend normalization is still stronger than raw storage. `backend/app/services/junction_groups.py` expands `JUNCTION-x` references and merges overlaps, `backend/app/routers/topology_editor.py` expands submitted ids before create/delete and still allows `merge_stations` to write single-station junctions, while the DB still contains nested and single-system rows (for example `junction_groups.id=17` stores `["JUNCTION-16", "JXLZ-11"]`, and several rows like ids `6/7/9/11/12/13/15` only cover one station or one system prefix).
+- Database / import-script line: this is still DB-content change + script churn, not a schema migration. Live DB snapshot remains `stations=1256`, `pipelines=1253`, `pipeline_systems=13`, `junction_groups=16`; `pipeline_systems` schema is `id/name/color/sort_order/layers_config` and `junction_groups` schema is `id/name/description/station_ids`. `backend/scripts/add_sj2.py` removes 长阳 and shifts the local SJ2 segment, `backend/scripts/add_we3.py` replaces 长阳 with 西沙屯 and shortens the local WE3 segment, and new `backend/scripts/reimport_sj23.py` is a much stronger full delete-and-reimport flow from Excel for SJ2/SJ3.
+- API/docs line: runtime code still uses a mixed contract. `backend/run.py` and `vite.config.ts` are on `8080`; `src/hooks/useTopology.ts` uses `BACKEND_BASE_URL` and calls `/topology/...`; `src/services/api.ts` keeps `/api/topology/...` for graph/stats and `/topology/correct/...` for correction. `docs/TOPOLOGY_SETUP_GUIDE.md`, `docs/TOPOLOGY_COMPUTATION_USAGE.md`, and new `docs/code-improvement-review.md` now document this mixed state instead of pretending everything is still the old `/api/topology/...` path.
+- Local experiment residue worth remembering next run: DB `stations.properties` currently has 89 rows carrying `positionMeta`, which looks like local topology-position experiments rather than stable imported source data.
+- No business code was modified by this heartbeat run. Memory updated only. Run completed at `2026-04-04 10:23:27 +08:00`.
*** End Patch

*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-22-起始压力导入与拖动保压显示第1阶段第2轮记录.md
+# 2026-04-22 起始压力导入与拖动保压显示第1阶段第2轮记录
+
+一、主目标
+1. 把“起始压力导入”并入默认主仿真入口。2. 修复地图拖动时压力显示丢失的问题。
+
+二、实施内容
+1. 在 `runMainSteadySimulation` 中改为默认先读取最新站场压力，再组装本轮仿真初值并执行求解。2. 主仿真启动时自动打开压力上图显示，避免用户还要再点一次。3. 调整渲染安全模式切换条件，移除“仅因地图交互就切轻量重绘”的逻辑，避免拖动触发节点重建导致压力标签丢失。
+
+三、为什么这样改
+1. 起始压力导入必须在主入口统一执行，否则会出现“同一页面两个入口行为不一致”。2. 拖动触发轻量重绘是压力消失的高概率根因，去掉交互触发重绘后，压力层可保持连续。
+
+四、验证方式
+1. 编译验证：执行 `npm run build`，构建通过。2. 手工验证：在拓扑页运行主仿真后，持续拖动与缩放地图，观察压力标签是否持续显示。
+
+五、影响范围
+1. 仅前端文件 `src/views/MapTopologyView.tsx`，后端接口与数据结构不变。
+
+六、风险与回退
+1. 风险：超大图在持续交互时性能压力会升高。2. 回退点：可把安全模式切换逻辑恢复为原条件，但会回到“拖动可能丢压”的旧行为。
*** End Patch

*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
 2026-04-04 12:27 CST
 
 - This run again passed the main-root landing check the right way: initial `cwd` was `C:\Users\Administrator\.codex\worktrees\6e57\smartgas-grid`, then the run switched into `F:\smartgas-grid`, and both runtime `cwd` plus `git rev-parse --show-toplevel` resolved to `F:\smartgas-grid` / `F:/smartgas-grid`. Judgement stayed anchored to the live repo only.
 - Current progress summary: this round is still real code change + database content change + local experiment/noise mixed together, not `automation env mismatch / worktree-path blockage`. `F:\smartgas-grid` has a real `.git` directory, all required `src` / `backend` / `docs` / DB targets exist, and `git worktree list --porcelain` still shows this path as the only live branch worktree on `HEAD eaf6ac4...` while the Codex worktrees stay detached at `6410a10...`.
 - Map zoom / hierarchy line: `src/utils/hierarchyRenderer.ts` is still the active LOD source for the national map, and `src/utils/mapRenderer.ts` consumes it. But the valve rule is still internally inconsistent: `valveMinZoom=11` while `getNodeImportance()` classifies valves as `LOW`, so `shouldShowNodeAtZoom()` still hides valves until `zoom >= 14`. `src/views/GlobalPipelineView.tsx` goes through `MapView` with `pipelineData`, while `src/views/MapTopologyView.tsx` still mounts `MapView` without `pipelineData` and keeps its own collapsed-graph editor path.
 - Junction / DB / API line: backend normalization remains stronger than raw storage. `backend/app/services/junction_groups.py` expands `JUNCTION-x` refs and merges overlaps, but `backend/app/routers/topology_editor.py` still lets `merge_stations` create single-station `junction_groups` while `create_junction_group` rejects fewer than two bottom-level stations. Live DB snapshot from this run is still `stations=1256`, `pipelines=1253`, `pipeline_systems=13`, `junction_groups=16`; `pipeline_systems.id` still has no `we3`, and `junction_groups` still contains nested plus single-station rows.
 - Import/docs/noise line: `backend/scripts/add_sj2.py` and `backend/scripts/add_we3.py` still look like local partial-segment scripts, while new `backend/scripts/reimport_sj23.py` is a full SJ2/SJ3 delete-and-reimport flow from Excel. `docs/TOPOLOGY_SETUP_GUIDE.md` and `docs/TOPOLOGY_COMPUTATION_USAGE.md` still update the wording toward backend `8080` plus mixed `/api/topology/...` and `/topology/...` usage. Local residue is still mixed in the same tree: `.npm-cache/`, `.agent/workflows/start.md`, and AI-assistant related files should not be treated as topology facts.
 - No business code was modified by this heartbeat run. Memory updated only. Run completed at `2026-04-04 12:27:02 +08:00`.
+
+2026-04-04 12:29 CST
+
+- This run again landed on `F:\smartgas-grid` correctly: initial `cwd` was `C:\Users\Administrator\.codex\worktrees\6e57\smartgas-grid`, switched `cwd` became `F:\smartgas-grid`, and `git rev-parse --show-toplevel` returned `F:/smartgas-grid`. The live repo remained the only fact source for this run.
+- Current progress summary: still a mix of code change + database content change + local experiment/noise, not a path blockage. `git status --short` shows the topology/data files split between staged and unstaged work: DB/import-script changes are mostly staged (`backend/data/smartgas.db`, `backend/scripts/add_sj2.py`, `backend/scripts/add_we3.py`, `backend/scripts/reimport_sj23.py`), while docs/map-view edits are still in the working tree (`docs/TOPOLOGY_SETUP_GUIDE.md`, `docs/TOPOLOGY_COMPUTATION_USAGE.md`, `src/utils/mapRenderer.ts`, `src/views/MapTopologyView.tsx`).
+- Map line: the national-map chain is definitely wired through `src/utils/hierarchyRenderer.ts` -> `src/utils/mapRenderer.ts` -> `MapView`, and `src/views/GlobalPipelineView.tsx` still passes `pipelineData` into that chain. But `src/views/MapTopologyView.tsx` still mounts `MapView` without `pipelineData`, keeps its own collapsed-graph editor path, maps `junction` to `station`, and lets unknown/source-like types fall back to `station`; so the map view and topology editor still do not share one point-type vocabulary.
+- Threshold line: `critical=6`, `high=10`, `medium=14`, `valveMinZoom=11`, `expandClusterZoom=12` are still the active LOD thresholds. The inconsistency remains real: valves are tagged `LOW`, `shouldShowNodeAtZoom()` filters by max visible importance first, and that means valve visibility still effectively opens at `zoom >= 14`, not at the configured `11`.
+- DB/import/API line: live DB snapshot is still `stations=1256`, `pipelines=1253`, `pipeline_systems=13`, `junction_groups=16`; `pipeline_systems.id` remains `cred,gn,gs,jxlz,ncsh,pt,sj2,sj3,sj4,we1,we2,zg,zm`, with no `we3`. Raw storage still contains single-station junction rows plus nested row `junction_groups.id=17 = ["JUNCTION-16","JXLZ-11"]`; backend normalization is cleaner than storage because `junction_groups.py` expands and merges, while `topology_editor.py` still has a split rule where `merge_stations` can create one-station groups but `create_junction_group` rejects fewer than two bottom-level stations.
+- DB content vs script churn needs to stay separated next run: counts show `SJ2=70 stations / 69 pipelines`, `SJ3=71 / 70`, `WE3=0 / 0`, so `backend/scripts/add_we3.py` should still be treated as local script churn rather than assumed-live content. Pipeline categories also remain mixed in the DB (`trunk`, `branch`, `陕京二线`, `陕京三线`, `陕京四线`, `南昌-上海支干线`, `嘉兴-甪直联络线`), so any stats logic that assumes only `trunk/branch` is still at risk.
+- Docs/API line: topology docs now consistently describe backend `8080` and openly document the mixed `/api/topology/...` plus `/topology/...` contract instead of the older single-prefix story. `src/services/api.ts` still mixes proxied `/api/topology/...` graph/stats with direct `/topology/correct/...`, while `src/hooks/useTopology.ts` stays on direct `/topology/...` through `BACKEND_BASE_URL`.
+- No business code was modified by this heartbeat run. Memory updated only. Run completed at `2026-04-04 12:29:32 +08:00`.
*** End Patch

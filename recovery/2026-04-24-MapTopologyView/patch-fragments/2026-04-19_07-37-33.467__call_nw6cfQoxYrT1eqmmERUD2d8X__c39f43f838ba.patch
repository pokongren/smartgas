*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-19-仿真AI分析落地说明.md
+一、结论
+仿真页的 AI 分析已落地完成，现在可以在主仿真面板里一键把结果送进 AI，并且页面会直接显示一段可读的 AI 分析摘要。
+
+二、做了什么
+1. 新增可见入口。把“发送仿真结果到AI分析”“复制结构化简报（给AI）”放到仿真主操作区，不再藏在隐藏区块里。  
+2. 新增摘要能力。根据求解状态、告警数、供需差、覆盖命中和基线差值生成“风险等级 + 4行结论”的 AI 分析摘要卡。  
+3. 新增自动模式指令。发送到 AI 时自动在消息前加入“/数据分析”，直接进入分析模式，减少手工切模式动作。
+
+三、为什么这么改
+1. 之前链路是有能力但入口被隐藏，现场使用时看不到、点不到。  
+2. 用户需要“仿真完立刻分析”，所以把分析动作放在主链路上：运行仿真 选择快照 发送AI。  
+3. 摘要先在本页给出，值班员不用先跳 AI 页面也能快速判断风险等级。
+
+四、影响范围
+1. 代码文件：`src/views/MapTopologyView.tsx`。  
+2. 后端接口与数据库结构未改动。  
+3. 现有仿真、快照、基线对比能力保持不变。
+
+五、验证结果
+1. 构建验证：已执行 `npm run build`，结果通过。  
+2. 功能验证：仿真面板已出现两个 AI 相关按钮；存在仿真结果时可发送到 AI；页面可见 AI 分析摘要。  
+3. 前后对比如下。
+
+| 对比项 | 修改前 | 修改后 |
+| --- | --- | --- |
+| AI入口 | 在隐藏区块里，不可见 | 在主操作区可见可点 |
+| 发送行为 | 需手工切分析模式 | 自动带“/数据分析”进入分析模式 |
+| 本页结论 | 无直接摘要 | 有风险等级和4行关键结论 |
*** End Patch

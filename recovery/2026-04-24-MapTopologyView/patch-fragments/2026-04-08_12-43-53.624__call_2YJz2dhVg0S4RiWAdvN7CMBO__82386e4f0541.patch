*** Begin Patch
*** Add File: docs/2026-04-08-WE1主仿真前端瘦身为纯操作面板总结_人话版.md
+# WE1 主仿真前端瘦身为纯操作面板总结
+
+## 1. 这次改了什么
+
+这次把 WE1 主仿真页里那些和实际操作无关的大块前端展示拿掉了，只保留真正要点的选择器和按钮。现在这个面板前端只负责让你选场景、选快照、设基线、运行、保存、刷新、清空和导出，不再在这一页堆运行结果摘要、基线对比、试运行覆盖、覆盖结果清单、固定演示口径这些说明型内容。
+
+## 2. 为什么这么改
+
+你提的点是对的。前端操作区应该是做事的地方，不该把解释、验收话术、覆盖分析都塞在这里。那些信息就算要保留，也应该留在导出文件或别的说明文档里，不应该挡住主操作区。
+
+## 3. 保留了什么
+
+1. 当前场景选择。
+2. 结果快照选择和加载按钮。
+3. 基线快照选择。
+4. 快捷操作按钮：切推荐场景、当前快照设基线、最近留档设基线。
+5. 主操作按钮：运行主仿真、保存快照、刷新快照、清空主仿真、导出演示清单。
+6. 错误提示块。
+
+## 4. 去掉了什么
+
+1. 运行结果摘要卡片。
+2. 基线对比卡片。
+3. 试运行覆盖卡片。
+4. 覆盖结果清单卡片。
+5. 固定演示口径卡片。
+6. 操作区里多余的状态说明文本。
+
+## 5. 改动位置
+
+主改文件是 [MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx)。
+
+## 6. 验证结果
+
+1. 执行 `npm run build`，通过。
+2. 当前主仿真面板已经收成纯操作布局，没有把说明性大块继续挂在前端显示里。
+
+## 7. 当前效果
+
+现在主仿真页更像一个干净的操作台，点开以后先看到的就是选择器和按钮，不会再被一长串解释卡片占住视线。
*** End Patch

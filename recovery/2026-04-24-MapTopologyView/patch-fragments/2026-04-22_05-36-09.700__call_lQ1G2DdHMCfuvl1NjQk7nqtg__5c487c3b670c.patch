*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-22-拓扑样式美化第1阶段第1轮记录.md
+# 2026-04-22 拓扑样式美化第1阶段第1轮记录
+
+一、主目标
+1. 把拓扑图节点和连线从“高亮刺眼、形状杂乱”调整为“更干净、更统一、演示更顺眼”的视觉风格，不改仿真逻辑。
+
+二、做了什么
+1. 修改了节点主色板，降低荧光感，统一到柔和色系。2. 枢纽节点由旋转菱形改为圆角方形，减少“锯齿感”和视觉突兀。3. 节点标签改为深色半透明胶囊底，不再只靠重阴影描边。4. 仿真前与仿真后两条渲染分支都同步应用了同一套节点样式，避免运行后样式跳变。5. 动态流线虚线参数从 `[15, 6, 3, 6]` 收敛到 `[12, 8]`，并把非仿真流动蓝色调成更柔和的青蓝色。
+
+三、为什么这么做
+1. 你反馈“这个不好看”的主要来源是点位形状冲突和标签过硬；统一形状和标签底板后，地图放大时观感更稳定。2. 只改渲染参数和样式字符串，风险最低，不会影响求解、映射和快照数据链路。
+
+四、影响范围
+1. 代码文件：`src/views/MapTopologyView.tsx`。2. 影响模块：拓扑节点绘制、仿真节点叠加绘制、连线动态虚线表现。3. 不影响：后端公式、仿真接口、快照存取、AI分析链路。
+
+五、验证结果
+1. 已执行 `npm run build`，构建通过。2. 结果判定：本轮改动无编译错误，可进入界面视觉验收。
+
+六、前后对比
+1. 改前：枢纽为旋转菱形，标签高对比描边，局部显得刺眼。2. 改后：枢纽为圆角方形，标签带半透明底板，虚线节奏更平缓，整体更干净。3. 本轮未附截图，原因是当前在代码阶段先保证可构建，建议你刷新 `http://localhost:3000/#/map-topology` 后对同一区域做主观验收。
*** End Patch

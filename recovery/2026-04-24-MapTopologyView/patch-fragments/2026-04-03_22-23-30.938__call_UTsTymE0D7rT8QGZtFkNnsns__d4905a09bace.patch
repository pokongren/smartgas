*** Begin Patch
*** Add File: F:\smartgas-grid\docs\阶段二方案_Codex_Subagents生成版.md
+# 阶段二方案：Codex 通过 Subagents 实现的落地方案
+
+## 1. 主目标
+
+第二阶段只盯住一个核心目标：
+
+**把系统从“基础能显示”升级到“重点区域能看清、能点开、能联动”。**
+
+这里的“能联动”，不是指把所有分析系统一次性做完。  
+而是指用户在地图上看到一个重点节点、交汇点、管段之后，能够继续往下看，不会断在当前页面。
+
+所以第二阶段只做三件大事：
+
+1. 做层级化显示  
+   不同缩放级别下，只显示应该显示的信息。
+
+2. 做复杂交汇点处理  
+   同坐标或近坐标的多个设施，不再糊成一团。
+
+3. 做地图到拓扑的基础联动  
+   地图上的节点和管段，能联到拓扑对象和详情入口。
+
+一句话概括：
+
+**第二阶段不是继续补底层字段，而是把“看清楚”和“接着看下去”这两件事真正做出来。**
+
+---
+
+## 2. 上下文与已知信息
+
+阶段一已经做过的事，决定了第二阶段不该再回头打基础仗。
+
+当前已知前提大致是：
+
+- 前后端已经开始围绕 `PipelinePackage` 这套中间结构收口
+- 后端 `/api/pipeline-packages` 已经在承担统一输出口
+- 地图主链路不再完全依赖旧静态数据
+- 一部分业务判断已经开始从“看名字猜类型”切到结构字段
+
+但现在系统还存在几个很直接的问题：
+
+- 缩小时信息太多，密集区域一团糊
+- 放大后交汇点虽然能看到一些东西，但不够稳定
+- 有些页面已经在吃统一结构，有些还没完全收过去
+- 地图和拓扑还像相邻页面，不像一套连续分析链路
+
+所以第二阶段的重点不是“再做一个功能页”。
+
+而是把下面这条链继续推进：
+
+`统一中间结构 -> 层级显示 -> 交汇点处理 -> 地图/拓扑联动`
+
+---
+
+## 3. 第二阶段范围
+
+这一部分必须说清楚，不然后面容易越做越散。
+
+### 3.1 本阶段纳入
+
+- 地图 LOD 层级显示
+- 复杂交汇点的聚合、偏移、详情展开策略
+- 地图节点/管段到拓扑对象的基础联动
+- 相关页面继续围绕统一转换层消费数据
+
+### 3.2 本阶段不纳入
+
+- 完整仿真闭环
+- 完整 AI 分析闭环
+- 全量页面重构
+- 大规模视觉改版
+- 所有旧静态数据文件的一次性清理
+- 调度决策或自动方案生成
+
+### 3.3 阶段边界
+
+第二阶段的边界要守住：
+
+- 只做“显示和联动能力增强”
+- 不开启第三阶段的平台化和运营化工作
+- 不为了一个极端场景，把整套渲染逻辑搞复杂
+
+---
+
+## 4. 阶段二成功标准
+
+这个阶段完成与否，不看写了多少文件。  
+只看下面这些条件是否满足。
+
+### 4.1 显示层成功标准
+
+- 缩放级别变化时，信息密度明显更合理
+- 低缩放级别不再塞满阀室和次要节点
+- 高缩放级别能逐步看到分输站、阀室等细节
+
+### 4.2 交汇点成功标准
+
+- 典型交汇区不再严重重叠
+- 用户能看出这里是多个设施，不是一个点
+- 点击聚合标记后，能继续查看具体节点信息
+
+### 4.3 联动成功标准
+
+- 点击地图节点，能定位到对应拓扑对象或详情入口
+- 点击地图管段，能识别所属系统、图层、干支线类型
+- 从交汇点详情面板选节点，能继续跳到下一层查看
+
+### 4.4 工程成功标准
+
+- 相关页面更多走统一转换层
+- 不再新增新的“临时数据入口”
+- 第二阶段主链路构建通过，基础回归通过
+
+如果这些没达到，就不算第二阶段过关。
+
+---
+
+## 5. 子代理拆分
+
+默认继续采用：
+
+**并行侦察 + 单点实施 + 独立验证**
+
+### 5.1 侦察代理 A：层级显示侦察
+
+职责：
+
+- 梳理当前不同 zoom 下的显示行为
+- 找出信息密度不合理的地方
+- 给出 LOD 规则建议
+
+重点看：
+
+- `src/utils/hierarchyRenderer.ts`
+- `src/utils/mapRenderer.ts`
+- `src/components/map-view/MapView.tsx`
+- `src/views/GlobalPipelineView.tsx`
+
+### 5.2 侦察代理 B：交汇点处理侦察
+
+职责：
+
+- 梳理现有聚合、展开、详情逻辑
+- 找出典型重叠场景
+- 判断哪些情况该聚合，哪些该偏移，哪些该进详情面板
+
+重点看：
+
+- `src/utils/mapRenderer.ts`
+- `src/utils/swarmAnalysis.ts`
+- `src/components/ClusterDetailPanel.tsx`
+
+### 5.3 侦察代理 C：拓扑联动侦察
+
+职责：
+
+- 梳理地图和拓扑之间已有的点击链路
+- 找出联动断点
+- 收敛第二阶段最值得先补的联动入口
+
+重点看：
+
+- `src/views/TopologyView.tsx`
+- `src/views/MapTopologyView.tsx`
+- `src/hooks/useTopology.ts`
+
+### 5.4 侦察代理 D：统一消费边界侦察
+
+职责：
+
+- 找出还在吃旧逻辑、旧数据口的页面
+- 判断哪些页面第二阶段必须统一，哪些可以延后
+- 控制本阶段改动边界
+
+### 5.5 实施代理：单点落地
+
+职责：
+
+- 只改第二阶段主链路相关代码
+- 优先落 LOD、交汇点处理、联动入口
+- 不顺手做第三阶段功能
+
+原则：
+
+- 只允许一个实施代理写主链路代码
+- 避免多人同时改地图渲染核心文件
+- 范围一旦失控，优先保“能看清、能点开、能联动”
+
+### 5.6 验证代理：独立回归
+
+职责：
+
+- 验证缩放行为
+- 验证交汇点场景
+- 验证地图到拓扑联动
+- 验证性能和回归风险
+
+所有子代理输出都统一包含：
+
+- 发现了什么
+- 证据是什么
+- 建议怎么处理
+- 涉及哪些文件或模块
+- 风险点和未确认项
+
+---
+
+## 6. 执行顺序
+
+### 第一步：并行侦察
+
+Codex 主代理同时启动 4 个侦察 subagents：
+
+- 子代理 1：层级显示
+- 子代理 2：交汇点处理
+- 子代理 3：拓扑联动
+- 子代理 4：统一消费边界
+
+这一轮不改代码，只收证据。
+
+目标是先收敛第二阶段最关键的 3 到 5 个问题。
+
+### 第二步：主代理收敛主方向
+
+第二阶段推荐只收敛一个主方向来带动整体推进。
+
+最推荐的主方向是：
+
+**先把“LOD 层级显示 + 交汇点处理”做成统一渲染策略，再把地图到拓扑的联动接上。**
+
+原因：
+
+- 这是用户最直接能感受到变化的一层
+- 这层不稳，联动做再多也会很别扭
+- 这条路径最符合阶段二“能力增强”的定位
+
+### 第三步：实施代理单点改造
+
+实施代理按下面顺序落地：
+
+1. 定义并接入 LOD 规则
+2. 实现交汇点显示策略
+3. 接地图到拓扑的基础联动入口
+4. 把相关页面继续往统一转换层上收
+
+### 第四步：验证代理独立回归
+
+验证代理重点检查：
+
+- 缩放级别切换是否合理
+- 典型交汇区是否可读
+- 聚合、偏移、详情面板是否顺畅
+- 点击节点/管段是否能继续联到拓扑或详情
+- 是否引入明显性能回归
+
+### 第五步：阶段封板
+
+主代理最终需要明确回答四件事：
+
+1. 现在是不是比之前更看得清了
+2. 交汇点是不是已经不是一团糊了
+3. 地图是不是已经能继续联到拓扑了
+4. 哪些能力明确留给第三阶段
+
+---
+
+## 7. 实施内容
+
+### 7.1 LOD 层级显示
+
+推荐按天然气长输管网的特点来，不照搬普通点聚合地图逻辑。
+
+建议规则：
+
+- `zoom < 6`：只显示气源站和关键压气站
+- `zoom 6-10`：显示主要压气站
+- `zoom 10-14`：显示压气站 + 分输站
+- `zoom > 14`：显示全部节点，包括阀室
+
+这一层重点不是好看。
+
+重点是：
+
+- 保留地理真实性
+- 控制信息密度
+- 避免一缩小就糊满整张图
+
+### 7.2 交汇点处理
+
+建议采用混合策略：
+
+- 少量重叠：偏移标记
+- 中等重叠：聚合标记 + 点击展开
+- 大量密集：聚合为主，详情面板展开
+
+推荐默认顺序：
+
+1. 先聚合
+2. 放大后再偏移
+3. 详情查看走面板
+
+这样做的原因是：
+
+- 不会为了展开把地理关系散坏
+- 用户理解成本更低
+- 实现和维护成本也更可控
+
+### 7.3 地图到拓扑联动
+
+第二阶段只做最实用的一版，不追求一步到位。
+
+建议先实现：
+
+- 点击地图节点 -> 高亮拓扑节点或打开详情入口
+- 点击地图管段 -> 高亮拓扑边并显示所属信息
+- 点击交汇点详情面板中的节点 -> 定位到对应拓扑对象
+
+先做核心单向联动。
+
+不要一上来就做所有页面全双向同步。
+
+### 7.4 页面统一消费
+
+第二阶段涉及到的页面，继续往统一转换层收：
+
+- `GlobalPipelineView`
+- `TopologyView`
+- `MapTopologyView`
+- 交汇点详情面板相关组件
+
+目标是：
+
+**页面功能可以不同，但尽量吃同一套中间结构。**
+
+---
+
+## 8. 验证方式
+
+### 8.1 缩放级别验证
+
+固定测试这几种视角：
+
+- 国家级
+- 区域级
+- 省级
+- 城市/站场级
+
+验证点：
+
+- 显示内容是否符合规则
+- 阀室是否不过度抢屏
+- 标签密度是否合理
+
+### 8.2 交汇点专项验证
+
+选 3 到 5 个典型场景：
+
+- 干线与支线交汇
+- 多站点同坐标
+- 多阀室密集区
+- 枢纽节点区域
+
+验证点：
+
+- 是否还能看清
+- 聚合数量是否对
+- 详情面板是否完整
+- 放大缩小时切换是否自然
+
+### 8.3 联动验证
+
+至少验证这些链路：
+
+- 地图点节点 -> 拓扑高亮
+- 地图点管段 -> 拓扑边高亮
+- 面板点节点 -> 跳转/定位
+
+### 8.4 性能验证
+
+关注：
+
+- 首次加载
+- 缩放响应
+- 聚合展开响应
+- 大量节点时卡不卡
+
+### 8.5 回归验证
+
+至少检查：
+
+- `GlobalPipelineView`
+- `TopologyView`
+- `MapTopologyView`
+- 图层切换
+- 节点点击
+- 管段点击
+
+---
+
+## 9. 风险与回退点
+
+### 9.1 主要风险
+
+- LOD 规则定得太狠，用户会觉得信息少了
+- 偏移和展开做太重，页面会显得花
+- 聚合、偏移、详情三套逻辑切换不顺会更乱
+- 联动做太深，阶段二范围容易失控
+
+### 9.2 风险控制
+
+- 优先规则驱动，不先堆复杂算法
+- 优先“能理解”，再追求“视觉高级”
+- 先做核心联动，不做全双向同步
+- 典型场景先打样，再推广
+
+### 9.3 回退点
+
+- 如果偏移效果不稳定，先回退到“聚合 + 面板”
+- 如果 LOD 争议大，先保守显示关键节点
+- 如果拓扑联动太重，先保地图到拓扑的单向联动
+- 如果页面统一消费推进太大，先只统一地图系页面
+
+---
+
+## 10. 最终交付物
+
+第二阶段建议最终交付这些东西：
+
+- 一份第二阶段侦察结论文档
+- 一套 LOD 层级显示规则
+- 一套交汇点处理策略
+- 一版统一地图渲染行为
+- 一版地图到拓扑联动入口
+- 一份专项验证清单和验证结果
+- 一份进入第三阶段的条件说明
+
+---
+
+## 11. Codex 实施方式
+
+### 11.1 主代理职责
+
+Codex 主代理负责：
+
+- 读取阶段一结果和当前主链路代码
+- 启动并协调侦察代理
+- 汇总证据并收敛第二阶段主方向
+- 指派单一实施代理改主链路代码
+- 指派验证代理独立回归
+- 最后做封板总结
+
+### 11.2 Subagents 工作流
+
+推荐流程：
+
+1. 主代理先扫描阶段一落地结果
+2. 并行启动 4 个侦察代理
+3. 主代理收敛主方向
+4. 启动 1 个实施代理改代码
+5. 启动 1 个验证代理做独立检查
+6. 主代理输出阶段结论
+
+### 11.3 推荐提示词模板
+
+#### 侦察代理模板
+
+```text
+你是侦察代理，只做阅读和分析，不改代码。
+
+目标：
+定位 smartgas-grid 第二阶段最关键的问题候选，并给出证据。
+
+你本轮负责的方向：
+- 层级显示 / 交汇点处理 / 拓扑联动 / 页面统一消费（由主代理指定）
+
+要求：
+1. 只看与你负责方向相关的代码和文档
+2. 不做修改
+3. 输出必须包含：
+   - 发现了什么
+   - 证据是什么
+   - 建议怎么处理
+   - 涉及哪些文件或模块
+   - 风险点和未确认项
+4. 结论尽量收敛，优先指出最值得先做的 1 到 2 个点
+```
+
+#### 实施代理模板
+
+```text
+你是实施代理，只负责明确边界内的代码修改。
+
+目标：
+围绕第二阶段主方向，完成层级显示、交汇点处理和基础联动中的核心改造。
+
+要求：
+1. 只改主代理明确分配的文件范围
+2. 不要顺手加入第三阶段功能
+3. 优先复用现有转换层和中间结构
+4. 修改完成后说明：
+   - 改了什么
+   - 为什么这么改
+   - 涉及哪些文件
+   - 还剩哪些风险
+5. 注意你不是一个人在代码库里，不要回退其他人的改动
+```
+
+#### 验证代理模板
+
+```text
+你是验证代理，不参与实施，只做独立检查。
+
+目标：
+验证第二阶段改动是否真的改善了层级显示、交汇点可读性和地图到拓扑联动。
+
+要求：
+1. 优先验证缩放行为、交汇点场景、点击联动、性能风险
+2. 输出必须包含：
+   - 发现了什么
+   - 证据是什么
+   - 建议怎么处理
+   - 涉及哪些文件或模块
+   - 风险点和未确认项
+3. 如果有场景没覆盖到，也要明确写出来
+```
+
+---
+
+## 12. 阶段二完成标准
+
+满足下面这些条件，才算第二阶段完成：
+
+- 不同缩放级别下显示策略清楚、稳定
+- 典型交汇点已经从“重叠不可读”变成“可看可点可展开”
+- 地图点击和拓扑高亮已经形成基本联动
+- 相关页面更多围绕统一中间结构消费
+- 主链路性能没有明显恶化
+- 第三阶段可以在此基础上继续做更深的分析和平台化扩展
+
+如果达不到这些条件，就不要急着进第三阶段。
+
+先把第二阶段的“看清楚 + 接着看”做扎实。
*** End Patch

*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-15-WE1白屏修复_视频复盘后稳定模式加固_人话版.md
+一、主目标
+1. 基于你提供的视频复盘结果，继续修“放大/移动即白屏”，目标是先稳住不崩。
+
+二、视频复盘结论
+1. 视频在约 3.5 秒从正常画面瞬间切到整页浅灰，属于渲染上下文级别故障特征，不像单个按钮逻辑错误。  
+2. 结合代码链路，白屏更接近“缩放时高频覆盖物重绘 + 动画刷新叠加”造成的渲染过载。
+
+三、本轮修复
+1. 新增稳定模式 `renderSafetyMode`（`full/lite`），缩放拖动中或图层过多时自动切 `lite`。  
+2. `lite` 模式下停高频流动动画推进，虚线偏移固定为 0，避免每 180ms 大规模重绘。  
+3. 节点特效在 `lite` 模式和交互中自动降级，关闭冲击波/掉血浮字类高开销效果。  
+4. 保持功能可用：颜色、主干状态、基本叠加仍在，只是先保稳定。
+
+四、验证
+1. 已执行 `npm run build`，构建通过。  
+2. 建议你复测口径：进入 `/map-topology` 后连续放大、拖动 15 秒，观察是否再出现整页浅灰白屏。
+
+五、交付物
+1. 代码：`src/views/MapTopologyView.tsx`。  
+2. 文档：`docs/2026-04-15-WE1白屏修复_视频复盘后稳定模式加固_人话版.md`。
*** End Patch

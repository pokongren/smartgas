*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
 - Updated `C:\Users\Administrator\.codex\automations\automation\HEARTBEAT_GUIDE.md` to add an explicit `automation env mismatch / worktree-path blockage` branch when runtime `cwd` or git root does not match the configured live repo directory.
 - Verified the TOML parses successfully with Python `tomllib`; parsed values confirm `id=automation`, `cwds[0]=F:\smartgas-grid`, and `execution_environment` is absent.
 - Snapshot check against `F:\smartgas-grid` confirms this is the live repo with active changes across `backend/`, `src/`, and `docs/`, so the previous empty-run issue was caused by automation environment selection rather than missing project content in the real repository.
 - Run completed at `2026-04-03 16:31:24 +08:00`.
+
+2026-04-03 16:36 CST
+
+- Re-ran the heartbeat against the configured live repo root `F:\smartgas-grid`; `cwd` and `git rev-parse --show-toplevel` now both resolve to `F:\smartgas-grid`, so the entry check passed and this run inspected the actual repository instead of an empty detached worktree.
+- Current state is real code + database + docs change, not a blockage: `git status --short` shows active edits across `backend/`, `src/`, and `docs/`, and `git log -5` shows recent live-branch commits (`eaf6ac4`, `b31a010`, `d3c9d08`) rather than the minimal `6410a10` snapshot.
+- Map zoom / hierarchy line: `src/utils/mapRenderer.ts` still owns the only active zoom thresholds (`VALVE_MIN_ZOOM=11`, `EXPAND_CLUSTER_ZOOM=12`), while `src/utils/hierarchyRenderer.ts` still has no consumers under `src/`; `MapTopologyView.tsx` is actively changing around junction handling, but the hierarchy-layering helper is still not wired into either the map or global topology view.
+- Source / junction line: backend normalization remains the effective source of truth. `backend/app/services/junction_groups.py` expands `JUNCTION-x` references and merges overlaps, `backend/app/routers/topology_editor.py` normalizes submitted ids before create/delete flows, and the live DB still contains nested raw data (`junction_groups.id=17` stores `["JUNCTION-16", "JXLZ-11"]`), so stored rows are not fully cleaned even though runtime responses are normalized.
+- Database / import-script line: the live DB still reports `stations=1256`, `pipelines=1253`, `pipeline_systems=13`, `junction_groups=16`, with `pipeline_system_ids=cred,gn,gs,jxlz,ncsh,pt,sj2,sj3,sj4,we1,we2,zg,zm`. Staged script changes show import/content churn rather than schema churn: `backend/scripts/add_sj2.py` removes `长阳分输站`, `backend/scripts/add_we3.py` replaces it with `西沙屯分输站`, `backend/scripts/reimport_sj23.py` is a new SJ2/SJ3 full reimport script, and `backend/data/smartgas.db` is modified in parallel.
+- API / docs alignment is still mixed: runtime code paths point to port `8080` (`backend/run.py`, `vite.config.ts`, `src/services/api.ts`, `src/hooks/useTopology.ts`), but multiple docs and helper scripts still reference `8000`, including `backend/README.md`, `docs/TOPOLOGY_SETUP_GUIDE.md`, `docs/TOPOLOGY_COMPUTATION_USAGE.md`, `backend/scripts/test_api.py`, and `backend/scripts/test_ai_stream.py`.
+- Run completed at `2026-04-03 16:36:41 +08:00`.
*** End Patch

*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
 - API/docs line: runtime and docs still describe a mixed `8080` contract. `backend/run.py` and `vite.config.ts` stay on `8080`; `src/hooks/useTopology.ts` goes straight to `/topology/...`; `src/services/api.ts` still mixes `/api/topology/...` with `/topology/correct/...`; and both topology docs now document that mixed reality instead of the old `8000` / single-prefix story.
 - Automation outputs only: rewrote `C:\Users\Administrator\.codex\automations\automation\HEARTBEAT_LAST_RUN.md` in plain-language UTF-8 markdown and appended this memory entry. No business code was modified. Run completed at `2026-04-04 16:27:25 +08:00`.
+
+2026-04-04 16:32 CST
+
+- This run again passed the main-root landing rule: initial automation cwd was `C:\Users\Administrator\.codex\worktrees\4c4a\smartgas-grid`, then the inspection switched into `F:\smartgas-grid`, and both runtime cwd plus `git rev-parse --show-toplevel` resolved to `F:\smartgas-grid` / `F:/smartgas-grid`. All conclusions stayed anchored to the live repo only.
+- Current progress summary: this round is still real code change + database content change + local experiment/noise mixed together, not `automation env mismatch / worktree-path blockage`. `git worktree list --porcelain` still shows `F:\smartgas-grid` as the live branch on `HEAD eaf6ac4...`, while the Codex worktrees remain detached at `6410a10...`.
+- Environment/diff line: `git status --short` still spans `src/`, `backend/`, `docs/`, plus local residue in `.agent/`, `.npm-cache/`, and AI-assistant / SCADA related files. `git diff --stat -- docs` is still centered on `docs/TOPOLOGY_SETUP_GUIDE.md` and `docs/TOPOLOGY_COMPUTATION_USAGE.md`, while the recent live commits remain `eaf6ac4`, `b31a010`, `d3c9d08`, `c19937f`, `3e47946`.
+- National-map line: the inspected chain remains `src/utils/hierarchyRenderer.ts` -> `src/utils/mapRenderer.ts` -> `src/views/GlobalPipelineView.tsx`. Thresholds still read `critical=6`, `high=10`, `medium=14`, `valveMinZoom=11`, `expandClusterZoom=12`, but `getNodeImportance()` still classifies `valve` as `LOW`, so `shouldShowNodeAtZoom()` still makes effective valve visibility behave closer to `zoom >= 14`; keep this scoped to the inspected national-map chain only.
+- National-map vs editor line: `src/views/GlobalPipelineView.tsx` still passes `pipelineData` into `MapView`, while `src/views/MapTopologyView.tsx` still mounts `MapView` without `pipelineData`, keeps its own editor graph, and still maps `junction` back to `station`. Safe phrasing remains: the national-map chain and the editor chain still do not share one node-type / zoom vocabulary.
+- Junction/DB line: backend runtime normalization is still cleaner than stored rows. `backend/app/services/junction_groups.py` still expands `JUNCTION-x` refs and merges overlaps, while `backend/app/routers/topology_editor.py` still has split write rules where `merge_stations` writes `station_ids=[keep_station_id]` but `create_junction_group` rejects fewer than two normalized station ids. Live DB snapshot from this run remains `stations=1256`, `pipelines=1253`, `pipeline_systems=13`, `junction_groups=16`; system ids remain `cred,gn,gs,jxlz,ncsh,pt,sj2,sj3,sj4,we1,we2,zg,zm`; storage still contains nested row `junction_groups.id=17=["JUNCTION-16","JXLZ-11"]`, 7 single-station groups, and `positionMeta` in 89 station-property rows.
+- Script-vs-live-data line: keep treating `backend/scripts/add_sj2.py`, `backend/scripts/add_we3.py`, and `backend/scripts/reimport_sj23.py` as script churn unless DB rows prove the result. This run still shows `SJ2=70/69`, `SJ3=71/70`, `WE3=0/0`, and `pipeline_systems` still have no `we3`, so `add_we3.py` must not be described as already landed in the live DB.
+- API/docs line: runtime and docs still describe a mixed `8080` contract. `backend/run.py`, `vite.config.ts`, and `src/services/apiBase.ts` point to `8080`; `src/hooks/useTopology.ts` goes straight to `/topology/...`; `src/services/api.ts` still mixes `/api/topology/...` graph/stats with direct `/topology/correct/...`; the two topology docs now document that mixed reality; older `8000` references still survive in `backend/README.md`, `backend/scripts/test_api.py`, and `backend/scripts/test_ai_stream.py`.
+- Verifier reminder for future runs: keep final wording scoped to inspected files and verified DB rows only, do not call the repo "fully unified", do not treat `add_we3.py` as already reflected in live DB content unless counts or rows prove it, and do not turn the national-map chain findings into a claim about the topology editor chain.
+- No business code was modified by this heartbeat run. Memory updated only. Run completed at `2026-04-04 16:32:12 +08:00`.
*** End Patch

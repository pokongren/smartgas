*** Begin Patch
*** Add File: F:\smartgas-grid\docs\2026-04-07-WE1第一张图主仿真五阶段Subagents方案.md
+# WE1 第一张图主仿真五阶段 Subagents 方案
+
+## 1. 主目标
+
+这次的主目标不是继续把第二张图 `TopologyView` 做得更花，而是把 WE1 仿真的主入口改回第一张图 `MapTopologyView`。用户以后默认应该在地理拓扑图上看仿真，也就是直接看到“哪一段主干在跑、哪一站压力变了、哪一段断了、故障往哪里传”，第二张图只保留给研发排查求解器和调试叠加数据，不再承担业务主入口。
+
+## 2. 上下文与已知信息
+
+1. 当前已经跑通的 WE1 主链在前端主要挂在 `src/views/TopologyView.tsx`、`src/hooks/useSimulation.ts`、`src/components/topology/SimPanel.tsx`，后端主链主要挂在 `backend/app/routers/topology_simulation.py`、`backend/app/services/topology_simulation.py`、`backend/app/services/we1_solver_input_service.py`。
+2. 当前第一张图对应的是 `src/views/MapTopologyView.tsx`。这页更接近现场看图习惯，因为它保留了地理位置、站场关系、主干线走向和全网地图语义。
+3. 当前第二张图能算，但不适合做主入口。原因不是算法错，而是它默认是抽象全网视图，WE1 只是很小一段，业务用户看完以后很难直接知道“到底哪根管有变化”。
+4. 当前 WE1 场景已经具备基础试点能力，至少包括 `steady_base`、`zhongwei_compressor_offline`、`zhongwei_trunk_break`、`yanchi_jingbian_limited` 这类场景。后端求解器、overlay、快照思路、验证脚本已经有基础，不需要推倒重来。
+5. 这次不优先改求解公式，不优先扩更多样板，不优先再做一套新页面。主方向只有一个：把“已经能算的仿真结果”收进第一张图，让它变成业务主页面。
+
+## 3. 主代理与子代理分工
+
+### 3.1 主代理
+
+1. 主代理负责锁定唯一方向，明确“第一张图做主仿真入口，第二张图降级调试页”这个口径，不让中途又回到“双入口并行长期共存”的状态。
+2. 主代理负责拆 5 个阶段，决定每个阶段只收哪一层，不让实施代理顺手把边界外的功能一起卷进来。
+3. 主代理负责汇总侦察证据，只选一个最可信的实施方向，不允许前端地图页和抽象拓扑页同时各改一半，最后谁也没收住。
+4. 主代理负责最终放行，判断每个阶段是否可以进入下一阶段，或者必须停在当前阶段补验证。
+
+### 3.2 侦察代理 A
+
+1. 只看 `MapTopologyView` 当前有哪些图层、哪些绘制方法、哪些交互入口已经存在。
+2. 只回答三件事：第一张图能不能直接吃 overlay，哪些字段能直接映射，哪些渲染层必须新增而不能硬改旧层。
+3. 不改代码，不给泛泛建议，只给文件、调用链和风险点。
+
+### 3.3 侦察代理 B
+
+1. 只看 `TopologyView`、`useSimulation`、`SimPanel`、仿真类型定义和接口返回。
+2. 只回答三件事：现有仿真结果最小必需字段是什么，哪些 UI 能原样迁到第一张图，`TopologyView` 将来保留成调试页时最少需要留什么。
+3. 不改代码，不碰地图页，只做事实收集。
+
+### 3.4 实施代理
+
+1. 只允许一个实施代理持有地图仿真主链相关文件，避免 `MapTopologyView`、仿真面板、场景切换、overlay 适配同时被多个人改散。
+2. 实施代理优先改前端承载层和消费层，后端只做必要补口，不为了“看起来整齐”去重写求解器。
+3. 每完成一个阶段，都要补一份人话版 Markdown 总结，写清楚“做了什么、为什么这么做、影响范围、怎么验证”。
+
+### 3.5 验证代理
+
+1. 验证代理独立检查地图页是否真能看懂结果，而不是只看接口返回 200。
+2. 验证代理要交叉检查 4 类东西：场景是否可选、地图是否能看出差异、快照对比是否一致、旧调试页是否还能用于排查。
+3. 验证代理失败时要先指出哪一层出问题，顺序固定为“场景入口 -> overlay 适配 -> 地图渲染 -> 快照/对比 -> 求解器”。
+
+## 4. 修改逻辑
+
+1. 先改产品口径。第一张图以后是主入口，第二张图以后是调试入口。这样场景入口、运行按钮、快照对比、结果提示都要围着第一张图重新收口。
+2. 再改技术收口。现有求解和 overlay 链路尽量复用，不重写后端主链，只把消费位置从 `TopologyView` 迁到 `MapTopologyView`。
+3. 再改显示逻辑。地图页不该只是“页面能跑”，而要让用户一眼看出主干方向、主干压力、主干异常、故障传播。也就是说，边要能变色变粗，站要能显示压力，必要时要能只看命中段。
+4. 再改调试边界。第二张图不能直接删，因为研发还需要它看求解器输出、节点压力文字、边属性和抽象拓扑关系，所以它保留，但只作为调试页，不再要求业务用户从这里理解仿真。
+5. 这次明确不做三件事。第 1 件是不扩更多线路试点，第 2 件是不重做求解算法，第 3 件是不新造第三套仿真页面。
+
+## 5. 执行顺序
+
+### 5.1 第 1 阶段第 1 轮到第 2 轮：冻结口径与双页链路摸底
+
+1. 这一阶段的人话目标是先把方向锁死，别一边做地图主入口，一边还舍不得第二张图。
+2. 主代理先冻结规则：`MapTopologyView` 是业务主入口，`TopologyView` 是调试页，后面所有实施都按这个口径推进。
+3. 侦察代理 A 检查 `src/views/MapTopologyView.tsx`、地图渲染相关工具、现有右侧或左侧控制区，确认仿真 panel 放在哪里最不打架。
+4. 侦察代理 B 检查 `src/views/TopologyView.tsx`、`src/hooks/useSimulation.ts`、`src/components/topology/SimPanel.tsx`、`src/types/simulation.ts`，确认当前仿真入口、场景结构、overlay 字段、快照逻辑。
+5. 主代理汇总两路侦察后，只产出一个结论：地图页需要“直接接入 panel + overlay 适配 + 自动聚焦 + 只看命中能力”，第二张图只保留调试能力。
+6. 这一阶段建议重点查看的文件是 `src/views/MapTopologyView.tsx`、`src/views/TopologyView.tsx`、`src/hooks/useSimulation.ts`、`src/components/topology/SimPanel.tsx`、`src/services/api.ts`、`backend/app/routers/topology_simulation.py`。
+7. 完成标准是主代理拿到一份明确的改造清单，能回答“改哪里、先改什么、哪些保留、哪些降级”，而不是还在泛泛讨论。
+
+### 5.2 第 2 阶段第 1 轮到第 3 轮：第一张图接入场景与仿真面板
+
+1. 这一阶段的人话目标是让第一张图先具备“能选场景、能运行、能看到摘要”的最小闭环。
+2. 实施代理把场景选择、运行仿真、保存快照、刷新快照、基线快照、快照对比入口迁入 `MapTopologyView` 或地图页专用侧栏组件。
+3. 这一步优先复用 `useSimulation` 和 `SimPanel` 逻辑。如果需要拆组件，就拆成“业务无关的仿真控制面板”和“页面容器”，不要再让仿真逻辑绑死在 `TopologyView`。
+4. 如果 `MapTopologyView` 现在的控制区已经太挤，就新增地图页专属仿真侧栏组件，但数据来源仍走统一 hook，不造第二套状态机。
+5. 这一阶段涉及的主要文件预计包括 `src/views/MapTopologyView.tsx`、`src/components/topology/SimPanel.tsx`、`src/hooks/useSimulation.ts`、`src/types/simulation.ts`、`src/services/api.ts`。
+6. 完成标准有 3 个。第 1 个是第一张图能切场景并运行，第 2 个是左侧或侧栏能看到摘要结果，第 3 个是第二张图即便还保留原入口，也不再是唯一主入口。
+
+### 5.3 第 3 阶段第 1 轮到第 4 轮：第一张图接入仿真覆盖层与自动聚焦
+
+1. 这一阶段的人话目标是让第一张图“看得出来”，不再只是“按钮能点，结果也回来了，但肉眼看不见”。
+2. 实施代理在地图页现有管段绘制逻辑上新增仿真覆盖层，不直接污染旧图层本体。覆盖层至少要吃这些字段：边颜色、宽度因子、方向、流量标注、节点压力、异常状态。
+3. 地图页默认要支持自动聚焦到 WE1 主样板范围，优先聚焦“古浪压气站 -> 中卫压气站 -> 盐池压气站 -> 靖边压气站”主干段。
+4. 需要加一个“只看仿真命中段”或同类开关。理由很简单，全网太大，不做过滤就还是看不出来。
+5. 如果地图页已经有层级显示、捏合、聚合和来源高亮，这一阶段一律通过“新增 overlay 图层”的方式兼容，不直接改历史图层规则。
+6. 这一阶段涉及的主要文件预计包括 `src/views/MapTopologyView.tsx`、地图渲染工具文件、可能的样式文件，以及少量仿真类型定义。
+7. 完成标准有 4 个。第 1 个是 `steady_base` 能在地图上看出主流向和主干压力。第 2 个是 `zhongwei_compressor_offline` 能看出中卫附近变化。第 3 个是 `zhongwei_trunk_break` 能看出断点上下游差异。第 4 个是用户不需要切到第二张图也能理解主要结果。
+
+### 5.4 第 4 阶段第 1 轮到第 3 轮：快照对比迁移与第二张图降级调试
+
+1. 这一阶段的人话目标是把“结果可回看、可比较、可排障”也一起收回第一张图，避免用户还得来回切页。
+2. 实施代理把快照对比、基线快照、加载快照、回放入口在地图主页面收口，让用户完成一次“基线 -> 故障场景 -> 对比”的闭环。
+3. 第二张图从这一步开始明确降级成调试页。它保留节点压力文字、抽象边属性、算法视角和求解器调试信息，但业务操作入口弱化。
+4. 如果路由层需要调整，就把 `/topology` 描述成调试/研发入口，把 `/map-topology` 或新主路由描述成业务仿真入口。
+5. 如果 `TopologyView` 里还有只属于调试的功能，就继续保留；如果里面有已经迁到地图页的业务按钮，就删掉或隐藏，避免双入口状态不一致。
+6. 这一阶段涉及的主要文件预计包括 `src/App.tsx`、`src/views/MapTopologyView.tsx`、`src/views/TopologyView.tsx`、`src/hooks/useSimulation.ts`、`src/components/topology/SimPanel.tsx`。
+7. 完成标准有 3 个。第 1 个是业务用户只靠第一张图就能完成基线对比。第 2 个是第二张图不会再和第一张图争主入口。第 3 个是同一场景的快照结果在两页口径一致。
+
+### 5.5 第 5 阶段第 1 轮到第 4 轮：验证封板与交付收口
+
+1. 这一阶段的人话目标是证明“第一张图承载主仿真”这件事不只是看起来顺，而是真的能稳定用。
+2. 验证代理独立跑现有 WE1 验证脚本，并补地图页人工回归清单。后端脚本继续以 `backend/scripts/verify_we1_phase4.py` 及其后续统一校验脚本为核心，前端回归以“场景切换、运行、看图、存快照、做对比”为核心。
+3. 主代理在这一阶段只做 2 件事：一是判定有没有必要补一个“只看命中段”的默认开关，二是决定当前是否封板，还是必须回到前一阶段补可视性。
+4. 这一阶段必须产出正式总结文档，至少包括“前后对比、影响范围、验证结果、剩余风险、第二张图现在保留什么用途”。
+5. 完成标准有 4 个。第 1 个是地图页成为默认讲解页。第 2 个是业务用户跑一遍能看懂。第 3 个是调试页仍可用于研发排障。第 4 个是主链验证通过，没有因为迁移入口把仿真主链做断。
+
+## 6. 验证方式
+
+1. 第 1 类验证是入口验证。要确认第一张图上能看到场景选择、运行仿真、快照入口，且这些操作打到的是现有统一仿真接口，不是新造旁路。
+2. 第 2 类验证是结果验证。要确认 `steady_base`、`zhongwei_compressor_offline`、`zhongwei_trunk_break`、`yanchi_jingbian_limited` 至少这 4 个场景在地图页都有明显差异，不是只有面板数值变了。
+3. 第 3 类验证是对比验证。要确认保存快照以后，基线和故障场景的差异能在地图页和面板里一起看出来，且口径一致。
+4. 第 4 类验证是调试验证。要确认第二张图虽然降级，但仍能用于研发确认节点压力、边状态、求解输出和 overlay 结构，避免一刀切以后失去排障入口。
+5. 第 5 类验证是回归验证。要继续跑后端现有 WE1 核心验证脚本，证明入口迁移没有把求解链路做坏。失败时先查前端消费链，再查接口，再查求解器，不要一上来就怀疑公式。
+
+## 7. 风险与回退点
+
+1. 风险 1 是地图页本身图层很多，仿真覆盖层如果直接硬改旧图层，很容易把层级显示、手工捏合、来源标记、聚合显示一起打坏。回退点是永远先加独立 overlay 图层，必要时可以单独关闭。
+2. 风险 2 是双入口长期共存导致状态不一致。地图页一套按钮，第二张图又一套按钮，用户最后不知道该信谁。回退点是从第 4 阶段开始强制弱化第二张图业务入口，只保留调试功能。
+3. 风险 3 是地图页虽然能跑，但默认仍显示全网，WE1 还是淹没。回退点是第 3 阶段必须上自动聚焦和命中过滤，否则不允许宣布“主入口迁移成功”。
+4. 风险 4 是实施代理顺手去改求解器，导致主线返工。回退点是这次默认不动求解公式，除非验证代理明确指出结果错误来自后端求解，而不是来自展示层。
+5. 风险 5 是只做页面，不做文档，过两轮以后没人知道为什么要保留第二张图。回退点是每个阶段都补人话总结，最终再补一份总总结。
+
+## 8. 最终交付物
+
+1. 一份正式方案文档，也就是这份“五阶段 Subagents 方案”。
+2. 一套第一张图主仿真的页面改造清单，明确主入口、调试入口、场景入口、快照入口分别落在哪里。
+3. 一套地图页仿真覆盖层改造结果，至少包含主干变色、线宽变化、节点压力或关键标签、自动聚焦和命中过滤能力。
+4. 一套第二张图降级调试页的边界说明，明确哪些功能保留，哪些功能迁走。
+5. 一套验证材料，包括后端脚本验证、前端场景回归和至少一份阶段总结文档。
+
+## 9. 当前结论
+
+1. 这次最合理的方向不是继续围着第二张图补解释，而是直接把第一张图做成主仿真页。
+2. 这件事技术上不需要推倒重来，因为求解器和 overlay 主链已经在，只是消费入口选错了。
+3. 真正要收的不是“还能不能算”，而是“业务用户能不能在第一眼就看懂”。
+4. 按这 5 个阶段推进，前 2 个阶段解决“能操作”，第 3 个阶段解决“能看懂”，第 4 个阶段解决“能回看和对比”，第 5 个阶段解决“能封板和交付”。
*** End Patch

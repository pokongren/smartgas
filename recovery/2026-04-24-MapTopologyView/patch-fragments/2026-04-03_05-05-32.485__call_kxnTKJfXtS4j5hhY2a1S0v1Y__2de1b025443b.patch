*** Begin Patch
*** Add File: F:\smartgas-grid\docs\code-improvement-review.md
+# 代码问题与优化建议
+
+## 文档目的
+
+本文档汇总当前代码分析中发现的主要问题、潜在风险与优化建议，便于后续按优先级逐项整改。
+
+适用范围：
+
+- 前端 React/Vite 视图层
+- 前后端接口契约
+- 拓扑计算与地图可视化链路
+- 数据组织与共享节点建模
+
+## 总体判断
+
+项目整体方向是清晰的：
+
+- 前端已经形成了地图总览、拓扑编辑、拓扑演示、AI 助手等多个功能面
+- 后端也已经拆分出基础数据、拓扑编辑、拓扑计算、AI 助手、SCADA 等路由
+- 数据库驱动的 `pipeline-packages` 聚合接口，已经具备替代前端硬编码数据的基础
+
+当前主要问题不在“缺功能”，而在以下几类工程问题：
+
+- 前后端接口契约未完全统一
+- 同一类能力存在多套实现路径
+- 部分兜底逻辑存在真实 bug
+- 页面组件体量过大，维护成本偏高
+- 数据共享规则放在前端硬编码，容易误判
+
+## 发现的问题
+
+### 1. 拓扑计算接口前后端契约不一致
+
+#### 现象
+
+前端 `useTopology` 使用的默认 API 基址为：
+
+- `http://localhost:8080/api`
+
+随后继续请求：
+
+- `/topology/build`
+- `/topology/path`
+- `/topology/paths`
+- `/topology/analyze/{graph_name}`
+
+最终实际请求地址会变成：
+
+- `/api/topology/build`
+- `/api/topology/path`
+- `/api/topology/analyze/...`
+
+但后端拓扑计算路由真实前缀为：
+
+- `/topology`
+
+即后端真实可用地址是：
+
+- `/topology/build`
+- `/topology/path`
+- `/topology/analyze/{graph_name}`
+
+而不是 `/api/topology/...`。
+
+#### 风险
+
+- `TopoViewer`/`TopologyDemoView` 在真实调用时容易直接 404
+- 前端封装代码看起来可用，但实际链路不可达
+- 后续新功能接入时会重复踩坑
+
+#### 优化建议
+
+- 统一拓扑计算接口前缀，只保留一套规范地址
+- 推荐方案：
+  - 统一所有业务接口都挂在 `/api/...`
+  - 或统一前端 `useTopology` 直接访问 `/topology/...`
+- 同步修正演示页中的 API 链接，避免继续暴露旧路径
+
+#### 优先级
+
+高
+
+---
+
+### 2. `TopologyView` 的回退逻辑存在坐标读取 bug
+
+#### 现象
+
+`TopologyView` 在后端接口失败时，会回退到前端数据包加载。
+
+但回退分支中节点坐标读取方式写成了类似：
+
+- `n.coordinate?.[0]`
+- `n.coordinate?.[1]`
+
+而项目里 `PipelineNode.coordinate` 的真实结构是：
+
+```ts
+{
+  longitude: number
+  latitude: number
+}
+```
+
+#### 风险
+
+- 一旦 `/api/topology/graph` 请求失败，前端兜底数据会被读成错误坐标
+- 拓扑图可能塌到 `(0,0)` 或其他异常位置
+- 这种问题只会在“后端不可用”时出现，排查成本高
+
+#### 优化建议
+
+- 立即修正回退逻辑的坐标读取方式
+- 给回退路径补一个最小测试或运行时断言
+- 对 `coordinate` 这类核心字段统一类型约束，避免对象/数组双格式并存
+
+#### 优先级
+
+高
+
+---
+
+### 3. 全局节点去重按站名处理，存在误合并风险
+
+#### 现象
+
+当前 `src/data/pipelines/index.ts` 中的节点去重逻辑，核心依据是：
+
+- 同名节点只保留第一次出现的节点
+- 后续同名节点全部 remap 到首个节点
+
+该策略虽然能处理“跨管线共享站点”，但过于粗糙。
+
+实际数据库中已经存在：
+
+- 同名同坐标站点
+- 同名但不同坐标站点
+
+也就是说，“名称相同”并不等于“业务上同一个点”。
+
+#### 风险
+
+- 不同物理站点被错误合并
+- 管线端点被错误吸附
+- 地图上出现错误连线
+- 拓扑分析、关键节点分析、路径分析都会被污染
+
+#### 优化建议
+
+- 不要在前端用“站名”作为全局主键
+- 推荐改为以下任一方案：
+  - 后端显式返回“共享站点 ID”
+  - 使用业务稳定键进行合并
+  - 使用名称 + 坐标容差双重判断
+  - 使用专门的共享节点/联络站建模表
+- 前端只消费后端确认后的共享关系，不再自行猜测
+
+#### 优先级
+
+高
+
+---
+
+### 4. API 访问方式没有统一，环境切换成本高
+
+#### 现象
+
+项目里目前同时存在多种访问方式：
+
+- `axios` 使用绝对地址 `http://localhost:8080`
+- 部分页面直接 `fetch('/api/...')`
+- Vite 通过 dev proxy 转发 `/api`
+
+这说明：
+
+- 开发环境、预览环境、生产环境的请求行为不完全一致
+- 不同模块对“后端入口”的理解不同
+
+#### 风险
+
+- 本地能跑，部署后失败
+- 弹窗页、新窗口页、独立演示页行为不一致
+- 接口迁移时需要同时改很多处
+
+#### 优化建议
+
+- 统一封装一个 API 访问层
+- 所有请求都走同一入口
+- 建议：
+  - 浏览器端优先使用相对路径 + 代理
+  - 或统一由环境变量注入单一 `API_BASE_URL`
+- 禁止页面组件内散落大量裸 `fetch`
+
+#### 优先级
+
+中高
+
+---
+
+### 5. 页面组件体量过大，数据、交互、UI 高度耦合
+
+#### 现象
+
+以下页面/组件体量明显偏大：
+
+- `GlobalPipelineView.tsx`
+- `MapTopologyView.tsx`
+- `AiAssistant.tsx`
+
+其中同时混杂了：
+
+- 常量数据
+- 业务逻辑
+- UI 渲染
+- 拖拽交互
+- 弹窗管理
+- 流式消息处理
+
+#### 风险
+
+- 修改一个局部功能容易影响整页行为
+- 代码复用困难
+- 测试颗粒度太粗
+- 新成员接手成本高
+
+#### 优化建议
+
+- 拆分为“数据层 / 逻辑层 / 表现层”
+- 典型拆分方向：
+  - `GlobalPipelineView`
+    - SCADA 数据配置文件
+    - 历史图表弹层 hook
+    - 面板展示组件
+  - `AiAssistant`
+    - `useChatStream`
+    - `useDrag`
+    - `AssistantPanel`
+    - 主窗/弹窗共享状态层
+  - `MapTopologyView`
+    - 图构建逻辑
+    - 编辑模式逻辑
+    - 位置提交逻辑
+    - 右侧面板
+
+#### 优先级
+
+中高
+
+---
+
+### 6. AI 助手主窗与独立窗重复实现消息发送逻辑
+
+#### 现象
+
+`AiAssistant` 与 `AiAssistantStandalone` 内部都维护了相似的：
+
+- 消息状态
+- 流式响应处理
+- `handleSend`
+- `handleKeyDown`
+
+这部分逻辑重复度较高。
+
+#### 风险
+
+- 后续修复一个 bug 时容易漏改另一份
+- 流式交互行为不一致
+- 维护成本逐步上升
+
+#### 优化建议
+
+- 抽一个共享 hook，例如：
+  - `useAiAssistantChat`
+- 将主窗与弹窗的差异收敛为：
+  - 布局不同
+  - 容器不同
+  - 关闭行为不同
+
+#### 优先级
+
+中
+
+---
+
+### 7. 大量 SCADA 数据仍硬编码在前端
+
+#### 现象
+
+`GlobalPipelineView.tsx` 中保留了大段 SCADA 基准数据常量。
+
+这类数据虽然便于演示，但长期放在视图文件里会造成：
+
+- 文件膨胀
+- 数据维护困难
+- 与真实后端数据源脱节
+
+#### 风险
+
+- 前后端数据不一致
+- 真实接入后迁移成本更高
+- 演示数据与生产数据边界不清晰
+
+#### 优化建议
+
+- 短期：
+  - 抽到独立数据模块
+  - 明确标注“演示数据”与“真实数据”
+- 中期：
+  - 通过 SCADA API 下发
+  - 前端只负责渲染与缓存
+
+#### 优先级
+
+中
+
+---
+
+### 8. 文档、代码、接口状态存在历史并存现象
+
+#### 现象
+
+项目中同时存在：
+
+- 新旧拓扑 API 路径
+- 新旧前端数据来源
+- 多套演示页面
+- 多套地图/拓扑视图
+
+这说明项目正处在“重构迁移中间态”。
+
+#### 风险
+
+- 开发者无法快速判断哪条链路是主路径
+- 旧代码虽未删除，但会持续误导后续开发
+- 维护时容易把新逻辑接到旧接口上
+
+#### 优化建议
+
+- 明确主路径：
+  - 哪个视图用于正式展示
+  - 哪个接口是当前规范
+  - 哪些页面只是 demo
+- 给旧模块补状态标记：
+  - `active`
+  - `legacy`
+  - `demo-only`
+- 清理无主线价值的过渡代码
+
+#### 优先级
+
+中
+
+## 建议的优化顺序
+
+### 第一阶段：先修稳定性与契约问题
+
+1. 统一拓扑计算接口前缀
+2. 修复 `TopologyView` 回退坐标 bug
+3. 修正演示页与真实 API 链路不一致问题
+4. 统一 API 基础访问方式
+
+### 第二阶段：处理数据正确性问题
+
+1. 重构节点去重策略
+2. 把共享站点规则迁到后端
+3. 对关键数据结构增加类型约束与校验
+
+### 第三阶段：做结构性优化
+
+1. 拆分超大页面组件
+2. 抽离 AI 助手共享 hook
+3. 抽离 SCADA 数据配置层
+4. 收敛 demo/legacy 页面
+
+## 推荐的落地动作
+
+### 短期可直接执行
+
+- 修正 `useTopology` 的基础路径或后端前缀
+- 修正 `TopologyView` 回退读取坐标方式
+- 修正文档和 demo 页面中的旧 API 地址
+- 为节点去重逻辑加日志，先观测误合并情况
+
+### 中期建议
+
+- 在后端 `pipeline-packages` 接口中显式输出共享节点信息
+- 统一所有前端请求走一层 API client
+- 拆分 `GlobalPipelineView` 与 `AiAssistant`
+
+### 长期建议
+
+- 建立拓扑主链路架构说明
+- 为核心视图补最小自动化测试
+- 为关键接口补契约测试
+
+## 结论
+
+当前项目的主要改进方向不是“重写”，而是“收口”：
+
+- 收口接口
+- 收口数据来源
+- 收口共享节点规则
+- 收口页面职责
+
+只要先把契约统一和数据正确性问题处理掉，后续组件拆分和功能扩展会顺很多。
*** End Patch

*** Begin Patch
*** Delete File: F:/smartgas-grid/docs/阶段二多Agent实施方案.md
*** Add File: F:/smartgas-grid/docs/阶段二多Agent实施方案.md
+# SmartGas 阶段二多 Agent 实施方案
+
+> 编制时间：2026-04-03
+>
+> 依据：
+> - `docs/阶段一分工实施方案.md`
+> - `docs/未来项目计划书.md`
+> - `docs/统一公式拓扑AI多Agent协同方案.md`
+> - 本轮新增的 subagents 工作方式约束
+
+---
+
+## 1. 主目标
+
+阶段二不是继续围绕“阶段一主链路闭环”做同类工作，而是承接到下一层业务能力建设：
+
+- 把 SCADA 历史能力从“能展示”推进到“能分析”
+- 把历史分析与拓扑对象打通，形成跨页面分析链路
+- 把 `GlobalPipelineView` 继续减重，避免后续阶段继续堆积复杂度
+- 把开发过程本身改造成适合多 Agent 并行推进的流程
+
+### 1.1 要解决的现象
+
+当前阶段二前，系统仍存在以下现象：
+
+1. SCADA 双库职责还不够硬，历史数据链路不够稳定。
+2. 历史分析能力分散，单站、枢纽、多指标查询口径不统一。
+3. 拓扑页与历史分析页之间联动偏弱，分析入口不统一。
+4. `GlobalPipelineView` 仍然过重，继续往里堆功能风险很高。
+5. 多人或多 Agent 并行开发时，容易出现重复改同一文件、验证后置、问题回修失控。
+
+### 1.2 本阶段范围
+
+本阶段纳入：
+
+- SCADA 双库硬化
+- 历史分析接口增强
+- SCADA 与拓扑历史联动
+- `GlobalPipelineView` 第二刀拆分
+- 阶段二多 Agent 写、验、改代码流程
+
+本阶段不纳入：
+
+- 完整 AI 深度上下文联动
+- 完整仿真闭环
+- 全量公式引擎重构
+- 大范围视觉重设计
+- 调度方案自动生成
+
+### 1.3 预期结果
+
+阶段二结束后，至少要得到：
+
+1. 双库链路清晰，历史查询职责稳定。
+2. 单站历史分析可用，枢纽多站对比可用，多指标切换可用。
+3. 拓扑对象可以稳定触发历史分析。
+4. `GlobalPipelineView` 继续减重，状态与面板逻辑进一步拆出。
+5. 阶段二采用固定的“侦察 -> 实施 -> 验证 -> 回修 -> 再验证 -> 主代理收口”流程。
+
+---
+
+## 2. 阶段二基础与任务范围
+
+## 2.1 阶段一已完成基础
+
+阶段二能并行推进，前提是阶段一已经完成了这些收口：
+
+- `MapTopologyView` 主链路已开始收口
+- 拓扑编辑接口已进入统一服务层管理
+- 页面内散落直连拓扑接口的方式已开始减少
+- `GlobalPipelineView` 已开始第一刀拆分
+- 页面职责已有初步边界
+  - `MapTopologyView` 偏编辑
+  - `TopologyView` 偏分析
+  - `GlobalPipelineView` 偏总览与运行态
+
+## 2.2 阶段二任务映射
+
+阶段二建议映射成四块业务任务：
+
+1. SCADA 双库硬化
+2. 历史分析增强
+3. 拓扑历史联动
+4. `GlobalPipelineView` 第二刀拆分
+
+每一块都必须按同样的子代理流程推进，而不是谁方便谁直接动手。
+
+---
+
+## 3. 子代理拆分
+
+阶段二统一采用三类子代理，加一个主代理收口。
+
+## 3.1 侦察代理
+
+职责：
+
+- 定位代码链路
+- 找根因候选
+- 识别依赖边界
+- 识别风险点
+- 给出实施切片建议
+
+规则：
+
+- 不改代码
+- 可以多个侦察代理并行
+- 侦察输出必须固定格式，不得只给泛泛建议
+
+适用到阶段二任务：
+
+- SCADA 双库：盘点 `database.py`、`scada_models.py`、`scada.py` 的读写路径
+- 历史分析：盘点单站、枢纽、多指标接口契约
+- 拓扑联动：定位 `MapTopologyView`、`TopologyView` 到历史分析入口的映射链路
+- 页面拆分：识别 `GlobalPipelineView` 的状态堆积点和副作用点
+
+## 3.2 实施代理
+
+职责：
+
+- 按侦察结论改代码
+- 只负责一个明确切片
+- 在自己的文件边界内完成实现
+
+规则：
+
+- 一个实施代理只负责一个切片
+- 不允许多个实施代理同时改同一文件
+- 不在未定稿的契约文件上盲改
+- 验证失败后的修 Bug，也归为“回修实施代理”
+
+适用到阶段二任务：
+
+- 双库边界修正
+- 历史接口统一响应结构
+- 单站与枢纽历史查询实现
+- 图表增强
+- 拓扑页接入历史分析入口
+- `GlobalPipelineView` 第二刀拆分
+
+## 3.3 验证代理
+
+职责：
+
+- 测试
+- 回归
+- 契约校验
+- 边界检查
+- 产出缺陷单
+
+规则：
+
+- 不改代码
+- 验证必须独立于实施代理
+- 验证失败必须明确指出失败项、证据、建议修复方式
+
+适用到阶段二任务：
+
+- 双库读写路径验证
+- 历史接口结果验证
+- 图表展示验证
+- 拓扑联动验证
+- 页面拆分回归验证
+
+## 3.4 主代理
+
+主代理只做四件事：
+
+1. 定义目标
+2. 拆任务
+3. 控冲突
+4. 做最终决策和整合
+
+主代理不做的事：
+
+- 不替侦察代理做根因分析
+- 不替实施代理写全部代码
+- 不替验证代理宣布默认通过
+
+---
+
+## 4. 执行顺序
+
+这是阶段二必须遵守的硬规则。
+
+## 4.1 标准顺序
+
+所有任务统一按下面顺序执行：
+
+1. 先侦察
+2. 再实施
+3. 最后验证
+
+如果验证失败：
+
+4. 进入回修实施
+5. 再验证
+6. 通过后才允许主代理收口
+
+## 4.2 串并行规则
+
+可以并行的部分：
+
+- 多个侦察代理可以并行
+- 多个实施代理可以并行，但文件边界必须互斥
+- 验证代理可以先做单切片局部验证，再做统一回归
+
+必须串行的部分：
+
+- 公共契约文件必须先定稿再分派实施
+- 所有切片合并后必须统一进入验证
+- 验证失败后必须先修，再允许继续合并下一步
+
+## 4.3 冲突规则
+
+这是阶段二方案新增的硬规则：
+
+1. 不要多个代理同时改同一片文件。
+2. 公共契约文件先由侦察代理和主代理定稿，再分派实施。
+3. 验证失败后，不允许直接继续新功能，必须先进入修复。
+
+建议主代理维护一份文件持有表，例如：
+
+| 切片 | 负责人 | 独占文件 |
+| --- | --- | --- |
+| SCADA 双库 | 实施代理 A | `backend/app/database.py` |
+| 历史接口 | 实施代理 B | `backend/app/routers/scada.py` |
+| 图表增强 | 实施代理 C | `src/components/scada/ScadaHistoryChart.tsx` |
+| 拓扑联动 | 实施代理 D | `src/views/MapTopologyView.tsx`, `src/views/TopologyView.tsx` |
+| 页面拆分 | 实施代理 E | `src/views/GlobalPipelineView.tsx`, `src/views/global-pipeline/*` |
+
+---
+
+## 5. 固定输出格式
+
+阶段二开始，所有子代理都按固定格式返回。
+
+## 5.1 侦察代理固定输出
+
+每个侦察代理固定返回：
+
+- 发现了什么
+- 证据是什么
+- 建议怎么处理
+- 涉及哪些文件
+- 风险点是什么
+
+示例：
+
+```md
+发现了什么：
+- `scada.py` 同时承担主库与历史库查询入口，职责混杂。
+
+证据是什么：
+- `backend/app/routers/scada.py`
+- `backend/app/database.py`
+
+建议怎么处理：
+- 将历史查询统一收口到历史库 session。
+
+涉及哪些文件：
+- `backend/app/database.py`
+- `backend/app/routers/scada.py`
+
+风险点是什么：
+- 改动后老的单站历史查询路径可能回归失败。
+```
+
+## 5.2 实施代理固定输出
+
+每个实施代理固定返回：
+
+- 改了什么
+- 依据是什么
+- 为什么这么改
+- 涉及哪些文件
+- 风险点是什么
+
+## 5.3 验证代理固定输出
+
+每个验证代理固定返回：
+
+- 验证了什么
+- 证据是什么
+- 发现了哪些问题
+- 建议怎么处理
+- 涉及哪些文件
+- 风险点是什么
+
+---
+
+## 6. 主代理收口职责
+
+## 6.1 选结论
+
+当多个侦察代理给出不同结论时，主代理负责选一个可执行方案，而不是让实施代理自己判断。
+
+## 6.2 控冲突
+
+主代理必须负责：
+
+- 防止多个实施代理改同一文件
+- 防止公共契约在未定稿前被多人同时修改
+- 防止验证标准在执行中漂移
+
+## 6.3 做最终决策和整合
+
+主代理负责：
+
+- 决定合并顺序
+- 决定先做哪些切片
+- 决定是否回退
+- 决定是否进入回修
+- 决定何时可以宣布阶段性交付
+
+## 6.4 主代理不做的事
+
+- 不替侦察代理做所有分析
+- 不替实施代理写全部代码
+- 不替验证代理做“默认通过”的决定
+
+---
+
+## 7. 阶段二任务映射
+
+下面把阶段二具体任务映射到这套 subagents 方式里。
+
+## 7.1 SCADA 双库硬化
+
+### 侦察代理
+
+- 盘点主库与历史库的读写链路
+- 识别迁移逻辑与兼容逻辑
+- 找出异常路径与兜底不足
+
+### 实施代理
+
+- 修正主库/历史库边界
+- 硬化历史查询统一入口
+- 增加异常兜底与日志
+
+### 验证代理
+
+- 验证历史查询是否统一走历史库
+- 验证主库是否不再承担高频历史查询职责
+- 验证异常场景是否可观测
+
+### 主要文件
+
+- `backend/app/database.py`
+- `backend/app/scada_models.py`
+- `backend/app/routers/scada.py`
+
+## 7.2 历史分析增强
+
+### 侦察代理
+
+- 梳理单站、枢纽、多指标查询契约
+- 梳理图表响应结构差异
+
+### 实施代理
+
+- 实现统一响应结构
+- 实现单站与枢纽查询
+- 实现多指标切换
+
+### 验证代理
+
+- 验证单线结果
+- 验证多线结果
+- 验证空数据边界
+- 验证图表口径一致性
+
+### 主要文件
+
+- `backend/app/routers/scada.py`
+- `src/components/scada/ScadaHistoryChart.tsx`
+- `src/components/scada/LuzhiHistoryPanel.tsx`
+
+## 7.3 拓扑历史联动
+
+### 侦察代理
+
+- 定位站点、枢纽到历史对象的映射链路
+- 找到触发入口
+
+### 实施代理
+
+- `MapTopologyView` 接入站点和枢纽历史分析入口
+- `TopologyView` 保留分析跳转入口
+- 建立对象映射规则
+
+### 验证代理
+
+- 验证点选站点是否打开正确历史目标
+- 验证点选枢纽是否打开正确多站历史
+- 验证映射规则是否不依赖临时字符串猜测
+
+### 主要文件
+
+- `src/views/MapTopologyView.tsx`
+- `src/views/TopologyView.tsx`
+- `backend/app/services/topology.py`
+- `backend/app/routers/scada.py`
+
+## 7.4 `GlobalPipelineView` 第二刀拆分
+
+### 侦察代理
+
+- 识别状态堆积点
+- 识别拖拽、面板、SCADA 注入副作用
+
+### 实施代理
+
+- 拆状态
+- 拆面板
+- 拆 Hook
+- 继续减轻主文件复杂度
+
+### 验证代理
+
+- 验证构建通过
+- 验证交互不回归
+- 验证页面行为一致
+
+### 主要文件
+
+- `src/views/GlobalPipelineView.tsx`
+- `src/views/global-pipeline/*`
+- `src/hooks/*`
+
+---
+
+## 8. 写、验、改代码一套流程
+
+## 8.1 标准流程
+
+```mermaid
+flowchart LR
+    A["主代理定义目标与切片"] --> B["侦察代理并行勘察"]
+    B --> C["主代理选结论并分派文件边界"]
+    C --> D["实施代理并行改代码"]
+    D --> E["主代理整合结果"]
+    E --> F["验证代理统一验证"]
+    F -->|通过| G["主代理收口并交付"]
+    F -->|失败| H["回修实施代理修复"]
+    H --> I["验证代理再次验证"]
+    I -->|通过| G
+    I -->|失败| H
+```
+
+## 8.2 每一步做什么
+
+### 第一步：主代理定义目标与切片
+
+输出：
+
+- 现象
+- 范围
+- 预期结果
+- 任务切片
+- 文件持有表
+
+### 第二步：侦察代理并行勘察
+
+输出：
+
+- 根因候选
+- 证据
+- 涉及文件
+- 风险点
+
+### 第三步：主代理选结论并分派文件边界
+
+输出：
+
+- 哪个侦察结论被采用
+- 哪个实施代理拿哪些文件
+- 哪些文件暂时禁止多人同时修改
+
+### 第四步：实施代理并行改代码
+
+输出：
+
+- 改了什么
+- 改动依据
+- 涉及文件
+- 风险点
+
+### 第五步：验证代理统一验证
+
+验证内容至少包括：
+
+- 构建验证
+- 接口验证
+- 图表验证
+- 页面联动验证
+- 回归验证
+
+### 第六步：回修实施代理修复
+
+输入：
+
+- `ValidationReport`
+- 失败场景
+- 文件范围
+
+输出：
+
+- 修复说明
+- 修复文件
+- 回归点
+
+---
+
+## 9. 验证代理的验证口径
+
+阶段二验证不能只看 `build` 是否通过。
+
+## 9.1 数据层验证
+
+- 历史查询是否统一走历史库
+- 主库是否不再承担历史查询职责
+- 双库异常时是否有明确兜底
+
+## 9.2 接口验证
+
+- 单站历史接口是否稳定
+- 枢纽历史接口是否稳定
+- 多指标查询是否口径统一
+- 响应结构是否一致
+
+## 9.3 前端图表验证
+
+- 单站能否正确显示单线
+- 枢纽能否正确显示多线
+- 图例与指标名称是否一致
+- 空数据时是否有明确状态
+
+## 9.4 拓扑联动验证
+
+- 点选站点能否打开正确历史
+- 点选枢纽能否打开正确多站历史
+- 映射规则是否稳定
+
+## 9.5 页面拆分回归验证
+
+- `GlobalPipelineView` 构建是否通过
+- 交互是否回归
+- 浮层、拖拽、历史入口是否正常
+
+---
+
+## 10. 风险与回退机制
+
+## 10.1 风险
+
+1. 双库边界改动容易引发旧查询回归。
+2. 历史接口统一时，前端图表可能因响应结构变化失效。
+3. 拓扑到历史对象映射若不稳定，会导致联动错误。
+4. 页面拆分过程中，状态迁移容易导致交互回归。
+5. 若多个实施代理同时改同一文件，阶段二会迅速失控。
+
+## 10.2 回退机制
+
+- 双库失败时：回退到兼容模式，但必须标记为临时状态。
+- 接口失败时：保留单站查询，隐藏未完成的枢纽能力。
+- 联动失败时：保留文本入口，暂不自动打开错误目标。
+- 页面拆分失败时：优先回退拆分，不强行保留不稳定结构。
+- 验证失败时：必须先进回修，不允许主代理跳过。
+
+---
+
+## 11. 阶段二推荐并行矩阵
+
+### 第一批可并行
+
+- 侦察代理 A：双库链路勘察
+- 侦察代理 B：历史接口契约勘察
+- 侦察代理 C：页面拆分点勘察
+
+### 第二批可并行
+
+- 实施代理 A：双库硬化
+- 实施代理 B：历史接口增强
+- 实施代理 C：页面拆分
+
+### 第三批可并行
+
+- 实施代理 D：图表增强
+- 实施代理 E：拓扑历史联动
+
+### 最后统一
+
+- 主代理整合
+- 验证代理统一验收
+- 回修实施代理修复
+
+---
+
+## 12. 主代理最终收口标准
+
+主代理只有在以下条件都满足时，才允许宣布阶段二某个切片完成：
+
+1. 侦察结论已选定
+2. 文件冲突已控制
+3. 实施已完成
+4. 验证已通过
+5. 若发生回修，回修后的再验证也通过
+
+如果不满足，就只能输出：
+
+- 当前状态
+- 未通过原因
+- 风险提示
+- 下一步建议
+
+不能直接输出“完成”。
+
+---
+
+## 13. 下一步建议
+
+如果要按这套 subagents 方式真正执行阶段二，最合适的下一步是先补两个配套产物：
+
+1. `ValidationReport` 结构文档
+2. 阶段二任务切片表
+
+因为只有这两个东西先定下来，主代理才真正能做到：
+
+- 明确分派
+- 控制冲突
+- 统一验证
+- 统一回修
+
+然后再开始正式并行推进阶段二的具体代码改动。
*** End Patch

*** Begin Patch
*** Add File: docs/2026-04-20-快照压力上图按钮第2阶段第6轮记录.md
+# 2026-04-20 快照压力上图按钮第2阶段第6轮记录
+
+一、结论  
+已新增“快照压力上图”按钮，默认不在图上铺压力标签，点击按钮后会把当前快照各点压力打到拓扑图上，再点可隐藏。
+
+二、实现内容  
+1. 在 `src/views/MapTopologyView.tsx` 新增状态 `showSnapshotPressureOverlay`，默认 `false`。2. 在仿真操作区新增按钮“快照压力上图/隐藏快照压力上图”。3. 节点渲染逻辑改为仅在 `showSnapshotPressureOverlay=true` 时显示快照压力标签。
+
+三、为什么这么做  
+1. 你要的是可控展示而不是一直覆盖，按钮开关最直接。2. 默认隐藏能减少图面干扰，做演示时再一键上图。
+
+四、验证结果  
+1. 执行 `npm run build`，结果通过。2. 预期行为：加载快照后点击“快照压力上图”，节点名称下显示快照压力标签；再次点击隐藏。
+
+五、影响范围  
+1. 仅影响 `/#/map-topology` 的前端展示层。2. 不影响快照数据、仿真求解和后端接口。
*** End Patch

*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
 - AI 还没完全收口的点继续记住：`selection/filters` 细粒度上下文、`contextual` 模式、弹窗复用主页面真实上下文、统一 API 层收口、`[ERROR]` 协议闭环，这几块最多算半接线/预留；别说成已经完整落地。
 - 本轮运行时间：`2026-04-05 14:31:29 +08:00`
+## 2026-04-05 16:23:30 +08:00
+
+- 这轮先按入口修正再巡检。初始 `cwd=C:\Users\Administrator\.codex\worktrees\5b09\smartgas-grid`，切换后 `cwd=F:\smartgas-grid`，`git rev-parse --show-toplevel=F:/smartgas-grid`，`git rev-parse --absolute-git-dir=F:/smartgas-grid/.git`。结论只基于 `F:\smartgas-grid` 主目录，没有拿 detached worktree 当事实来源。
+- 入口通过，不是 `automation env mismatch / worktree-path blockage`，也不是空壳仓库。`src/`、`backend/`、`docs/`、`backend/data/smartgas.db` 和本轮点名文件都在；`git worktree list` 里主目录还是 `F:/smartgas-grid @ eaf6ac4`，大量 `.codex/worktrees/* @ 6410a10 detached` 继续只算背景噪音。仓库一级目录里仍有个 `F?smartgas-griddocs` 噪音目录名，后续做路径判断别被它带偏。
+- 本轮主要还是“主目录未提交代码变化 + 数据库变化 + 本地实验并存 + AI 链路变化”，不是单纯 docs。现查 `git diff --stat` 是 `47 files changed, 4780 insertions(+), 2608 deletions(-)`；`docs/TOPOLOGY_COMPUTATION_USAGE.md` 和 `docs/TOPOLOGY_SETUP_GUIDE.md` 仍是 `181 insertions / 587 deletions`。
+- 地图口径继续按主目录现状收口：`src/utils/hierarchyRenderer.ts` 统一阈值还是 `critical/high/medium=6/10/14`，并保留 `valveMinZoom=11`、`expandClusterZoom=12`；`src/utils/mapRenderer.ts` 已接 `getNodeLODStrategy()` / `shouldShowNodeAtZoom()`；`src/views/GlobalPipelineView.tsx` 通过 `MapView -> mapRenderer` 吃到这套规则；但 `src/views/MapTopologyView.tsx` 仍保留 `TOPO_SIZES` 和 `collapsed graph` 自绘，没有并入统一 LOD。阀室不要写成“11 级就显示”，更稳的说法是：这条地图链里它同时受 `showValveRooms` 和重要度过滤控制，实际要到高缩放才稳定出现。
+- 枢纽/runtime 口径继续保守：`backend/app/services/junction_groups.py` 还是 `junction_groups_rebuilt` 优先、旧 `junction_groups` 兼容兜底、description 标记的 manual overlay 单独覆盖，并支持展开 `JUNCTION-x/junction-x`；`backend/app/routers/topology_editor.py` 的 `/api/topology/junctions` 返回 `load_runtime_junction_groups()`，同时带 `manual_editing_enabled` 和 `mode=runtime_readonly|legacy_editable`。`junction_groups` 表结构仍只有 `id/name/description/station_ids` 四列，`manual_overlay` 不是数据库字段。
+- 这轮数据库数字继续按主库实测：`stations=1256`、`pipelines=1251`、`pipeline_systems=13`、`junction_groups=25`、`junction_groups_rebuilt=7`；`pipeline_systems.ids=cred,gn,gs,jxlz,ncsh,pt,sj2,sj3,sj4,we1,we2,zg,zm`；`WE3=0/0`，`SJ2=70/69`，`SJ3=71/70`；`layers_config` 仍挂在 `pipeline_systems.layers_config` 字段，不是独立表。`backend/check_db.py --strict --show-junction-samples` 实跑结果是 `we3: missing`、`major_junction_count=0`、`issue_count=0`，说明 shadow 结构没炸，但 `WE3` 仍没进当前主库。
+- 别再把 `junction_groups_rebuilt.description` 当 runtime 事实。`id=7` 的 description 仍写 `kind=major_junction;systems=cred,sj2,we3`，但实库 `station_ids/system_ids/member_count` 只对应 `cred + sj2` 两线、2 个成员，而且 `junction_kind` 现值也是 `junction` 不是 `major_junction`。
+- 导入脚本和实库状态继续分开说：`backend/scripts/add_sj2.py`、`backend/scripts/add_we3.py`、`backend/scripts/reimport_sj23.py` 都是直接写主库 `backend/data/smartgas.db` 和 `pipeline_systems.layers_config`；其中 `reimport_sj23.py` 更像 SJ2/SJ3 主链路重导，`add_sj2.py/add_we3.py` 仍带局部补段/手工执行味道。脚本改了不等于主库已落，这轮 `WE3` 还是 0。
+- AI 链路继续按“主目录里的本地未提交半接线能力”表述。前端入口在 `src/components/ai-assistant/AiAssistant.tsx`，`src/components/ai-assistant/useAiAssistantChat.ts` 会把 `context + analysis_mode=subagents` 发到 `/api/ai-assistant/chat`，`src/components/ai-assistant/useAiAssistantPageContext.ts` 注入 `page/route/title/module/summary/selection/filters`；后端 `backend/app/routers/ai_assistant.py` 用 `/api/ai-assistant` 前缀，并按 `context/analysis_mode` 在 direct reply、legacy 和 orchestrated 分支间分流；`backend/app/services/ai_client.py` 默认还是 `AI_BASE_URL=https://api.openai.com/v1`、`AI_DEFAULT_MODEL=gpt-3.5-turbo`，缺 `AI_API_KEY` 时 fallback。`ai_analysis_orchestrator.py`、`analysis_validator.py`、`fix_task_router.py` 和前端两个 hook 目前仍是未跟踪文件，所以不能说成正式基线或正式上线。
+- docs 变化继续按“现实链路对口径”处理，不是装饰。`docs/TOPOLOGY_SETUP_GUIDE.md`、`docs/TOPOLOGY_COMPUTATION_USAGE.md` 都把默认端口写成 `8080`，也明确区分了 `/api/topology/...` 和 `/topology/...` 双路由现实；以后别再用旧 `8000` 口径，也别把 `useTopology` 这条链误说成还走 `/api/topology/...`。
+- 下轮 heartbeat 继续卡这几条红线：1) 只认 `F:\smartgas-grid` 主目录事实；2) 先重查数据库，别复用旧数字；3) 不把 `MapTopologyView.tsx` 说成已并入统一 LOD；4) 不把未跟踪 AI / rebuild / shadow 文件说成正式基线；5) `WE3` 相关判断以实库和 `check_db.py` 为准，不拿脚本文案或 description 残留代替主库事实。
+- 本轮运行时间：`2026-04-05T16:23:29.9765917+08:00`
*** End Patch

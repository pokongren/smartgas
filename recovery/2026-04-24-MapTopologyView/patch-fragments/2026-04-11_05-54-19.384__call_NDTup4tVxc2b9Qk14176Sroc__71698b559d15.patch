*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-11-WE1前端动效增强_第3阶段第1-3轮_人话版.md
+一、主目标
+1. 把 WE1 第一张图主仿真从“有数据”升级成“有冲击感、能一眼看懂风险传播”，并连续完成第3阶段3轮实现。
+
+二、上下文与已知信息
+1. 当前页面已有稳态求解、映射着色、文字简报和 AI 上下文同步，但故障场景视觉冲击不够，管线流动感和掉压感知偏弱。  
+2. 本轮只做前端增强，保持后端求解和接口契约不变。
+
+三、主代理与子代理分工
+1. 本轮由主代理直接完成实现与验证。  
+2. 侦察动作：定位 `MapTopologyView.tsx` 的节点和管线渲染循环。  
+3. 实施动作：按3轮逐层接入动画状态、渲染逻辑、全屏告警。  
+4. 验证动作：前端构建校验，确认编译通过。
+
+四、修改逻辑
+1. 第3阶段第1轮：动画驱动层  
+(1). 新增 `lineFlowPhase`，用于驱动管线虚线流动。  
+(2). 新增 `damageFlashVisible`，在 run_id 变化后短时开启“掉血浮字”窗口。  
+(3). 新增定时器 effect：120ms 推进流动相位，4.5 秒自动关闭掉血浮字。
+2. 第3阶段第2轮：节点与管线动效层  
+(1). 节点端增加“冲击波呼吸灯”：关键告警点和中卫中断核心点会出现外扩波纹。  
+(2). 节点端增加“数值掉血浮字”：基线对比出现负压差时显示 `-x.xx MPa` 并上浮淡出。  
+(3). 管线端增加“能量流动条”：高利用率线路采用动态虚线并随相位移动，故障关键线减速或停流。  
+3. 第3阶段第3轮：全局告警层  
+(1). 在 `zhongwei_trunk_break` 场景激活时加全屏红色暗角呼吸。  
+(2). 采用 `pointer-events: none`，保证不影响已有交互。
+
+五、执行顺序
+1. 先接入状态和定时器，再改节点渲染，再改管线渲染，最后加全局暗角与样式。  
+2. 每一轮都保持增量可回退，避免一次性大改造成定位困难。
+
+六、验证方式
+1. 编译验证  
+(1). 执行 `npm run build`。  
+(2). 结果：通过，前端可打包。  
+2. 功能验收口径  
+(1). 断输场景下可见暗角呼吸。  
+(2). 告警节点可见冲击波。  
+(3). 管线可见流动虚线，故障关键段明显减速或停流。  
+(4). 有基线差值时可见负压浮字。
+
+七、风险与回退点
+1. 风险：AMap 对 `strokeDashoffset` 的浏览器差异可能导致部分环境流动感弱化。  
+2. 回退点：只需回退 `MapTopologyView.tsx` 中本轮新增的状态、effect 和渲染片段即可，不影响后端。
+
+八、最终交付物
+1. 代码文件：`src/views/MapTopologyView.tsx`。  
+2. 总结文件：`docs/2026-04-11-WE1前端动效增强_第3阶段第1-3轮_人话版.md`。
*** End Patch

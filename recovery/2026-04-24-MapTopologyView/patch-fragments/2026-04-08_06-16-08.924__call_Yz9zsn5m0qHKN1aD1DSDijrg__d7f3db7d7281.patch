*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-08-WE1-maintenance-round9-legacy-entry-guard-summary.md
+一、主目标
+1. 本轮只做维护模式第9轮巡检补强，把“第一张图不能回退到旧 emergency simulate-failure 链”补成固定守卫，不进入新阶段，不改求解公式，不扩新试点，不新造第三套仿真页面。
+
+二、上下文与已知信息
+1. automation memory 显示 WE1 第一张图五阶段已封板，当前处于维护模式第8轮，后续运行只允许做巡检、回归和修复。
+2. 并行侦察后确认，`/map-topology` 仍是正式入口，`/topology` 仍是调试页，AI 助手页面语义也仍区分“正式主仿真入口”和“调试画布”。
+3. 当前维护脚本已经守住了入口文案、快照回归和第5阶段验收口径，但还没有显式验证第一张图和 `useSimulation` 没有重新引用旧 `/api/emergency/simulate-failure` 语义。
+
+三、主代理与子代理分工
+1. 主代理先读两份五阶段方案、现有总结和 automation memory，确认当前只能按维护模式推进。
+2. 侦察子代理A只看 [src/App.tsx](/F:/smartgas-grid/src/App.tsx)、[src/views/MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx)、[src/views/TopologyView.tsx](/F:/smartgas-grid/src/views/TopologyView.tsx)、[src/components/ai-assistant/useAiAssistantPageContext.ts](/F:/smartgas-grid/src/components/ai-assistant/useAiAssistantPageContext.ts)，确认正式入口语义没有回退。
+3. 侦察子代理B只看 [backend/scripts/verify_we1_maintenance.py](/F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py)、[backend/scripts/verify_we1_map_phase5.py](/F:/smartgas-grid/backend/scripts/verify_we1_map_phase5.py)、[backend/scripts/verify_we1_phase34_final.py](/F:/smartgas-grid/backend/scripts/verify_we1_phase34_final.py)、[backend/scripts/verify_we1_snapshot_archive.py](/F:/smartgas-grid/backend/scripts/verify_we1_snapshot_archive.py)，定位维护巡检最薄的守卫口。
+4. 实施子代理只改 [backend/scripts/verify_we1_maintenance.py](/F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py)，不碰页面主链、映射层和 solver。
+5. 验证子代理独立复跑维护总脚本、第3到4阶段联合回归、第5阶段验收和快照归档验证，确认主链仍稳定。
+
+四、修改逻辑
+1. 在 [backend/scripts/verify_we1_maintenance.py](/F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py) 新增 `map_view_rejects_legacy_failure_entry` 守卫，同时读取 [src/views/MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx) 和 [src/hooks/useSimulation.ts](/F:/smartgas-grid/src/hooks/useSimulation.ts)，明确禁止重新出现 `/api/emergency/simulate-failure`、`emergencyAPI.simulate`、`failure_node_id`、`failed_node_id` 这组旧链标记。
+2. 这么改的原因是当前最真实的维护风险不是主链坏掉，而是以后有人在第一张图或 `useSimulation` 里顺手接回旧故障推演接口；这类回退只看“按钮还在不在”抓不住，必须补成反向守卫。
+
+五、执行顺序
+1. 第1阶段第1轮读取 memory 和最近收口总结，确认当前处于维护模式第8轮，本轮不能推进新阶段。
+2. 第1阶段第2轮并行侦察前端入口语义和维护脚本守卫口径，只收敛一个实施方向。
+3. 第1阶段第3轮单点修改 [backend/scripts/verify_we1_maintenance.py](/F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py)。
+4. 第1阶段第4轮独立运行 `python F:\\smartgas-grid\\backend\\scripts\\verify_we1_maintenance.py`、`python F:\\smartgas-grid\\backend\\scripts\\verify_we1_phase34_final.py`，并由维护总脚本内部再次校验第5阶段验收和快照归档链路。
+
+六、验证方式
+1. 验证什么：验证维护模式脚本是否能抓住旧入口回退风险，验证第3到4阶段联合回归、第5阶段验收和快照归档仍然通过。
+2. 怎么验证：运行 `python F:\\smartgas-grid\\backend\\scripts\\verify_we1_maintenance.py` 和 `python F:\\smartgas-grid\\backend\\scripts\\verify_we1_phase34_final.py`。
+3. 结果怎么看：维护总脚本输出 `status=passed`、`mode=maintenance`，新增 `map_view_rejects_legacy_failure_entry` 检查通过；同时 `verify_we1_map_phase5.py` 在维护总脚本内返回 `next_mode=maintenance`、`all_mainline_scenarios_covered=true`，`verify_we1_snapshot_archive.py` 返回 `list_count=10`，说明主链、封板口径和快照归档都没被误伤。
+4. 失败时先查哪层：先查第一张图入口和 `useSimulation` 消费层，再查映射层与 source IDs 命中关系，再查快照归档层，最后才查 backend solver input 与稳态求解主链。
+
+七、风险与回退点
+1. 当前风险仍然是入口语义被日后修改时悄悄带回旧故障接口，而不是求解主链本身错误。
+2. 回退点很小，只涉及 [backend/scripts/verify_we1_maintenance.py](/F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py) 一处；如果后续发现守卫口径需要调整，优先继续修维护脚本，不动页面业务链。
+
+八、最终交付物
+1. 代码交付：[backend/scripts/verify_we1_maintenance.py](/F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py)。
+2. 文档交付：[2026-04-08-WE1-maintenance-round9-legacy-entry-guard-summary.md](/F:/smartgas-grid/docs/2026-04-08-WE1-maintenance-round9-legacy-entry-guard-summary.md)。
+3. memory 已更新到维护模式第9轮。
+
+九、前后对比
+| 项目 | 这轮之前 | 这轮之后 |
+| --- | --- | --- |
+| 维护入口守卫 | 只验证正式入口文案和动作存在 | 额外验证第一张图与 `useSimulation` 没有回退到旧 emergency 接口 |
+| 巡检定位能力 | 能发现“入口丢了” | 还能发现“入口还在，但内部偷偷接回旧链” |
+| 阶段状态 | 维护模式第8轮 | 维护模式第9轮，继续只做巡检回归和修复 |
+
+十、截图与替代
+1. 本轮没有合适截图，因为改动点是维护脚本守卫，不是页面视觉改动。
+2. 这次用“前后对比表 + 验证结果”代替截图，更适合记录维护巡检口径。
*** Update File: C:/Users/Administrator/.codex/automations/we1/memory.md
@@
-﻿一、运行时间
-1. 2026-04-08 13:10:04 +08:00。
-
-二、当前状态
-1. WE1 第一张图五阶段主体已完成，当前处于维护模式第8轮，不进入下一阶段。
-2. 本轮只做维护入口语义守卫补强，没有改 solver 公式，没有扩试点，没有新造第三套仿真页面。
-
-三、本轮动作
-1. 读取两份五阶段方案文档、最近维护总结和本 memory，确认第5阶段已封板，后续只允许维护巡检。
-2. 并行启动2个侦察子代理核对前端入口职责与维护脚本覆盖范围，收敛出唯一实施方向：只加固 [verify_we1_maintenance.py](F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py) 的入口语义守卫。
-3. 派1个实施子代理单点修改 [verify_we1_maintenance.py](F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py)，补入主/调试页切换文案守卫、第一张图正式动作守卫和 TopologyView 调试工具守卫。
-4. 派1个验证子代理独立复跑维护总脚本、阶段3到4联合回归、第5阶段验收和快照归档验证，确认新增守卫通过且主链未回退。
-5. 新增总结 [2026-04-08-WE1-maintenance-round8-entry-guard-summary.md](F:/smartgas-grid/docs/2026-04-08-WE1-maintenance-round8-entry-guard-summary.md)。
-
-四、改动文件
-1. [verify_we1_maintenance.py](F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py)。
-2. [2026-04-08-WE1-maintenance-round8-entry-guard-summary.md](F:/smartgas-grid/docs/2026-04-08-WE1-maintenance-round8-entry-guard-summary.md)。
-3. [memory.md](C:/Users/Administrator/.codex/automations/we1/memory.md)。
-
-五、验证结果
-1. `python F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py` 通过，`status=passed`，`mode=maintenance`，`entry_semantics` 新增 `view_switcher_labels_primary_vs_debug` 与 `topology_view_retains_debug_tooling` 且通过；第一张图正式主仿真动作也已纳入 `map_view_exposes_primary_entry_hint` 守卫。
-2. `python F:/smartgas-grid/backend/scripts/verify_we1_phase34_final.py` 通过，主辅样板 overlay 契约仍一致，关键场景仍收敛。
-3. `python F:/smartgas-grid/backend/scripts/verify_we1_map_phase5.py` 通过，`next_mode=maintenance`，`snapshot_archive.all_mainline_scenarios_covered=true`，说明第一张图主链仍稳定。
-4. `python F:/smartgas-grid/backend/scripts/verify_we1_snapshot_archive.py` 通过，`list_count=10`，快照创建、读取和列表链路正常。
-5. 首次验证即通过，没有进入修复重试，也没有触发“连续2次修复失败”的阻塞条件。
-
-六、失败先查哪层
-1. 先查入口与前端消费层，再查映射层与 source IDs 命中关系，再查快照归档层，最后查 backend solver input 与稳态求解主链。
-
-七、下一轮判断
-1. 不进入下一阶段。
-2. 后续运行继续按维护模式做巡检、回归和修复；如果再失败，优先回看 [verify_we1_maintenance.py](F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py) 的入口语义守卫，再决定是否需要下钻业务链。
+﻿一、运行时间
+1. 2026-04-08 14:12:37 +08:00。
+
+二、当前状态
+1. WE1 第一张图五阶段主体已完成，当前处于维护模式第9轮，不进入下一阶段。
+2. 本轮只做旧 emergency 入口回退守卫补强，没有改 solver 公式，没有扩试点，没有新造第三套仿真页面。
+
+三、本轮动作
+1. 读取两份五阶段方案文档、最近收口总结和本 memory，确认第5阶段已封板，后续只允许维护巡检。
+2. 并行启动2个侦察子代理核对前端入口职责与维护脚本覆盖范围，收敛出唯一实施方向：只加固 [verify_we1_maintenance.py](F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py) 的旧链回退守卫。
+3. 派1个实施子代理单点修改 [verify_we1_maintenance.py](F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py)，新增 `map_view_rejects_legacy_failure_entry`，显式禁止第一张图和 `useSimulation` 重新引用 `/api/emergency/simulate-failure`、`emergencyAPI.simulate`、`failure_node_id`、`failed_node_id`。
+4. 派1个验证子代理独立复跑维护总脚本和阶段3到4联合回归，并由维护总脚本内部再次校验第5阶段验收与快照归档，确认新增守卫通过且主链未回退。
+5. 新增总结 [2026-04-08-WE1-maintenance-round9-legacy-entry-guard-summary.md](F:/smartgas-grid/docs/2026-04-08-WE1-maintenance-round9-legacy-entry-guard-summary.md)。
+
+四、改动文件
+1. [verify_we1_maintenance.py](F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py)。
+2. [2026-04-08-WE1-maintenance-round9-legacy-entry-guard-summary.md](F:/smartgas-grid/docs/2026-04-08-WE1-maintenance-round9-legacy-entry-guard-summary.md)。
+3. [memory.md](C:/Users/Administrator/.codex/automations/we1/memory.md)。
+
+五、验证结果
+1. `python F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py` 通过，`status=passed`，`mode=maintenance`，新增 `entry_semantics.map_view_rejects_legacy_failure_entry=true`。
+2. 维护总脚本内复跑的 `python F:/smartgas-grid/backend/scripts/verify_we1_map_phase5.py` 通过，`next_mode=maintenance`，`snapshot_archive.all_mainline_scenarios_covered=true`。
+3. 维护总脚本内复跑的 `python F:/smartgas-grid/backend/scripts/verify_we1_snapshot_archive.py` 通过，`list_count=10`，快照创建、读取和列表链路正常。
+4. `python F:/smartgas-grid/backend/scripts/verify_we1_phase34_final.py` 通过，主辅样板 overlay 契约仍一致，关键场景仍收敛。
+5. 首次验证即通过，没有进入修复重试，也没有触发“连续2次修复失败”的阻塞条件。
+
+六、失败先查哪层
+1. 先查第一张图入口和 `useSimulation` 消费层，再查映射层与 source IDs 命中关系，再查快照归档层，最后查 backend solver input 与稳态求解主链。
+
+七、下一轮判断
+1. 不进入下一阶段。
+2. 后续运行继续按维护模式做巡检、回归和修复；如果再失败，优先回看 [verify_we1_maintenance.py](F:/smartgas-grid/backend/scripts/verify_we1_maintenance.py) 里的入口语义与旧链回退守卫，再决定是否需要下钻业务链。
*** End Patch

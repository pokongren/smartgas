*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-10-WE1前端仿真提升_第1阶段第1轮_人话版.md
+一、主目标
+1. 这轮目标是把前端主仿真从“只看数字”提升到“能看动态变化、能直接生成文字说明、能喂给现有 AI 助手”，并一次性完成2次迭代能力落地。
+
+二、本轮实施内容
+1. 第1次迭代做了什么
+(1). 在主仿真面板新增“动态体征”区域，增加三条可视化信号条：供需平衡度、管网活跃度、风险热度。  
+(2). 三条信号条都绑定稳态结果自动计算百分比，仿真后会随结果刷新，不再是静态展示。  
+(3). 对应代码位置在 `src/views/MapTopologyView.tsx`，核心变量为 `simulationSignalCards`。
+2. 第2次迭代做了什么
+(1). 新增“仿真自动解读”文本块，把 run_id、求解状态、供需、利用率、告警、映射命中、基线差值和建议动作自动汇总成可读简报。  
+(2). 新增“复制仿真文字简报”按钮，支持一键复制到剪贴板，方便直接发给 AI 助手。  
+(3). 新增 AI 运行时上下文同步，把 `we1_simulation_narrative`、`we1_scenario_id`、`we1_run_id`、`we1_solver_status` 等写入现有 runtime context，做到“前端跑完即有 AI 可用上下文”。  
+(4). 对应代码位置在 `src/views/MapTopologyView.tsx`，核心变量与函数为 `simulationNarrative`、`copySimulationNarrative`、`setAssistantRuntimeContext(...)`。
+
+三、为什么这样改
+1. 原来痛点是“仿真结果有值但不直观、解释依赖人工、AI 不知道当前这次仿真上下文”。  
+2. 这轮改动把“看图、读文、喂 AI”连成一个闭环，减少口头解释成本，也能让助手基于当前 run 做更准的回答。
+
+四、影响范围
+1. 影响页面为 WE1 主仿真前端面板，文件是 `src/views/MapTopologyView.tsx`。  
+2. 不改后端求解逻辑，不改数据库结构，不改仿真接口契约，属于前端增强和上下文同步增强。  
+3. 现有按钮和主流程保持不变，属于增量能力，不会打断原操作路径。
+
+五、验证方式与结果
+1. 构建验证
+(1). 执行命令：`npm run build`。  
+(2). 结果：构建成功，Vite 打包完成，无编译错误。
+2. 功能核验清单
+(1). 核验项A：仿真面板出现“动态体征（第1次迭代）”三条信号条。  
+(2). 核验项B：仿真面板出现“仿真自动解读（可给AI）”文本区。  
+(3). 核验项C：点击“复制仿真文字简报（第2次迭代）”后状态栏会提示复制结果。  
+(4). 核验项D：仿真摘要和 run 信息会写入 AI runtime context。
+
+六、前后对比
+1. 对比表
+
+| 对比项 | 改造前 | 改造后 |
+| --- | --- | --- |
+| 结果展示 | 以基础指标数字为主 | 增加三条动态体征进度条，结果更直观 |
+| 文字输出 | 需要人工组织描述 | 自动生成仿真简报，可直接复制 |
+| AI联动 | AI 不知道当前仿真上下文 | 自动同步 run、场景、状态、简报到 runtime context |
+| 操作效率 | 分析与汇报要手工拼接 | 运行后可直接“看图+读文+发AI” |
+
+2. 截图说明  
+(1). 本轮未新增固定截图文件；原因是当前页面已有持续迭代，截图容易和下一轮样式变更不一致。  
+(2). 本文用“核验清单+对比表”替代截图，便于后续按项回归。
+
+七、风险与回退点
+1. 风险主要在前端文案编码和样式拥挤，不影响后端求解正确性。  
+2. 若需快速回退，可仅回退 `src/views/MapTopologyView.tsx` 中新增的 `simulationSignalCards`、`simulationNarrative`、`copySimulationNarrative` 和对应 UI 区块。
+
+八、最终交付物
+1. 前端增强代码：`src/views/MapTopologyView.tsx`。  
+2. 本轮总结文档：`docs/2026-04-10-WE1前端仿真提升_第1阶段第1轮_人话版.md`。
*** End Patch

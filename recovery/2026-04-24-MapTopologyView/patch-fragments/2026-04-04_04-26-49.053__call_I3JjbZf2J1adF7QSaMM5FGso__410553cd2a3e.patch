*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
 - Map line: `src/utils/hierarchyRenderer.ts` and `src/utils/mapRenderer.ts` now form the live national-map LOD chain, but the valve rule is still internally inconsistent: `valveMinZoom=11`, yet `getNodeImportance()` marks valve nodes as `LOW`, so `shouldShowNodeAtZoom()` keeps them hidden until `zoom >= 14`. `src/views/GlobalPipelineView.tsx` uses this chain, while `src/views/MapTopologyView.tsx` still mounts `MapView` without `pipelineData` and keeps its own simplified point taxonomy (`station/valve/distribution/compressor`).
 - Junction/DB line: backend runtime normalization is still ahead of stored data. `backend/app/services/junction_groups.py` expands `JUNCTION-x` refs and merges overlaps, `backend/app/routers/topology_editor.py` still allows `merge_stations` to write single-station groups, and the live DB still contains nested plus single-station rows (`junction_groups.id=17` stores `["JUNCTION-16","JXLZ-11"]`; ids `6/7/9/11/12/13/15` are single-station groups).
 - Schema/import/docs line: this run still looks like import-script churn + DB-content change, not schema migration. `pipeline_systems` columns remain `id,name,color,sort_order,layers_config`; `junction_groups` columns remain `id,name,description,station_ids`; `backend/scripts/add_sj2.py`, `backend/scripts/add_we3.py`, and `backend/scripts/reimport_sj23.py` are all modified; `docs/TOPOLOGY_SETUP_GUIDE.md` and `docs/TOPOLOGY_COMPUTATION_USAGE.md` now explicitly document 8080 plus the mixed `/api/topology/...` and `/topology/...` contract.
 - No business code was modified by this heartbeat run. Memory updated only. Run completed at `2026-04-04 10:27:24 +08:00`.
+
+2026-04-04 12:26 CST
+
+- This run again landed on the configured live repo the right way: initial `cwd` was `C:\Users\Administrator\.codex\worktrees\6e57\smartgas-grid`, then the run switched into `F:\smartgas-grid`, and both runtime `cwd` plus `git rev-parse --show-toplevel` resolved to `F:\smartgas-grid` / `F:/smartgas-grid`. Judgement stayed anchored to the live repo only.
+- Current progress summary: this round is still real code change + database content change + local experiment/noise mixed together, not `automation env mismatch / worktree-path blockage`. `git log --oneline -5` still leads with live-branch commits (`eaf6ac4`, `b31a010`, `d3c9d08`), `git status --short` shows active edits across `src/`, `backend/`, and `docs/`, and `git diff --stat -- docs` is still concentrated in `docs/TOPOLOGY_SETUP_GUIDE.md` and `docs/TOPOLOGY_COMPUTATION_USAGE.md`.
+- Environment/path line: `F:\smartgas-grid` is a real git directory, not a pointer file; all required `src` / `backend` / `docs` / DB targets exist here. `git worktree list --porcelain` still shows `F:\smartgas-grid` as the only live branch worktree on `HEAD eaf6ac4...`, while the Codex worktrees remain detached at `6410a10...`; those detached worktrees were treated as background only, not as the source of truth.
+- Map zoom / hierarchy line: the national-map chain is still only partially unified. `src/utils/hierarchyRenderer.ts` defines `critical=6`, `high=10`, `medium=14`, `valveMinZoom=11`, `expandClusterZoom=12`, and `src/utils/mapRenderer.ts` consumes `shouldShowNodeAtZoom()`. But the valve rule is still internally inconsistent: `getNodeImportance()` marks `valve` as `LOW`, so the importance filter keeps valves hidden until `zoom >= 14`, which makes `valveMinZoom=11` ineffective in practice. `src/views/GlobalPipelineView.tsx` passes `pipelineData` into `MapView`, while `src/views/MapTopologyView.tsx` still mounts `MapView` without `pipelineData` and keeps a separate collapsed-graph/editor rendering path.
+- Type / junction line: the two map-related views still do not speak one vocabulary. `GlobalPipelineView.tsx` labels map node types as `source/compressor/distribution/valve/junction`, but `MapTopologyView.tsx` still maps `junction` to `station` in `TYPE_MAP`, tracks grouped members through `sourceNodeIds`, and routes junction CRUD through `topologyEditorApi`. Backend normalization remains stronger than stored data: `backend/app/services/junction_groups.py` expands `JUNCTION-x` references and merges overlaps, while `backend/app/routers/topology_editor.py` still has a split rule where `merge_stations` writes `station_ids=[keep_station_id]` but `create_junction_group` rejects fewer than two bottom-level stations.
+- Database / import-script / API line: this is still DB-content change + script churn + partial contract cleanup, not schema migration. Live DB snapshot remains `stations=1256`, `pipelines=1253`, `pipeline_systems=13`, `junction_groups=16`; `pipeline_systems` schema is still `id/name/color/sort_order/layers_config`, `junction_groups` schema is still `id/name/description/station_ids`, and system ids remain `cred,gn,gs,jxlz,ncsh,pt,sj2,sj3,sj4,we1,we2,zg,zm` with no `we3`. `backend/scripts/add_sj2.py`, `backend/scripts/add_we3.py`, and `backend/scripts/reimport_sj23.py` are still active import churn points. On the frontend, `src/services/api.ts` now splits between proxied `/api/topology/...` graph/stats requests and direct `/topology/correct/...` requests, while `src/hooks/useTopology.ts` goes straight to `/topology/...` via `BACKEND_BASE_URL`; the two topology docs now document that mixed `8080` reality instead of the old `8000` wording.
+- Local experiment residue still needs to be separated from business facts on future runs: `.npm-cache/`, `.agent/workflows/start.md`, `.agent/workflows/start.md.bak-garbled-20260403`, and other AI-assistant related changes are mixed into the same working tree as topology/data work.
+- No business code was modified by this heartbeat run. Memory updated only. Run completed at `2026-04-04 12:26:14 +08:00`.
*** End Patch

*** Begin Patch
*** Update File: src/views/MapTopologyView.tsx
@@
-                        {false && activeTab === 'simulation' && (
+                        {activeTab === 'simulation' && (
+                            <div className="space-y-3">
+                                <div className="rounded-xl border border-cyan-500/25 bg-cyan-950/20 p-3 shadow-[0_0_24px_rgba(6,182,212,0.12)]">
+                                    <div className="flex items-center gap-2 text-cyan-200 text-sm font-bold mb-2">
+                                        <span className="material-symbols-outlined text-base">tune</span>
+                                        仿真控制台
+                                    </div>
+                                    <div className="text-[10px] tracking-wider text-cyan-300/90 font-semibold mb-1.5">INPUT DATA</div>
+
+                                    <div className="space-y-2.5">
+                                        <div>
+                                            <div className="text-xs text-slate-200 mb-1">常规稳态输气</div>
+                                            <select
+                                                value={sim.currentScenario}
+                                                onChange={event => sim.setScenario(event.target.value)}
+                                                className="w-full bg-slate-900/80 border border-cyan-400/30 rounded-lg px-2.5 py-2 text-xs text-cyan-100 outline-none focus:border-cyan-300"
+                                            >
+                                                {MAINLINE_SCENARIOS.map(item => (
+                                                    <option key={item.id} value={item.id} style={{ background: '#0f172a' }}>
+                                                        {item.label}
+                                                    </option>
+                                                ))}
+                                            </select>
+                                        </div>
+
+                                        <div className="grid grid-cols-2 gap-2">
+                                            <div>
+                                                <div className="text-xs text-slate-300 mb-1">选择历史结果</div>
+                                                <select
+                                                    value={sim.selectedSnapshotRunId}
+                                                    onChange={event => sim.setSelectedSnapshotRunId(event.target.value)}
+                                                    className="w-full bg-slate-900/80 border border-cyan-400/30 rounded-lg px-2 py-2 text-xs text-cyan-100 outline-none focus:border-cyan-300"
+                                                >
+                                                    <option value="" style={{ background: '#0f172a' }}>未选择</option>
+                                                    {sim.snapshots.map(item => (
+                                                        <option key={item.run_id} value={item.run_id} style={{ background: '#0f172a' }}>
+                                                            {item.scenario_id} | {item.run_id.slice(0, 8)}
+                                                        </option>
+                                                    ))}
+                                                </select>
+                                            </div>
+                                            <div>
+                                                <div className="text-xs text-slate-300 mb-1">基线快照</div>
+                                                <select
+                                                    value={sim.baselineSnapshotRunId}
+                                                    onChange={event => sim.setBaselineSnapshotRunId(event.target.value)}
+                                                    className="w-full bg-slate-900/80 border border-cyan-400/30 rounded-lg px-2 py-2 text-xs text-cyan-100 outline-none focus:border-cyan-300"
+                                                >
+                                                    <option value="" style={{ background: '#0f172a' }}>未选择</option>
+                                                    {sim.snapshots.map(item => (
+                                                        <option key={item.run_id} value={item.run_id} style={{ background: '#0f172a' }}>
+                                                            {item.scenario_id} | {item.run_id.slice(0, 8)}
+                                                        </option>
+                                                    ))}
+                                                </select>
+                                            </div>
+                                        </div>
+
+                                        <button
+                                            onClick={() => setShowSimParamEditor(value => !value)}
+                                            className="w-full border border-cyan-500/35 bg-cyan-950/30 hover:bg-cyan-900/35 rounded-lg px-3 py-2 text-left text-sm text-cyan-200 transition-colors flex items-center justify-between"
+                                        >
+                                            <span>编辑静态化参数 (压力/流量)</span>
+                                            <span className="material-symbols-outlined text-base">{showSimParamEditor ? 'expand_less' : 'expand_more'}</span>
+                                        </button>
+                                    </div>
+
+                                    <div className="text-[10px] tracking-wider text-cyan-300/90 font-semibold mt-3 mb-2">SOLVER ENGINE</div>
+                                    <div className="grid grid-cols-2 gap-2">
+                                        <button
+                                            onClick={() => void sim.loadSelectedSnapshot()}
+                                            disabled={!sim.selectedSnapshotRunId || sim.snapshotLoading}
+                                            className="rounded-lg border border-emerald-400/30 bg-emerald-950/30 hover:bg-emerald-900/35 disabled:opacity-40 py-2 text-sm font-semibold text-emerald-200 transition-colors"
+                                        >
+                                            按压力快照运行
+                                        </button>
+                                        <button
+                                            onClick={handleRunSimulation}
+                                            disabled={sim.isLoading}
+                                            className="rounded-lg border border-cyan-400/35 bg-cyan-950/30 hover:bg-cyan-900/35 disabled:opacity-40 py-2 text-sm font-semibold text-cyan-100 transition-colors"
+                                        >
+                                            场景参数仿真
+                                        </button>
+                                    </div>
+
+                                    <div className="mt-2 rounded-md border border-slate-700/70 bg-slate-950/70 px-2.5 py-1.5 text-xs text-slate-300 flex items-center gap-2">
+                                        <span className={`inline-block w-2 h-2 rounded-full ${sim.overlay ? 'bg-emerald-400' : 'bg-slate-500'}`} />
+                                        {sim.isLoading ? '运行中' : sim.overlay ? '已完成' : '未运行'}
+                                    </div>
+
+                                    <div className="text-[10px] tracking-wider text-cyan-300/90 font-semibold mt-3 mb-2">OUTPUT VIEW</div>
+                                    <button
+                                        onClick={() => setStatusMsg('全网稳态结果已落图展示。')}
+                                        className="w-full rounded-lg border border-blue-400/35 bg-blue-950/30 hover:bg-blue-900/35 py-2 text-sm font-semibold text-blue-100 transition-colors"
+                                    >
+                                        全网稳态结果落图展示
+                                    </button>
+
+                                    <div className="grid grid-cols-3 gap-2 mt-2">
+                                        <button
+                                            onClick={() => void sim.saveSnapshot()}
+                                            disabled={sim.snapshotLoading || !sim.overlay}
+                                            className="rounded-lg border border-slate-600/70 bg-slate-900/70 hover:bg-slate-800/70 disabled:opacity-40 py-2 text-sm text-slate-200 transition-colors"
+                                        >
+                                            归档
+                                        </button>
+                                        <button
+                                            onClick={() => void sim.refreshSnapshots()}
+                                            disabled={sim.snapshotLoading}
+                                            className="rounded-lg border border-slate-600/70 bg-slate-900/70 hover:bg-slate-800/70 disabled:opacity-40 py-2 text-sm text-slate-200 transition-colors"
+                                        >
+                                            刷新
+                                        </button>
+                                        <button
+                                            onClick={() => {
+                                                sim.clearOverlay()
+                                                setNodeOverrides({})
+                                                setEdgeLengthOverrides({})
+                                                setGlobalDefaults({})
+                                                setParamValidationError(null)
+                                            }}
+                                            className="rounded-lg border border-slate-600/70 bg-slate-900/70 hover:bg-slate-800/70 py-2 text-sm text-slate-200 transition-colors"
+                                        >
+                                            重置
+                                        </button>
+                                    </div>
+                                </div>
+
+                                {showSimParamEditor && (
+                                    <SimParamEditor
+                                        seedNodes={seedNodesForEditor}
+                                        edges={edgesForEditor}
+                                        nodeOverrides={nodeOverrides}
+                                        edgeLengthOverrides={edgeLengthOverrides}
+                                        globalDefaults={globalDefaults}
+                                        validationError={paramValidationError}
+                                        onNodeOverridesChange={setNodeOverrides}
+                                        onEdgeLengthOverridesChange={setEdgeLengthOverrides}
+                                        onGlobalDefaultsChange={setGlobalDefaults}
+                                    />
+                                )}
+
+                                {sim.overlay && (
+                                    <div className="rounded-lg border border-green-500/20 bg-green-950/15 p-2.5 text-[11px] text-green-300">
+                                        <div className="flex items-center gap-1.5 mb-1">
+                                            <span className="material-symbols-outlined text-sm">check_circle</span>
+                                            <span className="font-bold">{sim.overlay.solver_status === 'converged' ? '求解收敛' : '需复核'}</span>
+                                            <span className="text-green-500 ml-auto">{sim.overlay.iterations} 次迭代</span>
+                                        </div>
+                                        <div className="text-slate-300 space-y-0.5">
+                                            <div>总供给 {sim.overlay.summary.total_supply.toFixed(1)} 万方/天</div>
+                                            <div>未满足 {sim.overlay.summary.unserved_demand.toFixed(1)} 万方/天</div>
+                                            <div>平均利用率 {(sim.overlay.summary.avg_utilization * 100).toFixed(1)}%</div>
+                                            <div>告警数量 {sim.overlay.summary.alert_count}</div>
+                                        </div>
+                                    </div>
+                                )}
+                            </div>
+                        )}
+
+                        {false && activeTab === 'simulation' && (
*** End Patch

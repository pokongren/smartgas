*** Begin Patch
*** Add File: docs/2026-04-08-WE1第一张图主仿真演示快捷操作第6阶段第2轮总结_人话版.md
+# WE1 第一张图主仿真演示快捷操作第6阶段第2轮总结
+
+## 1. 这一轮做了什么
+
+这轮给 WE1 第一张图主仿真页补了“演示快捷操作”区，目的是把现场最常用的几步压缩成直接按钮。现在你可以直接一键切到推荐演示场景，也可以把“当前选中的快照”或者“最近留档的快照”直接设成基线，不用再自己来回挑选。
+
+## 2. 为什么这么做
+
+前一轮已经把主仿真页做成能跑、能看、能导出，但现场操作还是偏碎。真实演示时，最常见的动作就是切到最适合讲的场景，再快速挂上基线看差值。这轮就是把这两件事做成低操作成本入口，让主仿真页更像可用的软件，而不是拼起来的功能集合。
+
+## 3. 具体改了什么
+
+1. 新增“演示快捷操作”面板，位置在 [MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx) 的主仿真入口区。
+2. 新增“一键切到推荐场景”按钮，直接用当前覆盖结果里的推荐场景切换主仿真场景。
+3. 新增“当前快照设基线”按钮，把你已经选中的结果快照直接设成基线。
+4. 新增“最近留档设基线”按钮，自动拿最近一次已留档的快照做基线。
+5. 新增 `latestCoveredSnapshotSummary` 计算逻辑，专门从历史快照里找“最近可用的留档快照”。
+6. 新增 3 个页面 helper，分别负责切推荐场景、当前快照设基线、最近留档设基线，并统一回写状态提示。
+
+## 4. 前后对比
+
+| 项目 | 修改前 | 修改后 |
+| --- | --- | --- |
+| 切演示场景 | 要自己看覆盖结果，再手工下拉切场景 | 可以一键切到推荐场景 |
+| 设基线 | 需要手工挑基线快照 | 可以直接把当前快照设成基线 |
+| 用最近留档做基线 | 没有直接入口 | 一键使用最近留档快照 |
+| 现场操作成本 | 需要多次点击和判断 | 常用动作收口到 3 个快捷按钮 |
+
+## 5. 影响范围
+
+这一轮仍然只改 [MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx)，不碰后端接口，不改求解逻辑，不影响其他页面。它属于纯前端操作层增强，但直接改善主仿真演示效率。
+
+## 6. 怎么验证的
+
+1. 构建验证：执行 `npm run build`，通过。
+2. 回归验证：执行 `python backend/scripts/verify_we1_phase34_final.py`，通过。
+3. 子代理验证：独立子代理复核快捷操作相关 helper 和按钮状态链，确认 `latestCoveredSnapshotSummary`、推荐场景切换、基线切换与现有 `useSimulation` 状态一致，没有新的功能性阻断。
+
+## 7. 截图与替代说明
+
+这轮没有补截图，因为按钮逻辑和状态链更适合用功能表和验证结果说明；后续如果要写面向业务的操作手册，再补主仿真页截图会更直接。
+
+## 8. 当前结果
+
+连续两轮已经收口完成。现在 WE1 第一张图主仿真页既能导出演示清单，也有演示快捷操作，主链已经从“能跑”推进到“更适合现场演示和验收”。
*** End Patch

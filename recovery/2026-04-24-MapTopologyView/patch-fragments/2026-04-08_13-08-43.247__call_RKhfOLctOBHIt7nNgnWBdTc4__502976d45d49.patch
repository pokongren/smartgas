*** Begin Patch
*** Update File: src/views/MapTopologyView.tsx
@@
                         {activeTab === 'simulation' && (
                             <div className="space-y-3">
+                                <div className="rounded-xl border border-cyan-400/20 bg-[radial-gradient(circle_at_top_left,_rgba(34,211,238,0.18),_transparent_45%),linear-gradient(135deg,rgba(8,47,73,0.65),rgba(15,23,42,0.92))] p-3 shadow-[0_0_30px_rgba(8,145,178,0.12)] transition-all duration-300">
+                                    <div className="flex items-start justify-between gap-3">
+                                        <div className="min-w-0">
+                                            <div className="text-[10px] uppercase tracking-[0.24em] text-cyan-200/80">Simulation Live</div>
+                                            <div className="mt-2 flex items-center gap-2">
+                                                <span className={`h-2.5 w-2.5 rounded-full ${steadySimLoading ? 'bg-amber-300 animate-pulse' : steadyOverlay ? (steadyOverlay.solver_status === 'converged' ? 'bg-emerald-300 shadow-[0_0_12px_rgba(110,231,183,0.9)]' : steadyOverlay.solver_status === 'max_iter' ? 'bg-amber-300 shadow-[0_0_12px_rgba(253,230,138,0.8)]' : 'bg-red-300 shadow-[0_0_12px_rgba(252,165,165,0.8)]') : 'bg-cyan-300 shadow-[0_0_12px_rgba(103,232,249,0.8)]'}`} />
+                                                <div className="truncate text-sm font-semibold text-white">{steadyOverlayStatusText}</div>
+                                            </div>
+                                            <div className="mt-2 flex flex-wrap items-center gap-2 text-[11px] text-slate-300">
+                                                <span className={`rounded-full border px-2.5 py-1 transition-all duration-300 ${steadyStatusToneClass}`}>{steadySimLoading ? '求解中' : steadyOverlay ? SOLVER_STATUS_LABELS[steadyOverlay.solver_status] : '等待运行'}</span>
+                                                <span className="rounded-full border border-white/10 bg-white/5 px-2.5 py-1 text-slate-200">场景：{steadyScenarioOption?.label ?? steadyScenarioId}</span>
+                                                {steadyOverlay && <span className="rounded-full border border-white/10 bg-white/5 px-2.5 py-1 text-slate-200">run_id：{steadyOverlay.run_id.slice(0, 12)}</span>}
+                                                {steadyOverlay && <span className="rounded-full border border-white/10 bg-white/5 px-2.5 py-1 text-slate-200">生成：{formatDateTimeLabel(steadyOverlay.generated_at)}</span>}
+                                            </div>
+                                        </div>
+                                        <div className="rounded-full border border-cyan-400/20 bg-cyan-500/10 px-3 py-1 text-[10px] uppercase tracking-[0.22em] text-cyan-100">Gas Flow</div>
+                                    </div>
+                                    <div className="mt-3 grid grid-cols-2 gap-2">
+                                        {steadyMetricCards.map((item, index) => (
+                                            <div key={`steady-metric-${item.label}`} className={`rounded-xl border border-white/10 bg-black/15 px-3 py-2 transition-all duration-300 hover:-translate-y-0.5 hover:border-cyan-300/30 ${steadyOverlay ? 'animate-[pulse_2.8s_ease-in-out_infinite]' : ''}`} style={steadyOverlay ? { animationDelay: `${index * 180}ms` } : undefined}>
+                                                <div className="text-[10px] uppercase tracking-[0.18em] text-slate-400">{item.label}</div>
+                                                <div className={`mt-1 text-sm font-semibold ${item.tone}`}>{item.value}</div>
+                                            </div>
+                                        ))}
+                                    </div>
+                                </div>
                                 <div className="rounded-lg border border-cyan-500/20 bg-cyan-950/10 p-3 space-y-3">
                                     <div className="text-[10px] uppercase tracking-wider text-cyan-200">主仿真操作</div>
*** End Patch

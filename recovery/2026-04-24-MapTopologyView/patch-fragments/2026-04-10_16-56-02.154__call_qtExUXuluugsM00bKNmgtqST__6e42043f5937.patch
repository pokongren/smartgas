*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-11-WE1前端仿真提升_第2阶段第1轮_人话版.md
+一、主目标
+1. 本轮目标是把主仿真页面再提升一档：画面变化更明显、文字解释更接地气、AI拿到的数据更结构化可复用。
+
+二、本轮实施内容
+1. 画面变化增强
+(1). 在仿真卡片顶部新增“运行轨迹”四段状态条，按“加载场景、求解计算、叠加渲染、同步AI”动态点亮，运行中节点会脉冲闪烁。  
+(2). 在三条动态体征进度条上新增流动高光效果，结果变化时视觉反馈更明显。  
+2. 文字解释增强
+(1). 新增 `simulationNarrativePlain` 人话解读，直接输出值班可读的“场景状态、供需差值、利用率、告警、建议动作”。  
+(2). 新增 `buildOpsSuggestion(...)` 建议动作规则，把求解状态、告警数、利用率转成可执行建议。  
+3. AI联动增强
+(1). 新增 `simulationStructuredBrief`，把本次仿真整理成结构化对象，包含 summary、coverage、baseline_delta、recommendation。  
+(2). 新增“复制结构化简报（给AI）”按钮，支持一键复制 JSON。  
+(3). 运行时上下文新增 `we1_simulation_narrative_plain` 与 `we1_simulation_structured_brief`，让现有 AI 助手直接消费本轮结果。
+
+三、修改逻辑
+1. 只改前端消费层，不改后端求解层。  
+2. 视觉层在 `MapTopologyView` 里做增量，不打断原有“运行主仿真、保存快照、导出演示清单”流程。  
+3. 文本层保留原简报，同时新增“人话版”和“结构化版”，满足“人看”和“AI看”两条链路。
+
+四、影响范围
+1. 代码文件：`src/views/MapTopologyView.tsx`。  
+2. 功能影响：仅 WE1 第一张图主仿真面板展示与上下文同步。  
+3. 风险边界：不涉及后端接口与数据库变更。
+
+五、验证方式与结果
+1. 构建验证  
+(1). 命令：`npm run build`。  
+(2). 结果：通过，前端可正常打包。  
+2. 功能核验  
+(1). 面板可见“运行轨迹”状态条，并会随仿真状态变化。  
+(2). 动态体征条可见高光流动效果。  
+(3). 可复制“仿真文字简报”和“结构化简报（JSON）”。  
+(4). AI 运行时上下文包含 narrative plain 与 structured brief 两个新字段。
+
+六、前后对比
+1. 改造前后
+
+| 项目 | 改造前 | 改造后 |
+| --- | --- | --- |
+| 运行过程展示 | 仅状态点+指标卡 | 增加四段运行轨迹，过程更直观 |
+| 文字解释 | 单一简报文本 | 增加人话解释，值班可直接读 |
+| AI输入 | 主要是长文本 | 增加结构化JSON，便于模型稳定解析 |
+| 操作闭环 | 看图后需人工转述 | 看图后可一键复制文本或JSON给AI |
+
+七、最终交付物
+1. 代码：`src/views/MapTopologyView.tsx`。  
+2. 文档：`docs/2026-04-11-WE1前端仿真提升_第2阶段第1轮_人话版.md`。
*** End Patch

*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-23-主仿真演示增强四线方案.md
+# 2026-04-23 主仿真演示增强四线方案
+
+一、主目标
+
+1. 把 WE1 主仿真从“已经能跑”继续推进到“更像一个可汇报、可演示、可解释的仿真软件”。
+2. 这一轮不去大改底层架构，重点补四条最影响现场效果的线：画面表现、结果解释、前后对比、演示剧本。
+3. 目标不是只让界面更花，而是让人看完以后能回答三个问题：现在发生了什么，和刚才比变了什么，这个变化意味着什么。
+
+二、上下文与已知信息
+
+1. 4 月 22 日到 4 月 23 日的巡检已经把主链路基本打通了，当前已经具备“读取起始压力、运行主仿真、节点压力过渡显示、快照压力上图、参数面板收口、主面板文案收口、构建与类型校验通过”这些基础能力。
+2. 当前最大的短板不在“能不能跑”，而在“跑完以后够不够直观、够不够像演示系统、够不够容易讲”。
+3. 当前界面里虽然已经有结果，但仍存在三个现实问题。第一，图面动效和状态层级还不够强，领导扫一眼未必知道哪里最重要。第二，结果出来以后还缺一段自动解读，用户要自己组织语言。第三，前后变化虽然部分能看，但还不够一眼看懂。第四，现场演示仍然依赖操作者临场发挥，没有固定的话术和动作顺序。
+
+三、主代理与子代理分工
+
+1. 主代理负责统一目标、冻结口径、决定每条线的实施顺序、收口验证标准，并在每一阶段结束后判断是否可以进入下一阶段。
+2. 侦察子代理职责定义如下。第一，前端侦察负责梳理 `MapTopologyView.tsx`、`SimParamEditor.tsx`、结果面板和节点渲染链路。第二，AI 侦察负责梳理仿真结果对象、快照结构、AI 助手上下文注入点和可复用的摘要字段。第三，演示侦察负责梳理当前按钮、文案、状态栏、截图点位和用户操作路径。
+3. 实施子代理职责定义如下。第一，前端实施负责图层、动效、前后对比、结果卡片和演示入口的代码修改。第二，AI 实施负责把仿真结果整理成结构化摘要，并把摘要送入 AI 助手界面，不在拓扑页直接做 AI 输出。第三，文档实施负责把演示剧本和操作说明整理成可直接照着说的稿子。
+4. 验证子代理职责定义如下。第一，功能验证检查场景切换、运行主仿真、快照对比、图层切换是否都能连续跑通。第二，演示验证检查“是否一眼能看出变化”。第三，工程验证检查 `npm run build`、`npx tsc --noEmit` 以及关键页面是否出现白屏或渲染回退。
+
+四、修改逻辑
+
+1. 画面表现线。
+(1) 目标是让图面一打开就有“这是仿真”的感觉，不再只是静态拓扑图加几个数字。
+(2) 修改层主要在前端展示层，核心文件优先是 `src/views/MapTopologyView.tsx`，必要时拆出单独的结果展示组件和样式文件。
+(3) 第一类改动是节点层。对关键站场增加更明显的状态层级，比如常态、关注、告警、严重告警四档，不只靠红绿两种颜色；对当前场景命中的关键站点加轻量脉冲或呼吸动画。
+(4) 第二类改动是管段层。把主干段和支线在视觉上进一步拉开，主干优先展示；运行后对命中的主干管段叠加方向性和强弱变化，至少让人看得出哪几段在变。
+(5) 第三类改动是结果层。主面板里的总供给、总需求、平均利用率、告警数，运行完成后做一次短时高亮过渡，让人知道“刚刚更新了”。
+(6) 第四类改动是图层控制层。把“起始压力”“仿真结果”“快照对比”做成更清楚的显示切换，不让用户靠猜。
+
+2. 结果解释线。
+(1) 目标是仿真跑完以后，AI 助手界面能自动给出一段简洁说明，告诉用户“这次结果最该关注什么”。
+(2) 这条线不放在拓扑图里直接说，统一放到 AI 助手界面，保持拓扑页只做仿真展示。
+(3) 后端或前端运行完成后，要先整理一份结构化结果摘要。摘要最少包含场景名、运行状态、总供给、总需求、平均利用率、告警数、压力变化最大的前几个节点、流量变化最大的前几段、是否有基线对比、映射覆盖情况。
+(4) AI 接入时优先走“结构化摘要 + 固定模板提示词”，先保证稳定，再考虑更自由的表达。
+(5) 输出口径分两层。第一层是 80 到 120 字的短结论，适合直接读。第二层是 3 段以内的人话解释，适合点开看详情。
+(6) 如果结果不完整，比如映射命中率低、基线缺失、求解状态不是已收敛，AI 说明里必须自动带一句“这次结论可信度受什么影响”，避免误导。
+
+3. 前后对比线。
+(1) 目标是让“仿真前 vs 仿真后”能被一眼看出来，而不是让用户盯着两个数字自己减。
+(2) 第一层对比放在节点。起始压力和仿真后压力并排显示，变化值用统一格式展示，比如“+0.35 MPa”“-0.62 MPa”。
+(3) 第二层对比放在管段。对比重点不求全量铺开，优先盯主干关键段，展示流量变化和利用率变化。
+(4) 第三层对比放在总览。主面板加“相对起始”“相对基线”两个口径中的至少一个，避免现在只看到当前值。
+(5) 第四层对比放在图面。对变化大的节点和管段做颜色抬升，变化小的保持弱化，这样用户扫一眼就知道问题集中在哪里。
+(6) 如果同时有快照基线和当前结果，就优先展示“相对基线”；如果没有基线，就退回“起始压力 vs 当前结果”。
+
+4. 演示剧本线。
+(1) 目标是形成一条标准化演示流程，让不同的人来操作都不至于讲乱。
+(2) 剧本必须绑定界面动作，不能只写“介绍一下系统”。每一步都要写清楚“点哪里、看哪里、说什么”。
+(3) 剧本默认按 4 幕组织。第一幕讲起始状态，第二幕讲运行过程，第三幕讲结果变化，第四幕讲 AI 解读和业务含义。
+(4) 每一幕都要有“主讲词”和“兜底词”。主讲词是正常演示时说的话，兜底词用于页面稍慢或结果加载中时补位。
+(5) 剧本里要明确至少两个固定演示场景。建议一个常规稳态，一个中卫附近主干中断。前者证明系统能稳定跑，后者证明系统能看出异常传播。
+
+五、执行顺序
+
+1. 第1阶段先做前后对比线和画面表现线的最小闭环。
+(1) 原因很直接，这两条最能立刻提升“看一眼就懂”的效果。
+(2) 本阶段只做最核心的三件事：起始值与结果值同屏、变化值上图、主面板结果更新高亮。
+
+2. 第2阶段再接结果解释线。
+(1) 在前后对比有了以后，AI 才有稳定素材可讲。
+(2) 这一阶段先不上自由发挥式 AI，先上结构化简报，再接 AI 助手解释。
+
+3. 第3阶段最后做演示剧本线。
+(1) 因为剧本必须建立在界面稳定之后，不然写了也会不停过期。
+(2) 这一阶段的产物不是代码为主，而是“页面动作 + 话术 + 观察点”的完整说明。
+
+六、验证方式
+
+1. 画面表现验证。
+(1) 验证什么。验证运行前、运行中、运行后，页面状态有没有明显层次变化。
+(2) 怎么验证。固定进入 `#/map-topology`，连续运行常规稳态和中断场景，分别观察主面板、节点、管段、状态条。
+(3) 结果怎么看。用户不看代码，只看画面也应该能说出“刚刚跑了”“哪里变了”“哪里最严重”。
+(4) 失败先查哪层。先查结果状态有没有写入前端，再查渲染层 class 和样式，再查动效是否被缩放保护逻辑关掉。
+
+2. 结果解释验证。
+(1) 验证什么。验证 AI 助手拿到的不是空口白话，而是真正基于本次仿真结果。
+(2) 怎么验证。固定跑两个场景，对比 AI 输出内容是否随场景变化，同时检查输出是否带上关键指标和风险说明。
+(3) 结果怎么看。常规稳态和主干中断两次输出，结论重点必须明显不同。
+(4) 失败先查哪层。先查结构化摘要生成，再查 AI 助手上下文注入，再查提示词模板。
+
+3. 前后对比验证。
+(1) 验证什么。验证同一节点、同一管段、同一总览指标是否能看到变化值。
+(2) 怎么验证。运行前截一张，运行后截一张，再看是否不靠人工计算也能辨认差异。
+(3) 结果怎么看。至少能一眼说出“哪个点掉压最多”“哪段变化最大”“总体利用率升了还是降了”。
+(4) 失败先查哪层。先查起始值缓存和仿真结果映射，再查对比组件，再查显示格式。
+
+4. 演示剧本验证。
+(1) 验证什么。验证一个不写代码的人能不能照着流程完成讲解。
+(2) 怎么验证。按剧本完整演示一遍，记录是否出现“找不到按钮”“不知道该看哪”“说到一半接不上”。
+(3) 结果怎么看。演示应当在 3 到 5 分钟内走完一轮，不依赖临场补救。
+(4) 失败先查哪层。先查页面入口是否分散，再查文案是否不统一，再查剧本是否和实际界面对不上。
+
+5. 工程验证。
+(1) 必跑 `npm run build`。
+(2) 必跑 `npx tsc --noEmit`。
+(3) 必做一次手工连续操作验证，至少包含场景切换、运行主仿真、显示图上压力、加载历史结果、对比基线。
+
+七、风险与回退点
+
+1. 最大风险是 `MapTopologyView.tsx` 继续堆逻辑，做着做着又变成一个大杂烩。回退点是把新功能优先做成子组件或独立函数，不在主文件里继续横向堆代码。
+2. 第二个风险是动画一多，地图缩放和拖动时又出现卡顿或白屏。回退点是所有动效都必须有降级开关，缩放级别低时自动关掉重动效。
+3. 第三个风险是 AI 解释看起来像在胡说。回退点是先用结构化简报兜底，AI 输出始终基于摘要，不允许直接凭空概括。
+4. 第四个风险是前后对比信息太多，最后又把界面挤乱。回退点是默认只显示核心对比，详细对比通过展开面板看。
+
+八、最终交付物
+
+1. 一套更像仿真系统的主拓扑演示界面，至少包括结果高亮、变化上图和更清晰的图层切换。
+2. 一条仿真结果进入 AI 助手的结构化摘要链路。
+3. 一套“起始状态 vs 仿真结果”的对比显示口径。
+4. 一份标准演示剧本说明，能直接照着做汇报。
+5. 每完成一项实施工作，追加对应的人话记录文档，持续沉淀在 `docs/` 目录。
*** End Patch

*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
+## 2026-04-05 06:25:53 +08:00
+
+- 本轮先按入口规则落到主目录执行：初始 `cwd=C:\Users\Administrator\.codex\worktrees\789d\smartgas-grid`，切换后 `cwd=F:\smartgas-grid`，`git rev-parse --show-toplevel=F:/smartgas-grid`。结论只基于 `F:\smartgas-grid`，没有拿 detached worktree 代替事实来源。
+- 不是 `automation env mismatch / worktree-path blockage`，也不是空壳仓库。`F:\smartgas-grid` 下有 `src/`、`backend/`、`docs/` 和本轮点名关键文件；`git worktree list` 显示主目录 `HEAD=eaf6ac4` 在分支上，大量 `.codex/worktrees/*` 仍停在 `6410a10` detached，只能当背景噪音。
+- 本轮主要是主目录里的未提交代码变化 + 数据库变化 + 本地实验混合，同时带有 AI 模块链路变化，不是单纯 docs 漂移。`git status --short` 同时命中地图层级、runtime junction、数据库/导入脚本、AI 助手、docs；未跟踪里还混有 `.npm-cache`、shadow json/db、临时脚本和备份文件。
+- 地图层级线：`src/utils/hierarchyRenderer.ts` 已统一 LOD 阈值 `critical/high/medium=6/10/14`，并带 `valveMinZoom=11`、`expandClusterZoom=12`；`src/utils/mapRenderer.ts` 已接 `getNodeLODStrategy()` / `shouldShowNodeAtZoom()`。`src/views/GlobalPipelineView.tsx` 通过 `MapView -> mapRenderer` 吃到这套口径，但 `src/views/MapTopologyView.tsx` 仍保留 `TOPO_SIZES` 和 collapsed graph 自有规则，所以现在只能说“全国地图链路基本收口”，不能说“地图和拓扑视图已经完全统一”。
+- 枢纽/runtime 线：`backend/app/services/junction_groups.py` 运行时优先读 `junction_groups_rebuilt`，再叠 `manual overlay`，并支持 `JUNCTION-x` 引用展开；`backend/app/routers/topology_editor.py` 的 `/api/topology/junctions` 已返回 `load_runtime_junction_groups()`、`manual_editing_enabled`、`mode`。当前实库快照是 `stations=1272`、`pipelines=1268`、`pipeline_systems=14`、`junction_groups=18`、`junction_groups_rebuilt=7`；`pipeline_systems.layers_config` 是字段，不是表；`junction_groups` 表里也没有独立 `manual_overlay` 列，manual overlay 标记仍主要编码在 `description`。以后不要再沿用旧 memory 里的 `junction_groups=16 / rebuilt=12`。
+- 导入脚本线：`backend/scripts/add_we3.py`、`backend/scripts/add_sj2.py`、`backend/scripts/reimport_sj23.py` 都改成固定写 `backend/data/smartgas.db`，并把新增管段 `category` 收口到 `trunk`，同时补了 `system/prefix` 自检；当前 `WE3/SJ2/SJ3` 前缀站/段分别是 `16/15`、`70/69`、`71/70`。这更像主库补数/重导链路的一部分；但 `backend/scripts/rebuild_junctions.py`、`backend/scripts/verify_junctions.py`、`backend/data/junction-rebuild-*.json`、`backend/data/smartgas.rebuild-preview.db`、`backend/data/raw_excel_index.db` 这批未跟踪文件仍应按本地重编实验和校验产物处理，别直接算正式基线。
+- AI 链路线：前端 `src/components/ai-assistant/useAiAssistantChat.ts` / `useAiAssistantPageContext.ts` 会把 `context + analysis_mode=subagents` 发到 `/api/ai-assistant/chat`；后端 `backend/app/routers/ai_assistant.py` 已分流到 `backend/app/services/ai_analysis_orchestrator.py`、`analysis_validator.py`、`fix_task_router.py`，`backend/app/services/ai_client.py` 默认还是 `AI_BASE_URL=https://api.openai.com/v1`、`AI_DEFAULT_MODEL=gpt-3.5-turbo`，缺 key 有 fallback。口径继续保守：这条链在主目录未提交改动里已经串起来了，但 orchestrator/validator/router 和前端两个 hook 仍是未跟踪文件，又带乱码残留，只能算“主目录里的本地未提交新链路”，不能说已经进正式基线。
+- docs 线不是装饰性修改。`docs/TOPOLOGY_COMPUTATION_USAGE.md`、`docs/TOPOLOGY_SETUP_GUIDE.md` 本轮继续重写收口，`git diff --stat -- docs` 是 `2 files changed, 181 insertions(+), 587 deletions(-)`，重点是把本地开发端口收口到 `8080`，并把 `/api/topology/...` 与 `/topology/...` 的并存现实写清楚。
+- 主代理自检结论：这轮没有把 detached worktree 当主目录事实来源；没有把 `MapTopologyView` 说成已经吃到统一 LOD；没有把 AI 编排链和 rebuild/verify/shadow 产物说成正式基线。下轮继续先做入口校验，再优先盯三件事：1) `MapTopologyView.tsx` 是否显式并入统一 LOD；2) `junction_groups_rebuilt` 数量是否继续漂移；3) AI 未跟踪新文件有没有正式收编。
+
 ## 2026-04-05 02:23:59 +08:00
*** End Patch

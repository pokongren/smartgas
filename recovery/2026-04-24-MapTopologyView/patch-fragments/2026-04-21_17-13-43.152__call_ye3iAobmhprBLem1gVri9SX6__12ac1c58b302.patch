*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-22-实时压力读取与仿真动画第1阶段第1轮记录.md
+# 2026-04-22 实时压力读取与仿真动画第1阶段第1轮记录
+
+一、做了什么
+1. 在拓扑仿真页新增“读取当前压力并仿真（带动画）”按钮，点击后先读后端当前压力，再触发主仿真。2. 新增实时初值组装逻辑：把当前站场压力映射为 `initial_conditions.node_overrides`，并与已有手工参数覆盖合并。3. 改造压力动画起点：首次仿真不再直接跳到结果，而是从当前压力平滑过渡到仿真结果。
+
+二、为什么这么做
+1. 之前流程是“直接跑仿真”，用户看起来像结果瞬间替换，不像真实输气过程。2. 这次把“当前压力”作为动画起点和求解初值，能直观看到压力上升或下降过程，也更符合操作认知。
+
+三、影响范围
+1. 前端文件：`src/views/MapTopologyView.tsx`。2. 后端接口未改，复用现有 `/topology-simulation/seed-nodes` 与 `/topology-simulation/solve-steady`。
+
+四、验证方式与结果
+1. 构建验证：执行 `npm run build`，结果通过。2. 手工验证口径：进入 `#/map-topology`，点击“读取当前压力并仿真（带动画）”，观察节点压力从当前值向新结果渐变。
+
+五、前后对比
+1. 说明：本轮未附截图，原因是当前在终端修复轮，未执行图形界面抓图。2. 对比表如下。
+
+| 对比项 | 修改前 | 修改后 |
+|---|---|---|
+| 仿真初值来源 | 主要依赖默认/手工参数 | 先读取当前压力，再合并手工参数 |
+| 首次压力动画 | 常出现“直接跳值” | 从当前压力平滑过渡到结果 |
+| 操作入口 | 运行主仿真 | 运行主仿真 + 读取当前压力并仿真（带动画） |
+
+六、回退点
+1. 若需回退，仅需撤销 `src/views/MapTopologyView.tsx` 中新增的实时读取与动画起点逻辑，不影响后端数据结构。
*** End Patch

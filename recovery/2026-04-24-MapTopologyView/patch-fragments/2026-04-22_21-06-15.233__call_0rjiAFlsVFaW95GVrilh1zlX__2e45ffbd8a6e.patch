*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-23-演示收口第1阶段第1轮记录.md
+# 2026-04-23 演示收口第1阶段第1轮记录
+
+一、主目标
+
+把主仿真页再收一轮，重点解决“参数说法容易误导”和“演示时容易因为类型问题翻车”这两件事。
+
+二、上下文与已知信息
+
+1. 参数面板里原先“进站”和“最低”共用 `min_pressure_mpa`，界面看上去像两项，底层其实是一项，台上讲解容易讲串。
+2. `npx tsc --noEmit` 原先没有通过，主要卡在错误边界、地图 demo 节点、主拓扑页地图配置、历史面板和仿真对比类型上。
+3. 这一轮目标不是重做架构，而是把现有功能收得更适合演示。
+
+三、主代理与实施结果
+
+1. 参数面板收口。
+(1) 重写了 `SimParamEditor.tsx`，把站场参数面板改成人话文案。
+(2) 把“进站”改成“进站参考”，并改成只读展示，不再和“最低约束”共用可编辑值。
+(3) 保留真正参与求解的三项可编辑内容：`出站设定`、`最低约束`、`温度设定`。
+(4) 管段长度继续保留在面板里，但这一轮只展示，不开放编辑，避免演示时把基础拓扑参数改乱。
+
+2. 稳定性收口。
+(1) `MapView.tsx` 补上了 `hubNodesSample` 引用，并且只在 demo 开关打开时才渲染 demo 枢纽节点。
+(2) `MapTopologyView.tsx` 给 `MapView` 补了默认 `center` 和 `zoom`，把类型缺口补齐。
+(3) `App.tsx` 的 `AppErrorBoundary` 做了兼容式类型收口，保证错误兜底页继续可用，同时让类型检查通过。
+(4) `StationHistoryPanel.tsx`、`useSimulation.ts`、`TopologyDemoView.tsx` 一起做了类型修正，避免演示页旁路文件拖垮全量检查。
+
+四、前后对比
+
+| 项目 | 调整前 | 调整后 |
+| --- | --- | --- |
+| 站场参数解释 | “进站”和“最低”像两套参数，实际共用一份数据 | “进站参考”只展示，“最低约束”单独编辑，口径清楚 |
+| 管段长度区 | 容易让人误以为这一轮还能现场改拓扑长度 | 明确成“只展示，不开放编辑” |
+| 地图 demo 枢纽 | 代码里直接引用，类型检查报错 | 只有 demo 开关开启时才参与渲染 |
+| 主拓扑页地图配置 | `MapView` 配置缺少中心点和缩放 | 已补齐默认视角 |
+| 全量类型检查 | 不能通过 | 已通过 |
+
+五、验证方式与结果
+
+1. 运行 `npx tsc --noEmit`，结果通过。
+2. 运行 `npm run build`，结果通过。
+3. 这一轮没有附截图，因为本轮主要在收口参数语义和编译稳定性；如需对照界面图，下一轮可以补“修改前后”截图。
+
+六、风险与回退点
+
+1. `AppErrorBoundary` 这次是兼容式收口，运行没问题，但后面如果要继续严格类型化，最好把 React 类型依赖再整体梳一遍。
+2. `MapTopologyView.tsx` 体量仍然很大，这轮只是补稳，没有拆文件；后面继续做演示增强时，仍建议拆出参数、叙述、覆盖层几个子模块。
+
+七、最终交付物
+
+1. 参数面板演示口径已收清楚。
+2. 主拓扑页关键类型问题已收掉。
+3. 全量类型检查和生产构建都已通过。
*** End Patch

*** Begin Patch
*** Add File: F:/smartgas-grid/docs/阶段二多Agent实施方案.md
+# SmartGas 阶段二多 Agent 实施方案
+
+> 编制时间：2026-04-03
+>
+> 依据：
+> - `docs/阶段一分工实施方案.md`
+> - `docs/未来项目计划书.md`
+> - `docs/统一公式拓扑AI多Agent协同方案.md`
+
+---
+
+## 1. 阶段二定位
+
+阶段一的目标是把主链路打通，让地图拓扑编辑、拓扑分析、接口访问和页面职责先稳定下来。
+
+阶段二不再继续围绕“编辑闭环”打转，而是承接到下一层业务能力：
+
+1. 把 SCADA 历史能力从单点展示推进到可分析、可对比、可复用。
+2. 把 SCADA、拓扑、枢纽关系打通，形成跨页面分析链路。
+3. 建立一套适合阶段二的多 Agent 开发流水线，让“写代码、验证、修 Bug、回归”可以并行推进。
+
+阶段二的核心不是堆更多页面，而是形成一套能持续吞吐工作的工程组织方式。
+
+---
+
+## 2. 阶段一已完成基础
+
+阶段二之所以能并行推进，前提是阶段一已经完成了几项关键收口。
+
+### 2.1 已具备的工程基础
+
+- `MapTopologyView` 的主链路已经开始收口。
+- 拓扑编辑接口已进入统一服务层管理。
+- 页面直接散落拼接拓扑接口的方式已经开始减少。
+- `GlobalPipelineView` 已开始做第一刀拆分。
+- 页面职责边界已经有初步划分：
+  - `MapTopologyView` 偏编辑
+  - `TopologyView` 偏分析
+  - `GlobalPipelineView` 偏总览与运行态
+
+### 2.2 已具备的能力基础
+
+- 后端已有 SCADA 实时与历史模型。
+- 后端已经具备独立历史库接入基础。
+- 前端已有历史曲线组件。
+- 前端已有枢纽、站点、管线、拓扑图面基础交互。
+- AI 助手已具备统一聊天入口，但阶段二不以深度 AI 上下文联动为主目标。
+
+### 2.3 阶段二可以直接复用的成果
+
+- API 基址统一方式
+- 共享聊天 Hook
+- 拓扑主链路页面结构
+- 枢纽与节点基础数据结构
+- 最小 smoke test 思路
+
+---
+
+## 3. 阶段二目标
+
+## 3.1 业务目标
+
+- 让 SCADA 历史数据真正支持分析，而不只是做演示曲线。
+- 让单站、枢纽、拓扑之间可以互相跳转和互相解释。
+- 让运行态视图开始具备“对比、回放、追溯”的分析能力。
+
+## 3.2 工程目标
+
+- 完成 SCADA 双库能力的工程化硬化。
+- 统一历史查询契约。
+- 统一历史数据与拓扑对象的映射关系。
+- 建立适合阶段二的多 Agent 并行交付流水线。
+
+## 3.3 交付目标
+
+- 单站历史曲线稳定可用。
+- 枢纽多站对比曲线可用。
+- 拓扑页面能触发历史分析。
+- 阶段二的开发流程可以一次性并行推进多个工作面，并自带验证和回修机制。
+
+---
+
+## 4. 阶段二范围
+
+## 纳入范围
+
+- SCADA 双库分层硬化
+- 历史查询接口增强
+- 枢纽维度多曲线查询
+- `GlobalPipelineView` 运行态表达增强
+- `MapTopologyView` / `TopologyView` 的历史分析入口
+- 阶段二多 Agent 写码、验证、修复流水线
+
+## 暂不纳入范围
+
+- 完整 AI 深度上下文联动
+- 调度方案自动生成
+- 完整仿真闭环
+- 全量公式引擎重构
+- 大范围视觉重设计
+
+说明：
+
+这些内容不是不做，而是不放进阶段二主交付，否则任务边界会再次失控。
+
+---
+
+## 5. 阶段二工作流拆分
+
+阶段二建议拆成四条产品工作流，加一条工程工作流。
+
+### 工作流 A：SCADA 双库硬化
+
+目标：
+
+- 把现有历史库从“能用”推进到“稳定、清晰、可维护”。
+
+主要任务：
+
+- 清理主库与历史库的职责边界
+- 明确迁移策略、读写路径、异常兜底
+- 明确 `scada_history` 查询约束
+
+主要涉及文件：
+
+- `backend/app/database.py`
+- `backend/app/scada_models.py`
+- `backend/app/routers/scada.py`
+
+验收口径：
+
+- 历史查询统一走历史库
+- 主库不再承担高频历史查询职责
+- 查询异常时有明确兜底和日志
+
+### 工作流 B：历史分析能力增强
+
+目标：
+
+- 从“单曲线展示”升级为“可用于分析”的历史数据能力。
+
+主要任务：
+
+- 单站历史查询增强
+- 枢纽多站对比曲线
+- 压力 / 温度 / 流量指标切换
+- 时间范围、图例、摘要信息统一
+
+主要涉及文件：
+
+- `backend/app/routers/scada.py`
+- `src/components/scada/ScadaHistoryChart.tsx`
+- `src/components/scada/LuzhiHistoryPanel.tsx`
+
+验收口径：
+
+- 单站查询返回单线数据
+- 枢纽查询返回多线数据
+- 图表能明确区分不同站点与指标
+
+### 工作流 C：SCADA 与拓扑联动
+
+目标：
+
+- 让历史分析不再孤立于图面之外。
+
+主要任务：
+
+- 从枢纽反查底层站点集合
+- 在 `MapTopologyView` 中触发历史分析
+- 在 `TopologyView` 中保留分析跳转入口
+- 建立拓扑对象到历史数据对象的映射规则
+
+主要涉及文件：
+
+- `src/views/MapTopologyView.tsx`
+- `src/views/TopologyView.tsx`
+- `backend/app/services/topology.py`
+- `backend/app/routers/scada.py`
+
+验收口径：
+
+- 点选站点能打开站点历史
+- 点选枢纽能打开多站对比历史
+- 对象映射规则稳定，不依赖临时字符串猜测
+
+### 工作流 D：GlobalPipelineView 第二刀拆分与运行态增强
+
+目标：
+
+- 继续给大页减压，同时增强运行态表达。
+
+主要任务：
+
+- 抽离历史面板状态管理
+- 抽离 SCADA 面板组织逻辑
+- 增强关键站场对比面板
+- 预留历史回放入口
+
+主要涉及文件：
+
+- `src/views/GlobalPipelineView.tsx`
+- `src/views/global-pipeline/*`
+- `src/hooks/*`
+
+验收口径：
+
+- 主文件复杂度继续下降
+- 新面板逻辑不再全部堆在单文件中
+- 页面行为不回归
+
+### 工作流 E：阶段二工程流水线
+
+目标：
+
+- 让阶段二的开发过程本身具备并行吞吐能力。
+
+主要任务：
+
+- 主 Agent 负责编排
+- 多个写代码 Agent 并行推进
+- 验证 Agent 独立验收
+- 修 Bug Agent 接缺陷单修复
+- 回归后再发布
+
+验收口径：
+
+- 不是“写完再看”，而是每条工作流都有清晰验证出口
+- 不允许跳过验证直接合并
+
+---
+
+## 6. 阶段二多 Agent 组织方案
+
+阶段二不建议只有三个 Agent。三个核心角色必须保留，但实际执行应至少拆成六类角色。
+
+## 6.1 核心角色
+
+### 主 Agent
+
+职责：
+
+- 读取总目标
+- 拆分任务
+- 调度写代码 Agent
+- 调度验证 Agent
+- 调度修 Bug Agent
+- 汇总交付结果
+
+不负责：
+
+- 不直接承担所有实现细节
+- 不自己给自己验收
+- 不在没有验证通过时宣布完成
+
+### 验证 Agent
+
+职责：
+
+- 检查接口契约
+- 检查双库路径
+- 检查图表查询结果
+- 检查页面联动结果
+- 输出缺陷单
+
+不负责：
+
+- 不写业务功能代码
+- 不扩展需求范围
+
+### 修 Bug Agent
+
+职责：
+
+- 接收缺陷单
+- 精准修复
+- 补测试、补日志、补兜底
+- 提交回归说明
+
+不负责：
+
+- 不做总编排
+- 不直接定义通过标准
+
+## 6.2 并行写代码 Agent
+
+为了“一次性做很多工作”，建议在阶段二增加并行开发 Agent。
+
+### 数据层 Agent
+
+负责：
+
+- 历史库、主库边界
+- 查询路径统一
+- 模型与迁移检查
+
+### API Agent
+
+负责：
+
+- `scada.py` 的接口增强
+- 枢纽查询协议
+- 查询参数与响应结构统一
+
+### 前端图表 Agent
+
+负责：
+
+- `ScadaHistoryChart`
+- 多站对比图逻辑
+- 指标切换与图例一致性
+
+### 拓扑联动 Agent
+
+负责：
+
+- `MapTopologyView`
+- `TopologyView`
+- 历史入口、对象映射、联动动作
+
+### 页面减压 Agent
+
+负责：
+
+- `GlobalPipelineView` 第二刀拆分
+- 状态管理外提
+- 历史面板与运行态表达整理
+
+---
+
+## 7. 一次性做很多工作的执行方式
+
+这里的关键不是简单“开很多 Agent”，而是把任务切成低耦合块。
+
+## 7.1 任务切片原则
+
+每个并行任务必须满足：
+
+1. 输入边界清晰
+2. 输出边界清晰
+3. 依赖少
+4. 能独立验证
+
+### 适合并行的任务
+
+- 双库配置硬化
+- 历史查询协议增强
+- 图表组件增强
+- 拓扑页历史入口接入
+- `GlobalPipelineView` 状态拆分
+
+### 不适合并行的任务
+
+- 同一接口契约的多方同时修改
+- 同一组件内部状态模型的大范围重构
+- 未定义数据结构前就并行开工
+
+## 7.2 推荐并行矩阵
+
+### 第一批可并行
+
+- 数据层 Agent：双库边界
+- API Agent：历史接口协议
+- 页面减压 Agent：`GlobalPipelineView` 第二刀拆分
+
+### 第二批可并行
+
+- 前端图表 Agent：多站对比图
+- 拓扑联动 Agent：历史入口接入
+
+### 第三批串行收口
+
+- 主 Agent 汇总
+- 验证 Agent 联调验收
+- 修 Bug Agent 回修
+
+---
+
+## 8. 写、验、改代码一套流程
+
+这是阶段二最重要的部分。
+
+## 8.1 标准流水线
+
+```mermaid
+flowchart LR
+    A["主Agent拆任务"] --> B["写代码Agent并行实现"]
+    B --> C["主Agent汇总分支结果"]
+    C --> D["验证Agent统一验证"]
+    D -->|通过| E["主Agent交付结果"]
+    D -->|失败| F["修Bug Agent修复"]
+    F --> G["再次验证"]
+    G -->|通过| E
+    G -->|失败| F
+```
+
+## 8.2 每一步做什么
+
+### 第一步：主 Agent 拆任务
+
+输出内容：
+
+- 任务切片清单
+- 每个 Agent 的边界
+- 依赖顺序
+- 验收口径
+
+### 第二步：写代码 Agent 并行实现
+
+每个 Agent 只改自己的边界文件。
+
+输出内容：
+
+- 改动说明
+- 风险点
+- 需要联调的接口
+
+### 第三步：主 Agent 汇总
+
+职责：
+
+- 对齐契约
+- 合并冲突
+- 补充遗漏
+
+### 第四步：验证 Agent 统一验证
+
+验证内容至少包括：
+
+- 构建验证
+- 接口验证
+- 图表验证
+- 页面联动验证
+- 回归验证
+
+### 第五步：修 Bug Agent 接缺陷单
+
+输入：
+
+- `ValidationReport`
+- 失败场景
+- 文件范围
+
+输出：
+
+- 修复说明
+- 新增测试
+- 回归点
+
+### 第六步：回归验证
+
+验证 Agent 重新跑关键场景。
+
+规则：
+
+- 不通过就继续修
+- 不允许直接宣布完成
+
+---
+
+## 9. 验证 Agent 的验证口径
+
+阶段二的验证不能只看构建是否通过。
+
+## 9.1 数据层验证
+
+- 历史查询是否真的走历史库
+- 主库是否不再承担历史查询
+- 异常查询是否有兜底
+
+## 9.2 接口层验证
+
+- 单站与枢纽查询协议是否一致
+- 多指标切换是否契约清晰
+- 响应结构是否稳定
+
+## 9.3 前端验证
+
+- 单站历史图能打开
+- 枢纽多站图能打开
+- 时间范围切换正常
+- 图例和线条映射正确
+
+## 9.4 联动验证
+
+- 从 `MapTopologyView` 触发历史分析是否正确
+- 从枢纽触发时是否能取到成员站
+- 页面跳转或浮层打开是否稳定
+
+## 9.5 回归验证
+
+至少覆盖：
+
+- 一个单站历史场景
+- 一个枢纽多站场景
+- 一个拓扑联动场景
+- 一个 `GlobalPipelineView` 面板场景
+
+---
+
+## 10. 修 Bug Agent 的修复规则
+
+修 Bug Agent 必须遵守以下规则：
+
+1. 只根据验证结果修
+2. 先修契约问题，再修展示问题
+3. 每个修复都要带回归点
+4. 不得无边界扩大修改范围
+
+### 优先级顺序
+
+1. 数据路径错误
+2. 接口契约错误
+3. 页面联动错误
+4. 图表表现错误
+5. 兜底与日志缺失
+
+---
+
+## 11. 阶段二依赖顺序
+
+```mermaid
+flowchart LR
+    A["工作流A<br/>双库硬化"] --> B["工作流B<br/>历史分析增强"]
+    A --> C["工作流C<br/>SCADA与拓扑联动"]
+    B --> C
+    B --> D["工作流D<br/>GlobalPipelineView 第二刀拆分"]
+    C --> E["工作流E<br/>统一验证与回修"]
+    D --> E
+```
+
+执行说明：
+
+1. 双库硬化先行，否则后面的历史分析会继续漂。
+2. 历史分析增强和页面拆分可以部分并行。
+3. SCADA 与拓扑联动必须建立在稳定查询契约之上。
+4. 验证与回修不是最后补，而是贯穿全过程。
+
+---
+
+## 12. 阶段二最小里程碑
+
+### M1：双库稳定
+
+- 历史库职责清晰
+- 历史查询路径稳定
+
+### M2：历史分析可用
+
+- 单站历史图稳定
+- 枢纽多站对比稳定
+
+### M3：拓扑联动成立
+
+- 图面对象能打开历史分析
+- 枢纽与历史查询打通
+
+### M4：多 Agent 流水线成立
+
+- 至少两类写代码 Agent 并行工作
+- 验证 Agent 输出统一报告
+- 修 Bug Agent 接缺陷单回修
+
+### M5：阶段验收
+
+- 构建通过
+- 核心场景回归通过
+- 文档和验收记录补齐
+
+---
+
+## 13. 建议的文件落点
+
+### 后端
+
+- `backend/app/database.py`
+- `backend/app/scada_models.py`
+- `backend/app/routers/scada.py`
+- `backend/app/services/analysis_validator.py`
+- `backend/app/services/fix_task_router.py`
+
+### 前端
+
+- `src/components/scada/ScadaHistoryChart.tsx`
+- `src/components/scada/LuzhiHistoryPanel.tsx`
+- `src/views/GlobalPipelineView.tsx`
+- `src/views/MapTopologyView.tsx`
+- `src/views/TopologyView.tsx`
+- `src/views/global-pipeline/*`
+
+### 文档与验证
+
+- `docs/阶段二多Agent实施方案.md`
+- `docs/阶段二联调与回归清单.md`
+- `backend/tests/*`
+
+---
+
+## 14. 风险提示
+
+- 如果阶段二把 AI 深度上下文联动一起硬塞进来，范围会失控。
+- 如果双库边界不先收口，历史分析越做越乱。
+- 如果没有独立验证 Agent，阶段二会再次回到“局部能跑、整体漂移”。
+- 如果修 Bug Agent 没有缺陷单边界，回修很容易扩散成新重构。
+
+---
+
+## 15. 结论
+
+阶段二最合理的推进方式，不是继续单线程补功能，而是把任务拆成可并行的工作流，再用多 Agent 机制做统一编排。
+
+最小有效组织方式是：
+
+1. 主 Agent 负责拆任务、排依赖、汇总结果
+2. 多个写代码 Agent 并行推进不同工作流
+3. 验证 Agent 独立给出通过与失败结论
+4. 修 Bug Agent 根据缺陷单精准回修
+5. 再次验证后才允许交付
+
+这套方式的价值不只是阶段二能做完，而是后续阶段三、阶段四也能直接复用。
+
+---
+
+## 16. 下一步建议
+
+建议下一步先做三件事：
+
+1. 先定义阶段二的任务切片表
+2. 先定义 `ValidationReport` 结构
+3. 先跑一个最小闭环：
+   - 双库查询修正
+   - 单站历史增强
+   - 验证 Agent 验证
+   - 修 Bug Agent 回修
+
+这三步跑通后，阶段二就能真正进入高吞吐并行开发状态。
*** End Patch

*** Begin Patch
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
 2026-04-04 02:21 CST
 
 - This run entered the configured live repo root directly: current `cwd` is `F:\smartgas-grid`, `git rev-parse --show-toplevel` resolves to `F:/smartgas-grid`, and `git rev-parse --git-dir` returns `.git`, so there is no `automation env mismatch / worktree-path blockage` this time.
 - Current progress summary: this round is mainly real code change + database change + local experiment mixed together, not a pure docs-only or blockage run. `git status --short` shows active edits in `backend/`, `src/`, `docs/`, plus local cache/noise under `.npm-cache/` and a garbled backup file, so the tree contains both meaningful work and local residue.
 - Repo position context stayed important: `git worktree list --porcelain` still shows the real development branch at `F:\smartgas-grid` on `HEAD eaf6ac4...`, while many Codex worktrees remain detached at the minimal `6410a10` snapshot. This run judged from the live root only.
@@
 - API / port / docs line: runtime code is moving toward a cleaner split with `src/services/apiBase.ts`, `src/services/api.ts`, `src/hooks/useTopology.ts`, `backend/run.py`, and `vite.config.ts` aligned on backend `8080` plus direct `/topology/...` access where needed. But docs are still mixed: `backend/README.md`, `docs/TOPOLOGY_SETUP_GUIDE.md`, and `docs/TOPOLOGY_COMPUTATION_USAGE.md` still mention `8000` and older `/api/topology/...` expectations. `docs/code-improvement-review.md` is a new review note about exactly this contract drift.
 - Local experiment / residue signals worth remembering next run: `.npm-cache/` staged deletes, `.agent/workflows/start.md` edits, `.agent/workflows/start.md.bak-garbled-20260403`, and several new AI-assistant/topology helper files look like local iteration noise mixed into the branch, so future heartbeat runs should keep separating core topology/data changes from tool/cache churn.
 - No business code was modified by this heartbeat run. Memory updated only. Run completed at `2026-04-04 02:21:02 +08:00`.
+
+2026-04-04 15:02 CST
+
+- This run again entered the configured live repo root directly: current `cwd` is `F:\smartgas-grid`, `git rev-parse --show-toplevel` resolves to `F:/smartgas-grid`, and `git rev-parse --git-dir` returns `.git`, so there is no `automation env mismatch / worktree-path blockage`.
+- Current progress summary: this round is still mainly real code change + database change + local experiment mixed together. `git status --short` shows active edits across `src/`, `backend/`, and `docs/`, while `.npm-cache/`, `.agent/workflows/start.md`, and `start.md.bak-garbled-20260403` remain local residue/noise rather than business deliverables.
+- Repo position check stayed stable: `git worktree list --porcelain` still shows the live branch at `F:\smartgas-grid` on `HEAD eaf6ac4...`, while the many Codex worktrees remain detached at `6410a10...`. This run judged only from `F:\smartgas-grid`.
+- Map zoom / hierarchy line: `src/utils/mapRenderer.ts` is still the only live zoom-threshold owner (`VALVE_MIN_ZOOM=11`, `EXPAND_CLUSTER_ZOOM=12`) and now classifies node/line types through `src/utils/pipelineDomain.ts`. `src/utils/hierarchyRenderer.ts` still has no consumers under `src`, so the claimed node-importance hierarchy is still not wired into either `src/views/MapTopologyView.tsx` or `src/views/GlobalPipelineView.tsx`.
+- Extra risk on this line: `src/utils/pipelineDomain.ts` now contains visible mojibake in the fallback Chinese keyword strings, and `src/views/GlobalPipelineView.tsx` also has heavy replacement-character corruption. That means part of the new “unified type inference” work may be carrying encoding damage rather than being a clean refactor.
+- Source / junction line: backend normalization remains the actual source of truth. `backend/app/services/junction_groups.py` still expands `JUNCTION-x` references and merges overlapping groups, `backend/app/routers/topology_editor.py` still normalizes submitted ids before create/delete, and `src/views/MapTopologyView.tsx` now routes junction and position actions through `src/services/topologyEditorApi.ts` with stricter guards against transient or already-occupied stations.
+- Raw DB shape still lags behind runtime normalization: `backend/data/smartgas.db` currently reports `stations=1256`, `pipelines=1253`, `pipeline_systems=13`, `junction_groups=16`, but there is still nested raw junction content (`junction_groups.id=17` stores `["JUNCTION-16", "JXLZ-11"]`). So runtime responses look flatter than stored rows.
+- Database / import-script line: this is still import-script change + DB content change risk, not schema change. `backend/scripts/add_sj2.py`, `backend/scripts/add_we3.py`, and `backend/scripts/reimport_sj23.py` are the main moving pieces; `pipeline_systems` ids remain `cred,gn,gs,jxlz,ncsh,pt,sj2,sj3,sj4,we1,we2,zg,zm`, and `layers_config` still maps cleanly to those prefixes.
+- ID-prefix snapshot for quick comparison next run: `stations` top prefixes are `we1=227`, `we2=221`, `cred=212`, `zm=136`, `zg=106`, `gn=71`, `sj3=71`, `sj2=70`, `sj4=56`; `pipelines` top prefixes are `we1=226`, `we2=222`, `cred=208`, `zm=136`, `zg=105`, `gn=77`, `sj3=70`, `sj2=69`, `sj4=55`.
+- API / docs line remains only half closed: runtime code (`src/services/apiBase.ts`, `src/services/api.ts`, `src/hooks/useTopology.ts`, `backend/run.py`, `vite.config.ts`) is converging on backend `8080` and allows direct `/topology/...`, but many docs and examples still point to `8000` and `/api/topology/...`, including `backend/README.md`, `docs/TOPOLOGY_SETUP_GUIDE.md`, and `docs/TOPOLOGY_COMPUTATION_USAGE.md`. `docs/code-improvement-review.md` is a new write-up tracking exactly this contract drift.
+- No business code was modified by this heartbeat run. Memory updated only. Run completed at `2026-04-04T15:02:00+08:00`.
*** End Patch

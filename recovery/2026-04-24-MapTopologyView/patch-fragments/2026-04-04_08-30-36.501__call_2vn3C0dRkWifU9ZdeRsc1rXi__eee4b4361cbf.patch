*** Begin Patch
*** Delete File: C:\Users\Administrator\.codex\automations\automation\HEARTBEAT_LAST_RUN.md
*** Add File: C:\Users\Administrator\.codex\automations\automation\HEARTBEAT_LAST_RUN.md
+# 项目心跳 - 2026-04-04 16:28 CST
+
+## 改动目标
+
+这轮不是改业务代码。
+目标就两件事：
+
+- 先确认巡检真的落在 `F:\smartgas-grid`
+- 再基于主目录事实，判断这轮到底是代码变化、数据库变化、本地实验，还是路径阻塞
+
+## 当前结论
+
+这轮主要是代码变化 + 数据库内容变化 + 本地实验噪音，不是 `worktree/path sync blockage`。
+入口是过的：初始 `cwd` 是 `C:\Users\Administrator\.codex\worktrees\4c4a\smartgas-grid`，切到 `F:\smartgas-grid` 之后，运行目录和 `git rev-parse --show-toplevel` 都落在主目录。
+
+## 这次做了什么
+
+1. 先做落点修正
+
+把巡检落点切到 `F:\smartgas-grid`，确认不是在 detached worktree 上做判断。
+
+2. 再做仓库级巡检
+
+查了 `git log --oneline -5`、`git status --short`、`git diff --stat`、`git diff --stat -- docs`、`git worktree list --porcelain` 和 `.git` 形态。
+
+3. 按三条主线取证
+
+- 全国地图缩放与层级显示：`src/utils/hierarchyRenderer.ts`、`src/utils/mapRenderer.ts`、`src/views/GlobalPipelineView.tsx`、`src/views/MapTopologyView.tsx`
+- 枢纽规则与数据库口径：`backend/app/services/junction_groups.py`、`backend/app/routers/topology_editor.py`、`backend/data/smartgas.db`
+- 导入脚本与 API/文档口径：`backend/scripts/add_sj2.py`、`backend/scripts/add_we3.py`、`backend/scripts/reimport_sj23.py`、`src/services/api.ts`、`src/hooks/useTopology.ts`、`src/services/apiBase.ts`
+
+4. 做口径复核
+
+重点防三种误判：
+
+- 把主目录之外的 detached worktree 当事实来源
+- 把 national-map 这条 inspected chain 说成全仓统一
+- 把脚本变更说成已经落进 live DB
+
+## 改动前的问题
+
+这轮最容易说错的，不是“有没有变化”，而是“变化到底落在哪里”。
+
+- 容易把 Codex detached worktree 当事实来源
+- 容易看到 `backend/scripts/add_we3.py` 在改，就直接说 `WE3` 已经落库
+- 容易把 `GlobalPipelineView` 这条地图链路的现状，说成整个仓库的统一口径
+
+## 改动后的结果
+
+### 1. 环境与路径
+
+- `F:\smartgas-grid\.git` 是真实目录，不是指针文件
+- `git worktree list --porcelain` 里，`F:\smartgas-grid` 是 live branch，对应 `HEAD eaf6ac4...`
+- 其他 Codex worktree 仍然基本都停在 detached `6410a10...`
+
+### 2. 全国地图缩放与层级显示
+
+当前真正接上的链路是：
+
+`src/utils/hierarchyRenderer.ts -> src/utils/mapRenderer.ts -> src/views/GlobalPipelineView.tsx`
+
+这条链路里现在能确认的阈值是：
+
+- `critical = 6`
+- `high = 10`
+- `medium = 14`
+- `valveMinZoom = 11`
+- `expandClusterZoom = 12`
+
+但阀室规则还没收平。
+`hierarchyRenderer.ts` 里虽然把 `valveMinZoom` 配成了 `11`，但 `getNodeImportance()` 仍把 `valve` 归到 `LOW`，而 `shouldShowNodeAtZoom()` 先按重要性过滤，所以阀室实际更接近 `zoom >= 14` 才稳定出现。
+
+`GlobalPipelineView.tsx` 和 `MapTopologyView.tsx` 也还不是一套口径：
+
+- `GlobalPipelineView.tsx` 会把 `pipelineData` 传给 `MapView`
+- `MapTopologyView.tsx` 明写了“不传 `pipelineData`”，地图只挂底图
+- `MapTopologyView.tsx` 仍把 `junction` 映射回 `station`
+
+所以这轮只能说“全国地图 inspected chain 已接上”，不能说“地图和拓扑视图已经统一”。
+
+### 3. 枢纽规则、数据库口径、导入脚本
+
+后端运行时口径，比数据库原始存储更干净：
+
+- `backend/app/services/junction_groups.py` 会展开 `JUNCTION-x`
+- 同一个文件还会把有交集的组自动并起来
+- `backend/app/routers/topology_editor.py` 里的 `create_junction_group()` 现在要求展开后至少 2 个底层站点
+
+但写入口径还是分裂的：
+
+- `create_junction_group()` 拒绝少于 2 个底层站点
+- `merge_stations()` 仍会写出 `station_ids=[keep_station_id]` 的单站 junction
+
+live DB 这轮快照还是：
+
+- `stations = 1256`
+- `pipelines = 1253`
+- `pipeline_systems = 13`
+- `junction_groups = 16`
+- `pipeline_system ids = cred,gn,gs,jxlz,ncsh,pt,sj2,sj3,sj4,we1,we2,zg,zm`
+
+库里还留着两类脏事实：
+
+- nested junction：`junction_groups.id=17=["JUNCTION-16","JXLZ-11"]`
+- single-station junction：至少还有 `6/7/9/11/12/13/15`
+
+导入脚本这轮也确实在动：
+
+- `backend/scripts/add_sj2.py` 现在围绕“西沙屯 -> 琉璃河 -> 永清”
+- `backend/scripts/add_we3.py` 定义了 `WE3`，并用“西沙屯分输站”替掉“长阳分输站”
+- `backend/scripts/reimport_sj23.py` 是更彻底的 `SJ2/SJ3` 重导入流程
+
+但这不等于已经落库。
+因为 live DB 仍然是：
+
+- `SJ2 = 70 / 69`
+- `SJ3 = 71 / 70`
+- `WE3 = 0 / 0`
+
+所以现在最多只能说“脚本在改”，不能说“WE3 已经进库”。
+
+### 4. API 与文档口径
+
+这里现在是混合态，不是单一前缀。
+
+- `backend/run.py` 和 `vite.config.ts` 走 `8080`
+- `src/hooks/useTopology.ts` 直连 `/topology/...`
+- `src/services/api.ts` 同时还在走 `/api/topology/...` 和 `/topology/correct/...`
+- `docs/TOPOLOGY_SETUP_GUIDE.md`、`docs/TOPOLOGY_COMPUTATION_USAGE.md` 这轮文档改动，就是把这个混合现实写清楚
+
+旧口径还没完全清掉：
+
+- `backend/README.md`
+- `backend/scripts/test_api.py`
+- `backend/scripts/test_ai_stream.py`
+
+这几个地方还写着 `8000`。
+
+## 影响范围
+
+- `F:\smartgas-grid\src\utils\hierarchyRenderer.ts`
+- `F:\smartgas-grid\src\utils\mapRenderer.ts`
+- `F:\smartgas-grid\src\views\GlobalPipelineView.tsx`
+- `F:\smartgas-grid\src\views\MapTopologyView.tsx`
+- `F:\smartgas-grid\backend\app\services\junction_groups.py`
+- `F:\smartgas-grid\backend\app\routers\topology_editor.py`
+- `F:\smartgas-grid\backend\data\smartgas.db`
+- `F:\smartgas-grid\backend\scripts\add_sj2.py`
+- `F:\smartgas-grid\backend\scripts\add_we3.py`
+- `F:\smartgas-grid\backend\scripts\reimport_sj23.py`
+- `F:\smartgas-grid\src\services\api.ts`
+- `F:\smartgas-grid\src\hooks\useTopology.ts`
+- `F:\smartgas-grid\src\services\apiBase.ts`
+- `F:\smartgas-grid\docs\TOPOLOGY_SETUP_GUIDE.md`
+- `F:\smartgas-grid\docs\TOPOLOGY_COMPUTATION_USAGE.md`
+
+## 验证方式
+
+- 确认 `cwd == F:\smartgas-grid`
+- 确认 `git rev-parse --show-toplevel == F:/smartgas-grid`
+- 查看 `git worktree list --porcelain`
+- 查看 `git log --oneline -5`
+- 查看 `git status --short`
+- 查看 `git diff --stat`
+- 查看 `git diff --stat -- docs`
+- 直接读取关键源码
+- 直接查询 `backend/data/smartgas.db`
+
+## 验证结果
+
+- 这轮落点正确，不是环境错位
+- 当前仓库是活仓，不是空壳 worktree
+- docs 变化主要在 `docs/TOPOLOGY_SETUP_GUIDE.md` 和 `docs/TOPOLOGY_COMPUTATION_USAGE.md`
+- 全国地图 inspected chain 已接上，但 editor chain 还是另一套口径
+- 阀室显示阈值仍然存在“配置 11，实际更像 14”的错位
+- runtime 的 junction 归一化比 DB 原始存储更干净
+- `WE3` 仍停留在脚本变更层，没有 live DB 证据支持“已落库”
+
+## 风险点
+
+- 地图缩放口径还没完全统一，阀室显示容易误判
+- `GlobalPipelineView.tsx` 和 `MapTopologyView.tsx` 的节点语义还在分叉
+- `merge_stations()` 和 `create_junction_group()` 的写入口径不一致，后面还会继续制造 single-station junction
+- `.agent/`、`.npm-cache/`、AI assistant 相关改动仍混在同一棵工作树里，容易污染业务判断
+
+## 前后对比
+
+这轮不适合放 UI 截图。
+原因很简单：
+
+- 没改业务 UI
+- 这轮核心是落点、代码口径和 DB 快照核对
+- 用结构图和对比表更直观
+
+### 结构图
+
+```mermaid
+flowchart LR
+    A["初始 automation cwd\nC:\\Users\\Administrator\\.codex\\worktrees\\4c4a\\smartgas-grid"] --> B["切到主目录\nF:\\smartgas-grid"]
+    B --> C["git top-level\nF:/smartgas-grid"]
+    C --> D["national-map chain\nhierarchyRenderer.ts -> mapRenderer.ts -> GlobalPipelineView.tsx"]
+    C --> E["editor chain\nMapTopologyView.tsx -> topology editor APIs"]
+    C --> F["live DB\nbackend/data/smartgas.db"]
+    C --> G["import scripts\nadd_sj2.py / add_we3.py / reimport_sj23.py"]
+    D --> H["阀室规则仍打架\n配 11，但实际更接近 14"]
+    E --> I["junction 仍映射回 station\n未与 national-map chain 统一"]
+    F --> J["库里仍有 nested + single-station junction"]
+    G --> K["WE3 仍是脚本变化\n不是已落库事实"]
+```
+
+### 对比表
+
+| 对比项 | 之前容易说错的点 | 这轮确认后的说法 |
+| --- | --- | --- |
+| 事实来源 | 容易把 detached worktree 当事实来源 | 这轮只以 `F:\smartgas-grid` 为准 |
+| 仓库状态 | 容易先判成 blockage | 这轮是代码变化 + DB 变化 + 本地噪音混合 |
+| 地图链路 | 容易说“地图全统一了” | 只能确认 national-map inspected chain 接上 |
+| 阀室阈值 | 容易照着 `valveMinZoom=11` 直接下结论 | 实际仍受 `LOW` 分层影响，更接近 `zoom >= 14` |
+| WE3 状态 | 容易把脚本变更说成已落库 | 现在只能说脚本在改，live DB 还是 `0/0` |
+| 枢纽状态 | 容易把 runtime 归一化说成 DB 已清洗 | runtime 更干净，DB 仍有 nested/single-station junction |
+
+## 测试结果
+
+这轮没有跑业务测试。
+因为没有改业务代码。这次是巡检型验证，不是修复型回归。
+
+## 回退方式
+
+这轮没有业务代码改动。
+如果要回退，只需要忽略这份总结和 `memory.md` 的追加记录。
*** Update File: C:\Users\Administrator\.codex\automations\automation\memory.md
@@
 - Verifier reminder for future runs: do not call the repo "fully unified", do not treat `add_we3.py` or other import scripts as already reflected in live DB content unless counts or rows prove it, and keep map conclusions scoped to the inspected national-map chain plus editor chain.
 - No business code was modified by this heartbeat run. Memory and automation summary updated only. Run completed at `2026-04-04T14:30:46+08:00`.
+
+2026-04-04 16:28 CST
+
+- This run again passed the main-root landing rule: initial automation cwd was `C:\Users\Administrator\.codex\worktrees\4c4a\smartgas-grid`, then the inspection switched into `F:\smartgas-grid`, and both runtime cwd plus `git rev-parse --show-toplevel` resolved to `F:\smartgas-grid` / `F:/smartgas-grid`. All conclusions stayed anchored to the live repo only.
+- Current progress summary: this round is still real code change + database content change + local experiment/noise mixed together, not `automation env mismatch / worktree-path blockage`. `F:\smartgas-grid\.git` is a real directory; `git worktree list --porcelain` still shows `F:\smartgas-grid` as the live branch on `HEAD eaf6ac4...`, while the Codex worktrees remain detached at `6410a10...`.
+- Environment/diff line: `git log --oneline -5` still shows live commits (`eaf6ac4`, `b31a010`, `d3c9d08`, `c19937f`, `3e47946`). `git status --short` still spans `src/`, `backend/`, `docs/`, plus local residue under `.agent/`, `.npm-cache/`, and AI-assistant related files. `git diff --stat -- docs` is still concentrated in `docs/TOPOLOGY_SETUP_GUIDE.md` and `docs/TOPOLOGY_COMPUTATION_USAGE.md`.
+- National-map chain line: the inspected chain remains `src/utils/hierarchyRenderer.ts` -> `src/utils/mapRenderer.ts` -> `src/views/GlobalPipelineView.tsx`, and wording must stay scoped to that chain only. Thresholds remain `critical=6`, `high=10`, `medium=14`, `valveMinZoom=11`, and `expandClusterZoom=12`, but `getNodeImportance()` still classifies `valve` as `LOW`, so `shouldShowNodeAtZoom()` still keeps valves hidden until `zoom >= 14`; this is a mismatch inside the inspected national-map chain, not a whole-repo statement.
+- National-map chain vs editor chain line: `src/views/GlobalPipelineView.tsx` still passes `pipelineData` into `MapView`, while `src/views/MapTopologyView.tsx` still mounts `MapView` without `pipelineData`, keeps its own editor graph, maps `junction` back to `station`, and uses `focusNode` only as a cross-view locator. Safe phrasing remains: these two inspected chains still do not share one node-type/zoom vocabulary.
+- Junction / DB / script line: backend runtime normalization is still cleaner than stored rows. `backend/app/services/junction_groups.py` still expands `JUNCTION-x` refs and merges overlapping groups, but `backend/app/routers/topology_editor.py` still has split write rules where `merge_stations` writes `station_ids=[keep_station_id]` while `create_junction_group` rejects fewer than two normalized station ids. Live DB snapshot from this run remains `stations=1256`, `pipelines=1253`, `pipeline_systems=13`, `junction_groups=16`; system ids remain `cred,gn,gs,jxlz,ncsh,pt,sj2,sj3,sj4,we1,we2,zg,zm`; storage still contains nested row `junction_groups.id=17=["JUNCTION-16","JXLZ-11"]`, several single-station rows, and `positionMeta` in 89 station-property rows.
+- Script-vs-live-data line: staged backend churn is real, but script presence still must not be described as already landed. `backend/scripts/add_sj2.py` still targets the `西沙屯 -> 琉璃河 -> 永清` segment, `backend/scripts/add_we3.py` still defines `WE3` with `西沙屯分输站`, and `backend/scripts/reimport_sj23.py` remains a stronger SJ2/SJ3 reimport flow; yet live DB counts still show `SJ2=70/69`, `SJ3=71/70`, and `WE3=0/0`, so `WE3` is still script churn rather than confirmed live content.
+- API/docs line: runtime and docs still describe a mixed `8080` contract. `backend/run.py`, `vite.config.ts`, and `src/services/apiBase.ts` point to `8080`; `src/hooks/useTopology.ts` goes straight to `/topology/...`; `src/services/api.ts` still mixes `/api/topology/...` graph/stats with direct `/topology/correct/...`; and the two topology docs now explicitly document that mixed reality, while older references to `8000` still survive in `backend/README.md`, `backend/scripts/test_api.py`, and `backend/scripts/test_ai_stream.py`.
+- Automation-only outputs updated this run: rewrote `C:\Users\Administrator\.codex\automations\automation\HEARTBEAT_LAST_RUN.md` in plain-language markdown and appended this memory entry. No business code was modified. Run completed at `2026-04-04T16:28:02+08:00`.
*** End Patch

*** Begin Patch
*** Add File: F:/smartgas-grid/docs/automation/project-heartbeat-2026-04-07-051140.md
+# 项目心跳巡检总结（2026-04-07 05:11 +08:00）
+
+## 主目标
+固定在 `F:\smartgas-grid` 主目录做一次只认主仓事实的巡检，先校验落点，再核对 git 现场、docs 变化、地图缩放与层级显示、枢纽规则与数据库口径、导入脚本、AI 模块链路；本轮只更新巡检文档和 automation memory，不改业务代码。
+
+## 上下文与已知信息
+- 主目录事实已确认：`git rev-parse --show-toplevel` 返回 `F:/smartgas-grid`，`git rev-parse --git-dir` 返回 `.git`，且 [`F:\smartgas-grid\.git`](F:/smartgas-grid/.git) 是实体目录，不是 worktree 指针文件。
+- `git worktree list --porcelain` 显示主仓 `HEAD` 在 `f75536d`，主仓分支相对远端 `ahead 3`；大量 detached worktree 仍停在 `6410a10`，本轮结论只认主仓，不拿 detached 顶结论。
+- 巡检开始前 docs 工作区无未提交 diff；docs 最近落点集中在 2026-04-07 04:57 的 WE1 阶段 4 文档。
+
+## 主代理与子代理分工
+| 角色 | 实际动作 | 结果 |
+| --- | --- | --- |
+| 主代理 | 锁定主目录事实、汇总证据、写本轮巡检文档和 memory | 完成 |
+| 侦察 A | 核对 `git status`、`git log`、`git diff`、`git worktree list`、`.git` 形态 | 完成 |
+| 侦察 B | 复核地图缩放/层级、枢纽 runtime 规则、导入脚本、AI 与仿真链路 | 完成 |
+| 验证 | 复跑数据库口径检查并确认本轮只改文档与 memory | 完成 |
+
+## 修改逻辑
+- 只新增本轮巡检 Markdown，并刷新 [`C:\Users\Administrator\.codex\automations\automation\memory.md`](C:/Users/Administrator/.codex/automations/automation/memory.md)。
+- 旧 memory 里地图 LOD 阈值已经落后于当前代码，本轮按代码事实纠偏为 `critical=4`、`high=9`、`medium=12`、`valveMin=11`、`expandCluster=12`。
+- 旧 memory 里的 git 现场也已过时，本轮按主仓最新事实更新到 `HEAD=f75536d`、`ahead 3`，并把“当前未提交重点”收敛到拓扑仿真链路，而不是继续沿用昨晚的旧表述。
+
+## 执行顺序
+1. 校验 `F:\smartgas-grid`、`.git` 实体目录和 `git worktree list --porcelain`。
+2. 复核 `git status --short --branch --untracked-files=all`、`git log --oneline -5`、`git diff --name-status`、`git diff --stat`、`git log --oneline -5 -- docs`。
+3. 复核地图主链 [`F:\smartgas-grid\src\views\GlobalPipelineView.tsx`](F:/smartgas-grid/src/views/GlobalPipelineView.tsx)、[`F:\smartgas-grid\src\components\map-view\MapView.tsx`](F:/smartgas-grid/src/components/map-view/MapView.tsx)、[`F:\smartgas-grid\src\utils\mapRenderer.ts`](F:/smartgas-grid/src/utils/mapRenderer.ts)、[`F:\smartgas-grid\src\utils\hierarchyRenderer.ts`](F:/smartgas-grid/src/utils/hierarchyRenderer.ts)。
+4. 复核枢纽与数据库口径 [`F:\smartgas-grid\backend\app\services\junction_groups.py`](F:/smartgas-grid/backend/app/services/junction_groups.py)、[`F:\smartgas-grid\backend\app\routers\pipeline_packages.py`](F:/smartgas-grid/backend/app/routers/pipeline_packages.py)、[`F:\smartgas-grid\backend\check_db.py`](F:/smartgas-grid/backend/check_db.py)。
+5. 复核导入/仿真链路 [`F:\smartgas-grid\backend\scripts\reimport_sj23.py`](F:/smartgas-grid/backend/scripts/reimport_sj23.py)、[`F:\smartgas-grid\backend\scripts\add_sj2.py`](F:/smartgas-grid/backend/scripts/add_sj2.py)、[`F:\smartgas-grid\backend\scripts\add_we3.py`](F:/smartgas-grid/backend/scripts/add_we3.py)、[`F:\smartgas-grid\src\hooks\useSimulation.ts`](F:/smartgas-grid/src/hooks/useSimulation.ts)、[`F:\smartgas-grid\backend\app\routers\topology_simulation.py`](F:/smartgas-grid/backend/app/routers/topology_simulation.py)。
+6. 复核 AI 链路 [`F:\smartgas-grid\src\components\ai-assistant\useAiAssistantChat.ts`](F:/smartgas-grid/src/components/ai-assistant/useAiAssistantChat.ts)、[`F:\smartgas-grid\backend\app\routers\ai_assistant.py`](F:/smartgas-grid/backend/app/routers/ai_assistant.py)、[`F:\smartgas-grid\backend\app\services\ai_analysis_orchestrator.py`](F:/smartgas-grid/backend/app/services/ai_analysis_orchestrator.py)。
+7. 写本轮文档并更新 automation memory。
+
+## 当前进展摘要
+
+### 1. 主目录与 git 现场
+- 主仓当前 `HEAD` 为 `f75536d`，最近 5 条提交是 `f75536d`、`5ec5af0`、`602f715`、`eaf6ac4`、`b31a010`。
+- 主仓分支相对远端 `ahead 3`。
+- 当前未提交 diff 主要集中在 6 个 tracked 文件和 4 个 untracked 文件：
+
+| 类型 | 文件 |
+| --- | --- |
+| tracked | `backend/app/routers/pipeline_packages.py` |
+| tracked | `backend/app/routers/topology_simulation.py` |
+| tracked | `backend/app/services/topology_simulation.py` |
+| tracked | `src/utils/hierarchyRenderer.ts` |
+| tracked | `src/utils/mapRenderer.ts` |
+| tracked | `src/views/MapTopologyView.tsx` |
+| untracked | `backend/app/services/we1_solver_input_service.py` |
+| untracked | `backend/app/services/we1_steady_validation_service.py` |
+| untracked | `backend/scripts/verify_we1_phase3.py` |
+| untracked | `backend/scripts/verify_we1_phase4.py` |
+
+- `git diff --stat` 显示当前未提交重点已经收敛到“WE1 solver input + topology simulation overlay + 地图层级/UI 配套”，不是 detached worktree 上的旧现场。
+
+### 2. docs 变化
+- 巡检开始前 `git diff --name-only -- docs` 为空，docs 工作区干净。
+- docs 目录最近落点是 [`F:\smartgas-grid\docs\2026-04-07-WE1阶段4-独立验收与回归闭环.md`](F:/smartgas-grid/docs/2026-04-07-WE1阶段4-独立验收与回归闭环.md) 和 [`F:\smartgas-grid\docs\2026-04-07-WE1阶段4-独立验收与回归闭环总结_人话版.md`](F:/smartgas-grid/docs/2026-04-07-WE1阶段4-独立验收与回归闭环总结_人话版.md)。
+- docs 历史最近 5 条提交仍停在 `602f715`、`eaf6ac4`、`b31a010`、`e5118a5`、`2f4fb37`，说明 docs 这轮还没有新的已提交业务结论。
+
+### 3. 地图缩放与层级显示
+- 全国地图主链仍是 [`F:\smartgas-grid\src\views\GlobalPipelineView.tsx`](F:/smartgas-grid/src/views/GlobalPipelineView.tsx) -> [`F:\smartgas-grid\src\components\map-view\MapView.tsx`](F:/smartgas-grid/src/components/map-view/MapView.tsx) -> [`F:\smartgas-grid\src\utils\mapRenderer.ts`](F:/smartgas-grid/src/utils/mapRenderer.ts) -> [`F:\smartgas-grid\src\utils\hierarchyRenderer.ts`](F:/smartgas-grid/src/utils/hierarchyRenderer.ts)。
+- 当前代码里的 LOD 阈值是：
+
+| 项目 | 当前代码值 |
+| --- | --- |
+| `criticalZoom` | `4` |
+| `highZoom` | `9` |
+| `mediumZoom` | `12` |
+| `valveMinZoom` | `11` |
+| `expandClusterZoom` | `12` |
+
+- `MapView` 仍通过 `renderPipelineNodesWithClustering()` 消费 `getNodeLODStrategy()`；当前未提交 diff 额外加了 `showFlowArrows: zoom >= 10`。
+- [`F:\smartgas-grid\src\views\MapTopologyView.tsx`](F:/smartgas-grid/src/views/MapTopologyView.tsx) 仍明确“不传 `pipelineData` 给 `MapView`”，只把 `MapView` 当底图，所以它还是独立渲染口径，不能写成已并入全国地图主链。
+
+### 4. 枢纽规则与数据库口径
+- 运行时主口径仍是 [`F:\smartgas-grid\backend\app\services\junction_groups.py`](F:/smartgas-grid/backend/app/services/junction_groups.py) 的 `load_runtime_junction_groups()`：
+  - 优先读 `junction_groups_rebuilt`
+  - 缺失时回退 `junction_groups`
+  - 最后叠 `manual overlay`
+  - overlay 与 base 冲突时按 station_id 覆盖
+- 这一 runtime 口径继续被 [`F:\smartgas-grid\backend\app\routers\pipeline_packages.py`](F:/smartgas-grid/backend/app/routers/pipeline_packages.py)、[`F:\smartgas-grid\backend\app\services\topology.py`](F:/smartgas-grid/backend/app/services/topology.py)、[`F:\smartgas-grid\backend\app\routers\scada.py`](F:/smartgas-grid/backend/app/routers/scada.py)、[`F:\smartgas-grid\backend\app\routers\topology_editor.py`](F:/smartgas-grid/backend/app/routers/topology_editor.py) 共用。
+- `python backend/check_db.py --db backend/data/smartgas.db` 结果：
+
+| 项目 | 当前结果 |
+| --- | --- |
+| `pipeline_systems.count` | `13` |
+| `pipeline_systems.ids` | `we2, cred, pt, zg, zm, gn, gs, ncsh, jxlz, sj4, sj2, sj3, we1` |
+| `we3` | `missing` |
+| `WE3 snapshot` | `stations=0, pipelines=0` |
+| `SJ2 snapshot` | `stations=70, pipelines=69` |
+| `SJ3 snapshot` | `stations=71, pipelines=70` |
+| `junction_groups.count` | `25` |
+| `junction_groups_backup_20260404.count` | `16` |
+| `junction_groups_rebuilt.count` | `7` |
+| `major_junction_count` | `0` |
+| `issue_count` | `0` |
+| `junction_groups_rebuilt.latest` | `2026-04-04T17:17:43.361829+00:00` |
+
+- 结论不变：runtime 规则没散，但 rebuilt 覆盖面仍明显小于旧表，旧表还没完全退场。
+
+### 5. 导入脚本与仿真进展
+- 传统导入脚本边界没变：[`F:\smartgas-grid\backend\scripts\reimport_sj23.py`](F:/smartgas-grid/backend/scripts/reimport_sj23.py)、[`F:\smartgas-grid\backend\scripts\add_sj2.py`](F:/smartgas-grid/backend/scripts/add_sj2.py)、[`F:\smartgas-grid\backend\scripts\add_we3.py`](F:/smartgas-grid/backend/scripts/add_we3.py) 仍是离线补数/重导工具，不是运行时主链。
+- 当前业务新进展主要在拓扑仿真 overlay：
+  - [`F:\smartgas-grid\src\views\TopologyView.tsx`](F:/smartgas-grid/src/views/TopologyView.tsx) 已接 `useSimulation({ pilotId: 'mainline_zhongwei_jingbian' })`
+  - [`F:\smartgas-grid\src\hooks\useSimulation.ts`](F:/smartgas-grid/src/hooks/useSimulation.ts) 请求 `GET /api/pipeline-packages/simulation-overlay`
+  - [`F:\smartgas-grid\backend\app\routers\topology_simulation.py`](F:/smartgas-grid/backend/app/routers/topology_simulation.py) 现在会从 `pipeline package + pilot + seed` 组装 solver input，再调用 [`F:\smartgas-grid\backend\app\services\topology_simulation.py`](F:/smartgas-grid/backend/app/services/topology_simulation.py) 的 `solve_steady`
+- 另一条未提交 UI 变化在 [`F:\smartgas-grid\src\views\MapTopologyView.tsx`](F:/smartgas-grid/src/views/MapTopologyView.tsx) 新增了 simulation tab，但它调用的是 `topologyAPI.simulate()` 那套链路，和 `useSimulation -> simulation-overlay` 不是同一条入口，巡检时不能混写成一回事。
+
+### 6. AI 模块链路
+- 前端入口仍是 [`F:\smartgas-grid\src\components\ai-assistant\useAiAssistantChat.ts`](F:/smartgas-grid/src/components/ai-assistant/useAiAssistantChat.ts)，固定请求 `/api/ai-assistant/chat`；有页面上下文时自动带 `analysis_mode=subagents`。
+- 后端入口仍是 [`F:\smartgas-grid\backend\app\routers\ai_assistant.py`](F:/smartgas-grid/backend/app/routers/ai_assistant.py)：
+  - 先尝试 direct raw-excel count/list/entity lookup
+  - 命中 orchestration 条件时走 orchestrated
+  - 否则回退 legacy
+- 分流规则仍在 [`F:\smartgas-grid\backend\app\services\ai_analysis_orchestrator.py`](F:/smartgas-grid/backend/app/services/ai_analysis_orchestrator.py)，模型调用出口仍在 [`F:\smartgas-grid\backend\app\services\ai_client.py`](F:/smartgas-grid/backend/app/services/ai_client.py)，工具层仍在 [`F:\smartgas-grid\backend\app\services\assistant_tools.py`](F:/smartgas-grid/backend/app/services/assistant_tools.py)。
+- 结论：AI 主链没断，也还不是单一路径，仍是 direct / orchestrated / legacy 并存。
+
+## 验证方式
+| 验证项 | 方法 | 结果 | 失败时先查哪一层 |
+| --- | --- | --- | --- |
+| 主目录落点 | `Get-Location`、`git rev-parse`、`.git` 实体目录检查 | 通过 | 先查 cwd 和 `.git` 形态 |
+| git 现场 | `git status`、`git log`、`git diff --stat`、`git worktree list` | 通过 | 先查主仓 `HEAD` 是否变化 |
+| docs 变化 | `git diff -- docs`、`git log -- docs` | 通过 | 先区分工作区和提交历史 |
+| 地图层级链路 | `rg` 检索 LOD 常量和调用入口 | 通过 | 先查 `hierarchyRenderer.ts`，再查 `mapRenderer.ts` |
+| 枢纽 DB 口径 | `python backend/check_db.py --db backend/data/smartgas.db` | 通过 | 先查 `smartgas.db`，再查 `junction_groups.py` |
+| 仿真链路 | `rg` 检索 `useSimulation`、`simulation-overlay`、solver input 入口 | 通过 | 先查前端 hook，再查 router/service |
+| AI 链路 | `rg` 检索 `/api/ai-assistant/chat`、`analysis_mode`、orchestrator、tool 层 | 通过 | 先查 `ai_assistant.py` 的 direct/orchestrated/legacy 分流 |
+
+## 风险与回退点
+- 风险 1：主仓存在未提交拓扑仿真改动，后续心跳不能把这批状态误写成“已经正式收口”。
+- 风险 2：`MapTopologyView` 仍是独立地图口径，后续文档不能写成已并入全国地图主链。
+- 风险 3：DB 里 `we3` 仍缺失，`junction_groups_rebuilt` 只有 7 组，数据库口径仍要继续盯 coverage，不是只看 `issue_count=0`。
+- 风险 4：AI 仍是多分支并存，后续排障要先确认用户命中的是 direct、orchestrated 还是 legacy。
+- 回退点：本轮只新增巡检文档和更新 memory；如果表述有误，直接回退这两个文档落点即可。
+
+## 最终交付物
+- 本轮巡检文档：[`F:\smartgas-grid\docs\automation\project-heartbeat-2026-04-07-051140.md`](F:/smartgas-grid/docs/automation/project-heartbeat-2026-04-07-051140.md)
+- automation memory：[`C:\Users\Administrator\.codex\automations\automation\memory.md`](C:/Users/Administrator/.codex/automations/automation/memory.md)
+
+## 前后对比
+| 项目 | 上轮记忆 | 本轮确认 |
+| --- | --- | --- |
+| 主仓 `HEAD` | `5ec5af0` | `f75536d` |
+| 分支相对远端 | `ahead 2` | `ahead 3` |
+| 当前未提交重点 | 拓扑仿真刚起步 | solver input + simulation overlay + 地图配套继续推进 |
+| 地图 LOD 阈值 | 文档里写成 `6/10/14/11/12` | 代码实际是 `4/9/12/11/12` |
+| docs 工作区 | 干净 | 开始巡检前仍干净 |
+| 枢纽 runtime 口径 | rebuilt 优先 | 仍是 rebuilt 优先 |
+| DB 关键告警 | `we3` 缺失 | 仍缺失 |
+| AI 主链 | direct / orchestrated / legacy 并存 | 仍并存 |
+
+## 截图说明
+本轮没有 UI 变更验收，也没有新增页面操作，不适合做前后截图，所以继续用上面的对比表和文件落点代替。
*** Update File: C:/Users/Administrator/.codex/automations/automation/memory.md
@@
 # automation memory
 
-- è¿è¡Œæ—¶é—´ï¼?026-04-07 02:11:20 +08:00
-- æœ¬è½®å›ºå®šåœ?`F:\smartgas-grid` ä¸»ç›®å½•å·¡æ£€ï¼Œåªè®¤ä¸»ä»“äº‹å®žï¼Œä¸æ‹¿ detached worktree é¡¶ç»“è®ºã€?- ä¸»ä»“å½“å‰ `HEAD` æ˜?`5ec5af0`ï¼Œç›¸å¯¹è¿œç«?`ahead 2`ï¼›å¤§é‡?detached worktree ä»åœåœ?`6410a10`ã€?- `.git` ç»å¯¹è·¯å¾„æ˜?`F:/smartgas-grid/.git`ï¼Œè¿™é‡Œæ˜¯ä¸»ä»“å®žä½“ç›®å½•ï¼Œä¸æ˜?worktree æŒ‡é’ˆæ–‡ä»¶ã€?- å·¡æ£€å‰?docs å·¥ä½œåŒºæ— æœªæäº?diffï¼›æœ¬è½®åªæ–°å¢žå·¡æ£€æ–‡æ¡£å’Œæ›´æ–?memoryï¼Œä¸æ”¹ä¸šåŠ¡ä»£ç ã€?- å½“å‰æœªæäº¤ä¸šåŠ¨å·²ä»?WE1 è½¬åˆ°æ‹“æ‰‘æ¨¡æ‹Ÿé“¾è·¯ï¼štracked æ˜?`backend/app/main.py`ã€`src/views/TopologyView.tsx`ï¼›untracked æ˜?`backend/app/routers/topology_simulation.py`ã€`backend/app/services/topology_simulation.py`ã€`src/components/topology/SimPanel.tsx`ã€`src/hooks/useSimulation.ts`ã€`src/types/simulation.ts`ã€?- åœ°å›¾é“¾è·¯ç»“è®ºï¼šå…¨å›½åœ°å›¾ä»èµ?`GlobalPipelineView -> MapView -> mapRenderer -> hierarchyRenderer`ï¼ŒLOD é˜ˆå€¼æœªå˜ï¼›`MapTopologyView` ä»æ˜¯ç‹¬ç«‹æ¸²æŸ“å£å¾„ï¼Œä¸èƒ½å†™æˆå·²å¹¶å…¥ä¸»é“¾ã€?- æž¢çº½é“¾è·¯ç»“è®ºï¼šè¿è¡Œæ—¶ä»æ˜¯ `junction_groups_rebuilt` ä¼˜å…ˆï¼Œç¼ºå¤±å›žé€€ `junction_groups`ï¼Œæœ€åŽå  `manual overlay`ï¼›æ—§è¡¨æ²¡æœ‰å®Œå…¨é€€åœºã€?- æ•°æ®åº“å·¡æ£€ç»“è®ºï¼š`pipeline_systems.count=13`ï¼Œ`we3` ä»ç¼ºå¤±ï¼›`junction_groups.count=25`ã€`junction_groups_rebuilt.count=7`ã€`issue_count=0`ï¼Œé‡ç‚¹ç»§ç»­ç›¯ rebuilt è¦†ç›–èŒƒå›´è€Œä¸æ˜¯åªçœ‹æ— æŠ¥é”™ã€?- å¯¼å…¥/æ¨¡æ‹Ÿç»“è®ºï¼šä¼ ç»Ÿå¯¼å…¥è„šæœ¬ä»æ˜¯ç¦»çº¿å·¥å…·è¾¹ç•Œï¼›å½“å‰æ–°å¢žè¿›å±•å·²è½¬åˆ°æ‹“æ‰‘æ¨¡æ‹Ÿé“¾è·¯ï¼Œ`TopologyView -> useSimulation -> /api/pipeline-packages/simulation-overlay -> topology_simulation router -> WE1 pilot seed/solver` å·²ç»æŽ¥ä¸Šå‰åŽç«¯å…¥å£ã€?- AI é“¾è·¯ç»“è®ºï¼šå‰åŽç«¯ä¸»é“¾é€šï¼Œä»æ˜¯ direct / orchestrated / legacy ä¸‰è·¯å¹¶å­˜ï¼›æŽ’éšœæ—¶å…ˆåˆ†æµï¼Œå†çœ‹å·¥å…·å±‚å’Œ RAG ä¾èµ–ã€?
+- 运行时间：2026-04-07 05:11:40 +08:00
+- 本轮固定在 `F:\smartgas-grid` 主目录巡检，只认主仓事实，不拿 detached worktree 顶结论。
+- 主仓当前 `HEAD` 是 `f75536d`，相对远端 `ahead 3`；大量 detached worktree 仍停在 `6410a10`。
+- `.git` 实际落点是 `F:/smartgas-grid/.git`，这里是主仓实体目录，不是 worktree 指针文件。
+- 巡检开始前 docs 工作区无未提交 diff；本轮只新增巡检文档和更新 memory，不改业务代码。
+- 当前未提交重点集中在拓扑仿真链路：tracked 是 `backend/app/routers/pipeline_packages.py`、`backend/app/routers/topology_simulation.py`、`backend/app/services/topology_simulation.py`、`src/utils/hierarchyRenderer.ts`、`src/utils/mapRenderer.ts`、`src/views/MapTopologyView.tsx`；untracked 是 `backend/app/services/we1_solver_input_service.py`、`backend/app/services/we1_steady_validation_service.py`、`backend/scripts/verify_we1_phase3.py`、`backend/scripts/verify_we1_phase4.py`。
+- 地图链路结论：全国地图仍走 `GlobalPipelineView -> MapView -> mapRenderer -> hierarchyRenderer`；当前代码里的 LOD 阈值实际是 `4/9/12/11/12`，并新增 `showFlowArrows >= 10`；`MapTopologyView` 仍是独立渲染口径，不能写成已并入主链。
+- 枢纽链路结论：运行时仍是 `junction_groups_rebuilt` 优先，缺失回退 `junction_groups`，最后叠 `manual overlay`；旧表没有完全退场。
+- 数据库巡检结论：`pipeline_systems.count=13`，`we3` 仍缺失；`junction_groups.count=25`、`junction_groups_rebuilt.count=7`、`issue_count=0`，重点继续盯 rebuilt 覆盖范围，不是只看无报错。
+- 导入/仿真结论：传统导入脚本仍是离线工具边界；当前新增进展继续集中在 `TopologyView -> useSimulation -> /api/pipeline-packages/simulation-overlay -> topology_simulation router -> solver input / solve_steady`，另有 `MapTopologyView -> topologyAPI.simulate()` 的独立入口，不能和 overlay 主链混写。
+- AI 链路结论：前后端主链通，仍是 direct / orchestrated / legacy 三路并存；排障时先分流，再看工具层和模型调用层。
*** End Patch

*** Begin Patch
*** Add File: F:\smartgas-grid\docs\ai-assistant-line-filter-and-table-summary-20260405.md
+# AI 助手线路筛选与表格渲染修复总结
+
+## 改动目标
+
+修掉 AI 助手在“带线路范围的列表/统计问题”上的两个明显问题：
+
+1. 用户问 `陕京四线压气站列表`，结果返回了全库 94 座压气站。
+2. 用户要求表格展示，但界面里只是纯文本，没有真正的表格线。
+
+## 改动前的问题
+
+### 1. 范围筛选没生效
+
+原来的直出逻辑只识别了：
+
+- `压气站列表`
+- `分输站列表`
+- `干线列表`
+
+但没有继续识别前面的线路范围，比如：
+
+- `陕京四线`
+- `西气东输一线`
+- `陕京4线`
+- `西一线`
+
+结果就是：
+
+- 命中了“压气站列表”
+- 但没命中“陕京四线”
+- 最后直接把全库压气站全吐出来了
+
+### 2. 表格只是文本
+
+后端原来返回的是普通文本列表，前端消息气泡也只是把字符串直接显示出来。
+
+所以就算后端返回了带 `|` 的 markdown 表格，前端也不会把它渲染成真正的表格。
+
+## 这次做了什么
+
+### 1. 给列表/统计问句补了线路系统过滤
+
+位置：
+
+- `backend/app/routers/ai_assistant.py`
+
+做法：
+
+- 读取 `pipeline_systems` 表
+- 从 `layers_config` 里提取对应线路的 `id_prefix`
+- 用这些前缀去过滤站场和管线
+
+这次修完以后：
+
+- `陕京四线压气站列表` 只会返回 `SJ4-*`
+- `陕京四线多少压气站` 也只会统计 `SJ4-*`
+
+当前本地库里，陕京四线压气站共 5 座：
+
+- 红墩界压气站
+- 鄂尔多斯压气站
+- 托克托压气站
+- 乌兰察布压气站
+- 张家口压气站
+
+### 2. 给线路别名做了兼容
+
+现在不只支持标准全称，也兼容一些常见说法，比如：
+
+- `陕京四线`
+- `陕京4线`
+- `陕四线`
+- `西一线`
+- `西二线`
+- `西三线`
+
+### 3. 列表和统计都改成表格输出
+
+位置：
+
+- `backend/app/routers/ai_assistant.py`
+
+现在列表和统计类回复会直接输出 markdown 表格。
+
+比如：
+
+```text
+陕京四线压气站完整列表如下，共 5 座：
+
+| 序号 | 名称 | ID | 类型 | 设计压力(MPa) |
+| --- | --- | --- | --- | --- |
+| 1 | 红墩界压气站 | SJ4-3 | 压气站 | 未知 |
+...
+```
+
+### 4. 前端补了表格渲染
+
+位置：
+
+- `src/components/ai-assistant/AiAssistant.tsx`
+- `src/components/ai-assistant/ai-assistant.css`
+
+做法：
+
+- 识别消息里的 markdown 表格块
+- 渲染成真正的 HTML `table`
+- 增加边框、表头底色、单元格分隔线
+
+这样现在用户在 AI 助手里看到的是“有线的表格”，不是带竖线的纯文本。
+
+### 5. 顺手修了 AI 助手自己的一个旧类型问题
+
+位置：
+
+- `src/components/ai-assistant/useAiAssistantChat.ts`
+
+把 `context` 的类型从宽泛的 `Record<string, unknown>` 调整成了 `AssistantChatContext`，避免这块继续报自己的类型错误。
+
+## 影响范围
+
+直接影响：
+
+- AI 助手里带线路范围的列表类问题
+- AI 助手里带线路范围的数量类问题
+- AI 助手消息区的表格显示效果
+
+不影响：
+
+- 地图页业务逻辑
+- SCADA 分析逻辑
+- 拓扑编辑逻辑
+
+## 验证结果
+
+### 1. 后端语法校验
+
+已对 `backend/app/routers/ai_assistant.py` 做 Python AST 校验，通过。
+
+### 2. 关键场景验证
+
+本地直接调用后端逻辑验证：
+
+#### `陕京四线压气站列表`
+
+现在返回：
+
+- 只包含陕京四线
+- 共 5 座
+- 且是表格格式
+
+#### `陕京四线多少压气站`
+
+现在返回：
+
+- 共 5 座
+- 同时附带完整明细表格
+
+#### `压气站列表`
+
+仍然能返回全库 94 座，不影响原有全量列表能力。
+
+### 3. 前端类型检查情况
+
+我顺手修掉了 AI 助手自己的 `context` 类型不匹配问题。
+
+但整仓 `tsc` 仍然没法一次过，原因是项目里还有其他现存类型错误，主要在：
+
+- `src/views/MapTopologyView.tsx`
+- `src/views/TopologyDemoView.tsx`
+
+这些错误和这次修复无关。
+
+## 前后对比
+
+这次主要是逻辑和渲染修复，没有现成“前后截图成对素材”可直接放进文档。
+
+原因：
+
+- 当前对话里只有问题截图，没有改后稳定成图截图
+- 这次更适合用表格说明差异
+
+| 场景 | 改动前 | 改动后 |
+| --- | --- | --- |
+| `陕京四线压气站列表` | 返回全库 94 座压气站 | 只返回陕京四线 5 座 |
+| `陕京四线多少压气站` | 容易统计成全库 | 只统计陕京四线 5 座 |
+| 列表展示 | 纯文本 | 真正带边框的表格 |
+| 线路别名 | 容易匹配不到 | 支持 `陕京4线`、`西一线` 等别名 |
+
+## 关键文件清单
+
+- `backend/app/routers/ai_assistant.py`
+- `src/components/ai-assistant/AiAssistant.tsx`
+- `src/components/ai-assistant/ai-assistant.css`
+- `src/components/ai-assistant/useAiAssistantChat.ts`
+- `docs/ai-assistant-line-filter-and-table-summary-20260405.md`
+
+## 风险点
+
+1. 线路范围匹配目前基于 `pipeline_systems.name` 和别名规则，后续如果还有更多现场简称，建议补一张正式别名表。
+2. 超长表格在小窗里会出现横向滚动，这是这次为了保留表头和边框做的取舍。
+3. 全仓 TypeScript 仍有其他历史报错，暂时不能把整仓 `tsc` 当成这次修复的最终验收标准。
+
+## 回退方式
+
+如果要回退，这次主要回退这几个文件：
+
+- `backend/app/routers/ai_assistant.py`
+- `src/components/ai-assistant/AiAssistant.tsx`
+- `src/components/ai-assistant/ai-assistant.css`
+- `src/components/ai-assistant/useAiAssistantChat.ts`
+
+文档文件删除即可，不影响运行。
*** End Patch

*** Begin Patch
*** Update File: src/views/MapTopologyView.tsx
@@
                     const baselineExtraLabel = _baselineOutPressure != null
                         ? `<div style="font-size: 9px; color: #c4b5fd; background: rgba(40,18,66,0.78); padding: 2px 4px; border-radius: 3px; margin-top: 2px; border: 1px solid rgba(196,181,253,0.35);">鍩?${_baselineInPressure!.toFixed(2)} | 鍑?${_baselineOutPressure.toFixed(2)} MPa | 娓?${(_baselineTemp ?? estimateTemperatureByPressure(_baselineOutPressure)).toFixed(1)}°C</div>`
                         : ''
+                    const displayExtraLabel = _simOutPressure != null
+                        ? `<div style="font-size: 9px; color: #67e8f9; background: rgba(0,20,30,0.82); padding: 2px 4px; border-radius: 3px; margin-top: 3px; border: 1px solid rgba(103,232,249,0.3);">进${_simInPressure!.toFixed(2)} | 出${_simOutPressure.toFixed(2)} MPa</div>`
+                        : _dbOutPressure != null || _dbInPressure != null
+                            ? `<div style="font-size: 9px; color: #86efac; background: rgba(0,15,0,0.75); padding: 2px 4px; border-radius: 3px; margin-top: 3px; border: 1px dashed rgba(134,239,172,0.4);">进${(_dbInPressure ?? (_dbOutPressure != null ? _dbOutPressure * 0.95 : 0)).toFixed(2)} | 出${(_dbOutPressure ?? 0).toFixed(2)} MPa</div>`
+                            : ''
+                    const displayBaselineExtraLabel = _baselineOutPressure != null
+                        ? `<div style="font-size: 9px; color: #c4b5fd; background: rgba(40,18,66,0.78); padding: 2px 4px; border-radius: 3px; margin-top: 2px; border: 1px solid rgba(196,181,253,0.35);">基进${_baselineInPressure!.toFixed(2)} | 基出${_baselineOutPressure.toFixed(2)} MPa | 基温${(_baselineTemp ?? estimateTemperatureByPressure(_baselineOutPressure)).toFixed(1)}°C</div>`
+                        : ''
@@
-                            ${node.name ? `<div style="position: absolute; top: ${size + 4}px; white-space: nowrap; font-size: 11px; color: ${isCriticalAlert ? '#f87171' : isWarningAlert ? '#fbbf24' : '#fff'}; text-shadow: 0 0 2px #000, 0 0 2px #000, 0 0 2px #000; z-index: 10; display: flex; flex-direction: column; align-items: center;">${node.name}${extraLabel}${baselineExtraLabel}</div>` : ''}
+                            ${node.name ? `<div style="position: absolute; top: ${size + 4}px; white-space: nowrap; font-size: 11px; color: ${isCriticalAlert ? '#f87171' : isWarningAlert ? '#fbbf24' : '#fff'}; text-shadow: 0 0 2px #000, 0 0 2px #000, 0 0 2px #000; z-index: 10; display: flex; flex-direction: column; align-items: center;">${node.name}${displayExtraLabel}${displayBaselineExtraLabel}</div>` : ''}
*** End Patch

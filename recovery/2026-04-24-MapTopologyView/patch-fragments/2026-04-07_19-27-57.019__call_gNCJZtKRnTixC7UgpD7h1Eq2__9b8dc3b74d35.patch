*** Begin Patch
*** Add File: F:\smartgas-grid\docs\2026-04-08-WE1第一张图主仿真映射层第2阶段第1轮总结_人话版.md
+# WE1 第一张图主仿真映射层第2阶段第1轮总结
+
+## 1. 这一轮做了什么
+1. 把左侧“仿真”页签收成正式主入口了，现在能在一个地方完成场景选择、运行仿真、保存快照、刷新快照、加载历史快照、清空当前结果。
+2. 把仿真结果摘要和映射覆盖摘要放进了仿真页签，不再只是“能跑”，而是能看到这次跑的是哪个场景、run_id 是什么、求解有没有收敛、映射命中到了多少显示节点和显示连线。
+3. 修了一个关键溯源问题：原来一条真实边如果在枢纽折叠后两端都落到同一个显示节点，会被直接吞掉，现在这些原始 `sourceEdgeIds` 会保存在显示节点的 `internalSourceEdgeIds` 里，不会直接丢失。
+4. 映射工具同步扩了一层，现在除了普通显示边，还会统计“被枢纽吞进去的内部边”有没有命中 overlay，未命中列表也会更接近真实情况。
+
+## 2. 为什么这么做
+1. 第一张图要先保证“主链能跑通”，也就是 `useSimulation -> overlay -> snapshots -> 页面诊断` 这一条线要完整。
+2. 如果不保住枢纽内部边的原始 ID，后面你看到的“未命中 overlay 边”会偏大，诊断结果会误导人，以为后端没出数据，其实是前端折叠时丢了来源。
+3. 这一轮故意不做地图着色，因为现在的目标是先确认“谁对应谁”，不是先把颜色刷上去。
+
+## 3. 影响范围
+1. 前端页面文件：`src/views/MapTopologyView.tsx`。
+2. 前端映射工具：`src/utils/simulationOverlayMapping.ts`。
+3. 这轮没有改 solver，没有改 overlay 后端契约，也没有改地图线条样式。
+
+## 4. 前后对比
+| 对比项 | 之前 | 现在 |
+| --- | --- | --- |
+| 仿真入口 | 只有简单运行/清空，正式入口不完整 | 场景、运行、快照保存/刷新/加载、清空都集中在一个面板 |
+| 运行结果解释 | 信息少，偏调试态 | 能看到场景、run_id、生成时间、求解状态、供需和告警摘要 |
+| 映射覆盖统计 | 只看普通显示边 | 普通显示边 + 枢纽内部边都会纳入统计 |
+| 枢纽吞边 | 折叠后可能直接丢原始边 ID | 原始边 ID 进 `internalSourceEdgeIds`，还能继续追踪 |
+
+## 5. 验证怎么做的
+1. 跑了 `npm run build`，前端构建通过。
+2. 跑了 `python backend/scripts/verify_we1_phase34_final.py`，WE1 主样板和辅样板联合回归通过。
+3. 走了独立子代理复核，结论是第2阶段第1轮通过，确认主链已收口、枢纽吞边已保留、没有越界到地图着色。
+
+## 6. 这轮没放截图的原因
+1. 这轮主要是代码链路和诊断面板结构调整，没有在当前线程里做页面实机截图。
+2. 为了代替截图，上面已经用“前后对比表”把变化点列清楚了，后面如果要补图，建议直接截左侧“仿真”页签的正式入口面板。
*** Add File: F:\smartgas-grid\docs\2026-04-08-WE1第一张图主仿真映射层第2阶段第2轮总结_人话版.md
+# WE1 第一张图主仿真映射层第2阶段第2轮总结
+
+## 1. 这一轮做了什么
+1. 把右侧节点详情里的仿真映射块改成人话了，现在能直接看到“这个显示节点来源于哪些原始节点”“命中了哪些 overlay 节点”“还有哪些没命中”。
+2. 给节点详情补了枢纽内部边映射说明。如果一个枢纽把多条真实边吞进内部，现在能看到这些内部边的来源 ID、命中 ID、未命中 ID、累计流量、平均利用率和最高告警级别。
+3. 关联显示连线的说明也补齐了，不再只有一串难懂的调试文案，而是能直接看“来源边是谁、命中了哪些 overlay 边、这几条边当前流量和利用率大概怎样”。
+
+## 2. 为什么这么做
+1. 你现在最需要的不是第二张图那种强表现，而是第一张图里“点开一个节点就知道仿真结果到底映射到了哪里”。
+2. 这样做以后，排查问题会更直接。比如某个压气站没压降，你可以先看是不是没命中原始节点；某个枢纽看起来怪，可以先看内部边是不是没接住。
+3. 这轮还是刻意停在诊断层，不动地图着色，这样可以先把逻辑解释清楚，再决定后面怎么落图。
+
+## 3. 影响范围
+1. 前端页面文件：`src/views/MapTopologyView.tsx`。
+2. 前端映射工具：`src/utils/simulationOverlayMapping.ts`。
+3. 这轮没有改地图线条颜色、线宽、节点压强落图，也没有动后端求解逻辑。
+
+## 4. 前后对比
+| 对比项 | 之前 | 现在 |
+| --- | --- | --- |
+| 节点映射说明 | 文案偏乱码/调试态，不利于判断 | 直接显示来源节点、命中节点、未命中节点、平均压力、最高告警 |
+| 枢纽内部边 | 页面上基本看不出内部边映射 | 能看到来源边、命中边、未命中边、流量、利用率、告警 |
+| 关联显示连线 | 只是一行简略调试输出 | 拆成更清楚的人话说明，适合排查映射是否合理 |
+
+## 5. 验证怎么做的
+1. 跑了 `npm run build`，前端构建通过。
+2. 跑了 `python backend/scripts/verify_we1_phase34_final.py`，回归脚本通过。
+3. 这轮已经发起独立子代理复核，重点只看“人话诊断”和“有没有越界到地图着色”；如果代理回来说有越界或解释不清，再按它的结论补修。
+
+## 6. 这轮没放截图的原因
+1. 当前线程里没有同步做页面操作录屏或截图。
+2. 这轮更适合补一张“右侧节点详情”截图，后续如果你要，我下一轮可以直接按这个块给你补图。
*** End Patch

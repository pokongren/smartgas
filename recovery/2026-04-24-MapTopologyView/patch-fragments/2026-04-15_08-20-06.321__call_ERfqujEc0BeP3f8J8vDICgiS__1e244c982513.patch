*** Begin Patch
*** Add File: F:/smartgas-grid/docs/2026-04-15-仿真面板精简说明.md
+一、结论
+本轮已把仿真面板里不影响主流程的展示项全部收起，页面只保留“场景选择、运行主仿真、保存快照、加载快照、刷新快照、清空主仿真、状态提示”，界面明显更干净。
+
+二、改动说明
+1. 做了什么
+(1) 收起“Simulation Live”大卡、运行轨迹、动态体征条和指标卡。(2) 收起“推荐快捷操作”区块。(3) 收起“复制仿真文字简报、复制结构化简报、导出演示清单”和两段长文本解读。(4) 保留并突出核心四按钮与快照选择能力，同时新增简洁状态行显示求解状态与 run_id。
+2. 为什么这么做
+(1) 这些区域信息密度高但重复，不是日常操作必需，容易干扰值班视线。(2) 先做减法能快速把主链路跑顺，后续再按需加回更稳。
+
+三、影响范围
+1. 文件范围
+(1) 仅修改 `src/views/MapTopologyView.tsx` 的 `activeTab === 'simulation'` 面板渲染区。(2) 未改后端接口、数据结构、存储逻辑。
+2. 功能影响
+(1) 仿真主流程可用性不变。(2) 被收起功能仍在代码中，后续可快速恢复。
+
+四、验证结果
+1. 构建验证
+(1) 已执行 `npm run build`，构建通过。
+2. 前后对比
+(1) 修改前：仿真页包含大卡、轨迹、体征条、推荐操作、多个导出与长解读，操作入口分散。(2) 修改后：仿真页只保留核心操作和状态提示，层级更短、视觉负担更低。
+3. 截图说明
+(1) 本轮未附截图，原因是当前阶段先做代码级快速收口验证；如需我可下一轮补“改前/改后”两张对比图。
*** End Patch

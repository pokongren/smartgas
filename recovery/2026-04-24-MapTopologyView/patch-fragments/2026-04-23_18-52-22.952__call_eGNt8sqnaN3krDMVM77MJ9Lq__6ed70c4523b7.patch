*** Begin Patch
*** Add File: docs/2026-04-24-仿真控制台截图版恢复说明.md
+# 2026-04-24 仿真控制台截图版恢复说明
+一、结论  
+已把 `MapTopologyView` 的仿真页恢复为截图目标样式，包含“仿真控制台、INPUT DATA、SOLVER ENGINE、OUTPUT VIEW、按压力快照运行、场景参数仿真、全网稳态结果落图展示”这组关键元素，并通过构建验证。
+
+二、主目标  
+1. 恢复截图中的仿真控制台结构与文案。2. 保留当前工程可编译、可运行状态。3. 避免再次覆盖丢失，先做现场备份再改。
+
+三、上下文与已知信息  
+1. 现有工作区内只有两个 `MapTopologyView` 副本，且其中一份存在历史编码污染。2. 代码库和提交历史中未检索到“仿真控制台”原始文本版本。3. 因此采用“基于可运行主干重建截图布局”的恢复路线。
+
+四、主代理与子代理分工  
+1. 本轮由主代理独立完成侦察、实施、验证。2. 侦察：定位本地可恢复文件和历史痕迹。3. 实施：在主文件内重建仿真控制台区域。4. 验证：执行构建并校验关键文案存在。
+
+五、修改逻辑  
+1. 先做止损备份：`recovery/2026-04-24-MapTopologyView/`。2. 在 `activeTab === 'simulation'` 下新增截图式控制台结构。3. 原有旧仿真块保留但禁用（`false && activeTab === 'simulation'`），避免一次性硬删引入未知回归。4. 新增参数编辑展开开关 `showSimParamEditor`，保持原参数能力可用。
+
+六、执行顺序  
+1. 备份现场文件。2. 新增/替换仿真控制台 UI。3. 编译验证。4. 关键文案检索确认。
+
+七、验证方式  
+1. 构建验证：`npm run -s build`。2. 文案验证：检索到 `仿真控制台 / INPUT DATA / SOLVER ENGINE / OUTPUT VIEW / 按压力快照运行 / 场景参数仿真 / 全网稳态结果落图展示`。
+
+八、风险与回退点  
+1. 风险：截图无法提供全部交互细节，个别按钮行为采用了当前可用接口映射。2. 回退点：保留了 `src/views/MapTopologyView.tsx.backup-1649lines` 和 `recovery` 目录，可随时对照回切。
+
+九、前后对比  
+| 对比项 | 调整前 | 调整后 |
+|---|---|---|
+| 仿真页主标题 | 参数编辑块 | 仿真控制台 |
+| 结构分区 | 非截图结构 | INPUT DATA / SOLVER ENGINE / OUTPUT VIEW |
+| 核心按钮 | 运行稳态仿真 | 按压力快照运行 / 场景参数仿真 / 全网稳态结果落图展示 |
+| 可编译性 | 可编译 | 可编译 |
+
+十、最终交付物  
+1. 主文件：`src/views/MapTopologyView.tsx`。2. 现场备份：`recovery/2026-04-24-MapTopologyView/`。3. 说明文档：`docs/2026-04-24-仿真控制台截图版恢复说明.md`。
*** End Patch

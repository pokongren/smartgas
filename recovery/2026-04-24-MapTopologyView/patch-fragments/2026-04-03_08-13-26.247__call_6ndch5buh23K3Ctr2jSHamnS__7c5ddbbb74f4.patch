*** Begin Patch
*** Delete File: F:/smartgas-grid/docs/阶段二多Agent实施方案.md
*** Add File: F:/smartgas-grid/docs/阶段二多Agent实施方案.md
+# SmartGas 阶段二多 Agent 实施方案
+
+> 编制时间：2026-04-03
+>
+> 依据：
+> - `docs/阶段一分工实施方案.md`
+> - `docs/未来项目计划书.md`
+> - `docs/统一公式拓扑AI多Agent协同方案.md`
+>
+> 方法约束：
+> 本方案采用更严格的 `Subagents` 工作法：
+> - 先明确现象、范围、预期结果
+> - 先侦察，再实施，最后验证
+> - 不允许多个代理同时修改同一文件
+> - 每个子代理必须按统一格式汇报
+> - 主代理只做结论、控冲突、做最终整合
+
+---
+
+## 1. 主目标
+
+阶段一已经把拓扑编辑主链路和页面职责边界初步收住。阶段二不再继续围绕“地图拓扑编辑闭环”打转，而是承接到下一层业务能力：
+
+### 1.1 先明确要解决的现象
+
+当前项目在阶段二相关能力上，主要有四类明显现象：
+
+1. SCADA 历史数据虽然已经可查，但还没有真正形成稳定的数据底座。
+2. 单站历史曲线能看，但枢纽、多站对比、拓扑联动能力还不稳定。
+3. 历史分析、拓扑分析、前端页面状态之间还没有形成统一工作链路。
+4. 现有多 Agent 方案偏“职责说明”，还没有固化成真正可并行执行、可回修的写代码流程。
+
+### 1.2 先明确本阶段范围
+
+本阶段只做下面这些内容：
+
+- SCADA 双库能力工程化硬化
+- 历史分析接口增强
+- 枢纽/多站历史分析能力
+- `MapTopologyView` / `TopologyView` 与历史分析联动
+- `GlobalPipelineView` 第二刀拆分
+- 阶段二的多 Agent 写代码、验证、修 Bug 流水线
+
+本阶段不纳入：
+
+- 完整 AI 深度上下文联动
+- 完整仿真闭环
+- 调度方案自动生成
+- 全量公式引擎重构
+- 大范围视觉重设计
+
+### 1.3 先明确预期结果
+
+阶段二的预期结果必须是可验收的，而不是泛目标：
+
+1. 历史查询统一走历史库，主库和历史库职责清楚。
+2. 单站历史曲线、枢纽多站对比曲线可稳定使用。
+3. 拓扑对象可以触发历史分析入口。
+4. `GlobalPipelineView` 复杂度继续下降。
+5. 多 Agent 开发流程形成固定模式：
+   - 主代理拆任务
+   - 侦察代理定位问题与范围
+   - 实施代理分片改代码
+   - 验证代理独立验收
+   - 修 Bug 代理按缺陷单回修
+
+---
+
+## 2. 阶段二范围
+
+## 2.1 纳入范围
+
+### 工作流 A：SCADA 双库硬化
+
+目标：
+
+- 历史数据查询路径固定
+- 双库职责明确
+- 异常与兼容策略清楚
+
+主要文件：
+
+- `backend/app/database.py`
+- `backend/app/scada_models.py`
+- `backend/app/routers/scada.py`
+
+### 工作流 B：历史分析能力增强
+
+目标：
+
+- 单站历史分析稳定
+- 枢纽多站对比可用
+- 压力 / 温度 / 流量多指标切换可用
+
+主要文件：
+
+- `backend/app/routers/scada.py`
+- `src/components/scada/ScadaHistoryChart.tsx`
+- `src/components/scada/LuzhiHistoryPanel.tsx`
+
+### 工作流 C：SCADA 与拓扑联动
+
+目标：
+
+- 站点可触发历史分析
+- 枢纽可触发多站历史分析
+- 拓扑对象到历史对象的映射规则稳定
+
+主要文件：
+
+- `src/views/MapTopologyView.tsx`
+- `src/views/TopologyView.tsx`
+- `backend/app/services/topology.py`
+- `backend/app/routers/scada.py`
+
+### 工作流 D：`GlobalPipelineView` 第二刀拆分
+
+目标：
+
+- 历史相关状态继续外提
+- SCADA 面板组织逻辑继续拆分
+- 页面复杂度继续下降
+
+主要文件：
+
+- `src/views/GlobalPipelineView.tsx`
+- `src/views/global-pipeline/*`
+- `src/hooks/*`
+
+### 工作流 E：阶段二多 Agent 流水线
+
+目标：
+
+- 把“写代码、验证、回修”真正落成固定流程
+- 不是写完再看，而是过程内置验证
+
+主要文件：
+
+- `docs/阶段二多Agent实施方案.md`
+- `backend/app/services/analysis_validator.py`
+- `backend/app/services/fix_task_router.py`
+
+## 2.2 不纳入范围
+
+- 全量 AI 调度助手升级
+- 全量公式引擎统一改造
+- 截断仿真全链路重构
+- 调度方案生成与比选
+
+原因：
+
+这些内容价值高，但会显著扩大边界，破坏阶段二的交付节奏。
+
+---
+
+## 3. 代理拆分
+
+本方案不再把代理简单分成“很多开发 Agent”，而是先按方法论固定三类代理，再在实施代理内部按模块分片。
+
+## 3.1 主代理
+
+主代理只做三件事：
+
+1. 选结论
+2. 控冲突
+3. 做最终决策和整合
+
+主代理不负责：
+
+- 不直接深度改代码
+- 不自己给自己验收
+- 不在验证未通过时宣布完成
+
+主代理输入：
+
+- 阶段目标
+- 当前代码基线
+- 侦察代理报告
+- 验证报告
+
+主代理输出：
+
+- 任务切片
+- 文件归属矩阵
+- 最终合并决策
+- 回修优先级
+
+## 3.2 侦察代理
+
+职责：
+
+- 定位代码链路
+- 找根因候选
+- 缩小改动范围
+- 明确受影响文件
+- 提前暴露依赖和风险
+
+侦察代理不改代码，只做分析和定位。
+
+建议按工作流拆侦察代理：
+
+- 侦察代理 A：SCADA 双库与数据链路
+- 侦察代理 B：历史分析接口与契约
+- 侦察代理 C：前端历史图表与拓扑联动
+- 侦察代理 D：`GlobalPipelineView` 第二刀拆分
+
+## 3.3 实施代理
+
+职责：
+
+- 按主代理划定的文件边界实施修改
+- 只改自己负责的文件
+- 补最小必要测试、日志、兜底
+
+实施代理可以有多个，但必须遵守一个铁规则：
+
+**同一时间，不允许多个实施代理修改同一文件。**
+
+建议按模块拆实施代理：
+
+- 实施代理 A：双库与数据层
+- 实施代理 B：历史分析接口
+- 实施代理 C：前端图表与历史面板
+- 实施代理 D：拓扑联动入口
+- 实施代理 E：`GlobalPipelineView` 结构拆分
+
+## 3.4 验证代理
+
+职责：
+
+- 测试
+- 回归
+- 边界检查
+
+验证代理不改代码，只输出问题和证据。
+
+验证内容至少包括：
+
+- 数据路径验证
+- 接口契约验证
+- 页面联动验证
+- 回归验证
+
+## 3.5 修 Bug 代理
+
+职责：
+
+- 根据验证报告修复问题
+- 优先修契约和数据路径问题
+- 修完后提交回归请求
+
+修 Bug 代理不是自由发挥型代理，只能按缺陷单工作。
+
+---
+
+## 4. 执行顺序
+
+本方案明确采用固定顺序：
+
+`先侦察，再实施，最后验证`
+
+## 4.1 第一阶段：侦察
+
+侦察代理先输出：
+
+- 发现了什么
+- 证据是什么
+- 建议怎么处理
+- 涉及哪些文件
+- 风险点是什么
+
+侦察结束后，主代理只做两件事：
+
+1. 合并侦察结论
+2. 划分文件归属和实施边界
+
+## 4.2 第二阶段：实施
+
+实施代理按主代理分配的切片改代码。
+
+执行规则：
+
+1. 不允许多个实施代理同时改同一文件。
+2. 共享契约文件必须先定稿，再分派后续实施。
+3. 如果两个实施代理都需要同一个文件，必须由主代理决定归属。
+
+## 4.3 第三阶段：验证
+
+验证代理统一跑：
+
+- 构建验证
+- 契约验证
+- 页面联动验证
+- 回归验证
+
+验证失败后：
+
+- 不回到实施代理自由修
+- 由主代理汇总失败项
+- 再分发给修 Bug 代理
+
+## 4.4 第四阶段：回修
+
+修 Bug 代理根据验证报告修复。
+
+修复后必须重新进入验证代理回归。
+
+---
+
+## 5. 文件冲突规则
+
+这是本方案和之前方案相比最重要的新增约束。
+
+## 5.1 文件独占原则
+
+每个实施代理在一个执行批次内只拥有自己的文件集合。
+
+示例：
+
+- 实施代理 A
+  - `backend/app/database.py`
+  - `backend/app/scada_models.py`
+- 实施代理 B
+  - `backend/app/routers/scada.py`
+- 实施代理 C
+  - `src/components/scada/ScadaHistoryChart.tsx`
+  - `src/components/scada/LuzhiHistoryPanel.tsx`
+- 实施代理 D
+  - `src/views/MapTopologyView.tsx`
+  - `src/views/TopologyView.tsx`
+- 实施代理 E
+  - `src/views/GlobalPipelineView.tsx`
+  - `src/views/global-pipeline/*`
+
+## 5.2 共享文件处理原则
+
+如果某个共享文件无法避免，例如：
+
+- `backend/app/routers/scada.py`
+- `src/views/GlobalPipelineView.tsx`
+
+则必须采用：
+
+1. 先由侦察代理确认共享点
+2. 主代理决定唯一实施归属
+3. 其他代理只能提建议，不能直接改
+
+## 5.3 合并顺序
+
+建议固定为：
+
+1. 数据层
+2. 接口层
+3. 前端图表层
+4. 拓扑联动层
+5. 页面拆分层
+6. 验证与回修
+
+---
+
+## 6. 子代理统一输出格式
+
+图片里这个要求必须强制执行。本方案规定所有子代理都按同一格式汇报。
+
+## 6.1 侦察代理输出格式
+
+```md
+### 发现了什么
+
+### 证据是什么
+
+### 建议怎么处理
+
+### 涉及哪些文件
+
+### 风险点是什么
+```
+
+## 6.2 实施代理输出格式
+
+```md
+### 做了什么改动
+
+### 改动依据是什么
+
+### 涉及哪些文件
+
+### 需要谁继续配合
+
+### 风险点是什么
+```
+
+## 6.3 验证代理输出格式
+
+```md
+### 发现了什么问题
+
+### 证据是什么
+
+### 建议怎么处理
+
+### 涉及哪些文件
+
+### 风险点是什么
+```
+
+## 6.4 修 Bug 代理输出格式
+
+```md
+### 修了什么
+
+### 修复依据是什么
+
+### 涉及哪些文件
+
+### 还剩什么风险
+
+### 建议如何回归验证
+```
+
+---
+
+## 7. 主代理收口规则
+
+主代理不应该再像之前那样“什么都管”，这里重新收口边界。
+
+主代理只负责：
+
+1. 选择结论
+2. 控制冲突
+3. 做最终决策和整合
+
+具体动作包括：
+
+- 决定阶段二每一批要做哪几个工作流
+- 决定文件归属
+- 决定实施顺序
+- 决定哪些问题进入回修
+- 决定是否通过阶段验收
+
+主代理不负责：
+
+- 不自己改实施代码
+- 不自己做验证报告
+- 不自己直接跳过验证
+
+---
+
+## 8. 阶段二的多 Agent 一套流程
+
+```mermaid
+flowchart LR
+    A["主代理先定义目标/范围/预期结果"] --> B["侦察代理并行定位问题与改动边界"]
+    B --> C["主代理汇总侦察结论并分配文件归属"]
+    C --> D["实施代理按文件边界并行改代码"]
+    D --> E["主代理合并实施结果并检查冲突"]
+    E --> F["验证代理统一测试、回归、边界检查"]
+    F -->|通过| G["主代理做最终收口和发布"]
+    F -->|失败| H["主代理生成缺陷单"]
+    H --> I["修Bug代理修复"]
+    I --> F
+```
+
+这个流程解决了两个旧问题：
+
+1. 不是所有代理一起上来改代码。
+2. 不是写完以后才想起验证。
+
+---
+
+## 9. 验证口径
+
+阶段二验证不能只看 `npm run build`。
+
+## 9.1 数据层验证
+
+- 历史查询是否真实走历史库
+- 主库是否不再承担历史查询职责
+- 异常时是否有兼容策略和日志
+
+## 9.2 接口层验证
+
+- 单站和枢纽查询契约是否稳定
+- 多指标切换是否契约清楚
+- 返回结构是否可被前端稳定消费
+
+## 9.3 前端验证
+
+- 单站历史图能否稳定打开
+- 枢纽多站历史图能否稳定打开
+- 时间范围切换是否正常
+- 图例、颜色、曲线映射是否正确
+
+## 9.4 联动验证
+
+- 从 `MapTopologyView` 触发历史分析是否正确
+- 从枢纽触发时是否能正确取到底层站点集合
+- 从 `TopologyView` 触发分析入口是否稳定
+
+## 9.5 回归验证
+
+至少覆盖四个场景：
+
+1. 单站历史曲线场景
+2. 枢纽多站对比场景
+3. 拓扑页面触发历史分析场景
+4. `GlobalPipelineView` 面板状态场景
+
+---
+
+## 10. 修 Bug 代理规则
+
+修 Bug 代理必须遵守：
+
+1. 只根据验证结果修
+2. 先修数据路径和契约，再修展示
+3. 每个修复都要带回归点
+4. 不得无边界扩大修改范围
+
+优先级顺序：
+
+1. 数据路径错误
+2. 接口契约错误
+3. 页面联动错误
+4. 图表表现错误
+5. 日志和兜底缺失
+
+---
+
+## 11. 一次性做很多工作的并行方式
+
+阶段二可以并行，但不是无约束并行。
+
+## 11.1 可并行的批次
+
+### 第一批可并行
+
+- 侦察代理 A：双库链路
+- 侦察代理 B：历史分析接口
+- 侦察代理 C：前端历史图表
+- 侦察代理 D：`GlobalPipelineView` 第二刀拆分
+
+### 第二批可并行
+
+在主代理完成文件归属后，下面可以并行实施：
+
+- 实施代理 A：双库与数据层
+- 实施代理 C：图表组件增强
+- 实施代理 E：`GlobalPipelineView` 拆分
+
+### 第三批可并行
+
+等接口契约稳定后再并行：
+
+- 实施代理 B：历史分析接口
+- 实施代理 D：拓扑联动入口
+
+## 11.2 不可并行的点
+
+- 同一个文件不能并行改
+- 同一个契约不能多方同时改
+- 未定稿的响应结构不能直接让前后端同时开工
+
+---
+
+## 12. 阶段二最小落地建议
+
+如果只落一个最小闭环，建议先做这一条：
+
+### 场景
+
+- `GlobalPipelineView` 点击站点压力指标
+- 打开单站历史曲线
+- 支持后端统一历史查询
+- 支持验证代理验收
+- 支持修 Bug 代理回修
+
+### 原因
+
+- 依赖最少
+- 页面最成熟
+- 最适合验证这套 `Subagents` 流程是否真的能跑起来
+
+### 最小执行步骤
+
+1. 主代理先定义目标、范围、预期结果
+2. 侦察代理定位单站历史链路问题
+3. 主代理划分文件归属
+4. 实施代理改数据层、接口层、图表层
+5. 验证代理统一验收
+6. 修 Bug 代理修失败项
+7. 再回归
+
+---
+
+## 13. 阶段二里程碑
+
+### M1：双库稳定
+
+- 历史库职责清晰
+- 历史查询路径稳定
+
+### M2：历史分析可用
+
+- 单站历史稳定
+- 枢纽多站对比稳定
+
+### M3：拓扑联动成型
+
+- 拓扑对象能打开历史分析
+- 枢纽与历史查询打通
+
+### M4：多 Agent 流程成型
+
+- 侦察、实施、验证、回修四步闭环跑通
+- 文件冲突控制有效
+- 验证报告固定格式可复用
+
+### M5：阶段验收
+
+- 核心场景回归通过
+- 文档和验收记录补齐
+
+---
+
+## 14. 结论
+
+修改后的阶段二方案，不再是“多开几个 Agent 同时写代码”的宽泛方案，而是严格按下面的方法执行：
+
+1. 主代理先定义目标、范围、预期结果
+2. 侦察代理先定位问题和根因候选
+3. 主代理按文件归属切片
+4. 实施代理在不冲突的前提下并行改代码
+5. 验证代理统一测试、回归、边界检查
+6. 修 Bug 代理按缺陷单修复
+7. 主代理只做最终结论和整合
+
+这套方式更慢一点，但更稳，也更适合阶段二这种跨数据、接口、前端、联动的工作。
*** End Patch

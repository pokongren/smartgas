*** Begin Patch
*** Add File: F:/smartgas-grid/docs/阶段一分工实施方案.md
+# SmartGas 阶段一分工实施方案
+
+> 编制时间：2026-04-03
+>
+> 依据：
+> - `docs/未来项目计划书.md`
+> - `docs/代码整改任务清单.md`
+> - 当前阶段一已启动的代码整改结果
+
+---
+
+## 1. 阶段一目标
+
+阶段一不追求把所有能力一次做满，而是先把项目最关键的主链路真正打通：
+
+1. 地图拓扑编辑闭环可用
+2. 拓扑分析链路与编辑结果保持一致
+3. 核心页面职责边界清楚
+4. 全局主页面复杂度开始下降
+5. 接口访问和最小回归方式先收敛
+
+当前阶段一的推进原则是：
+
+- 先打通主链路，再做增强能力
+- 先收契约，再收结构
+- 先保证可验收，再扩大改造范围
+
+---
+
+## 2. 阶段一范围
+
+### 本阶段纳入
+
+- `MapTopologyView` 拓扑编辑闭环
+- 拓扑编辑接口收口
+- 枢纽创建 / 删除 / 刷新恢复 / 最小撤销闭环
+- `GlobalPipelineView` 第一刀结构拆分
+- 页面职责边界梳理
+- 最小 smoke test 和构建验证
+
+### 本阶段暂不纳入
+
+- SCADA 双库分层
+- AI 深度上下文联动
+- 调度方案生成
+- 完整仿真闭环
+- 大范围视觉改版
+
+---
+
+## 3. 本轮实际分工
+
+本轮按“主线执行 + 并行子任务分析”推进，避免一边改代码一边丢上下文。
+
+### 主线执行
+
+负责人：Codex 主代理
+
+负责内容：
+
+- 确定阶段一第一批落地切口
+- 实施 `MapTopologyView` 接口收口
+- 补强枢纽创建校验
+- 编写阶段一实施文档
+- 汇总并串联子任务结果
+
+### 子任务 A：前端拓扑编辑页分析
+
+负责人：`Bohr`
+
+负责内容：
+
+- 盘点 `MapTopologyView.tsx` 现有编辑能力
+- 找出散落接口调用点
+- 识别枢纽创建、拆分、恢复链路的薄弱点
+
+输出目标：
+
+- 精确函数清单
+- 创建枢纽校验建议
+
+### 子任务 B：后端拓扑编辑契约核对
+
+负责人：`Darwin`
+
+负责内容：
+
+- 核对 `backend/app/routers/topology_editor.py`
+- 整理拓扑编辑相关接口、请求体、响应体
+- 区分编辑视角和计算视角接口
+
+输出目标：
+
+- `topologyEditorApi.ts` 可直接落地的契约摘要
+
+### 子任务 C：阶段一结构拆分建议
+
+负责人：`Ramanujan`
+
+负责内容：
+
+- 结合 `未来项目计划书` 与 `GlobalPipelineView.tsx`
+- 提炼阶段一工作流分组
+- 确定依赖顺序和验收口径
+
+输出目标：
+
+- 可直接写入实施方案的工作流建议
+
+---
+
+## 4. 工作流划分
+
+### 工作流 A：拓扑主链路闭环
+
+目标：
+
+- 让 `MapTopologyView` 成为稳定的编辑入口
+
+主要任务：
+
+- 统一拓扑编辑 API 调用
+- 补齐枢纽创建、删除、刷新恢复闭环
+- 补强节点选择有效性校验
+- 保证撤销链路最小可用
+
+主要涉及文件：
+
+- `src/views/MapTopologyView.tsx`
+- `src/services/topologyEditorApi.ts`
+- `backend/app/routers/topology_editor.py`
+
+验收口径：
+
+- 用户可以完成多选、创建枢纽、删除枢纽
+- 刷新后枢纽状态能恢复
+- 页面不再散落直连 `fetch('/api/topology/...')`
+
+### 工作流 B：页面职责收口
+
+目标：
+
+- 明确编辑页、分析页、全局页的职责边界
+
+主要任务：
+
+- 收口 `MapTopologyView` 到“编辑与保存”
+- 收口 `TopologyView` 到“分析与展示”
+- 限制 `GlobalPipelineView` 继续承载无关编辑逻辑
+
+验收口径：
+
+- 后续新增逻辑能按职责进入正确页面
+- 至少有一份职责边界说明可遵循
+
+### 工作流 C：GlobalPipelineView 第一刀拆分
+
+目标：
+
+- 在不阻塞主链路的前提下，继续给大页面减压
+
+推荐拆分顺序：
+
+1. 面板可见性和拖拽状态 Hook
+2. 左侧管线树组件
+3. 地图 marker 的 SCADA 注入副作用
+
+主要涉及文件：
+
+- `src/views/GlobalPipelineView.tsx`
+- `src/views/global-pipeline/*`
+- `src/hooks/*`
+
+验收口径：
+
+- 主组件体量继续下降
+- 第一层状态管理不再全部堆在一个文件里
+- 构建通过且页面行为不回归
+
+### 工作流 D：统一访问与回归验证
+
+目标：
+
+- 防止阶段一改完后再次出现接口漂移和联调断点
+
+主要任务：
+
+- 统一相关 API 访问方式
+- 建立最小 smoke test 清单
+- 固化构建和人工联调步骤
+
+验收口径：
+
+- `npm run build` 通过
+- 阶段一涉及页面不存在新的接口访问分叉
+- 有明确的回归清单可复用
+
+---
+
+## 5. 依赖顺序
+
+```mermaid
+flowchart LR
+    A["工作流 A<br/>拓扑主链路闭环"] --> C["工作流 C<br/>GlobalPipelineView 第一刀拆分"]
+    A --> D["工作流 D<br/>统一访问与回归验证"]
+    B["工作流 B<br/>页面职责收口"] --> C
+    B --> D
+    C --> D
+```
+
+执行说明：
+
+1. `A` 先行，因为阶段一的核心就是主链路闭环
+2. `B` 与 `A` 并行，避免边改边继续把职责写乱
+3. `C` 在主链路稳定后推进第一刀拆分
+4. `D` 贯穿全程，不放到最后补
+
+---
+
+## 6. 本轮已落地内容
+
+截至当前，阶段一已经落地的第一批工作如下：
+
+### 6.1 已完成代码改造
+
+- 新增 `src/services/topologyEditorApi.ts`
+- 将 `MapTopologyView.tsx` 中的枢纽与点位接口调用收口到统一服务
+- 删除页面内散落的 `resolveApiPath + fetch` 直连方式
+- 补强枢纽创建前的选择校验
+
+### 6.2 已补强的校验规则
+
+- 选择节点会先去重
+- 至少要求两个有效底层站点
+- 含失效节点时阻止提交
+- 含临时节点时阻止提交
+- 含已归属其他枢纽的站点时阻止重复捏合
+
+### 6.3 当前收益
+
+- 页面不会再自己散落拼拓扑编辑接口
+- 枢纽创建更不容易写入脏数据
+- 后续继续拆 `MapTopologyView` 时，接口层已经有统一落点
+
+---
+
+## 7. 下一批实施清单
+
+阶段一下一批建议按下面顺序继续推进：
+
+1. 为 `MapTopologyView` 补“编辑职责说明”并逐步剥离分析类功能
+2. 给 `GlobalPipelineView` 先拆出面板状态和拖拽状态 Hook
+3. 继续清理拓扑相关页面剩余不统一访问方式
+4. 建立阶段一 smoke test 文档和人工联调路径
+
+---
+
+## 8. 阶段一最小里程碑
+
+### M1：主链路打通
+
+- 枢纽创建、删除、恢复链路可用
+
+### M2：边界收口
+
+- 三个核心页面职责明确
+
+### M3：结构收口
+
+- `GlobalPipelineView` 完成第一刀拆分
+
+### M4：阶段验收
+
+- 构建通过
+- 主链路回归通过
+- 文档和验收记录补齐
+
+---
+
+## 9. 风险提示
+
+- 如果继续允许页面散落直连接口，阶段一很容易再次出现契约漂移
+- 如果不先约束枢纽创建入口，前端会继续把临时节点或重复站点写进组合关系
+- 如果 `GlobalPipelineView` 不尽快做第一刀拆分，后续阶段二、阶段三的改造成本会持续上升
+
+---
+
+## 10. 结论
+
+阶段一已经不是“先想想要不要做”，而是已经进入“主链路先落地”的执行状态。
+
+当前最合理的推进方式仍然是：
+
+1. 用工作流 A 保证主链路闭环
+2. 用工作流 B 防止页面职责继续漂移
+3. 用工作流 C 给大页面做第一刀减压
+4. 用工作流 D 贯穿验证与回归
+
+这样推进，风险最低，也最符合当前项目现状。
*** End Patch

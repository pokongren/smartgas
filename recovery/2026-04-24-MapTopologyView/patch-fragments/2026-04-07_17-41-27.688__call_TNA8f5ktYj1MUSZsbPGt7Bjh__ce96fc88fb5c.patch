*** Begin Patch
*** Add File: F:\smartgas-grid\docs\2026-04-08-WE1第一张图主仿真入口第1阶段第1轮总结_人话版.md
+# WE1 第一张图主仿真入口第1阶段第1轮总结_人话版
+
+## 1. 这轮做了什么
+
+这轮只做了第 1 阶段该做的事，也就是把第一张图里的正式仿真入口收回到 WE1 正式主链，不提前做第 2 阶段的地图命中映射。
+
+具体落地是：
+
+1. 在 [src/views/MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx) 里保留并激活了“第一张图接正式 WE1 主链”的那一块 UI。
+2. 第一张图当前激活的仿真区已经走 `useSimulation -> simulation-overlay / snapshots` 这条正式链，不再把旧的 `/api/emergency/simulate-failure` 当成正式仿真入口。
+3. 原来页面里还残留的旧仿真块和中间重复块，这一轮先全部失效处理，避免同一页同时出现两套仿真入口继续打架。
+4. 第一张图当前已经能在面板里完成这些正式动作：选场景、运行仿真、保存快照、刷新快照、加载历史快照、清除当前结果。
+5. 第一张图当前已经能看到正式结果摘要：求解状态、场景、迭代次数、总供气、总需求、未满足需求、告警数。
+
+## 2. 为什么这么做
+
+这轮没直接做地图上的线段着色和节点落图，不是没想到，而是故意先不做。
+
+原因很简单：
+
+1. 现在第一张图和后端 overlay 原始对象之间，节点、枢纽、显示边的 ID 还没统一。
+2. 如果这轮就硬把 overlay 往地图上落，结果大概率是“看起来接上了，但落图不准”，后面一定返工。
+3. 所以这轮先把“正式入口走对、结果对象走对、快照走对”收住，等下一轮再做映射层，这样不会把主链和显示层搅在一起。
+
+## 3. 影响范围
+
+这轮只动了 1 个业务文件：
+
+1. [src/views/MapTopologyView.tsx](/F:/smartgas-grid/src/views/MapTopologyView.tsx)
+
+这轮没有改：
+
+1. 后端求解器
+2. `simulation-overlay` 接口契约
+3. WE1 验证脚本
+4. 地图命中映射层
+5. `TopologyView` 调试页逻辑
+
+## 4. 前后变化
+
+| 项目 | 之前 | 现在 |
+| --- | --- | --- |
+| 第一张图正式仿真入口 | 旧入口和新入口混在一起，用户容易点到旧链 | 第一张图只剩一块激活的正式 WE1 主链仿真区 |
+| 正式仿真链路 | 地图页里还残留旧 `/api/emergency/simulate-failure` 语义 | 地图页激活块走 `useSimulation + simulation-overlay/snapshots` |
+| 地图页能做什么 | 旧仿真和新仿真口径混乱 | 能选场景、运行、存快照、读快照、看结果摘要 |
+| 地图结果显示 | 旧块和重复块容易打架 | 先只显示正式摘要，地图落图留给下一轮 |
+
+## 5. 验证方式
+
+这轮按“独立验证”口径做了两类验证。
+
+1. 前端构建验证：执行 `npm run build`
+2. 后端主链验证：执行 `python backend/scripts/verify_we1_phase34_final.py`
+
+验证结果：
+
+1. `npm run build` 通过
+2. `verify_we1_phase34_final.py` 通过
+3. 说明这轮没有把现有 WE1 正式主链和前端构建打坏
+
+## 6. 这轮还没做什么
+
+这轮明确没做这些事：
+
+1. 没做节点、枢纽、显示边到 overlay 原始对象的映射
+2. 没做地图上的命中线段着色
+3. 没做聚合节点压力汇总规则
+4. 没把 `TopologyView` 降级成调试页
+
+这些都留给后续阶段，不是漏了。
+
+## 7. 下一轮该做什么
+
+下一轮应该直接进入第 2 阶段，也就是地图仿真映射层。
+
+下一轮主目标只有一个：
+
+1. 把第一张图显示节点、显示边、枢纽节点，和 overlay 原始节点、原始边对齐，形成可解释的映射层。
+
+下一轮通过标准：
+
+1. 显示节点能回溯至少 1 个 overlay 节点 ID
+2. 显示边能回溯真实 source edge IDs
+3. 未命中的节点数和边数能统计、能提示、能解释
+
+## 8. 截图说明
+
+这轮没有附截图。
+
+原因：
+
+1. 这轮是在代码和构建层完成第 1 阶段收口
+2. 当前没有跑浏览器交互截图流程
+3. 这里用“前后对比表 + 构建结果 + 验证脚本结果”替代截图留痕
*** End Patch

一、做了什么
1. 这轮没去改业务逻辑，专门按当前代码把答辩方案重新收了一遍，重点是把“能讲的”和“代码真有的”对齐。
2. 新增了一份方案文档《按当前代码收口的答辩方案》，把主线从旧版的一页闭环，改成了更符合现状的“双页接力 + 一条证据链”。

二、为什么这么做
1. 现在最大的风险不是功能少，而是讲稿和页面入口对不上。老师真要跟着你点，一旦发现主仿真页没有 AI 面板，之前那套“同页自动解读”口径就露馅了。
2. 当前代码里最稳的卖点已经不是“AI 自动串一切”，而是“主仿真页能跑、快照能存、基线能比、AI 查库有证据模板”。这玩意儿拿去答辩，更像工程系统，不像演示拼盘。

三、这轮抓到的关键代码事实
1. `src/App.tsx` 里对 `#/map-topology` 做了 `hideAiAssistantOnHeavyPage`，说明第一张图主仿真页默认隐藏 AI 助手。
2. `src/components/topology/SimPanel.tsx` 里真有“运行仿真、保存快照、刷新快照、加载快照、基线快照、快照对比、试运行清单”这一整套。
3. `src/views/MapTopologyView.tsx` 里真有场景 playbook、基线快照风险提示、runSimulation、saveSnapshot、baseline 对比入口，说明第一张图的主卖点应该是“求解 + 快照”。
4. `src/views/TopologyView.tsx` 里真有快照数、当前 run、快照对比、节点和管段变化展示，说明第三张图更适合讲“验收和对比”。
5. `backend/app/services/raw_excel_ai_direct.py` 现在真能稳定吐出“证据：数据来源、时间范围、样本量、关键数值 + 完整性提示”，这块可以拿来当老师最爱听的可追溯卖点。

四、前后对比
| 项目 | 旧口径 | 这轮收口后 |
| --- | --- | --- |
| 主流程形态 | 一页闭环 | 双页接力 |
| AI 在主仿真页角色 | 同页自动接话 | 分页负责查库和解释 |
| 主卖点 | 功能多 | 主仿真、快照、基线对比、证据链 |
| 风险点 | 页面和讲稿容易打架 | 页面入口和话术基本对齐 |

五、影响范围
1. 这轮只新增方案文档和总结文件，不碰现有页面功能。
2. 对你现场最大的帮助是口径收紧了，老师如果跟着页面看，基本不会一眼看出“讲的不是代码里的东西”。

六、验证结果
1. 我已经按代码把关键入口过了一遍，方案里提到的“主仿真页隐藏 AI、展示页有快照对比、AI 直出有证据模板”都有对应代码支撑。
2. 这轮没跑 UI 联调截图，原因是目标不是改功能，是先把答辩口径从“讲设想”改成“讲现状”。

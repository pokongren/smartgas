一、主目标
1. 巡检当前代码库，优先落地低风险且可自动处理的问题，并给出可复查的验证结论。

二、上下文与已知信息
1. 真实代码仓库位于 `F:\smartgas-grid`，当前存在大量并行中的未提交改动，本轮默认只接受边界清晰、冲突面小的修复。
2. `npx tsc --noEmit` 可运行且通过，说明本轮更适合处理“真值判断吞掉合法值”“兼容层字段解析”这类逻辑性小问题，而不是大范围类型清理。

三、主代理与子代理分工
1. 主代理负责读取自动化记忆、确认真实仓库、汇总候选问题、验收子代理结果、决定保留哪些改动并输出最终结论。
2. 侦察代理并行检查两条线：一条查 `||` 默认值和空值判断，一条查 SCADA 历史面板里的字段解析和时间范围逻辑。
3. 验证代理独立复核已跟踪文件的单行修复，确认影响范围和遗留风险。

四、修改逻辑
1. 已确认并保留的修复在 [`src/hooks/useTopology.ts:218`](/F:/smartgas-grid/src/hooks/useTopology.ts#L218)，把 `line.designPressure || 10.0` 改成 `line.designPressure ?? 10.0`。原因是这里真正要兜底的是“缺值”，不是把合法的 `0` 一起吞掉。
2. 巡检过程中还发现未跟踪新文件 [`src/components/scada/StationHistoryPanel.tsx`](/F:/smartgas-grid/src/components/scada/StationHistoryPanel.tsx) 已包含两处低风险调整：`parseSeriesKey` 改为保留完整指标后缀，`timeRange` 改为按全部可见点求真实最小和最大时间。该文件当前是独立新增组件，本轮只做验收记录，不再继续扩散修改。

五、执行顺序
1. 第1阶段第1轮先读取自动化记忆，确认当前 worktree 为空仓库，真实代码在 `F:\smartgas-grid`。
2. 第1阶段第2轮并行侦察 `useTopology`、`TopologyView`、`StationHistoryPanel`、`scada.py` 等链路，筛出低风险候选点。
3. 第1阶段第3轮只保留证据最充分的一处已跟踪文件修复，并做独立验证和总结落档。

六、验证方式
1. 静态验证：在 `F:\smartgas-grid` 执行 `npx tsc --noEmit`，结果通过，本轮未引入新增 TypeScript 报错。
2. 改动复核：`git diff -- src/hooks/useTopology.ts` 确认已跟踪文件只发生一行行为变化。
3. 调用链复核：[`src/types/pipeline.ts:149`](/F:/smartgas-grid/src/types/pipeline.ts#L149) 声明 `designPressure?: number`，与 `?? 10.0` 的兜底语义一致；[`backend/app/routers/scada.py:834`](/F:/smartgas-grid/backend/app/routers/scada.py#L834) 到 [`backend/app/routers/scada.py:841`](/F:/smartgas-grid/backend/app/routers/scada.py#L841) 的序列键格式也与 `StationHistoryPanel` 的解析逻辑一致。

七、前后对比
1. `useTopology` 修复前：

```ts
design_pressure_mpa: line.designPressure || 10.0,
```

2. `useTopology` 修复后：

```ts
design_pressure_mpa: line.designPressure ?? 10.0,
```

八、风险与回退点
1. [`src/views/TopologyView.tsx:672`](/F:/smartgas-grid/src/views/TopologyView.tsx#L672)、[`src/views/TopologyView.tsx:673`](/F:/smartgas-grid/src/views/TopologyView.tsx#L673)、[`src/views/TopologyView.tsx:685`](/F:/smartgas-grid/src/views/TopologyView.tsx#L685)、[`src/views/TopologyView.tsx:735`](/F:/smartgas-grid/src/views/TopologyView.tsx#L735) 仍有同类 `|| null` 写法，可能把合法 `0` 显示成空；但该文件并行改动很多，本轮不碰。
2. [`src/views/TopologyView.tsx:2030`](/F:/smartgas-grid/src/views/TopologyView.tsx#L2030) 一带 `diameterMm || 600` 也像同类问题，但业务语义还没确认，下一轮先侦察再决定。
3. 构建级回归仍受本机 `esbuild spawn EPERM` 影响，本轮只有类型检查级验证，没有完成 `npm run build` 级别确认。
4. 若后续业务明确规定 `designPressure=0` 永远非法，回退点很简单，只需恢复 [`src/hooks/useTopology.ts`](/F:/smartgas-grid/src/hooks/useTopology.ts) 这一行；但更合理的做法仍是上游做数据校验，而不是在映射层吞值。

九、最终交付物
1. 已跟踪文件修复：[`src/hooks/useTopology.ts`](/F:/smartgas-grid/src/hooks/useTopology.ts)。
2. 本轮总结文件：[`2026-04-23-双小时代码巡检修复第1阶段第3轮总结_人话版.md`](/F:/smartgas-grid/2026-04-23-双小时代码巡检修复第1阶段第3轮总结_人话版.md)。
3. 本轮没有截图。原因是修复属于逻辑兜底和数据解析问题，没有稳定且必要的界面前后视觉差异，已用代码前后对比替代。

一、做了什么
1. 这轮先把答辩材料收成了三份可直接上手的文档，分别是15分钟主线脚本、按轮执行清单、30秒故障回退卡片。
2. 顺手把 AI 直出查库回复补了证据四件套模板，现在这类回答会稳定带上数据来源、时间范围、样本量、关键数值，不至于老师一追问就飘。

二、为什么这么做
1. 眼下最要命的不是功能不够多，是主线不够稳，所以先把“讲什么、怎么讲、出事怎么退”三件事定死。
2. AI 证据格式之前容易看心情，直出查库这条链路最常用，先把它收紧，答辩时最值钱。

三、影响范围
1. 文档新增在 `docs` 目录，属于答辩交付物，不碰现有业务页面逻辑。
2. 代码只动了 `backend/app/services/raw_excel_ai_direct.py`，影响的是站场/管线直查、直数、直列表这几类 AI 直出回复。

四、验证结果
1. 结构验证：三份文档都按“一、1.(1)”这套中文层级写了，现场能直接照着走。
2. 逻辑验证：主线、执行清单、回退卡片三份材料前后口径一致，都是“查库→仿真→解读”。
3. 代码验证：这轮还没跑联调命令，下一轮建议实际发几条固定问题，把证据四件套回包看一遍。

五、前后对比
| 项目 | 调整前 | 调整后 |
| --- | --- | --- |
| 答辩材料 | 分散在旧方案文档里，能看但不好直接讲 | 收成三份现场可用材料 |
| 主流程口径 | 容易把 AI 触发仿真也混进去 | 明确只走手动主仿真 |
| AI查库证据 | 有时有，有时没 | 直出回复统一补证据四件套 |

六、截图说明
1. 这轮没有新跑截图，原因是现成仓库里已经有全国总览、AI问答、仿真落图三张可用图，先复用最省事也最稳。

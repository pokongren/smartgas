一、主目标
修复地图拓扑页点“运行仿真”后立刻白屏的问题，根因是仿真参数面板里有一列边流量参数在读一个没传进去的对象，触发了对 `WE1-T-66` 的空引用。

二、上下文与已知信息
1. 报错内容是 `Cannot read properties of undefined (reading 'WE1-T-66')`，说明不是后端算错，而是前端在按节点或管段 id 读表时，表对象本身是空的。
2. 具体崩点在 `src/components/topology/SimParamEditor.tsx`，它会直接读 `edgeFlowOverrides[edge.id]`，但 `MapTopologyView.tsx` 原来没有把 `edgeFlowOverrides` 传进去。

三、修改逻辑
1. 在 `MapTopologyView.tsx` 里补了 `edgeFlowOverrides` 的本地状态，并把它连同回调一起传给 `SimParamEditor`。
2. 把“边长”和“流量”两张独立输入表一起组装成 `initialInput.edge_overrides` 传给仿真接口，避免界面改了参数但计算侧收不到。

四、前后对比
1. 之前：点仿真后，参数面板一渲染到 `WE1-T-66` 这条边，就会因为 `edgeFlowOverrides` 为空直接报错白屏。
2. 现在：参数面板能正常读到边流量覆写对象，仿真入参也能带上边长和流量两类覆盖值。

五、验证结果
1. 已确认 `MapTopologyView.tsx` 里补上了 `edgeFlowOverrides` 状态、`onEdgeFlowOverridesChange` 传参，以及 `edge_overrides` 组装逻辑。
2. 这一步做的是静态验证，还没有重新跑前端构建；如果需要，我可以继续跑一次本地启动或构建确认。

六、影响范围
1. 影响的是地图拓扑页里的“仿真”标签页和精细化参数面板。
2. 不影响后端仿真算法，只是把前端空引用和参数传递补齐。


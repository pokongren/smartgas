# 智脉平台：AI 灵脉助手与 MCP 智能中枢

## 一、模块定位与破局思考

传统信息系统的“竖井化”弊病导致了数据孤岛：台账在数据库（只能被 DB 开发查）、规程在文档库（只能靠人工阅览）、波及推演在算法库（需要工程师写代码调用）。
智脉平台革命性地引入了 **MCP (Model Context Protocol)** 架构，将大语言模型（LLM）升级为深度感知业务逻辑的“AI 灵脉助手（智能中枢）”，实现了针对上述“三库”的三维立体化大一统。

## 二、全栈感知：“三库一模”工作流深度解剖

AI 灵脉助手（大模型）本身不具备对生产参数的计算与辨识能力，但通过 MCP 协议（函数调用 Function Calling），它被授权成为所有底层计算工具的“调度总指挥”。

```mermaid
sequenceDiagram
    participant Command(现场指挥员)
    participant AI(AI 灵脉助手)
    participant DB(第一库: 关系数据库)
    participant Graph(第三库: 图算法引擎)
    participant RAG(第二库: 知识库 ChromaDB)
    
    Command->>AI: “张三阀室起火，请做影响分析并给出处置规程”
    
    rect rgb(240, 248, 255)
        Note over AI: 1. 拆解意图，并发调用底层三大工具链
        AI->>DB: [SQL API] 查询张三阀室设计压力与上下游属性
        DB-->>AI: 返回压力10MPa、所属干道
        
        AI->>Graph: [NetworkX API] 测算波及面
        Graph-->>AI: 返回将导致下游3个门站停供
        
        AI->>RAG: [Embeddings] 检索《高压阀室突发起火预案》
        RAG-->>AI: 召回关键文档：需立即遥控关停上下游15km内切断阀
    end
    
    Note over AI: 2. 三元数据熔融与降维综述
    AI-->>Command: 返回综合报告（参数描述 ＋ 停供清单 ＋ 实操抢险步骤）
```

## 三、核心技术底座支撑

### 3.1 意图理解与 NL2SQL 翻译
基于国产一流大模型（如 MiniMax），无需调度员掌握任何计算机语言，系统自动将如“列出设计输量大于 500 万方的站场”等口语化诉求映射为精准的查询语句。

### 3.2 向量知识库 (ChromaDB + Embeddings)
海量的应急工单与安全守则，被切片（Chunking）后转化为高维空间中的浮点型向量序列存放于第二库 ChromaDB 内。系统利用“语义相似度空间距离计算”进行匹配召回，远胜于老式搜索引擎的字面关键字正则匹配，做到了真正的“懂行”。

## 四、业务价值
MCP 智能总线真正实现了将极其复杂的“人工跨系统翻盘核算”操作，压降到只需**一句话指令触发**。它标志着天然气管网系统由“被动监控辅助”时代迈入“大模型主动决策指挥”时代。

# 智脉平台 - 天然气管网应急指挥系统

## 项目概述

**智脉平台**是一套面向天然气管网运营的智能化应急指挥系统，整合**数据管理**、**拓扑分析**、**仿真推演**、**AI 辅助决策**四大核心能力，为管网调度、应急响应、规划决策提供一站式解决方案。

---

## 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                      前端层 (React + TypeScript)         │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│  │ 政企视图 │ │ 科技视图 │ │ 拓扑管理 │ │ AI工作流 │       │
│  │(数据大屏)│ │(监控中心)│ │(管网编辑)│ │(智能助手)│       │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │
├─────────────────────────────────────────────────────────┤
│                      API 层 (FastAPI)                    │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│  │ 基础数据 │ │ 应急管理 │ │ 拓扑分析 │ │ AI服务   │       │
│  │  /api   │ │/emergency│ │/topology │ │/assistant│       │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │
├─────────────────────────────────────────────────────────┤
│                      服务层 (Python)                     │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│  │拓扑算法 │ │仿真引擎  │ │RAG知识库 │ │工作流引擎│       │
│  │NetworkX │ │物理模型  │ │ChromaDB │ │LangChain│       │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │
├─────────────────────────────────────────────────────────┤
│                      数据层 (SQLite)                     │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐                   │
│  │ 站场表  │ │ 管线表  │ │ 事件表  │                   │
│  │stations │ │pipelines│ │emergency│                   │
│  └─────────┘ └─────────┘ └─────────┘                   │
└─────────────────────────────────────────────────────────┘
```

---

## 核心功能模块

### 1. 数据管理中心

| 功能 | 说明 |
|------|------|
| 站场管理 | 压气站、分输站、阀室等设施的增删改查 |
| 管线管理 | 干线、支线管线的属性维护与拓扑关联 |
| 数据导入 | Excel 批量导入，自动校验与去重 |
| 数据质检 | 孤立节点检测、坐标异常检查、重复连接识别 |

**数据模型：**
```python
Station:  id, name, type, longitude, latitude, design_pressure, capacity
Pipeline: id, name, start_station_id, end_station_id, length_km, diameter_mm, category
```

---

### 2. 拓扑管理系统

**核心能力：**
- **可视化编辑**：Canvas 力导向图，拖拽式节点布局
- **智能验证**：自动发现孤立节点、错误连接、重复连接
- **分层过滤**：按管线分组、按类型筛选、孤立节点开关
- **关键节点识别**：介数中心性算法找出核心枢纽

**技术亮点：**
```
- 纯 Canvas 实现，零依赖
- 力导向布局自动排列
- 3000+节点流畅渲染
- 缩放/平移/搜索定位
```

---

### 3. 物理仿真引擎

基于真实管网物理参数的计算模型：

| 计算类型 | 公式/方法 | 用途 |
|---------|----------|------|
| **管存计算** | Papay 压缩因子 + 真实气体状态方程 | 计算管道储气量 |
| **流量计算** | Panhandle B 公式 | 评估输送能力 |
| **延迟推演** | 管存 ÷ 流量 = 断供延迟时间 | 应急响应决策 |
| **影响分析** | 图遍历算法 | 故障影响范围 |

**核心代码：**
```python
class Pipeline:
    def calculate_linepack_volume(self) -> float:
        """管存算子：计算管道在工况下的储气量"""
        Z = self.calculate_z_factor(pressure, temperature)
        return geometric_volume * (pressure / standard_pressure) / Z
    
    def calculate_flow_rate(self) -> float:
        """流量算子：基于 Panhandle B 公式"""
        return C_pb * E * (diameter ** 2.53) * sqrt(pressure_diff / (G ** 0.961 * T * Z * L))
    
    def calculate_delay_ticks(self) -> int:
        """延迟算子：断流后能支撑多久"""
        return linepack_volume / flow_rate
```

---

### 4. AI 应急助手

**RAG 知识库：**
- 整合应急预案、操作规程、历史案例
- 向量检索 + 大模型生成回答

**智能工作流：**
- 故障上报 → 影响分析 → 方案生成 → 处置跟踪
- 可视化流程编排

**MCP 工具集成：**
- 外部系统调用（GIS、SCADA、气象等）

---

## 项目结构

```
smartgas-grid/
├── backend/                     # FastAPI 后端
│   ├── app/
│   │   ├── main.py             # 应用入口
│   │   ├── models.py           # SQLModel 数据模型
│   │   ├── schemas.py          # Pydantic 请求/响应模型
│   │   ├── database.py         # 数据库连接
│   │   ├── routers/            # API 路由
│   │   │   ├── basic.py        # 基础数据 CRUD
│   │   │   ├── topology_editor.py  # 拓扑管理 API
│   │   │   ├── emergency.py    # 应急指挥
│   │   │   ├── workflow.py     # AI 工作流
│   │   │   └── ai_assistant.py # AI 助手
│   │   └── services/           # 业务逻辑
│   │       ├── topology.py     # 图算法服务
│   │       └── simulation_service.py  # 仿真服务
│   └── data/smartgas.db        # SQLite 数据库
│
├── src/                         # React 前端
│   ├── views/
│   │   ├── TopologyView.tsx    # 拓扑管理视图（核心）
│   │   ├── GlobalPipelineView.tsx  # 全国管网视图
│   │   ├── WorkflowView.tsx    # AI 工作流视图
│   │   ├── CorpView.tsx        # 政企数据大屏
│   │   └── TechView.tsx        # 科技监控中心
│   ├── components/
│   │   └── ai-assistant/       # AI 助手组件
│   ├── styles/
│   │   └── topology-view.css   # 拓扑视图样式
│   └── App.tsx                 # 路由配置
│
└── docs/                        # 文档
    ├── architecture.md          # 架构设计
    ├── MATHEMATICAL_FORMULAS.md # 物理公式
    └── *.md                     # 各类报告
```

---

## 技术栈

| 层级 | 技术 | 选型理由 |
|------|------|---------|
| 前端 | React + TypeScript | 类型安全，组件化开发 |
| 样式 | Tailwind CSS | 原子化样式，快速开发 |
| 构建 | Vite | 快速热更新，优化打包 |
| 后端 | FastAPI | 异步高性能，自动文档 |
| ORM | SQLModel | 类型安全，兼容 SQLAlchemy |
| 数据库 | SQLite | 轻量，开发部署简单 |
| 图算法 | NetworkX | 成熟的图论算法库 |
| AI | LangChain + ChromaDB | RAG 检索增强生成 |
| 部署 | Uvicorn | ASGI 高性能服务器 |

---

## 核心 API 一览

### 基础数据
```
GET    /api/stations              # 获取所有站场
GET    /api/pipelines             # 获取所有管线
GET    /api/topology/graph        # 获取拓扑全景数据
POST   /api/stations              # 创建站场
POST   /api/pipelines             # 创建管线
```

### 拓扑管理
```
GET    /api/topology/editor-data  # 获取编辑器数据（含状态）
POST   /api/topology/connection   # 创建连接
DELETE /api/topology/connection/{id}  # 删除连接
GET    /api/topology/validate     # 验证拓扑合法性
GET    /api/topology/suggestions/{id}  # 连接建议
```

### 仿真计算
```
POST   /api/emergency/simulate    # 断流推演
GET    /api/emergency/impact/{pipeline_id}  # 影响范围分析
GET    /api/emergency/alternative-routes    # 备用路径
```

---

## 使用流程

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  1.数据导入  │ → │  2.拓扑验证  │ → │  3.拓扑修复  │ → │  4.仿真计算  │
│             │    │             │    │             │    │             │
│ 上传Excel   │    │ 运行验证    │    │ 创建连接    │    │ 管存计算    │
│ 或手动录入  │    │ 查看问题    │    │ 删除错误    │    │ 流量模拟    │
│             │    │             │    │             │    │ 故障推演    │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
                                                              ↓
                                                       ┌─────────────┐
                                                       │  5.方案生成  │
                                                       │             │
                                                       │ 保存方案    │
                                                       │ 导出报告    │
                                                       └─────────────┘
```

### 详细操作流程

#### 第一阶段：数据准备

**1.1 导入站场数据**
- 操作：上传Excel/手动录入
- 数据：站场名称、类型、经纬度、压力参数
- 验证：检查重复ID、坐标是否合理
- 结果：站场列表入库

**1.2 导入管线数据**
- 操作：上传Excel/手动录入
- 数据：管线名称、起点、终点、长度、管径
- 验证：检查起点/终点是否存在
- 结果：管线列表入库

---

#### 第二阶段：拓扑检查

**2.1 一键检查拓扑问题**
- 操作：点击"拓扑验证"按钮
- 系统检查：
  - ✓ 孤立节点（无管线连接）
  - ✓ 错误连接（端点不存在）
  - ✓ 重复连接（同一对站场多条线）
  - ✓ 自环连接（起点=终点）
- 输出：问题列表

**2.2 查看拓扑图（力导向图）**
- 操作：打开"拓扑视图"
- 观察：
  - 红色虚线圈 = 孤立节点（需处理）
  - 金色圈 = 关键节点（重点保护）
  - 红色连线 = 错误连接（需修复）
- 交互：
  - 点击节点查看详情
  - 拖拽调整布局
  - 缩放查看局部

---

#### 第三阶段：拓扑修复

**3.1 处理孤立节点**

方式A：创建新连接
1. 选中孤立节点
2. 点击"创建连接"
3. 选择目标节点
4. 填写管线参数（长度、管径）
5. 保存

方式B：删除无效节点
1. 确认该节点确实不需要
2. 删除节点

**3.2 修复错误连接**
- 操作：
  1. 在问题列表中找到错误连接
  2. 查看详情（哪个端点不存在）
  3. 选择修复方式：
     - 修改端点（重新选择起点/终点）
     - 删除连接（数据错误）

**3.3 验证修复结果**
- 操作：再次运行"拓扑验证"
- 确认：问题数为0

---

#### 第四阶段：仿真计算

**4.1 管存计算**
- 操作：选择管线 → 点击"计算管存"
- 输入：设计压力、运行温度（可选，用默认值）
- 输出：
  - 几何体积
  - 标况体积（管存）
  - 压缩因子Z
- 用途：了解管网储气能力

**4.2 流量模拟**
- 操作：选择管线/全网 → 点击"流量模拟"
- 输入：首站压力、末站压力、温度
- 输出：
  - 日输气量（万方/天）
  - 每10分钟流量
- 用途：评估输送能力

**4.3 故障推演**
- 操作：
  1. 选择故障节点
  2. 设置故障类型（完全断裂/部分损坏）
  3. 设置关断时间
- 输出：
  - 影响范围（哪些管线受影响）
  - 延迟时间（多久会断气）
  - 备用路径（如有）
- 用途：制定应急预案

---

#### 第五阶段：方案生成

**5.1 保存仿真方案**
- 操作：点击"保存方案"
- 填写：
  - 方案名称
  - 场景描述
  - 关键参数
- 结果：方案入库，可后续对比

**5.2 方案对比**
- 操作：选择多个方案 → 点击"对比"
- 输出：参数对比表、结果对比图
- 用途：选择最优方案

**5.3 导出报告**
- 操作：点击"导出报告"
- 格式：PDF/Word
- 内容：
  - 拓扑现状
  - 仿真参数
  - 计算结果
  - 建议措施

---

## 快速启动

```bash
# 1. 启动后端
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload

# 2. 启动前端
npm install
npm run dev

# 3. 访问
http://localhost:5173/#/topology
```

---

## 当前状态

| 模块 | 完成度 | 说明 |
|------|--------|------|
| 基础数据管理 | ✅ 100% | CRUD + 批量导入 |
| 拓扑可视化 | ✅ 100% | Canvas 力导向图 |
| 拓扑验证 | ✅ 100% | 4 类问题检测 |
| 拓扑编辑 | ✅ 100% | 创建/删除连接 |
| 物理仿真 | ✅ 100% | 管存/流量/延迟 |
| AI 助手 | 🔄 70% | RAG + 工作流框架 |
| 应急指挥 | 🔄 60% | 基础功能待完善 |

---

## 项目亮点

1. **物理准确性**：基于真实气体状态方程，非简化模型
2. **性能优化**：3000+节点流畅渲染，智能分层过滤
3. **零依赖可视化**：纯 Canvas 实现，无第三方库负担
4. **AI 融合**：RAG 知识库 + 工作流编排
5. **模块化设计**：各层解耦，易于扩展

---

## 核心概念解释

### 什么是力导向图？

> **力导向图 = 把节点当成小球，连线当成弹簧，让它们自己弹到最合适的位置**

**三个力：**
- **排斥力**：节点互相推开（防止重叠）
- **弹簧力**：连线像弹簧（保持连接节点靠近）
- **中心引力**：把节点拉向画布中心（防止跑出去）

**结果**：节点自动排列成"舒服"的形状，既不太挤也不太散。

### 管存计算是什么？

> **管存 = 管道里储存的天然气量**

高压管道像一个大储气罐，即使停气也能继续供气一段时间。
计算公式考虑：
- 管道几何体积
- 运行压力
- 气体压缩因子（高压下气体比理想气体更容易压缩）
- 运行温度

### 故障推演有什么用？

> **模拟"如果某处断裂，下游多久会断气"**

计算逻辑：
```
延迟时间 = 管存 ÷ 当前流量
```

用途：
- 评估应急响应时间窗口
- 制定关阀顺序
- 启动备用气源

---

## 文件位置

- **项目路径**：`f:\smartgas-grid`
- **后端入口**：`backend\app\main.py`
- **前端入口**：`src\App.tsx`
- **拓扑视图**：`src\views\TopologyView.tsx`
- **数据库**：`backend\data\smartgas.db`

---

*文档生成时间：2026-03-06*

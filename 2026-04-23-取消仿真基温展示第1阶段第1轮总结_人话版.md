一、主目标
把仿真节点角标里的“基温”取消，只保留“基进/基出”压力信息。

二、做了什么
1. 修改了 [MapTopologyView.tsx](F:/smartgas-grid/src/views/MapTopologyView.tsx)。2. 去掉了基线角标里拼接的 `| 基温xx.x°C`。3. 顺手删掉了只给这个角标服务的 `_baselineTemp` 计算，避免留下无用变量和类型报错。

三、为什么这么改
1. 你给的截图里“基温29.8℃”属于仿真地图节点的基线角标，不是结果面板主体数据。2. 这次只删这段展示，影响最小，不会把别处温度显示一起带掉。3. 基线压力信息还保留，用户仍能看“基进/基出”。

四、影响范围
1. 受影响文件是 [MapTopologyView.tsx](F:/smartgas-grid/src/views/MapTopologyView.tsx)。2. 受影响内容是仿真节点名下方那条紫色基线角标。3. 其他温度展示，比如结果面板、出站温度、基线出站温度，没有动。

五、前后对比
| 场景 | 修改前 | 修改后 |
| 1 | `基进9.40 | 基出9.90 MPa | 基温29.8°C` | `基进9.40 | 基出9.90 MPa` |
| 2 | 角标依赖 `_baselineTemp` 计算 | 不再依赖该计算 |

六、验证结果
1. 搜索 `基温`、`_baselineTemp` 后已无残留。2. 执行 `npx tsc --noEmit`，通过。3. 当前代码保留了 `基进/基出` 角标，没有把整条基线信息删掉。

七、风险与回退点
1. 这次只影响基线角标展示，风险很低。2. 如果后续你想把“基进/基出”也一起取消，只要再收一轮这几处基线标签即可。3. 回退时只需要恢复 [MapTopologyView.tsx](F:/smartgas-grid/src/views/MapTopologyView.tsx) 这次差异。

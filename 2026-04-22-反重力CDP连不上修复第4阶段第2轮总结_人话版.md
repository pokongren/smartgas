一、主目标
1. 修复反重力里 Agent Auto Approve 扩展的 CDP 长期离线问题，让自动批准功能恢复可用且可持续生效。

二、上下文与已知信息
1. 弹窗提示“需要重启一次启用 CDP”，但实际日志显示 `User chose: undefined`，说明本轮弹窗动作没有真正完成配置写入。2. 进程启动参数里没有 `--remote-debugging-port`，端口 `9222` 不监听，所以扩展日志一直是 `CDP availability: false`。

三、主代理与子代理分工
1. 侦察负责查三处证据：进程命令行参数、扩展输出日志、端口监听状态。2. 实施负责固化启动参数并落地到脚本与 `argv.json`。3. 验证负责重启后复查端口和扩展日志，确认从 false 变 true。

四、修改逻辑
1. 修改 [launch-antigravity-clean.ps1](/F:/smartgas-grid/scripts/launch-antigravity-clean.ps1)，新增 `CdpPort` 参数，启动参数强制追加 `--remote-debugging-port=$CdpPort`，并把 `remote-debugging-port` 写入 `argv.json`。2. 修改 [ensure-antigravity-compatible.ps1](/F:/smartgas-grid/scripts/ensure-antigravity-compatible.ps1)，新增 `CdpPort` 参数并透传给启动脚本。3. 实际写入 `C:\Users\Administrator\AppData\Roaming\Antigravity\argv.json`，确保存在 `"remote-debugging-port": 9222`。

五、执行顺序
1. 第4阶段第2轮侦察：读取 Agent Auto Approve 日志，确认 CDP offline。2. 第4阶段第2轮实施：改脚本并写入 `argv.json`。3. 第4阶段第2轮实施：执行兼容修复脚本重启反重力。4. 第4阶段第2轮验证：检查 9222 监听和扩展日志状态。

六、验证方式
1. 端口验证：`Get-NetTCPConnection` 显示 `9222` 已监听。2. 日志验证：新日志目录 `C:\Users\Administrator\AppData\Local\AntigravityStableProfile\logs\20260422T005938` 中，`6-Agent Auto Approve.log` 已出现 `CDP availability: true`、`Connected to page 9222:*`、`CDP session started`。

七、风险与回退点
1. 风险：若后续不用当前脚本启动反重力，且外部快捷方式未带 `--remote-debugging-port=9222`，CDP 可能再次离线。2. 回退点：重新执行 `scripts\ensure-antigravity-compatible.ps1 -CdpPort 9222` 即可快速恢复。

八、最终交付物
1. 代码交付：[launch-antigravity-clean.ps1](/F:/smartgas-grid/scripts/launch-antigravity-clean.ps1)、[ensure-antigravity-compatible.ps1](/F:/smartgas-grid/scripts/ensure-antigravity-compatible.ps1)。2. 配置交付：`argv.json` 已包含 `remote-debugging-port=9222`。3. 前后对比如下：

| 项目 | 修复前 | 修复后 |
| --- | --- | --- |
| CDP端口 | 9222 未监听 | 9222 已监听 |
| 扩展状态 | `CDP availability: false` | `CDP availability: true` |
| 会话状态 | 无CDP会话 | `CDP session started` |

九、截图与替代说明
1. 本轮未新增截图，原因是关键判定点是运行态端口和扩展日志；已用日志证据与前后对比表完成可复盘说明。

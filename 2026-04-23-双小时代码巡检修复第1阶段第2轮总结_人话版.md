一、主目标
修复一个可自动处理、低风险、不会干扰当前主线并行开发的默认值判断问题，避免合法的 `0` 被错误覆盖。

二、上下文与已知信息
1. 真实代码仓库位于 `F:\smartgas-grid`，当前存在大量用户侧未提交改动，本轮只允许单点修复。
2. `npx tsc --noEmit` 通过，说明本轮优先处理逻辑类低风险问题，不是语法或类型阻塞。
3. 在 [`src/hooks/useTopology.ts`](F:\smartgas-grid\src\hooks\useTopology.ts) 的 `convertPipelineLine` 中，发现 `line.designPressure || 10.0` 会把合法的 `0` 当成“没值”。

三、主代理与子代理分工
1. 主代理负责读取自动化记忆、定位真实仓库、筛选候选问题、决定只修一个低风险点，并汇总验证结果。
2. 侦察代理负责并行巡检 props 透传、空值判断、默认值和字段映射，收敛到“真值判断吞掉合法 0”这一方向。
3. 验证阶段独立复核改动合理性，并再次执行静态检查确认无新增报错。

四、修改逻辑
1. 修改文件：[`src/hooks/useTopology.ts`](F:\smartgas-grid\src\hooks\useTopology.ts)
2. 修改原因：这里真正要兜底的是 `undefined` 或 `null`，不是数值 `0`。原来的 `||` 会吞掉合法值，导致上游显式传入 `0` 时被回退成 `10.0`。
3. 修改方式：把 `line.designPressure || 10.0` 改成 `line.designPressure ?? 10.0`，只在字段缺失时才使用默认设计压力。

五、前后对比
1. 修改前：

```ts
design_pressure_mpa: line.designPressure || 10.0,
```

2. 修改后：

```ts
design_pressure_mpa: line.designPressure ?? 10.0,
```

六、影响范围
1. 影响仅限 [`src/hooks/useTopology.ts`](F:\smartgas-grid\src\hooks\useTopology.ts) 内 `PipelineLine -> TopoEdge` 的映射逻辑。
2. 当上游显式传入 `designPressure: 0` 时，建图和后续请求现在会保留这个值；当未传值时，仍保持默认 `10.0`。
3. 不涉及接口结构、后端协议、页面布局和已有交互。

七、验证方式
1. 静态验证：在 `F:\smartgas-grid` 执行 `npx tsc --noEmit`。
2. 差异复核：检查 `git diff -- src/hooks/useTopology.ts`，确认只有这一行行为变化。
3. 同类问题巡检：补查 `TopologyView` 等展示层，记录后续候选但本轮不扩改高冲突文件。

八、验证结果
1. `npx tsc --noEmit` 通过，本轮未引入新的 TypeScript 报错。
2. `git diff -- src/hooks/useTopology.ts` 显示仅一行从 `||` 改为 `??`。
3. 独立复核结果成立：这是一处低风险、语义更准确的空值兜底修复。

九、风险与回退点
1. 当前风险较低，唯一行为变化是“保留合法 `0`”。
2. 如果业务后续明确规定 `designPressure` 永远不能为 `0`，应在上游做数据校验，而不是在这里靠 `||` 吞值。
3. [`src/views/TopologyView.tsx`](F:\smartgas-grid\src\views\TopologyView.tsx) 仍有同类展示层写法，如第 672、673、685、735 行附近，但该文件当前并行改动很多，本轮不动。

十、最终交付物
1. 代码修复文件：[`src/hooks/useTopology.ts`](F:\smartgas-grid\src\hooks\useTopology.ts)
2. 人话总结文件：[`2026-04-23-双小时代码巡检修复第1阶段第2轮总结_人话版.md`](F:\smartgas-grid\2026-04-23-双小时代码巡检修复第1阶段第2轮总结_人话版.md)
3. 本轮没有截图，因为这是纯逻辑默认值修复，前后差异更适合用代码对比说明。

一、主目标
1. 修复反重力“连不上、发消息无响应”的问题，并把代理链路固定为可用协议，避免再次被错误改写。

二、上下文与已知信息
1. 关键报错是 `Failed to establish a socket connection to proxies: SOCKS 127.0.0.1:56948`，同时出现语言服务启动超时。2. 实测 `127.0.0.1:56948` 支持 HTTP 代理，不支持 SOCKS 代理。3. 之前脚本存在把本地 HTTP 代理强制转成 SOCKS 的逻辑，导致反重力走错协议。

三、主代理与子代理分工
1. 主代理负责收敛根因、决定修改方向和收口验证。2. 侦察职责由日志与端口核查承担，定位到“端口可用但协议错误”。3. 实施职责为脚本代理逻辑修复与配置重写。4. 验证职责为重启后日志复查和连通性复测。

四、修改逻辑
1. 修改 [launch-antigravity-clean.ps1](/F:/smartgas-grid/scripts/launch-antigravity-clean.ps1)，去掉“`http://127.0.0.1:*` 自动转 `socks5://`”逻辑，代理按系统值原样使用。2. 修改 [ensure-antigravity-compatible.ps1](/F:/smartgas-grid/scripts/ensure-antigravity-compatible.ps1)，同样去掉强制转 SOCKS 逻辑。3. 强制重写 `C:\Users\Administrator\AppData\Roaming\Antigravity\argv.json` 为 `http://127.0.0.1:56948`，并同步用户级 `HTTP_PROXY/HTTPS_PROXY/ALL_PROXY`。

五、执行顺序
1. 第4阶段第1轮侦察：读取注册表代理、监听端口、最新 exthost 与 ls-main 日志，锁定错误协议。2. 第4阶段第1轮实施：修改两份启动修复脚本中的代理协议转换逻辑。3. 第4阶段第1轮实施：重写 argv 与用户代理环境变量。4. 第4阶段第1轮验证：重启反重力并检查新日志是否还出现 SOCKS 连接失败与语言服务超时。

六、验证方式
1. 网络侧验证：`curl --proxy http://127.0.0.1:56948 https://www.googleapis.com` 可建立代理连接。2. 日志侧验证：新日志目录 `C:\Users\Administrator\AppData\Local\AntigravityStableProfile\logs\20260415T175107` 不再出现 SOCKS 56948 连接失败和“Timed out waiting for language server start”。3. 服务侧验证：日志出现 `Language server started`、`LS lspClient started successfully`、`fetchAvailableModels` 请求记录。

七、风险与回退点
1. 风险：若本机代理软件后续把 `ProxyServer` 协议再次切换，旧缓存或外部工具仍可能覆写配置。2. 回退点：保留脚本改动后，可随时再次执行 `scripts\ensure-antigravity-compatible.ps1` 一键重建启动链路；若仍异常，再执行带 `-ResetProfile` 的完整重建。

八、最终交付物
1. 已交付脚本修复文件：[launch-antigravity-clean.ps1](/F:/smartgas-grid/scripts/launch-antigravity-clean.ps1)、[ensure-antigravity-compatible.ps1](/F:/smartgas-grid/scripts/ensure-antigravity-compatible.ps1)。2. 已交付本轮人话总结文件：本文件。3. 前后对比如下：

| 项目 | 修复前 | 修复后 |
| --- | --- | --- |
| 代理协议 | `socks5://127.0.0.1:56948` | `http://127.0.0.1:56948` |
| 关键错误 | SOCKS 连接失败、LS 启动超时 | SOCKS 错误消失、LS 正常启动 |
| 模型链路 | 无法稳定进入 | 已出现模型拉取请求日志 |

九、截图与替代说明
1. 本轮未附界面截图，原因是修复判定依赖实时日志与端口连通证据；已用“日志证据 + 前后对比表”替代，便于复盘与复现。

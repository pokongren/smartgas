一、主目标

1. 修复一个可自动处理、低风险且证据明确的问题，避免 SCADA 历史图弹窗忽略调用方传入的初始时间范围。

二、上下文与已知信息

1. 当前自动化 worktree `C:\Users\Administrator\.codex\worktrees\148e\smartgas-grid` 停在本地初始提交，实际代码位于 `F:\smartgas-grid`。2. `F:\smartgas-grid` 当前分支为 `拓扑设想`，工作区已有大量用户改动，所以本轮只选择一个局部兼容层问题实施，避免和现有改动冲突。3. 前端 `npx tsc --noEmit` 通过，`npm run build` 因环境 `esbuild spawn EPERM` 失败，不能据此判定代码构建异常。

三、主代理与子代理分工

1. 主代理负责收敛实施点并落地修改。2. 侦察侧并行核对了 Git 检出点、真实仓库位置、前后端语法与类型状态。3. 验证侧独立复跑了 `npx tsc --noEmit`，确认本次修改未引入新的前端类型错误。

四、修改逻辑

1. 文件为 [ScadaHistoryChartCompat.tsx](F:/smartgas-grid/src/components/scada/ScadaHistoryChartCompat.tsx)。2. 原实现虽然接收了 `initialHours`，但只拿它拼了 `key`，没有继续透传给 `ScadaHistoryChart`，导致下游组件始终按默认值 `6` 小时初始化。3. 原实现还用了 `initialHours || 6`，这会把合法值 `0` 误判成 `6`，使“全部”时间范围无法生效。4. 本次改为先计算 `resolvedHours = initialHours ?? 6`，再同时用于 `key` 和组件透传，既保留默认值，又不吞掉 `0`。

五、前后对比

1. 变更对比如下。

| 场景 | 修改前 | 修改后 |
| --- | --- | --- |
| 调用方传 `initialHours=12` | 兼容层不透传，下游仍按默认 `6` 初始化 | 透传为 `12`，下游按 `12` 初始化 |
| 调用方传 `initialHours=0` | `0` 被 `|| 6` 吃掉，等同 `6` 小时 | `0` 被保留，表示“全部” |
| 调用方不传 `initialHours` | 默认 `6` | 仍默认 `6` |

六、影响范围

1. 受影响入口主要是 `GlobalPipelineView` 内通过兼容层打开的历史图弹窗。2. 修改只在兼容层生效，不改变图表主体逻辑，不影响其它调用未传 `initialHours` 的现有行为。

七、验证方式

1. 静态验证：在 `F:\smartgas-grid` 执行 `npx tsc --noEmit`，结果通过。2. 代码验证：人工核对兼容层现在已把 `initialHours` 透传给 `ScadaHistoryChart`，且默认值逻辑改为 `??`，不会再误伤 `0`。3. 未执行浏览器手工回归，因为本环境 `vite build` 被 `esbuild spawn EPERM` 阻断，无法完成本地前端进程验证。

八、风险与回退点

1. 当前仓库存在大量未提交改动，本轮只修改一个局部文件，冲突风险较低。2. 若后续发现某些调用方故意依赖“始终 6 小时”的旧行为，可直接回退本文件本次差异。3. 构建链路仍有环境权限问题，下一轮若要扩大巡检范围，建议优先解决 `esbuild spawn EPERM`，否则很多前端回归只能停留在类型层。

九、最终交付物

1. 代码修复文件：[ScadaHistoryChartCompat.tsx](F:/smartgas-grid/src/components/scada/ScadaHistoryChartCompat.tsx)。2. 本轮总结文件：[2026-04-23-双小时代码巡检修复第1阶段第1轮总结_人话版.md](F:/smartgas-grid/2026-04-23-双小时代码巡检修复第1阶段第1轮总结_人话版.md)。
